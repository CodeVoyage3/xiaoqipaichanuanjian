# S16-T01 验收边界

日期：2026-09-10（Asia/Shanghai）
状态：`GOVERNANCE_FROZEN / IMPLEMENTATION_DISPATCH_AUTHORIZED / NOT_IMPLEMENTED`

## 治理证据

- fresh `origin/main = 9eed5973caf6a7cc05a9b70a9a5c27ebd9c02682`；GitHub latest API 只读核验 `v1.0.7`、`draft=false`、`prerelease=false`。
- Stage17 = `CLOSED`；S17-T01 = `GUI_ACCEPTED / RELEASED / CLOSED`。
- 正式主工作区仍为原 1 个修改测试文件 + 4 个未跟踪文件，未 restore/reset/clean/stash/delete/commit；Stage16 治理在独立 clean worktree 完成。
- 当前代码已审查：Batch/Product/ProductTask、policy/stage 与 product aggregation、Dashboard/待排查/详情/筛选、Inno Setup、Updater launch/restart；S16-T01 不需要 Schema/migration。
- 两张批准原型已由 Sol 原图核对并以文件名与 SHA256 冻结在 Task；Terra 必须先读取后实施。
- `FULL = NOT_RUN / NO_FULL`；治理轮未运行测试、Build、EF，未访问正式数据库。

## Sol 独立技术验收待办

- 完整 diff 与范围：只读预测，无业务写入、无规则复制、无 Schema/migration、Reminder/Updater/installer/版本/发布通道无 diff。
- 口径：窗口 `(today, today+N]`、四阶段节点复用现有 policy、商品级去重、代表批次确定性、数字等于明细行数。
- 性能：一次有界候选读取生成 12 个计数，无 N+1/12 次扫描；TEMP/GUID 100k Batch 总览与明细首屏证据。
- 自动化：专项、直接相关回归、必要 Release App build、EF no drift、migration `9`；FULL 继续不跑，除非出现可证实跨模块风险并先说明。
- 原型逐项：模块位置、风险层级、数字点击、明细摘要/表格/分页、留白与密度一致；无图表、大色块、新导航、动画或原型外业务按钮。

## 用户 GUI 验收门禁

Sol 技术通过后只提供最小清单，用户以两张批准原型为主要视觉对照。Sol 不得自判 GUI PASS；S16-T01 未获用户回执前不得 `ACCEPTED/CLOSED`、不得开始 S16-T02、不得 tag/Release/publish v1.0.8。
