# S16-T01 技术验收

日期：2026-09-10（Asia/Shanghai）
状态：`IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`

## 2026-09-10 返修技术验收

- 全新 GPT-5.6 Terra（reasoning `medium`）从治理后 fresh `origin/main@a0bf7e61c721beb61188c47de42044cf7147e9d2` 的独立 clean worktree完成 `9ecd1572e94c82e02c6305545c9592508037de72` + 修正 `49ab29b02dc46168cb949d0cee0329686c6facdd`；实际仅修改 `MainWindow.xaml`、`MainWindow.xaml.cs`、`Stage4ViewModels.cs`、`S16T01FutureExpiryRiskTests.cs`。
- Sol 原型/diff 复核 `PASS`：首页在原位置使用动态浅蓝摘要与真正 5 列 × 4 行 Border 矩阵，固定表头、横纵分隔、单元格和 12 个原点击参数完整；无图表、新导航、动画、KPI 大卡或业务按钮。
- 明细语义 `PASS`：摘要为“后续阶段”，八列同时显示当前阶段与后续阶段；后续阶段直接由点击来源 ViewModel 参数映射并复用既有 badge，不新增风险计算；日期表头为“预计进入后续阶段日期”。
- 固定表头/滚轮 `PASS`：DataGrid 高度 `420`，使用原生 ColumnHeader 与内部纵横 ScrollViewer；PreviewMouseWheel 只调用内部 LineUp/LineDown 并消费事件，分页保留。隔离真实 WPF 探针在 50 行/2 页下得到 `wheelOffset=3 / headerFixed=True`。
- 返修精确专项 `1/1 PASS`；必要 Release App build `0 warning / 0 error`；EF `No changes have been made...`；migration `9`；`git diff --check PASS`。未运行旧 9/9、101/101、100k 性能或 FULL，未访问正式数据库。
- 新 GUI 入口：`C:\Users\39037\AppData\Local\Temp\S16-T01-GUI-Rework-Acceptance\启动S16-T01返修验收.cmd`。使用本次 Release 程序集 SHA256 `4EB11599FD0DD8C3B009972221E330A528A68448D3DED0C41355DC4CD58CE418`；隔离 TEMP/GUID 数据由真实 Domain/policy/stage 生成。自检结果：`home=52 detail=52 rows=50 pages=2 columns=8 wheelOffset=3 headerFixed=True back=Dashboard`，cmd 主窗口稳定出现。

## 2026-09-10 用户 GUI 验收失败

用户在真实程序中对照批准原型后判定失败，旧技术验收不等于 GUI 通过。失败点为：首页没有形成原型要求的有表头、横纵分隔和明确单元格的真正矩阵；大量商品的明细鼠标滚轮不可正常浏览；明细缺少“后续阶段”，摘要与日期字段语义不够明确。

返修技术验收必须逐项证明：首页固定五列表头与三行矩阵结构；12 个入口仍携带正确窗口/目标阶段；摘要、来源单元格、每行“后续阶段”一致且不重复计算；八列表头完整；DataGrid 有界高度、原生表头固定、内部滚轮只滚商品行且分页保留；无 Domain/Schema/migration/业务写入或设计扩张。

既有业务与性能证据（S16 9/9、直接回归 101/101、100k Batch 616/378 ms）仅作为未变范围的历史继承证据，不在本轮重跑。本轮只接受返修直接专项、必要 Release build、EF 无漂移与 migration `9`；`FULL = NOT_RUN / NO_FULL`。

## 实现与范围

- 治理基线 `c00d33abef1cd87b856bbaf718e2f4404606cbdb`；Terra source `8143dd47c4c38dedfc03cf1958fb7f1b405a0b05`，已普通 push 到 `origin/codex/s16-t01-terra`，远端 SHA 相同。
- 实施者为本卡新建 GPT-5.6 Terra，reasoning `medium`，独立 clean worktree；Terra 开工前读取了两张批准原型。
- 真实实现 diff 为 6 个文件、`406 insertions / 6 deletions`：新增单一只读风险查询，最小接入现有 Dashboard/Shell，并增加专项及既有 UI 静态审计的范围修正。
- Domain、Schema/migration/ModelSnapshot、Reminder、Updater、installer、版本与发布通道均无 diff；正式数据库访问 `NO`。
- 正式主工作区原 1 个修改测试文件 + 4 个未跟踪文件保持未触碰。

## Sol 独立复核

- 口径 `PASS`：复用 `ExpiryPolicyCalculator.CalculateStageDates`；窗口 `(businessDate, businessDate+N]`；active、正库存、Managed/V1、scope baseline 完成；每单元格按 `ProductId` 去重；数字等于明细行数；代表批次按目标日期、BatchId 确定；结果按日期、商品编码、ProductId 稳定排序。
- 查询 `PASS`：数据库侧限定未来 210 天并只读投影；一次候选读取形成 12 个单元格；明细每页 50；无业务写入。
- 批准原型逐项 `PASS（静态/代码验收）`：模块位于现有优先处理之后，风险层级与 12 个数字入口一致；明细保留现有侧栏且首页高亮，提供明确返回；摘要、7 列只读表格、分页、密度与留白为可实现等价还原；未出现图表、大色块、新导航、动画或业务操作按钮。真实 WPF 观感仍由用户 GUI 验收确认。
- UIUX-R02 首轮直接回归曾因旧静态审计切片越过新增明细页而 `100/101`；Terra 将审计边界收紧到 Dashboard 根区域，未篡改原断言，Sol 最终复跑 `101/101 PASS`。

## 新鲜门禁证据

- S16-T01 专项：`9/9 PASS`。
- TEMP/GUID SQLite 100k Batch：总览 `616 ms`，明细首屏 `378 ms`，候选 `100000`，结果 `1000`。
- 直接相关回归：`101/101 PASS`（ExpiryStageCalculator、ProductTaskAggregation、V1F02PreReminder、Stage4ViewModel、UIUX-R02/R03）。
- Release App build：`PASS / 0 error`；3 个 `NU1900` 是当前环境无法读取 NuGet 漏洞元数据的网络警告。首次在线 restore 同因失败，改用本机既有包缓存后恢复成功。
- EF：`No changes have been made to the model since the last migration.`；migration `9`。
- `git diff --check = PASS`；受限路径 diff 为空；worktree clean；`FULL = NOT_RUN / NO_FULL`。

## 用户 GUI 验收门禁

旧技术验收已被本次 GUI 失败门禁取代。返修完成且 Sol 独立技术验收通过后，状态只能回到 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`，并提供新的隔离 GUI 验收入口等待用户本人复验。

用户明确回执前不得将 S16-T01 标记为 `ACCEPTED/CLOSED`，不得开始 S16-T02，不得 tag/Release/publish v1.0.8。
