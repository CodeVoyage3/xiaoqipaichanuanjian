# V1-F03-I01｜今日排查计划查询与 Excel 导出技术验收

- 日期：2026-09-02
- 结论：`TECHNICALLY_ACCEPTED / WAITING_I02_APPROVAL`
- 开工治理提交：`1aebc33`
- Terra 实现提交：`db8be19`、`1ac63a2`
- 角色：全新 GPT-5.6 Terra（medium）只实施 I01；Sol 在 Terra 停止后独立审查与复跑。

## 交付结论

- 新增 Application 导出请求/用例，支持全部合法 open Task 或唯一正数 TaskId 精确选择；重复、空、非正数及任何不存在/非法选择整体拒绝。
- 资格门禁覆盖 Managed、批准 policy/version 1、匹配完成 ScopeBaseline、合法当前 TaskItem 与 Product/Batch 关系；Excluded、Unresolved、completed、无基线及 stopped/不一致事实不进入导出。
- 输出按 `ProductCode → TaskId → BatchId → TaskItemId` 稳定排序，一批次一行；A～L 为固定业务列，M～Y 为隐藏身份/快照列，`本次排查数量` 保持空白。
- 复用 `DocumentFormat.OpenXml 3.5.1` 与 `ProductCategoryScopes`；日期/数量使用 Excel 数值类型，条码和长 ID 保持文本精度；包含筛选、冻结首行、列宽、横向打印、单页宽度与重复标题行。
- 同目录临时文件成功后再以不覆盖方式移动；同名、非法路径、目录不存在、占用/权限/写入失败均不返回伪成功，精确清理本次临时文件。
- 导出查询使用 `AsNoTracking`，没有数据库业务写入；没有新增 Schema、migration、依赖、WPF 或 I02 逻辑。

## Sol 独立审查与返修

- 初审发现 SQLite 读取的 `UpdatedAtUtc` 可能为 `Unspecified`，直接 `ToUniversalTime()` 会发生本地时区偏移；同时打印单页宽度缺少 `FitToPage` 明示，部分选择与快照断言不足。
- Sol 未修改生产代码；同一 Terra 返修提交 `1ac63a2` 统一 UTC kind、补齐 `FitToPage`，并扩展全部/部分选择、非法基线/停止批次/版本及隐藏快照覆盖。
- 返修后完整 diff 复审通过；实现范围仅 2 个生产文件和 1 个专项测试文件。

## Sol 新鲜自动化门禁

- I01 专项：3/3，通过 3、失败 0、跳过 0。
- ProductTask/query/lifecycle 相关回归：142/142，通过 142、失败 0、跳过 0。
- Release 全量：767/767，通过 767、失败 0、跳过 0。
- Release build：0 warning / 0 error。
- EF `migrations has-pending-model-changes`：无模型漂移。
- `migrations list --no-connect`：9 条，最后一条为 `20260901155124_AddPolicyAndBaselineFoundation`。
- `git diff 1aebc33..HEAD --check` 通过；无 migration、ModelSnapshot、`.csproj`、`.slnx` 或依赖变化；`StoreExpiryInspector` 进程为 0。
- 本轮命令均使用 `--no-restore` / `--no-build` 的明确组合；未冒充在线 NuGet 漏洞审计。
- 未启动 WPF，未访问生产数据库；测试使用隔离临时数据库和临时 Excel 文件。

## 最终边界与停点

- I01 技术验收通过；当前仅交付查询与新 Excel 文件生成。
- 不包含 WPF、回导、Draft、陈旧应用、正式提交、Reminder、Schema 或持久化 ExportRecord。
- 不创建或执行 I02～I04，不进入 Stage 8、Stage 9；下一唯一审批点为 `V1-F03-I02｜排查结果读取、陈旧校验与 Draft 应用`。
