# S16-T03｜首页优先处理摘要化与未来风险可见性优化｜验收门禁

日期：2026-09-10（Asia/Shanghai）
状态：`IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`

## Sol 独立技术验收

- 实际任务 `>5` 显示 5 条；`=5` 显示 5 条；`<5` 按实际显示；`=0` 保持现有空状态。
- 顶部真实待排查总数不变；首页搜索总数不被展示上限截断；首页摘要与完整列表前 5 条排序一致。
- “查看全部待排查任务 →”命令和完整任务页筛选/分页/数量/排序不变。
- 首页结构仍为优先处理在未来效期风险上方；无内部滚动、分页、展开/收起或新按钮。
- S16-T01 未来风险业务与结构无 diff；S16-T02 功能无 diff；Schema/migration 无变化。
- 只跑本卡直接专项与必要 Release build、EF/migration；`FULL = NOT_RUN / NO_FULL`。

## 用户最小 GUI 验收

- A：任务很多时首页优先处理仅 5 条。
- B：首页仍为优先处理 → 未来效期风险。
- C：普通窗口无需滚过大量任务即可看到未来风险。
- D：“查看全部待排查任务”仍进入完整任务页。
- E：未来风险视觉与点击保持已验收状态。

技术通过后仅记录 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`，等待用户；不得自判 GUI、关闭 Stage16 或发布 v1.0.8。

## Sol 技术验收结果

- Terra implementation：`4027b6c195b6ff89d22c315c22a30b33a799079c`；实际改动为 2 个生产文件与 3 个直接测试文件。
- 真实 SQLite Query + ViewModel/搜索 + 未来风险冻结静态契约专项：`8/8 PASS`。21 条查询场景保持 `OpenTaskCount=21`，首页仅 `P-00`～`P-04`；0/少于/等于/大于 5 均通过。
- “查看全部待排查任务 →”绑定、完整列表 `FixedPageSize=50` 与未来风险 XAML/查询均无 diff；未增加首页内部滚动、分页、展开或按钮。
- Release build `PASS`（0 warning / 0 error）；EF model no drift；migration `9`；Schema/migration 文件无 diff；`FULL = NOT_RUN / NO_FULL`。
- 隔离 GUI 探针 `PASS total=8 summary=5 full=8 priorityY=190.0 riskY=288.0 risk=52 back=Dashboard`；未访问正式数据库。
- 用户入口：`%TEMP%\S16-T03-GUI-Acceptance\启动S16-T03验收.cmd`。当前仍需用户完成 A～E，不得自判 GUI PASS。
