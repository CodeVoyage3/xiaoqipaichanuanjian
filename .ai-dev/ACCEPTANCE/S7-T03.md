# S7-T03 技术验收记录（用户 GUI 待验收）

日期：2026-08-31。结论：**Sol 独立技术门禁通过；S7-T03 尚未正式完成或归档。**

## 授权与实现责任

- 用户本轮明确要求阅读并执行 `D:/下载/S7-T03.md`。仓库任务卡 `.ai-dev/TASKS/S7-T03.md` 与附件 SHA-256 均为 `D2AA74A4860ADBD646DD82041DC399C6351616464BBA379F661627C6C32ACD0C`。
- 开始基线：`master`，`ade169b40fdfd0a4a091da91711c029feace1229`，工作区 clean。
- 本卡全新 GPT-5.6 Luna（max）：`s7t03_luna`，会话 `01a0565b-2818-7c02-a364-68e31d439884`。生产代码、xUnit 测试和审查修复均由这一 Luna 实施；Sol 负责独立审查、验证、用户验收辅助脚本及治理文档。
- 实现提交：`da8b43233fe965784701283a5ecf0839f72750ed`。仅本地提交，未推送。
- S7-T02 的既有治理偏差仍保留原记录：原 Sol 先产生未提交实现，随后全新 T02 Luna 接任审查、修正并承担最终实现责任。没有将历史改写为 Luna 从零实现，没有 reset 或重做 T02。

## 本卡交付与范围审查

- 现有 WPF Shell 增加“数据备份与恢复”，复用既有样式；提供手动备份、只读列表、明确选择、默认取消的二次确认、结果及退出提示。
- 最小 Application 查询只列出应用管理的 `backup-*.db` / `pre-restore-*.db`，按时间倒序，复用 T02 既有校验权威；无效、临时或 ReparsePoint 文件不作为可恢复项。查询不创建目标数据库目录。
- T01 创建备份与 T02 Restore 核心流程不变。T02 仅增加内部只读校验入口以供列表复用；UI 不实现复制、替换、哈希、integrity 或 migration 判断。
- 维护门禁等待已进入的数据库操作结束并阻止新操作；沿用 Draft 保存稳定门禁与 Import/Submission/History 忙碌状态。Reminder 在维护前停止，已持有的 context 离开作用域后才执行恢复。
- 恢复成功、critical failure、未知恢复失败或恢复开始后的异常均锁定业务入口并要求退出；只有已知安全失败才恢复正常运行。Tray/窗口退出、无 Tray 降级与启动初始化门禁均有处理，不引入热重载。
- Stage 3～6 权威业务算法、Domain、DbContext、配置、schema、migration 与项目依赖未改。Stage 4～6 现有 UI/runtime 文件仅作本卡要求的调用门禁及生命周期协调。没有导出、自动备份、删除策略、全局 UI 重构或 S7-T04。

## Sol 本轮独立复验

以下为本轮重新执行结果，不是复用 Luna 报告或历史计数。

| 门禁 | 结果 |
| --- | --- |
| S7-T03 专项 | 30/30 |
| S7-T02 | 9/9 |
| S7-T01 / 共享快照 | 12/12 |
| Stage 6 四个权威类 | 52/52 |
| Stage 5 四个权威类 | 51/51 |
| Stage 4 八个权威类 | 179/179 |
| Stage 3 十个权威类 | 170/170 |
| Release 全量 | 680/680，失败 0，跳过 0 |
| Release build | 0 warning / 0 error |
| EF model | 无模型漂移 |
| migration | 8 条，ID 与开始基线一致 |
| dependency / schema diff | 无变化 |
| Git whitespace | 暂存完整实现 `git diff --cached --check` 通过 |
| 用户辅助脚本检查 | `SCRIPT_FILESYSTEM_CHECK_PASS` |
| 应用进程 | 0；Sol / Luna 未启动 WPF |

执行命令：

```powershell
dotnet build StoreExpiryInspector.slnx -c Release --no-restore -p:NuGetAudit=false
dotnet test tests/StoreExpiryInspector.Tests/StoreExpiryInspector.Tests.csproj -c Release --no-build --no-restore --logger 'trx;LogFileName=release-all.trx' --results-directory obj/S7T03Verification/Sol
dotnet test tests/StoreExpiryInspector.Tests/StoreExpiryInspector.Tests.csproj -c Release --no-build --no-restore --filter 'FullyQualifiedName~S7T03DatabaseBackupRestoreViewModelTests|FullyQualifiedName~S7T02DatabaseRestoreTests|FullyQualifiedName~S7T01LocalDatabaseBackupTests|FullyQualifiedName~PreImportSnapshotServiceTests' --logger 'trx;LogFileName=s7-and-shared.trx' --results-directory obj/S7T03Verification/Sol
dotnet ef migrations has-pending-model-changes --project src/StoreExpiryInspector --configuration Release --no-build
dotnet ef migrations list --project src/StoreExpiryInspector --configuration Release --no-build --no-connect
powershell -NoProfile -ExecutionPolicy Bypass -File tests/S7T03GuiScriptCheck.ps1
git diff ade169b40fdfd0a4a091da91711c029feace1229 -- '*.csproj' global.json src/StoreExpiryInspector/Infrastructure/Migrations src/StoreExpiryInspector/Domain src/StoreExpiryInspector/Infrastructure/Configurations src/StoreExpiryInspector/Infrastructure/StoreDbContext.cs
```

本机原始证据在 `obj/S7T03Verification/Sol/`：`release-all.trx`、`s7-and-shared.trx`（51/51）、`canonical-groups.json`、`build.txt`、`ef-model.txt`、`ef-migrations.txt`、`gui-script-check.txt`。这些是本机生成文件，不随 Git 归档。

Stage 3～6 数字从本轮全量 TRX 按下列精确类集合统计，所有结果均为 Passed；不是额外宣称执行了独立分阶段命令：

- Stage 3：ApplicationStartupCoordinatorTests、BatchCheckedZeroLifecycleUseCaseTests、ConfirmedImportLifecycleOrchestratorTests、ExpiryStageCalculatorTests、PostImportLifecycleUseCaseTests、ProductStockZeroLifecycleUseCaseTests、ProductTaskAggregationTests、S3T07CombinationEvidenceTests、S3T07StartupEvidenceTests、StartupRecalculationTests。
- Stage 4：InspectionDraftUseCaseTests、InspectionSubmissionUseCaseTests、InspectionTaskQueryTests、ManualInventoryAdjustmentUseCaseTests、S4T06ImportViewModelTests、S4T07InspectionDetailViewModelTests、S4T08InspectionSubmissionViewModelTests、Stage4ViewModelTests。
- Stage 5：InspectionHistoryEditUseCaseTests、InspectionHistoryQueryTests、S5T03InspectionHistoryViewModelTests、S5T04InspectionHistoryEditViewModelTests。
- Stage 6：DailyReminderUseCaseTests、S6T02DailyReminderRuntimeTests、S6T03TrayAndReminderSchedulerTests、S6T04SettingsAndAutoStartTests。

`--no-connect` 只核实仓库 migration 集合，不能证明本轮正式库实际应用状态。离线 build 不代表在线 NuGet 漏洞审计成功，既有环境限制仍保留。

## 审查修正与验证边界

同一 Luna 已修正：列表错误被吞成空列表、未知恢复失败后的错误解锁、详情离开到备份页漏加载、查询创建目录副作用、运行时尚未配置时可调用维护、无 Tray 时关闭行为、页头列定义，以及测试日志注入未透传等问题。最终以以上重新运行结果为准。

专项测试覆盖：真实 SQLite 的有效/损坏/半成品备份筛选，重复操作，选中项重验与确认取消，恢复期间及成功后的真实维护锁阻止普通写委托，失败严重性区分，活动数据库操作等待，Import/History 忙碌与 Draft 保存失败/待保存门禁。App 的 scheduler、关闭与 Tray 接线包含静态审查/测试，不能替代真实 Windows 生命周期验收。

早期 Import 忙碌测试抛出模拟错误时，Shell 忽略注入 logger，曾尝试调用默认正式日志路径；Luna 已修为透传注入 logger，生产默认日志行为不变。Sol 复查正式日志文件元数据仍为 1140 bytes / 2026-08-31 10:17:37，未见本轮写入变化；未清理或修补正式日志。不得把这一过程描述为所有早期测试从未尝试访问正式日志。

用户辅助脚本检查只操作工作区假目录；真实注册表边界由模拟替换并检查不存在原注册表调用。覆盖基线拒绝、重复 Prepare、保护库损坏拒绝、备份身份/哈希与 sidecar 检查、隔离标记拒绝、Finish 中断续行、重复 Finish、保留原备份及恢复原自启动快照。没有执行真实 Start，也没有读写真实 HKCU。实际注册表恢复待用户 Finish 回执。

## 正式数据保护与待验收事项

- 自动化 SQLite 测试使用隔离数据库；正式库未被用作恢复测试。Sol / Luna 没有启动应用，没有移动正式运行目录，没有修复 Junction。
- 本轮可读取正式库元数据：299008 bytes，LastWriteTime `2026-08-28 12:06:27`，与开始记录一致；应用进程 0。
- 本轮 SHA-256 读取仍报装入点/Junction 错误：**未重新确认正式库 SHA-256，也未通过工具完整枚举确认正式 sidecar/staging 状态**。
- 历史已批准 SHA-256 为 `F3D423DF14B882D7BFE87780A81CF5879F074AF4880601CBEDB6B475A964F522`，只作为用户 Prepare/Finish 的待现场核对基线，不冒充本轮已读成功。
- 用户指南：`docs/S7-T03-GUI验收.md`；辅助脚本：`docs/S7-T03-GUI.ps1`。Prepare 必须先核对正式库再保护整个原运行目录，测试用新空目录；Finish 核对并恢复原运行目录和原自启动值，清理有标记的隔离目录，结束后不再启动。
- **待用户本人验收**：真实 WPF 创建/取消/恢复/退出/重开、目标数据一致、保护备份、托盘/提醒、1024×600 与 125% 操作，以及 `GUI_RESTORE_BYTES_PASS` / `RESTORE_PASS` 原始回执。

停止于用户人工验收门禁。收到 GUI 与正式环境恢复证据前不得标记 S7-T03 正式完成；不得创建、编号、派发或实施 S7-T04。
