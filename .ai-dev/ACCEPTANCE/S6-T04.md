# S6-T04 验收记录｜提醒时间设置与开机自启动

- 验收日期：2026-08-31
- 结论：通过；S6-T04 正式完成并归档，停止在 S6-T04 门禁
- 实现提交：`f6fdd2495e5b42200b046153b3b65a06790d60d6`
- 技术验收：Codex Sol
- Windows 人工验收：用户本人通过

## 实现范围

- 复用现有 `Settings.ReminderMinuteOfDay` 持久化每日提醒时间，默认值保持 `10:00`；输入严格采用本地 `HH:mm`，非法时间不写入。
- 运行中保存提醒时间后调用既有 `DailyReminderScheduler` 重新调度；改到已过去时间仍由 S6-T01 判断 `due`，今日已成功提醒时仍由既有同日一次门禁阻止重复提醒。
- 启用现有最小“设置”入口，以 WPF 对话框提供每日提醒时间和“仅当前 Windows 用户”的开机自启动开关；未建立第二份 UI 状态或完整偏好设置中心。
- 开机自启动使用 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`，仅管理 `StoreExpiryInspector` 值并指向当前应用可执行文件；Windows 注册表实际值是显示状态的唯一权威。
- 开启、重复开启、关闭、重复关闭均保持可撤销和幂等；注册表读写失败会明确提示且不使提醒主业务或应用崩溃。
- 继续复用 S6-T03 的单实例、托盘和 scheduler；未新增 Windows Service、计划任务、独立后台进程或第二套提醒链路。

## 技术门禁

| 门禁 | 结果 |
|---|---|
| S6-T04 `S6T04SettingsAndAutoStartTests` | 17/17，通过 |
| S6-T03 `S6T03TrayAndReminderSchedulerTests` | 12/12，通过 |
| S6-T02 `S6T02DailyReminderRuntimeTests` | 10/10，通过 |
| S6-T01 `DailyReminderUseCaseTests` | 13/13，通过 |
| Stage 5 四个权威测试类 | 51/51，通过 |
| Stage 4 八个权威测试类 | 179/179，通过 |
| Stage 3 十个权威测试类 | 170/170，通过 |
| Release 全量 | 635/635，0 失败、0 跳过 |
| Release build | 0 warning、0 error |
| EF model | 无模型漂移 |
| migration | 8 条，未新增 |
| schema / dependency | 无变化、无新增依赖 |
| `git diff --check` | 通过 |

## Windows GUI 与数据保护验收

- 用户本人使用隔离数据库完成真实 Windows 验收；设置页可打开，提醒时间可保存，开机自启动可开启、持久显示并关闭。
- Windows 当前用户启动项开启核验为 `AUTOSTART_ON_PASS`，命令精确指向当前 Release 应用；关闭核验为 `AUTOSTART_OFF_PASS`。
- 首次到点出现一次集中提醒，内容为 2 个待排查商品且最高紧急阶段为“过期”；关闭至托盘、托盘恢复与托盘退出正常。
- 同一业务日再次启动未重复显示正常提醒；重新打开设置时已保存提醒时间与自动启动开启状态均正确。
- 验收完成的隔离数据库 SHA-256：`D381F92BD41C99296DF42761313012D2BADCAFFD559B8A74B55AA38CFFC4042B`。
- 验收结束后正式数据库恢复结果为 `RESTORE_PASS`：`299008` bytes，SHA-256 `F3D423DF14B882D7BFE87780A81CF5879F074AF4880601CBEDB6B475A964F522`，应用进程 0，原自动启动状态已原样恢复。
- 恢复后未再次启动应用；隔离验收临时目录及完成副本已清理，正式业务数据不受影响。

## 范围审查

- 未修改 S6-T01 每日提醒业务语义、S6-T02 Windows 通知成功语义或 S6-T03 托盘与 scheduler 核心语义。
- 未修改 Stage 3 Lifecycle、Stage 4 Draft / Reconfirm / InventoryAdjustment / Submission、Stage 5 History / Revision 或 Excel 导入。
- 未修改 schema、EF Configuration、DbContext、migration、target framework、工程文件或依赖。
- 未引入机器级注册表、管理员权限、Windows Service、计划任务、静默启动新语义、Snooze、提醒历史、声音设置、Stage 7 能力或全局 UI/UX 重构。
- 未创建、编号、派发或实施 S6-T05。

## 结论与停止门禁

S6-T04 已满足提醒时间配置与持久化、运行中重新调度、同日一次门禁、当前用户级开机自启动启停与真实状态读取、托盘及 Reminder 回归、Windows 人工验收和正式数据保护要求。技术门禁全部通过；S6-T04 正式完成并归档，后续工作必须等待用户单独批准。
