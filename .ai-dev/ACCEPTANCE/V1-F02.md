# V1-F02｜效期节点提前 3 天预提醒最终验收

- 日期：2026-09-02
- 结论：`ACCEPTED / CLOSED`
- 开工治理提交：`61a057e41cff5f0a04e5489efc90c38ae2a28078`
- 实现提交：`ec5f897`、`2bd799b`
- 验收角色：Sol 独立代码与技术验收；用户本人完成真实 WPF GUI 验收。

## 交付结论

- 5折、2折、收仓、过期四节点均按 `ReminderDate = StageEffectiveDate - 3 个日历日` 派生，候选窗口为 `ReminderDate <= BusinessDate < StageEffectiveDate`。
- `StageEffectiveDate` 与 `ReminderDate` 分离；预提醒不提前改变 Stage，不创建或伪造 ProductTask、Inspection、Revision、LifecycleEvent、HandledAttentionVersion 或其他正式业务事实。
- 预提醒只放行 Managed、合法 version 1 policy、匹配完成 ScopeBaseline、库存为正且 active 的 Batch；Excluded、Unresolved、无匹配基线、库存 0 和 stopped Batch 均为零候选。
- 同一 Batch 的四节点以 `(BatchId, TargetStage)` 独立识别；同商品多批次按商品聚合并保留批次数摘要。
- 正式 Task 与预提醒复用同一次 Windows 集中通知；正式 HighestStage 与“即将5折 / 即将2折 / 即将收仓 / 即将过期”分区展示。
- 继续复用 Stage 6 的 `LastReminderDate`、同日幂等、日期回拨、失败重试、scheduler、托盘、提醒时间与自启动权威；未建设第二套通知系统。

## Sol 独立技术门禁

- V1-F02、Reminder、scheduler、startup 及 V1-F01 边界定向回归：88/88。
- Release 全量首次为 763/764，唯一失败是既有 S7-T03 草稿保存并发测试 5 秒超时；该项单独复跑 1/1，通过后完整新鲜复跑为 764/764。
- Release build：0 warning / 0 error。
- EF `migrations has-pending-model-changes`：无模型漂移。
- migration list：9 条，最后一条仍为 `20260901155124_AddPolicyAndBaselineFoundation`。
- 相对开工治理提交，生产 diff 仅涉及共享 policy 节点日期、Reminder 候选/汇总、通知文案和专项测试；无 Schema、migration、ModelSnapshot、依赖、`.csproj` 或 `.slnx` 变化，`git diff --check` 通过。

## 用户真实 WPF GUI 验收

- 仅预提醒场景：实际弹窗显示即将5折、即将2折、即将收仓、即将过期各 1 个商品，总数 4；没有“今日待排查”或假任务入口，首页正式待排查为 0。
- 用户退出后同日再次启动并等待，明确确认“同日未重复”。
- 正式 Task + 预提醒场景：只出现一个集中弹窗；今日待排查 1、最高阶段过期、四类预提醒各 1、涉及商品总数 5；后台列表实际显示 1 个正式过期任务，分区和计数正确。
- GUI 使用专用测试数据；Excluded / Unresolved 已包含在 fixture 中但未进入 4 个预提醒计数。
- 用户从托盘退出后执行 Fresh，原始回执为 `V1F02_FRESH_RUNTIME_PASS; empty database installed; old data remains recoverably retained.`；脚本在替换前强制应用进程为 0。
- 用户明确旧正式数据后续不再使用；Fresh 已将当前运行数据库替换为全新空库并移除隔离标记，旧数据旁置保留、未删除。工具进程受既有挂载点限制无法现场重读该路径，本轮清理结论以用户 PowerShell 原始回执为准，不伪装为工具侧读取成功。

## 最终边界与停点

- V1-F02 已完成并关闭；policy_version 仍固定为 1，migration 仍为 9。
- ProductCode、BatchKey、MaxArrivalQty、库存 0、正式 0 件停止、ProductTask、AttentionVersion / HandledAttentionVersion、Inspection / Revision、ScopeBaseline / BatchBaseline、导入/startup、备份恢复权威未回归。
- 不进入 V1-F03、Stage 8、Stage 9 或其他任务；等待产品经理下一次单独批准。
