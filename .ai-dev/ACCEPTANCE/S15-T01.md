# S15-T01 验收边界

当前状态：`TECHNICALLY_ACCEPTED / WAITING_USER_GUI_ACCEPTANCE`

## 最小专项

1. 初始界面只有“稍后提醒 / 立即更新”，无“取消更新”，不展示长 Release Body 或技术状态。
2. 下载显示整数百分比和确定进度条，不出现 bytes；下载结束进入更新中。
3. 门店可见阶段只有“下载中 / 更新中 / 安装中”；签名、包重验、准备安装、maintenance、manifest、ACK、journal 等内部状态不直出 UI。
4. 取消更新仅在现有机制安全支持取消的下载阶段可见；更新中和安装中隐藏/禁用。
5. 发现新版时 X 等同稍后提醒；下载中 X 仅复用安全取消；更新中/安装中不形成不安全中断。
6. 原有稍后提醒固定二次告知继续通过，文案与流程不重做。

## 范围与门禁

- 实际 diff 仅限现有更新 UI/ViewModel/状态映射及直接专项测试；若必须读取既有状态，只允许最小连接，不改底层语义。
- 无新状态机、依赖、服务、协议字段、数据库字段、migration 或 ModelSnapshot；无导航和业务 UI 变化。
- NormalLaunchHandshake、Updater phase/journal/rollback、manifest/signature、检查频率、非强制更新、托盘与每日提醒语义保持不变。
- 运行最小直接专项和必要 Release build；`FULL = NOT_RUN / NO_FULL`。
- 完成后状态进入 `IMPLEMENTED / TARGETED_AND_RELEASE_BUILD_PASSED / READY_FOR_REVIEW`，然后停止；不 tag、不 Release、不发布 v1.0.6、不创建 S15-T02。

## 当前执行记录

- 实现提交：`2fce2c999c06302622fd3b77623109c7e0ecd5ba`；worktree clean；未 push/tag/Release。
- 用户可见映射：真实包下载回调 -> `下载中 N%`；下载前元数据/清单/签名及下载后校验、准备/maintenance 等 -> `更新中…`；独立 Updater `Process.Start` 前 -> `安装中…`。
- 百分比：`Math.Clamp(BytesReceived * 100 / TotalBytes, 0, 100)` 转整数；UI 不显示 bytes 或 MB。
- 取消：只在 `IsDownloading` 为 true 时显示并执行现有 linked cancellation token；更新中/安装中隐藏且 X 不关闭窗口。
- 最小专项：`6/6 PASS`。Release App build：`0 error`；3 个 `NU1900` 仅因无法获取 NuGet 漏洞元数据。`FULL = NOT_RUN / NO_FULL`。
- scope/diff-check：PASS。无底层协议、Updater/journal/rollback、manifest/signature、SQLite/migration、导航、业务 UI、托盘或每日提醒逻辑变化。

## 技术评审与 main 集成记录

- Sol 已独立读取并审查真实 Terra diff：`TECHNICAL_REVIEW = PASS`；返修项 `0`。
- integration commit：`46890b83e15b5a971a6dc6e8418c85de6b841d5a`；cherry-pick 无冲突；六个生产/直接测试文件与 Terra `2fce2c999c06302622fd3b77623109c7e0ecd5ba` 逐文件等价。
- tests：`INHERITED 6/6 PASS / NOT_RERUN`。
- Release build：`INHERITED PASS (0 error) / NOT_RERUN`；历史 `NU1900 ×3` 是网络漏洞元数据警告，非 blocker。
- Full：`NOT_RUN / NO_FULL`。
- 用户最终 GUI 验收：`NOT_RUN / WAITING_USER_GUI_ACCEPTANCE`。
