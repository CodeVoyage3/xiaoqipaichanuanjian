# Acceptance S4-T07｜排查详情、草稿、重新确认与库存修正 UI

- 任务卡提交：`38d69913f05f5766c5426f9796827c44fb29f0cc`
- Luna 实现提交：`cb218e193e0309aa1360b08b274128152b3fbc12`
- Sol 审查修正提交：`78f1b736f7092eaf01c12b86013ce36905d0ad71`
- 实现：GPT-5.6 Luna（max）
- 独立审查与验收：GPT-5.6 Sol
- 结论：通过

## 一、交付范围

本卡把 `InspectionTaskQuery.GetDetail`、`InspectionDraftUseCase.SaveDraft / ReconfirmItem / ClearDraft` 与 `ManualInventoryAdjustmentUseCase.Execute` 接入真实 WPF，交付 Dashboard/列表详情入口、四种业务状态与查询错误、批次详情、正常批次折叠、检查信息、CheckedQty 空白/0/正数、单 Task 串行自动保存、Retry、逐项重新确认、清空草稿、正库存/同值/0 库存修正及来源页返回。

UI 未查询 EF、复制 Stage/生命周期规则或本地伪造 Reconfirm/关闭状态；未调用 `InspectionSubmissionUseCase`，CheckedQty=0 只保存 Draft。未新增实体、schema、migration、依赖、通用导航/MVVM框架或 S4-T08 功能；未创建 S4-T08/S4-T09。

## 二、独立审查修正

Sol 完整审查 Luna diff 后修正以下真实问题，并增加专项测试锁定：

- 保存失败后详情刷新也失败时，Retry 必须保留并重试当前未保存输入，不能退回数据库旧值。
- 返回、导航与 Ctrl+F 必须等待当前 Task 保存队列稳定，避免 fire-and-forget 导航丢失输入。
- 正库存成功与 NoChange 反馈接入页面可见反馈区。
- 0 库存已提交但 Dashboard/PendingTasks 刷新失败时，不得误报“库存修正失败”或诱导重复修正；改为独立刷新并明确报告联动刷新失败。
- 详情终止态显示协调错误，批次行补充键盘焦点可见状态。

## 三、自动化与技术门禁

- S4-T07 专项：24/24。
- S4-T01～S4-T07 精确回归：150/150。
- Stage 3 十个权威测试类：170/170。
- Release 全量：498/498，0 失败、0 跳过。
- Release build：0 警告、0 错误。
- EF model drift：无变化。
- migration：仍为 8 条。
- 无项目依赖变化；`git diff --check` 通过；无 S4-T08/S4-T09 任务文件。

专项覆盖 open/completed/system_closed/not_found/Error、Draft null/0/正数恢复、dirty patch、完整顶部快照、快速编辑串行与最新快照、保存失败/Retry/离开、Reconfirm、ClearDraft 成功与失败、正库存/NoChange/0 库存取消与确认、Dashboard/PendingTasks 刷新、PropertyChanged/CanExecute、重复操作门禁及不调用正式提交。

## 四、真实 Release WPF 验收

Sol 使用最终 Release EXE 和隔离 `%LocalAppData%\StoreExpiryInspector` 数据目录完成实机验收：

- 真实选择并导入权威工作簿：806 个商品、3706 个正常批次键，生成 538 个待排查商品；导入后 Dashboard/列表无需重启即时可用。
- 从列表进入真实 TaskId；商品头展示名称、编码、条码、最高阶段、有效库存和待排查数。
- 详情真实展示 6 个待排查批次及各自 Stage；5 个正常活动批次默认折叠。
- 检查人、日期和 CheckedQty 空白/0/正数可录入；自动保存后离开再进入，草稿真实恢复；列表 `HasValidDraft=True`。
- 快速编辑的并发、乱序与最终最新快照由专项测试锁定；实机连续录入无卡死或按钮无响应。
- 通过隔离库构造一致的 Batch/TaskItem `AttentionVersion` 变化，Release UI 显示单行“效期阶段或到货情况已更新，请重新确认”；真实 Reconfirm 后提示和按钮消失，并由 Application 持久化。
- ClearDraft 有原生确认；成功后检查人、日期和所有数量恢复未保存/未填写。失败保留本地输入由专项测试锁定。
- 正库存 17→18 成功并刷新详情；再次提交 18 显示“库存未变化”。
- 0 库存危险确认以“取消”为默认安全动作；取消后库存仍为 18、任务保持 open。再次确认后详情真实返回 `system_closed`，显示“该任务已由系统结束”，列表中该筛选任务消失，Dashboard 待排查由 538 刷新为 537。
- 返回列表保留搜索词、筛选与页码事实；精确像素滚动位置未建设，按批准边界返回原页顶部。
- 默认窗口 1268×754、最大化 2048×1250 无明显裁切；当前系统 150% DPI 下基本可用。

验收结束后已关闭进程、恢复原用户数据目录并删除隔离测试数据；最终核验：`ORIGINAL=True`、`TEST_ARCHIVE=False`、`PROCESS=False`。

## 五、保留项与停止门禁

精确 1024×600、恰好 125% 缩放和在线 NuGet 漏洞审计仍按冻结路线保留给 S4-T09，不在 S4-T07 冒充关闭。

S4-T07 已独立审查、验收并归档。当前立即停止；未创建、编号、派发或预实现 S4-T08/S4-T09，未进入 Stage 5，等待用户下一步批准。
