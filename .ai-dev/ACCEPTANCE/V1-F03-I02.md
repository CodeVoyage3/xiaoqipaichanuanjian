# V1-F03-I02｜排查结果读取、陈旧校验与 Draft 应用技术验收

- 日期：2026-09-02
- 结论：`TECHNICALLY_ACCEPTED / WAITING_I03_APPROVAL`
- 开工治理提交：`cce1018`
- Terra 实现与返修范围：`8337dcf..cd91032`
- 角色：全新 GPT-5.6 Terra（medium）只实施 I02；Sol 在 Terra 停止后独立审查、退回和复跑。

## 交付结论

- 新增独立 `InspectionPlanResultReader`，只读解析 I01 `inspection_plan_v1`；严格验证 A～Y 表头、格式版本、隐藏身份/快照和数量类型，不复用商品源 `ExcelTemplateReader`。
- 空白保持 null，0 和正整数保持原值；非数字、小数、负数、Int32 越界及任何公式均为行错误。行重排允许，删除行不 patch，重复 TaskItemId/BatchId 的相关行全部标错。
- 预检重新读取当前 Task/Product/TaskItem/Batch/ScopeBaseline/Draft/Inspection 事实，返回汇总、逐行错误、逐 Task 可应用状态与原因；预检零数据库写入。
- 陈旧门禁覆盖 Task 状态/更新时间/集合数量、正式 Inspection、稳定身份、AttentionVersion、Stage、tracking、到货/MaxArrival/库存快照、stopped、库存归零、RequiresReconfirmation、无效 Draft、Excluded/Unresolved/非法 policy/基线。
- 确认请求显式选择唯一正数 TaskId，并要求规范化非空排查人、有效 CheckDate/BusinessDate 与 UTC 保存时间；default 日期同样拒绝。
- 确认在单一外层事务内再次调用同一当前事实校验，再按 Task 复用 `InspectionDraftUseCase.SaveDraft`。任一二次冲突或第二 Task 保存失败均整批回滚；不自动 Reconfirm。
- 文件中存在的 blank 行可清现有 DraftItem 为 null；文件中缺失行保持既有值；相同内容重复应用复用 no-change；多 Task 可一次原子 patch。

## Sol 独立审查与返修

- 初审发现 SharedString 表头空引用、行错误被误升为文件级拒绝、删除行可绕过 omitted TaskItem 的 Reconfirm、tracking 快照未比对、预览结构与专项覆盖不足，均退回同一 Terra 修复。
- Sol 首次扩展专项为 28/31；3 个失败来自测试 fixture 违反既有 SQLite CHECK/FK，而非业务实现。Terra 按真实约束修复 fixture，并用隔离测试数据库参数化注入不一致数据验证第二 Task 保存失败回滚。
- 后续补齐旧/混合格式、系统表头、损坏文件、重复身份、TaskItem 集合变化、生命周期防御、输入门禁、二次冲突、blank/缺失差异、幂等及多 Task 原子测试；最终专项由 Sol 新鲜通过。

## Sol 新鲜自动化门禁

- I02 专项：38/38，通过 38、失败 0、跳过 0。
- I02 + I01 + Draft/Task/lifecycle 相关回归：213/213，通过 213、失败 0、跳过 0。
- Release 全量：805/805，通过 805、失败 0、跳过 0。
- Release build：0 warning / 0 error。
- EF `migrations has-pending-model-changes`：无模型漂移。
- `migrations list --no-connect`：9 条，最后一条为 `20260901155124_AddPolicyAndBaselineFoundation`。
- 相对开工治理提交，实现仅新增 2 个 Application 文件和 1 个专项测试文件；无 migration、ModelSnapshot、`.csproj`、`.slnx` 或依赖变化，`git diff --check` 通过。
- `StoreExpiryInspector` 进程 0；未启动 WPF、未访问生产数据库。测试仅使用隔离临时 SQLite 和临时 Excel。
- 本轮使用现有 restore 产物运行 `--no-restore`，不冒充在线 NuGet 漏洞审计。

## 最终边界与停点

- I02 技术验收通过；仅交付读取、预览、陈旧校验和既有 Draft patch。
- 不包含 WPF、正式 Inspection、多 Task 正式提交、Task completed、Batch 0 件停止、Revision、Reminder、Schema 或持久化回导记录。
- 不创建或执行 I03/I04，不进入 Stage 8、Stage 9；下一唯一审批点为 `V1-F03-I03｜多任务正式提交编排`。
