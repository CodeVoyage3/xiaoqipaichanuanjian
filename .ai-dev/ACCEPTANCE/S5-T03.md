# S5-T03 技术验收记录｜排查历史只读 UI

- 日期：2026-08-30
- 当前结论：首次 GUI 在“查看详情”处闪退，验收不通过；原 S5-T03 Luna 已最小修复，Sol 独立复验通过，隔离程序已更新，停止等待用户 GUI 重验。尚未正式完成或归档。
- 批准任务卡：`.ai-dev/TASKS/S5-T03.md`
- 开始基线：`master`，`b3c5515d2e6f3215aa79f5325fd00de77f2f8358`，工作区 clean。
- 执行：全新 GPT-5.6 Luna（max）；独立范围审查与技术验收：GPT-5.6 Sol。
- 实现提交：`3f44388193fc03cfde82ee169912f868ba32e219`。

## 范围和验收边界

- 只接入既有 WPF Shell 的排查历史列表、正式详情与逐明细 Revision 查看。
- 复用 `InspectionHistoryQuery.List / GetDetail / GetItemRevisions`，不调用历史编辑用例，不重写 Stage 3 / Stage 4 写逻辑。
- 延续实际 Stage 4 表格与导航视觉样式，不新增 schema、migration、依赖、导出或复杂筛选。
- Sol 不启动 WPF、不使用电脑操控做 GUI 验收。

## 实现与独立审查

- Shell 原位置启用“排查历史”，仅成功进入该页面时加载；从编辑详情离开仍经过既有保存稳定与提交互斥门禁。构造 Shell 不额外查询历史。
- 列表可选择和复制条码 / 编码，通过“查看详情”显式导航；详情显示全部正式明细，选中明细行加载 Revision。返回、刷新和重试仅触发只读查询。
- 列表和 Revision 保持后台结果顺序，不按当前库存、Batch 状态或 Draft 过滤；所有数据显示既有 DTO，不调用阶段计算或历史编辑用例。
- `InspectionHistoryViewModel` 分离加载 / 空 / 失败 / 不存在状态，并用请求版本防止迟到结果覆盖当前选择。
- 复用 Stage 4 原生表格、颜色、字体与导航样式；数字列局部使用 Segoe UI，时间标注 UTC，详情可滚动、标识可只读复制。没有改动全局 `App.xaml` 样式或既有页面业务。
- 审查修正了构造时提前读取、列表选中即跳页影响复制、明细选择绑定、窄窗口明细区高度及竞态测试假阳性风险。
- 既有测试只更新“历史入口 disabled”与“排查记录”旧文案断言，未放宽其余 Stage 4 门禁。

## Sol 独立技术门禁

| 门禁 | 结果 |
|---|---|
| S5-T03 `S5T03InspectionHistoryViewModelTests` | 10/10 |
| S5-T02 `InspectionHistoryEditUseCaseTests` | 16/16 |
| S5-T01 `InspectionHistoryQueryTests` | 9/9 |
| Stage 4 八个权威测试类 | 179/179 |
| Stage 3 十个权威测试类 | 170/170 |
| Release 全量 `dotnet test StoreExpiryInspector.slnx -c Release --no-restore` | 567/567，0 失败、0 跳过；包含 S4-T10 静态审计 |
| Release build `dotnet build StoreExpiryInspector.slnx -c Release --no-restore` | 0 warning / 0 error |
| EF `migrations has-pending-model-changes` | 无模型漂移 |
| migration 源码 / EF list / 正式库历史表 | 均为 8 |
| dependency diff / 已解析依赖 | 无变更 |
| Application / Domain / Infrastructure / Migrations diff | 空 |
| `git diff --check` | 通过 |

专项按 `FullyQualifiedName~测试类名` 过滤，使用 Release 配置。Stage 4 精确类为：`InspectionTaskQueryTests`、`InspectionDraftUseCaseTests`、`ManualInventoryAdjustmentUseCaseTests`、`InspectionSubmissionUseCaseTests`、`Stage4ViewModelTests`、`S4T06ImportViewModelTests`、`S4T07InspectionDetailViewModelTests`、`S4T08InspectionSubmissionViewModelTests`。

Stage 3 精确类为：`ExpiryStageCalculatorTests`、`ProductTaskAggregationTests`、`StartupRecalculationTests`、`ProductStockZeroLifecycleUseCaseTests`、`PostImportLifecycleUseCaseTests`、`BatchCheckedZeroLifecycleUseCaseTests`、`ConfirmedImportLifecycleOrchestratorTests`、`S3T07CombinationEvidenceTests`、`S3T07StartupEvidenceTests`、`ApplicationStartupCoordinatorTests`。170/170 是本次可复现计数，沿用 S5-T01 / S5-T02 的历史计数说明，不改写 Stage 4 历史档案。

EF 使用应用项目、`--configuration Release --no-build`；migration list 另加 `--no-connect`，其“不显示 Pending 状态”提示不是连接正式库失败的验收结论。正式库实际已应用数量通过独立只读 SQL 核对。dependency diff 相对开始基线为空，`dotnet list package --format json` 的解析版本不变；没有把离线版本检查宣称为在线漏洞审计通过。

## 数据核验与 GUI 前置条件

- 开始时正式库 `app.db` 为 299008 bytes，SHA-256：`F3D423DF14B882D7BFE87780A81CF5879F074AF4880601CBEDB6B475A964F522`。
- 在无 WAL 文件、应用未启动的基线上以 SQLite `mode=ro&immutable=1` 查询：Inspection / InspectionItem / Revision = 0 / 0 / 0；migration = 8；`integrity_check = ok`。
- 正式库只能用于空历史 GUI 验证。详情和 Revision 链 GUI 验收需要具备对应记录的测试数据库；本任务不得向正式库植入演示数据。
- 技术验收后正式库 SHA-256 与开始值一致；没有启动 WPF，没有覆盖正式数据，没有准备未经授权的演示库。

## 用户 GUI 待验收清单

1. 从现有导航进入“排查历史”。
2. 列表信息可读，条码 / 商品编码清楚且可复制。
3. 查看记录可见完整正式详情、全部明细与当前正式数量。
4. 有 Revision 的明细可见修改前数量、修改后数量、修改时间。
5. 无 Revision 的明细显示“无修改记录”，无虚假初始项。
6. 视觉与 Stage 4 一致；没有编辑、保存修改、回滚、删除或撤销入口。
7. 1024×600 / Windows 125% 下可操作，滚动与键盘选择可用。

## 停止门禁

技术门禁通过后停止等待用户 GUI 验收。只有用户明确反馈 GUI 通过，才可正式完成和归档 S5-T03；不得创建、编号、派发或实施 S5-T04。

## GUI 首轮失败与原任务修复（2026-08-30）

- 用户反馈：点击“查看详情”无响应并闪退；随后明确批准原 S5-T03 Luna 最小修复、Sol 独立复验与用户 GUI 重验。不创建新任务或代理。
- Windows Application 日志 1026：16:55:45、16:56:18 两次出现 `InvalidOperationException`，提示父级 DataGrid 的 `SelectionUnit` 不允许选择行；事件 1000 指向本次隔离验收 Release 程序。
- 调用链：`OpenDetail` 通知 `SelectedRecord` → 列表 `SelectedItem` 双向绑定 → `DataGridRow.OnIsSelectedChanged`。列表同时设置 `SelectionUnit="Cell"`，因此在详情查询前发生未处理异常。此前 ViewModel 和静态测试没有拦截这一真实 WPF 绑定冲突；上方 567/567 是首次技术门禁的历史结果，不是本次修复后的结论。
- 本次仅修复历史列表选择绑定及对应回归，不改 Stage 3/4 权威逻辑、schema、migration、依赖或历史编辑能力。
- 隔离样本和崩溃现场保留，未启动 WPF 代验。正式源库旁置保护，共享保护副本位于 `obj/S5T03GuiAcceptance/ProtectedProduction/app.db`；两者均核验为 299008 bytes 和指定 SHA-256。用户本机与工具侧 C 盘目录视图不一致，工具侧失败 Junction 尚未清除；不能将旁置保护核验误报为正式运行环境已恢复。GUI 重验后另行确认实际运行进程为 0、精确清理隔离数据、恢复并核验正式环境，恢复后不再启动。

### 修复交付与 Sol 独立复验

- 修复提交：`4341c213dc0377a661992aecdde93ffead034b7d`；仍由原 S5-T03 Luna 执行，Sol 审查、独立验收并提交。
- 生产代码只删除历史列表 `SelectedItem` 双向绑定一行，保留 `SelectionUnit="Cell"` 和复制配置。按钮仍将当前记录作为 `OpenDetailCommand` 参数；明细表 `FullRow` 选择与 Revision 加载不变。未改变样式、键盘入口、Application、Domain、Infrastructure、Migrations 或依赖。
- 强化现有静态契约断言，禁止单元格选择列表再绑定 `SelectedItem`；现有 ViewModel 测试改为不预置 `SelectedRecord` 的按钮参数导航，并覆盖返回后再次打开。不新增测试框架，没有将静态/VM 检查冒充真实 WPF GUI 通过。
- Sol 在已编译新测试上临时恢复旧绑定，用 `--no-build --no-restore --filter FullyQualifiedName~HistoryViewIsReadonlyAndKeepsCopyableDataGrids` 验证 1/1 失败，失败点确为新增 `SelectedItem` 禁止断言；随后恢复修复版并重新构建、运行全部测试。

| 本次独立门禁 | 结果 |
|---|---|
| S5-T03 / S5-T02 / S5-T01 | 10/10、16/16、9/9 |
| Stage 4 八个权威类 | 179/179 |
| Stage 3 十个权威类 | 170/170 |
| Release 全量 | 567/567，0 失败，0 跳过 |
| Release build | 0 warning / 0 error |
| EF 模型 / migration 源码及 EF list | 无漂移 / 8 |
| 正式保护副本只读 SQL | migration=8，integrity=ok，foreign_key_check=0 |
| 依赖 diff / 解析版本 | 无变更 / 与修复前一致 |
| git diff --check | 通过 |
| WPF 启动 / GUI 代验 | 未执行；用户重验仍是阻断门禁 |

测试使用前述精确类过滤，Release 配置；TRX 暂存于 `obj/S5T03GuiAcceptance/Verification`（将随验收临时数据清理）。首轮本次构建遇到 NU1900 在线漏洞服务不可达，随后仅以命令行参数 `dotnet restore StoreExpiryInspector.slnx --ignore-failed-sources -p:NuGetAudit=false` 刷新构建资产再验收；未修改工程或包版本。0 warning 是后续离线构建结果，不是在线漏洞审计通过。EF list 使用 `--no-connect`，不把该模式的 Pending 提示当作正式库连接结果。

### 隔离重验版本

- 已将本次 Release EXE / DLL / PDB 同步到 `obj/S5T03GuiAcceptance/Release` 并逐文件比对，启动脚本 DLL 哈希门禁同步更新；未重播种或修改用户本机隔离库。
- DLL SHA-256：`0FB1675D079E86DF9B7D0C0F598A134ECE09304B72E917F2E059B01F0CB7DB6C`；EXE SHA-256：`CC24680A9370722A84B0D3577BA835E47EFDD7DDE2B985BA1637F74BFFAB3674`。
- 种子库 SHA-256 仍为 `3733039A24A377512B103CD4CB1C077AB80672ABC2AF37AA46625C1F12417926`；正式保护副本仍为指定 299008 bytes / `F3D423DF14B882D7BFE87780A81CF5879F074AF4880601CBEDB6B475A964F522`。
- 更新后启动脚本 `-SelfTest` 通过新建、重复打开、已有未标记数据拒绝、只读检查不创建目录测试；只在共享临时目录执行，没有访问默认运行路径或启动应用。检查时应用进程为 0。
- 用户仍使用 `START-S5-T03-GUI.cmd`，重点重验查看详情、返回后再次打开、3 条明细、2→5→3 链、无修改记录和单元格复制。若再失败，保留现场并反馈，不删除数据或启动正式副本。收到明确通过后才处理正式恢复和归档；不创建 S5-T04。
