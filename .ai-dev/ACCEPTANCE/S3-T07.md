# S3-T07 验收记录｜Stage 3 最小运行编排与真实入口接入

- 验收状态：通过
- 验收日期：2026-08-27
- 任务卡提交：`1cca86032eb855dcc79d6968c401c437bdbf013e`
- Luna 实现提交：`313596fd250931e96dda79eedd366abdc881c493`
- Luna 组合证据补充提交：`dd1a83b87082d80990a4ff2655788ecde91a3eca`
- 验收模型：GPT-5.6 Sol

## 实现与事务事实

- `ConfirmedImportLifecycleOrchestrator` 在同一外层 SQLite 事务中组合既有 Stage 2 执行器与 S3-T04/S3-T05；后置失败会撤销 Import、Workbook、BackupRecord、Issue、Product/Batch 导入变化及全部 Stage 3 状态，不留下 Succeeded 半成品。
- `ConfirmedImportExecutor` 只增加 12 行事务所有权适配：无外层事务时保持原有自有事务，有外层事务时不抢先 commit/rollback；没有加入任何 Stage 3 规则或引用。
- `ExplicitProductStocks` 只冻结 Planner 已判定合法且明确出现的库存事实；空白、非法与冲突不产生明确 0。本次未出现商品/批次不进入后置查询或处理。
- 明确库存 0 只调用 S3-T04；否则在当前有效库存大于 0 时调用 S3-T05。真正 new/arrival/恢复使用 ImportPlan 与执行前冻结事实，不从导入后相等值反推。
- `ApplicationStartupCoordinator` 在单一事务中组合 S3-T03 与 `LastNormalRunDate`；日期回拨直接跳过补算且不修改状态，正常成功后原子更新运行日期。
- `App.xaml.cs` 只在 WPF 边界取得系统日期/UTC、初始化数据库、调用启动 Coordinator 并使用既有本地日志；Domain/Application 没有新增系统时钟读取。

## 独立验证证据

- S3-T07 四组专项与组合证据：23/23 通过。
- S3-T01～S3-T06 精确回归：147/147 通过。
- Stage 3 合计精确证据：170/170 通过。
- Release 全量：348/348 通过；Release build：0 警告、0 错误。
- EF `has-pending-model-changes`：无模型变化；migration 仍为既有 8 条。
- `git diff --check` 通过；实现提交工作区干净。

## 真实 WPF 入口验收

- Sol 直接启动 Release `StoreExpiryInspector.exe`，唯一主窗口标题为“门店效期排查软件”，主窗口正常显示，无白屏或启动阻断。
- 默认用户数据目录启动前不存在数据库，本次创建全新空库；实库查询确认 8 条 migration 已应用、20 张表存在、`app_state.last_normal_run_date = 2026-08-27`。
- 本地日志实际写入 `startup_recalculation_completed`；验收后主窗口正常关闭。
- 本次真实启动未使用既有门店业务数据库，也未修改用户历史数据。

## 架构债检查

- `ConfirmedImportExecutor.cs` 当前 1,154 行，较基线只增加事务组合适配，Stage 3 状态机未进入该类。
- `ConfirmedImportLifecycleOrchestrator.cs` 467 行，职责限于定向冻结/解析事实、调用两个既有 UseCase 与统一事务；虽偏长但未复制效期、任务、归零、新到货或恢复规则，当前拆分只会增加无收益抽象。
- `ApplicationStartupCoordinator.cs` 86 行；`App.xaml.cs` 46 行。入口没有 EF 查询或业务状态机。
- 未发现新 God Service、Repository/UnitOfWork、EventBus/Outbox、通用状态机/工作流、第二套阶段算法、依赖、实体、字段或 migration。

## 结论

S3-T07 正式验收通过。真实导入后的原子生命周期处理与真实应用启动补算入口已经串通；S3-T01～S3-T06 仍是业务规则的唯一权威实现。S3-T06 未被错误接入，没有提前实现 Stage 4 正式排查提交。

下一步只允许执行 Stage 3 整体验收、架构债检查和交接归档；不得创建 Stage 4 任务卡。
