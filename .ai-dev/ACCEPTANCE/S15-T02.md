# S15-T02 验收边界

当前状态：`GUI_ACCEPTED / CLOSED`

## 最小专项

1. 多行 ReleaseNotes 完整进入 ViewModel。
2. 超过 1000 字符的 ReleaseNotes 不被客户端截断；控制字符清理与 metadata 256KB 整体上限保持。
3. 初始窗口存在“本次更新”区域，完整内容进入固定最大高度的内部滚动区。
4. ReleaseNotes 为空或仅空白时，“本次更新”标题和内容区全部隐藏，不显示空白占位。
5. 不重复显示 DiagnosticBanner；客户端不摘要、删项、关键词过滤或改写 Release 内容。
6. S15-T01 的初始取消隐藏、下载百分比、更新中、安装中及安全取消边界不回归。

## 范围与门禁

- diff 仅限 `GitHubReleaseUpdateChecker.cs` 的 1000 字符截断、ViewModel 只读 ReleaseNotes、真实更新窗口滚动区及直接测试。
- 不改变更新检查、下载、Updater、安全协议、状态机、数据库或业务逻辑；不新增依赖、协议、服务、migration 或字段。
- 只跑直接专项和必要 Release App build；`FULL = NOT_RUN / NO_FULL`。

## 人工 GUI 门禁

技术验收通过后，用户只检查“发现新版本”一屏：出现“本次更新”、全部模拟说明可见或可滚动、版本与按钮正常、窗口未被长说明异常撑大。S15-T01 三阶段 GUI 证据直接继承，不重复要求。

## Terra 实施证据

- implementation / remote SHA：`0afddbdd5cb42f1f7f9d02ca84d1267842a30e00`；remote branch：`origin/codex/s15-t02-terra`；worktree clean。
- diff：`GitHubReleaseUpdateChecker.cs`、`UpdateNotificationViewModel.cs`、`WpfDialogService.cs`、`S9T03UpdateCheckTests.cs`、`S15T01UpdateProgressUiTests.cs`；scope check `PASS`。
- 精确专项：`10/10 PASS`。覆盖多行与 >1000 字符尾部、控制字符、256KB 上限、完整滚动区、null/空/空白隐藏、DiagnosticBanner 不出现及 S15-T01 核心状态。
- Release App build：`0 warning / 0 error`；本次 `NU1900 = NONE`。
- 过程说明：一次过宽 S9T03 类组合过滤为 `29/30`，唯一未过为既有 synthetic WPF core-ready 27 秒超时；未重试该非门禁项，随后精确专项全部通过。
- `FULL = NOT_RUN / NO_FULL`；正式数据库访问 `NO`。
- Terra 提交时技术评审为 `NOT_RUN / WAITING_SOL_REVIEW`，GUI 为 `NOT_RUN`，实现当时尚未合并 main。

## Sol 技术接受与 main 集成

- Sol 独立真实 diff 审查：`TECHNICAL_REVIEW = PASS`；返修项 `0`。
- Terra SHA：`0afddbdd5cb42f1f7f9d02ca84d1267842a30e00`；integration SHA：`f3cb2451442e5dba540796743f038072be7fd32c`；cherry-pick 无冲突，生产代码等价。
- targeted：`INHERITED 10/10 PASS / NOT_RERUN`。
- Release build：`INHERITED 0 warning / 0 error / NOT_RERUN`。
- Full：`NOT_RUN / NO_FULL`；正式数据库访问：`NO`。
- 过宽 S9T03 `29/30` 的唯一 synthetic WPF core-ready 27 秒超时：`NON_BLOCKER / NOT_RERUN`。
- 技术接受阶段 GUI 为 `NOT_RUN / WAITING_USER_GUI_ACCEPTANCE`；实现随后已随治理提交进入 main。

## 用户 GUI 最终验收

- 用户回执：`PASS / GUI_ACCEPTED`。
- “本次更新”区域显示正常。
- 完整门店版更新说明显示正常，长内容滚动正常，窗口尺寸正常。
- 当前版本/最新版本及“稍后提醒 / 立即更新”正常。
- technical review `PASS`、targeted `10/10 PASS`、Release build `0 warning / 0 error` 为继承证据；`FULL = NOT_RUN / NO_FULL`；正式数据库访问 `NO`。
- 本轮 `NOT_RERUN / governance-only`。S15-T02 = `GUI_ACCEPTED / CLOSED`。
