# S4-T03 独立验收记录｜手工库存修正与商品归零编排

- 验收日期：2026-08-28
- 结论：通过
- 任务卡提交：`016e6305d9b3b426014502aeca689142b02fd0eb`
- 实现提交：`7e4ad06c47c79d8bf29e4e4f779cebf8f3c71e80`
- 验收补证提交：`1e03e7da474b34121e7f51ecc49af3a434e16306`
- 执行：GPT-5.6 Luna（max）
- 独立审查与验收：GPT-5.6 Sol

## 一、交付范围

本卡只新增：

- `src/StoreExpiryInspector/Application/ManualInventoryAdjustmentUseCase.cs`
- `tests/StoreExpiryInspector.Tests/ManualInventoryAdjustmentUseCaseTests.cs`

未修改 WPF/ViewModel、实体、EF Configuration、DbContext、migration、ModelSnapshot、项目依赖、Stage 2/3 生产代码或 `ConfirmedImportExecutor`；未实现正式 Inspection/提交、S3-T06、历史编辑、HandledAttentionVersion 或提交后 Draft 处置。

## 二、业务与事务事实

- 请求只接受 ProductId、已解析非负整数、0 库存显式确认和 UTC 时间；0 确认门禁先于同值判断。
- 每次真实变化创建 InventoryAdjustment，ExcelStockQtySnapshot 取 Product 当前 ExcelStockQty；连续 10→8→6 的两条历史均保留 Excel 10 快照。
- Product 只更新 EffectiveStockQty、EffectiveStockSource=`manual`、UpdatedAtUtc；同值返回 NoChange，不创建历史或更新时间。
- 正库存修正不调用 S3-T05/S3-T06，不改变 Batch、AttentionVersion、Task、Draft、Inspection 或 LifecycleEvent，也不把库存上升解释为新增到货。
- 0 库存先在当前事务保存 Adjustment 与 Product=0，再唯一调用 S3-T04，并把本次 AdjustmentId 作为 SourceAdjustmentId；S3-T04 继续复核来源存在、同 Product 且修正值为 0。
- Product/Batch/Task/Draft/LifecycleEvent 的归零状态全部由 S3-T04 产生，S4-T03 没有复制状态机。
- 自有事务失败整体 rollback 并清空 tracker；存在外层事务时不抢占提交/回滚，调用方最终 rollback 可撤销正数及 0 库存全部变化。

## 三、独立审查补证

Sol 初审确认生产实现边界正确，但原专项只证明手工恢复正库存不会创建 Batch，未实际证明后续真正新批次仍由 S3-T05 接管，因此未直接放行。

补证使用真实 SQLite 完整执行 `手工归零 → 手工恢复正库存 → Stage 2 new Batch 事实 → S3-T05`：旧 Batch 保持 `stopped/product_stock_zero` 和旧 generation，新 Batch 由 PostImportLifecycleUseCase 进入 Product 当前 generation 并生成对应任务；S4-T03 未创建或启动 Batch。

## 四、独立验证结果

- S4-T03 真实 SQLite 专项：30/30，通过。
- S4-T02 草稿专项回归：30/30，通过。
- S4-T01 查询专项回归：10/10，通过。
- Stage 3 十个权威测试类精确回归：170/170，通过。
- Release 全量：418/418，通过，0 失败、0 跳过。
- Release build：0 警告、0 错误。
- EF model drift：`No changes have been made to the model since the last migration.`
- migration：仍为既有 8 条。
- 项目文件无 diff，无新增依赖。
- `git diff --check`：通过。
- 验收归档前工作区：干净。

## 五、完整业务图与后续边界

- 正库存路径用稳定主键逐字段快照证明：除 Product 三个批准字段和新增一条 Adjustment 外，其余 Product、Batch、Task/Item、Draft/Item、Inspection/Item/Revision、LifecycleEvent 全部不变。
- 0 库存无确认、非法值、非 UTC、Adjustment insert 故障及 S3-T04 中途故障均有真实 SQLite 零写入或整体回滚证据。
- 下一次合法 Excel 增量导入会恢复 ExcelStockQty、EffectiveStockQty 与来源 `excel`，历史 Adjustment 保留，未出现商品不受影响。
- 本卡不定义 HandledAttentionVersion 或正式提交后的 Draft 处置；两项仍须在 S4-T04 创建前专项核验。
- UI/UX Pro Max 仍只用于 S4-T05 创建前的独立设计治理，本卡没有视觉实现。

## 六、阶段门禁

S4-T03 正式验收通过。当前不得自动创建 S4-T04；等待用户明确批准下一张任务卡。
