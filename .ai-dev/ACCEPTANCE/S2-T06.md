# S2-T06 独立验收记录｜确认导入事务

- 验收结论：通过
- 验收日期：2026-08-27
- 任务卡提交：`77acb975b016369be7c0a9531f462f9cfe9daf4f`
- 初始实现提交：`7f67320109facb38f1d60c3b68c26fc3131c561d`
- 事务边界修正提交：`8904c67ecc970a2c8c38d7c92f96af0bdd915715`
- 前置失败覆盖提交：`e8be71295a52be8b4aa8035fbd550c53968fe8dc`
- 执行模型：GPT-5.6 Luna（max）
- 独立验收：GPT-5.6 Sol

## 范围与实现结论

- 只新增确认导入执行器与专项测试，并将 S2-T04 文件读取辅助由 private 改为 internal；未改实体、配置、DbContext、migration、依赖、Excel 规则或 UI。
- 执行器只消费既有确认契约和 ImportPlan，不调用 Reader、Classifier 或 Planner；文件再次复核及快照成功后才开启事务。
- 单一事务写入 Succeeded Import、verified/pre_import BackupRecord、本次 Product/Batch 增量、ImportIssue 和冻结 ImportWorkbook；显式字段白名单，不使用反射或通用框架。
- 未出现及冲突数据不被写入；没有任务、草稿、排查、库存修正、效期或生命周期状态变化。

## 独立审查发现与修正

初始提交存在四项阻断：Commit 后仍可能因生产后置校验返回失败；未拒绝带已有 ChangeTracker 状态的 context；只有 Batch 动作的正常商品未更新 Product last_seen；以 BackupRecord 插入失败替代了 Commit 前故障。Luna 按要求删除提交后可失败逻辑、在快照前拒绝非干净 context、补齐 batch-only 商品留痕，并用 EF TransactionCommitting interceptor 证明真实提交前回滚。

第二轮审查又发现 S2-T06 编排层没有直接覆盖文件删除/独占与快照目标失败。最终测试补齐这三类真实执行器链路，均证明未创建正式记录且业务数据不变。

## 独立验证证据

- S2-T06 专项：16/16。
- S2-T01 至 S2-T05 回归：115/115。
- Release 全量：131/131。
- Release build：0 警告、0 错误。NuGet 资产会重放网络告警 `NU1900`，故编译门禁仅屏蔽该代码无关告警；官方源漏洞查询另行成功，两个项目均无已知漏洞。
- EF：8 条 migration；`has-pending-model-changes` 明确报告模型无变化。
- 真实样表：SHA-256 `20fe1898dba98f48bcd8b83673f2001fe3ed0dfc01a89aa4ebdff9d1af6cacce`，397,308 字节，未变化。
- `git diff --check` 通过；实现范围只有执行器、确认守卫一行可见性调整与专项测试；无 `.db`、`.db-wal`、`.db-shm` 或 `.tmp` 残留。

## 原子性与业务结论

- A/B/C 专项只导入 A；B、C 及其历史批次逐字段不变，last_seen 不变，无任何隐式状态或事件。
- 库存冲突不选值，合法 0 只保存库存事实；MaxArrivalQty 只在更大时上升，不触发恢复、任务或生命周期。
- 行问题、批次/库存冲突和规划问题由既有 Preview 映射为稳定 ImportIssue；异常行不进入正常计划，非食品只计跳过数。
- 七个数据库故障边界（含真正 Commit 前拦截）全部回滚 Import/Backup/Product/Batch/Issue/Workbook，失败 context 清理且快照文件保留。
- Commit 成功即返回成功；不存在已提交后再因额外数据库校验而向上报告失败的路径。

## 架构判断与未实现范围

执行器代码较长，但只承担 S2-T06 的契约门禁、明确字段应用与单事务编排；解析、文件内校验、diff、确认契约和快照仍由既有组件负责。私有方法使用显式白名单，没有新增接口、Repository、UnitOfWork、反射或未来框架，当前无需为拆文件引入额外抽象。

本卡未实现最近两份工作簿裁剪、撤销、恢复、效期、任务、提醒、UI 或 Stage 3+。`S2-T07` 尚未创建或派发。
