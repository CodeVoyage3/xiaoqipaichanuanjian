# S3-T06 验收记录｜正式排查0件后的批次停止生命周期用例

- 验收状态：通过
- 验收日期：2026-08-27
- 任务卡提交：`7d3175b7cbcecc3d52df4d7a680b91463151756c`
- Luna 实现提交：`3e1f51926528438ad6ec666bf3bc63b0cbcf680e`
- 验收模型：GPT-5.6 Sol
- 后续门禁：未创建 S3-T07；必须等待用户明确批准

## 实现事实

- 生产与测试 diff 只新增 `Application/BatchCheckedZeroLifecycleUseCase.cs` 和 `BatchCheckedZeroLifecycleUseCaseTests.cs`；没有实体、EF 配置、DbContext、migration、依赖、导入执行器、WPF 或前置卡生产规则变化。
- 用例同时要求请求 CheckedQty 为 0 与持久化 InspectionItem.CheckedQty 为 0，并验证 Product、Batch、Inspection、InspectionItem 存在及全部复合归属；发生时间必须为 UTC 且不早于正式提交时间。
- 首次转换只接受结构一致的 active Batch，设置 stopped、batch_checked_zero、显式停止/更新时间并清空 NextTriggerDate；CurrentStage、到货量、generation、AttentionVersion、HandledAttentionVersion 和其他历史字段保持。
- matching `batch_checked_zero + ProductId + BatchId + SourceInspectionId` LifecycleEvent 是持久化幂等锚点；精确一个 canonical event 时无副作用返回，重复或 malformed matching event 明确拒绝。
- matching event 查询先于当前 Batch 停止转换，因此 S3-T05 合法恢复后旧正式 0 请求重放不会重新停止或污染恢复版本、任务和事件。
- 只新增目标 Batch 对应的批准事件，Reason 为 batch_checked_zero，SourceInspectionId 为正式来源，其他 source 为空；不修改 Product、其他 Batch、Task/Item、Draft/Item 或 Inspection/Item/Revision。
- 自有事务统一提交/回滚；外层事务不被抢先提交/回滚。真实 SQLite trigger 覆盖事件写入故障，Batch 与 Event 整体回滚并清理 tracked 状态。

## 独立审查结论

- Sol 逐行检查正式事实验证、matching event 顺序、异常 Batch 状态、AttentionVersion 不变、MutationJournal 与外层事务路径，未发现需要返工的正确性缺陷。
- 当前 schema 没有 matching event 唯一索引；实现会在业务写入前读取最多两条并拒绝重复事实。结合 SQLite 单写事务，本卡批准的顺序重放与恢复后旧事实重放可由现有持久化事件可靠承担，无需 migration。
- 同一 InspectionItem 的 `0→正数→0` 多轮修订身份未在本卡扩展；按任务门禁保留给 Stage 5 基于 Revision 另行治理。

## 独立验证证据

- S3-T06 专项真实 SQLite：13/13 通过，0 失败、0 跳过；22 项要求通过合并场景逐项断言。
- S3-T01～S3-T05 精确分组回归：134/134 通过，0 失败、0 跳过。
- Release 全量：325/325 通过，0 失败、0 跳过。
- Release build：0 警告、0 错误。
- EF `has-pending-model-changes`：明确报告模型无变化。
- migration：仍为既有 8 条。
- `git diff --check` 通过；实现验收工作区干净。

## 组合与禁止范围确认

- 真实组合已覆盖：停止后 S3-T03 不命中；旧最高未突破时 S3-T05 不恢复；首次突破时 S3-T05 恢复并写恢复事件；恢复后旧 0 请求重放不停止；S3-T04 商品归零后 S3-T05 永不恢复旧 Batch。
- `ConfirmedImportExecutor` 未修改且没有 S3-T06/S3-T05/效期/任务聚合引用，Stage 3 状态机没有进入 Stage 2 导入执行器。
- 未创建/编辑正式 Inspection，不完成 ProductTask，不修改 Draft，不实现历史编辑恢复、WPF、提醒、Stage 4+ 或 S3-T07。
- 未创建 Repository、UnitOfWork、通用状态机/Lifecycle Engine、EventBus、Outbox、新事件类型、字段、迁移或依赖。

## 结论

S3-T06 正式验收通过。合法正式 InspectionItem 0 件事实已经能够在可组合事务中唯一停止目标 Batch、写入可审计且可幂等识别的 batch_checked_zero 事件，并与 S3-T03、S3-T04、S3-T05 的查询、优先级和恢复规则无歧义衔接。

当前继续停留在 Stage 3。S3-T07 尚未创建，等待用户下一步明确批准。
