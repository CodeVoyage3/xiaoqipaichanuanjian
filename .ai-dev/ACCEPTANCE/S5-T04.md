# S5-T04 技术验收记录｜正式排查历史修改 UI

- 日期：2026-08-30
- 当前结论：技术验收、用户 GUI 验收、正式环境恢复与隔离清理全部通过；S5-T04 正式归档。
- 用户批准：阅读并执行附件 `D:/下载/S5-T04.md`。
- 批准卡：`.ai-dev/TASKS/S5-T04.md`；与附件 SHA-256 一致：`C8562B60BE23A13AADDF4D4D1D0E7B05C42033A1B85F4E8DECEFA320AF302A3B`。
- 开工基线：`master @ 3241906d8fa7fb67a3b3d7167ff77c113b4fe40e`，clean。
- 执行：新 GPT-5.6 Luna（max），独立审查/验收/提交：Sol。未复用 S5-T03 Luna。

## 执行边界

- 只在正式历史详情中对明确选中的明细数量提供修改、确认、提交与结果反馈。
- 只调用已验收 `InspectionHistoryEditUseCase.Execute`；列表、详情、Revision 继续复用 `InspectionHistoryQuery`。
- 不改 Application / Domain / Infrastructure、Stage 3/4 权威行为、schema、migration、依赖、全局主题；无回滚/删除/撤销、批量修改或 S5-T05 / Stage 6。
- Ponytail 用于沿用已有委托、RelayCommand、ViewModel 与 xUnit；UI/UX Pro Max 用于现有 WPF 风格下的可见标签、确认、禁用/忙碌反馈及窄窗口可读性。不采用其通用 Toolkit/新依赖或新设计系统建议。

## 开工只读核验

- Release 全量基线 567/567，0 失败、0 跳过；输出 `obj/S5T04Verification/Baseline/baseline.trx`。这是开工基线，不等于修改后的验收通过。
- EF `has-pending-model-changes` 无漂移；`migrations list --no-connect` 为 8 条；包引用和本地解析的直接/传递依赖基线已记录。
- 沿用已知离线 NU1900 限制，仅执行 `dotnet restore StoreExpiryInspector.slnx --ignore-failed-sources -p:NuGetAudit=false`，未改工程或依赖，不宣称在线漏洞审计成功。
- 应用进程为 0。工具可见正式旁置原库为 299008 bytes / SHA-256 `F3D423DF14B882D7BFE87780A81CF5879F074AF4880601CBEDB6B475A964F522`；SQLite `mode=ro&immutable=1` 核验 migration=8、integrity=ok、foreign_key_check=0。未用于写入测试。
- 用户本机实际运行库恢复证据仍是 `.ai-dev/ACCEPTANCE/S5-T03-RESTORE-RESULT.json`；工具侧默认 C 盘旧 Junction 与该视图不同，不能把旁置源库检查冒充重新访问本机正式路径。本任务不修复该链接、不启动 WPF、不用电脑操控 GUI。

## 独立审查关注点

- 固定请求 InspectionId / InspectionItemId / 新数量；确认期间及提交期间均防重入、切换目标、刷新及 Shell 导航，公开方法也必须守卫，不能只依赖 `CanExecute`。
- 同值也调用后端并刷新正式结果；使旧查询失效后必须终止旧 Loading 状态。
- 修改事务成功与后续查询成功区分：保存已完成但刷新失败时明确提示，不能伪装修改失败或新 Revision。
- 结果提示不得位于随编辑结束或详情失效而隐藏的父容器；表格仍只读，条码/编码可复制，既有列宽和选择绑定修复不得回退。

## 实现与独立技术验收

- 实现提交：`5818379d751c1aa479489d5febcad6208dd92e9b`。
- 正式历史详情仅增加单条明细数量编辑；输入仅接受非负整数，提交前显式确认，确认及提交期间固定 Inspection / InspectionItem 目标并禁止重入、切换、刷新和 Shell 导航。
- changed / no_change / not_found / 异常分别反馈；changed 与 no_change 均从数据库重读详情和 Revision。写入成功但刷新失败时明确区分，绝不伪造当前值或 Revision。
- Luna 专项与 Sol 独立专项均为 S5-T04 16/16；Sol 另以真实临时 SQLite 验证 changed 后产生 Revision、同值不新增 Revision。S5-T03 10/10、S5-T02 16/16、S5-T01 9/9，合计 35/35。
- Sol 反向验证：临时破坏“同值仍重读正式数据”后对应测试 0/1；临时把结果提示放回可隐藏父容器后对应测试 0/1。两项错误均被测试拦截，随后恢复正确源码，并以恢复前哈希核对一致。
- Stage 4 八个权威测试类 179/179；Stage 3 十个权威测试类 170/170；Release 全量 583/583，均 0 失败、0 跳过。
- Release build：0 警告、0 错误。EF `has-pending-model-changes`：无模型漂移；migration 列表仍为 8 条。
- 工程文件、Application / Domain / Infrastructure / Migrations 均未修改；直接及传递包解析版本与开工记录一致，无依赖变更。`git diff --check` 通过（仅 Git 行尾提示）。
- 技术验收后只读核验：旁置正式原库 299008 bytes，SHA-256 `F3D423DF14B882D7BFE87780A81CF5879F074AF4880601CBEDB6B475A964F522`；应用进程 0。未启动 WPF、未对正式库写入测试数据。
- 关键 TRX：`obj/S5T04Verification/Sol/s5-t04.trx`、`stage5-regression.trx`、`stage4-regression.trx`、`stage3-regression.trx`、`release-all.trx`；两项反向验证分别为 `mutation-no-change.trx` 与 `mutation-feedback.trx`。

## 范围审计

- 不存在范围越界：没有改后端契约、schema、migration、依赖或 Stage 3/4 业务行为；没有批量编辑、删除、回滚、生命周期联动、S5-T05 或 Stage 6。
- 技术实现完成时曾停止在用户 GUI 门禁前；后续隔离环境准备、用户验收与恢复结果见下方归档段落。

## 用户 GUI 门禁（归档前历史）

- 技术验收后用户本人验证修改、Revision、同值、非法输入、取消、切换目标与 1024×600 / 125% 可操作性。
- S5-T03 隔离库已按批准清理；正式库不得用于测试。当时在收到用户批准前未准备 S5-T04 隔离环境，也未提供正式库启动指令。
- 未收到用户明确 GUI 通过前，不正式完成或归档 S5-T04；不得创建、编号、派发或实施 S5-T05。

## 用户 GUI 通过与恢复归档（2026-08-30）

- 用户本人通过隔离启动脚本运行本次 Release，明确反馈“通过”并提供截图；Sol 未启动或操控 WPF 代验。
- 截图确认正式历史详情可见，用户把无 Revision 明细的正式排查数量从 4 修改为 7，当前数量刷新为 7，并出现一条 `4 → 7` Revision；现有三明细、表头、选择及修改入口均正常显示。用户的“通过”同时接受此前提供的同值、非法输入、取消、防重入、返回/复制和窄窗口验收清单。
- 启动脚本在切换前核验正式库为 299008 bytes / SHA-256 `F3D423DF14B882D7BFE87780A81CF5879F074AF4880601CBEDB6B475A964F522`，将其旁置保护后才部署隔离种子；种子库 migration=8、integrity=ok，包含 1 条正式历史、3 条明细和 Revision 数量 2/0/0。正式库未写入演示数据。
- 用户关闭应用后执行恢复脚本；虽未保留终端截图，但共享原始回执为 `RESTORE_PASS`。回执时间 `2026-08-30T11:11:12.7543090Z`，正式运行路径 `C:\Users\39037\AppData\Local\StoreExpiryInspector`，数据库 299008 bytes / 指定 SHA-256，`ProcessCount=0`、`IsolatedRuntimeRemoved=true`、`ProtectedStagingRemoved=true`、`ApplicationStartedByScript=false`。
- 原始回执持久化为 `.ai-dev/ACCEPTANCE/S5-T04-RESTORE-RESULT.json`；共享回执原文件 SHA-256：`BC1F59B88E85B9E89D1ACB7B4B876A2E6DB42FE8FEA3AEB7BF6D8C49B6EBE877`。
- 工具侧默认 C 盘目录仍显示历史遗留异常 ReparsePoint，能读取大小及确认无隔离标记/WAL/SHM/journal，但直接哈希仍报 Windows 649；没有尝试强删或修复。用户本机恢复结论以刚执行的本机脚本回执为准，与 S5-T03 已记录的跨视图限制一致。
- Sol 在固化回执后精确清理共享 `obj/S5T04GuiAcceptance`；生产代码、schema、migration、依赖均未再次修改，恢复后未再次启动应用。技术测试结果沿用本记录前述独立门禁，不冒充本轮重跑。
- S5-T04 正式完成并归档。停止；不得创建、编号、派发或实施 S5-T05，等待用户下一步明确批准。
