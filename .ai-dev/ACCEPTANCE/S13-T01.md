# S13-T01 验收规范

S13-T01 = REAL_V104_TO_V105_GUI_FAILED / NORMAL_LAUNCH_FIX_TECHNICALLY_ACCEPTED / V106_HOTFIX_CANDIDATE_PENDING / NOT_ACCEPTED
默认 `NO_FULL`。stable v1.0.5 已发布；真实 v1.0.4 -> v1.0.5 在安装后自动启动新版处失败。same-schema normal-launch hotfix 已获 Sol 技术接受，但 v1.0.6 候选、发布与用户真实升级验收均未开始。

## H. v1.0.4 -> v1.0.5 事故与 v1.0.6 hotfix

- [x] 用户真实回执：更新提示、下载、安装程序启动、安装完成与手动启动 v1.0.5 PASS；安装后自动启动新版 FAIL。
- [x] same-schema `Committed` 成功路径复用既有 normal-launch intent/token、进程身份、candidate tree、Loaded/ACK 与超时清理。
- [x] 仅完整绑定的 same-schema intent 可与 schema evidence 区分；畸形、错绑或 schema-bound evidence 仍 fail-closed。
- [x] Sol 独立专项 `43/43`、相邻高风险 `8/8`；App/Updater Release build 均 `0 warning / 0 error`。
- [x] EF `NO_MODEL_DRIFT`；migration 9；App/Updater PE subsystem 2；diff-check、限定 secret scan 与范围门禁 PASS。
- [x] 所有动态证据仅使用隔离 worktree 与 TEMP/GUID；未访问正式安装或正式数据库；`NO_FULL`。
- [ ] v1.0.6 候选生成、签名、独立候选门禁与发布。
- [ ] 用户真实 v1.0.5 -> v1.0.6 GUI/升级验收。

## A. 状态与 24 小时策略专项

- [ ] 当前已是可信 latest：正常进入业务。
- [ ] 成功可信确认更高 stable：先 durable 写入 `FORCED_UPDATE_REQUIRED`/RequiredVersion，再触发 GUI。
- [ ] 强制状态跨 App/Windows 重启语义保持。
- [ ] 已知新版后断网/GitHub 暂时失败：仍阻断，不降级到 24 小时。
- [ ] 无强制/安全阻断、最近成功检查 <24h、临时网络失败：允许业务并轻提示。
- [ ] 无强制/安全阻断、最后成功检查 >24h、仍网络失败：阻断，仅重试/退出。
- [ ] 失败检查不刷新 LastSuccessfulCheckUtc；可信成功检查刷新。
- [ ] 从未成功检查且合法首次基准 <24h：仅首次有限宽限；>24h：要求联网。
- [ ] 普通重启、反复失败、单个状态/anchor 文件删除不能续期；状态损坏、schema 异常、字段矛盾 fail-closed。
- [ ] `currentUtc < LastObservedUtc` 的时间回拨不能增加宽限，要求可信联网重验。
- [ ] signature、public-key identity、manifest identity/format/missing、hash、size、protocol、release/tag/version、source、target migration、archive 安全错误均不走网络宽限。
- [ ] 已持久化安全阻断后，下一次普通网络失败不能解除。

## B. 强制 GUI、托盘与运行期专项

- [ ] 强制窗无“稍后提醒/忽略/跳过/取消后继续”，只允许立即更新或退出；右上角关闭不能恢复业务。
- [ ] 主窗口、全部导航/业务命令、导入、今日/待排查/历史/设置、数据修改均不可操作。
- [ ] 关闭主窗不能转入可操作托盘；托盘“显示主窗口”仍回到强制门禁；退出允许。
- [ ] 更新失败/网络中断仍保持强制态，只可重试/退出。
- [ ] 启动门禁运行于普通业务初始化/开放之前，但 Stage9 recovery/verification/ACK/rollback/maintenance/schema/Updater recovery 优先。
- [ ] 启动检查与单一 6 小时周期检查可控验证；长期不关闭程序仍能发现新版；无重复并发/分钟级轮询。

## C. 合法升级路径专项

- [ ] 当前可直达 latest（例 1.0.5 -> 1.0.6）。
- [ ] 需中间节点（例 1.0.5 -> 1.0.7 -> 1.0.9）。
- [ ] 多条合法路径按冻结 BFS/版本次序确定性选择。
- [ ] draft、prerelease、无 manifest/signature、签名错、source/migration 不匹配、protocol 不支持或 package identity 错的 Release 不入图。
- [ ] 无合法路径 fail-closed，提示官方升级方式；不硬装 latest、不自动 Setup。
- [ ] 只下载/审计当前下一跳，不预下载所有历史包。
- [ ] 首次点击立即更新持久化 AutoContinue；一跳后重启、先完成 ACK/recovery，未到目标则自动继续且不再询问。
- [ ] 网络中断时 AutoContinue 不开放业务；恢复后重新验证并继续。
- [ ] 最终可信确认当前==required/latest 后，原子清除 ForcedUpdateRequired、AutoContinue、RequiredVersion，再开放业务。

## D. v1.0.5 v1.0.4-only 发布门禁

- [x] 产品范围：撤销 v1.0.2/v1.0.3 -> v1.0.5 生产兼容要求；历史环境阻断不记 compatibility FAIL。
- [x] 最终 production-signed manifest 为 `minVersion=maxVersion=1.0.4`，source migration min/max 严格为 production migration 9。
- [x] 最终候选的 manifest/signature/ZIP、App/Updater 1.0.5.0、Updater GUI subsystem 2 与 migration 9 通过独立资产门禁。
- [x] stable v1.0.5 已发布，四项公开匿名下载资产与冻结候选逐字节等价，tag/source 与 v1.0.4 未改写门禁通过。
- [ ] 用户使用正式 v1.0.4 App 与其自带 Updater 完成真实升级六项回执。

## E. focused / 回归 / 静态门禁

- [ ] 新 S13 状态机、持久化、路径、周期、GUI/托盘专项全部通过。
- [ ] 直接相关 S9-T03 update checker、S9-T04 signed package/manifest/source range、S9-T06 pending recovery/入口及必要 Stage9 upgrade-safety focused 通过。
- [x] `git diff --check`、scope review、secret scan PASS；无 v1.0.4 Release/tag/资产改动。
- [x] Release build 0 warning/0 error；EF `NO_MODEL_DRIFT`；migrationCount=9。
- [x] App/Updater candidate publish 身份正确；Updater PE subsystem `2 (Windows GUI)`。
- [x] 本轮自动化使用 TEMP/GUID isolated data root，未探测或访问正式数据库/安装根/数据根/备份。

## F. full 裁决

当前治理判断：`NO_FULL`。只有冻结 diff 触及 Updater 核心状态机、rollback、journal、maintenance、candidate ACK、pending recovery 核心、Backup/Restore、production Schema/migration、Import 核心事务、Task generation 核心算法，或 targeted 出现无法解释的跨模块回归，Sol 才书面重评。若需要，只在最终冻结后执行一次 fresh unfiltered Release full；开发阶段不得反复 full。

## G. 发布与用户 GUI（后续独立授权）

- [x] v1.0.5 最终候选已生成、签名并技术接受；这不等于发布。
- [x] 发布前 D 区 v1.0.4-only 最终候选门禁已关闭。
- [x] v1.0.5 已经明确授权发布；公开资产等价验收 `PASS`。
- [ ] 用户最终仅真实验证 v1.0.4 -> v1.0.5：modal 阻断主界面；点击更新正常启动；全程无黑色控制台；自动重启到 v1.0.5；原数据正常；再次启动仍为 v1.0.5。
- [ ] 24 小时状态机、跨重启、回拨、路径图、安全错误等已自动化证明的内容不要求用户重复手工复现。

说明：v1.0.4 -> v1.0.5 不要求用户确认“无稍后提醒”，因为该入口 UI 属于 v1.0.4。新的强制升级策略将在由 v1.0.5 发起的后续正式升级中生效。

`HISTORICAL_TEST_ISOLATION_UNCERTAINTY` 继续保留；不得把隔离证据写成正式数据结论。
