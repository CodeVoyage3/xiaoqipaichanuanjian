# Acceptance S4-T06｜数据导入 UI 与现有导入能力接通

- 任务卡提交：`03da9559248dc4eba36594169f4ced06f72f3413`
- P0 补充提交：`99a5c4fcd0a68ae52bc8608543ad49870e49fd2a`
- Luna 实现提交：`e4b495a7055adb33c945c86922f993d3a3a1c898`
- Sol 审查修正提交：`90f90fee3558614414978373d42ad29cd89b5b94`
- 实现：GPT-5.6 Luna（max）
- 独立审查与验收：GPT-5.6 Sol
- 结论：通过

## 一、交付范围

本卡把 Stage 2 已有 Reader、Classifier、Planner、Guard 和 `ConfirmedImportLifecycleOrchestrator` 接入真实 WPF 数据导入页，交付原生文件选择、解析预览、异常/冲突展示、确认导入、Loading、失败重试、成功反馈及 Dashboard/待排查列表即时刷新。

UI 未自行实现导入合法性、阶段、库存归零、Undo、Product/Batch 生命周期或任务生成规则；未直接调用 `ConfirmedImportExecutor` 或 `ImportUndoEligibilityService`。未新增实体、schema、migration、依赖、导入状态机、解析器、MVVM/导航框架或后续页面；未创建 S4-T07。

## 二、P0 文件身份门禁

- 文件选择器取消是无操作：保留原 Preview、`ImportPreviewIdentity`、确认契约和 `CanConfirm`，不重新解析。
- 接受新路径时，在新解析开始前同步清空旧 Preview/Identity/Contract/CanConfirm。
- B 解析失败后页面只显示 B 的失败，A 不恢复可确认；调用 Confirm 不会执行 A。
- B 解析成功后，当前唯一身份、确认守卫和持久化来源均属于 B。
- 版本门禁阻止 A 的迟到异步结果覆盖 B；重复确认在执行期间只进入一次。
- 独立审查发现执行阶段失败后的 Retry 曾被 `CanConfirm=false` 提前返回；已在同一确认入口修正为“重试仍重跑既有确认守卫”，未建立平行执行逻辑。

上述取消、立即失效、B 失败、B 成功、迟到 A、执行失败重试、PropertyChanged 与并发确认均有专项自动化证据。

## 三、自动化与技术门禁

- S4-T06 Import ViewModel + Stage 4 ViewModel：28/28。
- S4-T01～S4-T06 精确回归：126/126。
- Stage 3 十个权威测试类：170/170。
- Release 全量：474/474，0 失败、0 跳过；因工作区权威 Excel 样本被 WPS 独占，最终全量在同一提交 `90f90fe` 的本地干净克隆中执行，避免关闭或修改用户工作簿。
- 正式仓库 Release build（`--no-restore`）：0 警告、0 错误。
- EF model drift：`No changes have been made to the model since the last migration.`
- migration：仍为 8 条；无项目依赖变化，无 S4-T07 文件。
- `git diff --check`：通过。

在线 NuGet 漏洞审计再次失败：NuGet 服务索引 TLS/凭据不可用；干净克隆还原也明确产生 `NU1900`。未把历史审计或离线还原冒充本轮在线成功。

## 四、真实 Release WPF 验收

Sol 从最终 Release EXE 使用隔离 `%LocalAppData%\StoreExpiryInspector` 数据目录实际验收，结束后关闭应用、恢复原用户数据目录并清除隔离验收数据：

- 数据导入入口真实可用；排查记录、设置仍为 disabled。
- Windows 原生文件选择器可用；取消后 A 预览和确认能力保持不变。
- 选择非法 B 后页面切换为 B 错误且确认入口消失，未恢复 A。
- 权威样本被 WPS 锁定时显示明确读取错误与 Retry，无静默失败。
- 有效工作簿可真实预览、确认；确认期间 Loading 可见且按钮禁用，成功显示 ImportId。
- 先导入普通样本，再导入已过期批次样本；Dashboard 的最近导入、总数/过期数/紧急任务与待排查列表均无需重启即时刷新。
- 默认窗口、最大化窗口无明显裁切或无响应；当前系统 `AppliedDPI=144`（150%）下基本可用。

## 五、Stage 4 最终实机闭环保留项

- 精确 1024×600 真实截图：未完成。
- 恰好 125% 系统缩放实测：未完成；本卡实际验证为 150%。
- 在线 NuGet 漏洞审计：仍受本机 TLS/凭据问题阻塞。

以上三项继续保留到 Stage 4 最终闭环，不伪造完成，不扩入 S4-T06 开发范围。

## 六、停止门禁

S4-T06 已独立审查并归档。当前立即停止；未创建、编号、派发或预实现 S4-T07，未进入 Stage 5，等待用户下一步批准。
