# S3-T04 验收记录｜商品库存归零生命周期用例

- 验收状态：通过
- 验收日期：2026-08-27
- 任务卡提交：`3ca70dedb0393f808ceaf38b306d1a300771792c`
- Luna 初始实现：`c6cccede3d6992962bf443f9b431670e23bdcb9e`
- Luna 最终实现：`cb1dc1ee990c8d45ae246f00368c1cd0de60c1dd`
- 验收模型：GPT-5.6 Sol
- 后续门禁：未创建 S3-T05；必须等待用户明确批准

## 实现事实

- 生产与测试 diff 只新增 `Application/ProductStockZeroLifecycleUseCase.cs` 和 `ProductStockZeroLifecycleUseCaseTests.cs`；没有实体、EF 配置、DbContext、migration、依赖、导入、S3-T01/T02/T03 或 UI 变化。
- 用例只接受指定 Product，要求当前事务可见的 EffectiveStockQty 明确为 0；不读取 Excel、不查询缺失商品、不用 LastSeenImportId 推断归零。
- 首次归零把 Product 的 IsStockZeroTerminated 置 true，并以 checked arithmetic 将 LifecycleGeneration 加一；既有 Batch 保留历史代号与 CurrentStage，统一停止、使用 `product_stock_zero` 原因并清空 NextTriggerDate。
- 开放 ProductTask 原地变为 `system_closed`；有效 Draft 原地失效并保留 Draft/DraftItem 内容；正式 Inspection 与其他历史不修改。
- 仅按实际转换写 `product_stock_zero`、`task_auto_closed`、`draft_invalidated`；BatchId 为空，来源最多一个。Import 必须存在；Adjustment 必须存在、属于目标 Product 且 AdjustedStockQty 为 0。
- 自有事务统一提交/回滚；加入外层事务时不擅自提交。真实 SQLite trigger 验证 Product、Batch、Task、Draft、Event 整体回滚，外层失败后 tracked 状态恢复。

## Sol 缺陷审查与修复

- 初始实现对“已归零商品以更晚 OccurredAtUtc 重复调用”的完整终态 Batch 仍会改写停止/更新时间，违反幂等。Sol 未放行并要求修复。
- 最终实现只要 Batch 已满足 `stopped + product_stock_zero + StoppedAt 非空 + NextTriggerDate null` 就直接跳过，与新请求时间无关；新增更晚时间重复调用回归。
- Sol 同时收紧 Adjustment 来源必须实际调整为 0，并补齐 A/B/C 逐字段隔离及 Task/Draft/LifecycleEvent 真实 SQLite 约束拒绝测试。

## 独立验证证据

- S3-T04 专项真实 SQLite：22/22 通过，0 失败、0 跳过。
- S3-T03 专项回归：12/12 通过，0 失败、0 跳过。
- S3-T02 专项回归：21/21 通过，0 失败、0 跳过。
- S3-T01 专项回归：47/47 通过，0 失败、0 跳过。
- Release 全量：280/280 通过，0 失败、0 跳过。
- Release build：0 警告、0 错误。
- EF `has-pending-model-changes`：明确报告模型无变化。
- migration：仍为既有 8 条。
- `git diff --check` 通过；实现验收工作区干净。

## 禁止范围确认

- `ConfirmedImportExecutor` 未修改且未引用商品归零用例，Stage 3 状态机没有进入 Stage 2 导入执行器。
- 未实现 Excel 接入、人工库存修正 UI、真正新增到货、旧批次恢复、新批次生命周期启动、批次排查 0 停止、WPF/提醒、Stage 4+ 或 S3-T05。
- 未创建 Repository、UnitOfWork、通用状态机、Lifecycle Engine、事件工厂、EventBus 或 Outbox。

## 结论

S3-T04 正式验收通过。商品明确归零的 Product、全部历史 Batch、开放 Task、有效 Draft 与三类批准事件已形成唯一、可组合、可回滚的 Application 状态转换；库存恢复不会恢复旧 Batch。

当前仍停留在 Stage 3，不得自动创建、派发或实现 S3-T05，等待用户下一步明确批准。
