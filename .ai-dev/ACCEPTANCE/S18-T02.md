# S18-T02 Acceptance｜排查详情扫码识别、信息区紧凑化与按钮视觉一致性收口

日期：2026-09-10（Asia/Shanghai）
状态：`R2_MINOR_GUI_REWORK_AUTHORIZED / NOT_ACCEPTED`

## S18-T02-R2 最小门禁

- [ ] 左侧仅保留商品名称与商品编码，整体在现有左栏垂直居中/平衡并继续左对齐；顶部商品卡高度、中栏、右栏不变。
- [ ] 条码控件自身外围灰色矩形框不再绘制；背景自然，黑条与 quiet zone 不受影响。
- [ ] `TryEncode`、95-bit、guard、checksum、模块宽、非法降级及 ProductBarcode 数据链无 diff；BUSINESS LOGIC DIFF=`0`。
- [ ] 三处分割线、数字只读复制、排查信息、Button 模板、S18-T01、Reminder、Schema/migration、Updater/Installer、版本及其他页面无 diff。
- [ ] 只运行 R2 最小静态/UI 契约；一次 Release App build；隔离 GUI 候选启动。继承 R1 `46/46 + fixture 1/1`，不得重跑。
- [ ] Schema/migration=`NO CHANGE / 9`；正式数据库=`NO ACCESS`；FULL=`NOT_RUN / NO_FULL`。

## S18-T02-R1 小返修门禁

- [x] 顶部商品信息｜条码、条码｜当前库存、当前库存｜待排查批次之间各有一条浅灰细竖线；高度/留白贴近原型且顶部卡未增高。
- [x] 条码数字继续绑定 `Detail.ProductBarcode`，使用只读、可聚焦、可选中的无边框透明 TextBox，居中且不呈输入框视觉。
- [x] 鼠标选择和 Ctrl+C 由原生只读 TextBox 支持；保留低成本原生右键复制，不新增命令/行为/依赖。
- [x] 有效 EAN-13 图形、95-bit 模式、quiet zone、模块宽与非法降级生产逻辑无 diff；排查信息、Button 模板和其他页面无 diff。
- [x] R1 专项覆盖数字值/只读/可选中契约、三条分隔线和冻结路径；直接详情回归通过，Release App build 通过。
- [x] Schema/migration=`NO CHANGE / 9`；正式数据库=`NO ACCESS`；FULL=`NOT_RUN`。

## S18-T02-R1 Sol 独立技术验收记录

- governance=`cb02504b981b8b97d491881d7dc713b39a55e2e1`；implementation baseline 同 governance；全新 GPT-5.6 Terra / medium implementation=`bb9c4afd7ceef975752f7493bfe66d46e6ebc1ba`。
- 实际 diff 仅 `src/StoreExpiryInspector/UI/MainWindow.xaml` 与 `tests/StoreExpiryInspector.Tests/S18T02Ean13BarcodeTests.cs`；生产改动为三条复用 `TableDividerBrush` 的 1 DIP × 64 DIP 分割线，以及复用既有 `ReadOnlyIdentityTextBoxStyle` 的只读条码 TextBox。
- EAN-13 控件/编码、Detail ViewModel、Button 模板、排查信息布局、Domain、Migration/ModelSnapshot 无 diff；migration=`9`。
- Sol 独立相关专项/直接回归=`46/46 PASS`；保留 GUI fixture=`1/1 PASS`；Release App build=`0 warning / 0 error`；`diff --check=PASS`；FULL=`NOT_RUN`。
- 隔离根=`C:\Users\39037\AppData\Local\Temp\e151f2fa-f8fe-4058-a97f-d3d5f489410c`；Barcode=`6974396950994`。Release 候选已成功启动；最终视觉、鼠标复制和手机实扫仍由用户验收。

R1 技术通过后仍只能为 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`，用户只复核分割线、复制、手机扫码和紧凑布局。

## 技术验收门禁

- [x] 全新 GPT-5.6 Terra / medium / clean worktree，baseline 为治理提交后的 fresh origin/main。
- [x] diff 仅覆盖 S18-T02；无 Schema/migration/ModelSnapshot、正式数据库、Updater/Installer、版本或发布变化，migration 仍为 9。
- [x] 图形唯一编码 `ProductBarcode` 原值；不存在 `ProductCode` 替代、编造或自动修正。
- [x] 仅支持 checksum 正确的 13 位数字 EAN-13；guard、L/G/R、quiet zone、等宽模块和锐利显示正确；无新依赖。
- [x] 空、非数字、非 13 位、错误 checksum 安全降级且不阻断详情/排查。
- [x] 顶部一张卡保持左/中/右结构，左侧重复商品条码移除，现有库存与修正库存行为保持。
- [x] 排查信息、排查人、排查日期同一行/基线/垂直居中，原绑定、DatePicker、校验和草稿行为保持。
- [x] 共享 Button 模板从公共层根治；Primary normal/hover/pressed 白字，Danger 实底白字，Secondary 深字，Disabled 弱化；无逐页白字补丁。
- [x] 已静态巡检批准页面/弹窗及四个已知入口；S18-T01 `660 × 360 DIP` 布局、尺寸、文案和行为不变。
- [x] S18-T02 专项与直接相关回归通过；一次 Release App build 为 0 warning / 0 error；FULL=`NOT_RUN / NO_FULL`。
- [x] TEMP/GUID 隔离 GUI 含名称、ProductCode、有效 ProductBarcode、库存和待排查批次；Sol 已启动 Release 候选，页面 Barcode 原值=`6974396950994`。

## Sol 独立技术验收记录

- Terra implementation=`bf32c192554d3e890b3c7126adb9d5196654d289`；生产 diff 仅 `App.xaml`、`Ean13Barcode.cs`、`InspectionDetailViewModel.cs`、`MainWindow.xaml`，另有两个 S18-T02 专项/fixture 测试文件。
- 完整 95-bit 向量、13 位 ASCII/checksum、guard/L/G/R、每侧 10 模块 quiet zone、固定 2 DIP 模块与 300 DIP 居中画布均经 diff/专项核对；有效原值不变，非法值仅显示中性降级。
- `ProductBarcode` 直接沿用既有 Query/DTO/ViewModel 链；csproj/依赖、Domain、Migration、ModelSnapshot diff=`0`，migration=`9`。
- Sol 独立命令：S18-T02 条码专项 + S4-T07 Detail ViewModel + InspectionTaskQuery + S18-T01 冻结回归=`47/47 PASS`；GUI fixture=`1/1 PASS`；Release App build=`0 warning / 0 error`；`diff --check=PASS`。
- 隔离根：`C:\Users\39037\AppData\Local\Temp\a632547d-dae2-4c60-b71e-27858e6bd5ca`；商品=`S18-T02 隔离扫码验收商品`，ProductCode=`S18-T02-GUI-001`，ProductBarcode=`6974396950994`，库存=`18`，待排查批次=`1`。
- Sol 已启动该 Release 候选；本机 Computer Use 连接返回 `apps=[]`，无法读取窗口截图/可访问树，因此视觉观感与手机实扫不作 Sol 自判，留给下方用户 GUI 验收。
- 旧 UIUX-R02 的固定宽度历史断言与本卡新增条码中栏冲突；UIUX-R03 存在读取错误文件的既有断言。本轮均未修改、未纳入批准门禁。FULL、Installer、Updater、正式数据库均未运行/访问。

技术通过后状态只能为 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`。

## 用户 R1 最终 GUI 验收

- [x] A｜顶部信息区分割线已按原型补齐。
- [x] B｜条形码下方数字可鼠标选中并复制。
- [x] C｜手机扫码仍可正常识别。
- [x] D｜页面整体布局仍紧凑、无新增错位。

## 用户 R2 最终 GUI 验收

- [ ] A｜商品名称 + 商品编码不再明显上贴，下方大块空白改善，整体视觉自然。
- [ ] B｜条码图形外围单独的灰色矩形框已消失。
- [ ] C｜手机扫码仍正常。

用户本人确认前不得标记 CLOSED，不得关闭 Stage18 或准备 v1.0.9 发布。
