# S20-T01 验收

日期：2026-09-13（Asia/Shanghai）
状态：`REPAIR_IMPLEMENTED / SOL_REVIEW_PENDING / NOT_ACCEPTED`

## 待完成门禁

- 最小专项测试。
- App/Updater/MSBuild 与 publish assembly Version identity。
- Candidate `CurrentSchemaIdentity.Migrations` 与 EF `GetMigrations()` 精确相等。
- ZIP/archive audit、production RSA-PSS reverse verify、production `RevalidateForInstall=Verified`。
- 独立 ISCC 进程、持久 stdout/stderr/PID/工作目录/exit code、Setup 文件稳定后 Asset Freeze。
- 四资产及 `release-receipt.json`；`mode=NOT_FOR_PUBLICATION`、`publishAuthorized=false`。

`FULL=NOT_RUN / NO_FULL`。不重跑 GUI、fault rollback 或 migration9→10 正式 E2E，不执行任何发布动作。

## 当前技术结果

- `ReleaseCandidateBuilderTests=4/4 PASS`；`git diff --check` PASS。
- 非法 Version=`FAILED/VERSION_IDENTITY`；非法 SHA=`FAILED/CANDIDATE_SOURCE`；未知合同=`FAILED/RELEASE_CONTRACT`；真实 v1.0.9 SHA 对输入 1.1.0=`FAILED/VERSION_IDENTITY`。
- 首次完整 dry run `2de255bb-3f64-41a7-98fa-5e73a8eaeae8` 在 `PRODUCTION_REVALIDATION` 停止，`publishAuthorized=false`；ISCC/Setup/Asset Freeze=`NOT_RUN`。
- repair `80952753e7ff266f9866cba5713a6c500869ddc9` 改为直接调用生产 `RevalidateForInstall`；对失败 run 原始 ZIP/manifest/signature 定向复验=`Verified`。
- 第二次完整 dry run `441570f8-a606-4da0-aaec-868995948fc5` 在 `SCHEMA_IDENTITY` 停止，原因是 Candidate `18230c3e6013a098874426575e6a14c202fa7f7c` 不包含未来的 S20-T01 probe；该 `FAILED/SCHEMA_IDENTITY` receipt 原样保留。
- 修复后 Candidate schema probe=`1/1 PASS`：probe 从 Builder HEAD 测试程序集执行，输入历史 Candidate 单独编译的 `StoreExpiryInspector.UpdateSafety.dll`，assembly SHA256=`064b6084b240748fe3da78b3417d156babfdf38960c683afa87356c3c56a21bd`，读取 migration count=`10`、latest=`20260912083448_AdjustCatchupWindowConstraint`。
- Candidate 不含该 probe；未从 contract、PowerShell hardcode、源码正则或 fixture 获取 schema。第三次完整 dry run=`NOT_RUN`；最终四资产、ISCC exit 与 `RELEASE_CANDIDATE_READY` 仍未验收。
