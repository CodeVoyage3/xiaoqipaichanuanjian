# V1-F01 Sol 独立复核｜设计与真实数据模拟

日期：2026-09-01。原复核曾将无上限 5% 列为候选推荐；Sol 退回后由全新的 GPT-5.6 Terra（medium）补入必需上下限，Sol 已独立重跑通过。结论：**状态为 `READY_FOR_PRODUCT_DECISION`；未批准生产实现。**

## 范围与角色

- Sol 先创建 `.ai-dev/TASKS/V1-F01.md`，再由全新的 GPT-5.6 Terra（medium）执行。
- Terra 只提交四份分析 / 设计文档和一份只读汇总脚本；Sol 独立阅读全部交付并重跑脚本。
- 本卡没有修改生产代码、既有测试、项目文件、依赖、schema、migration 或正式数据库，没有启动 WPF，没有进入 Stage 8。

## 原始数据只读核验

- 源文件：`D:\下载\东莞壹号广场KKV店商品效期折扣判断表_2026-09-01-17-19-31.xlsx`
- 开始、Terra 结束、Sol 复核后的文件大小均为 `2522641 bytes`。
- 三次核对 SHA-256 均为 `BBD91AE4E40E5381D749F8DB8F4CC0A600FB88D8C1CF6EA160C7C33EC1A3F0F6`。
- 原表未修改、未覆盖、未复制入 Git；分析输出仅为忽略目录 `obj/V1F01/` 下的聚合 JSON，不含完整门店明细。
- 本轮未检查、创建、删除或修复 `%LOCALAPPDATA%\StoreExpiryInspector` 的 Junction；该历史事故按用户修正口径保持关闭。

## Sol 独立重跑与对账

Sol 使用工作区捆绑 Python 两次运行：

```text
python -B tools/v1_f01_analyze.py --source <原表> --output obj/V1F01/sol-summary-1.json
python -B tools/v1_f01_analyze.py --source <原表> --output obj/V1F01/sol-summary-2.json
```

- 初版两份汇总 SHA-256 均为 `A1994D90486FA911A8628934079DC30C4425E9399035D0BD3285F427E66F81C1`。加入 `5% clamp(3,60)` 后，Sol 再次独立运行两份修正版汇总，SHA-256 均为 `D8EB4F0CE86E4EB17375432636CF57473CCC789C2DDC25D99B15706FA5704EFC`；固定输入和固定业务日期可重复。
- 工作簿：1 个 `Sheet1`，20 列，32,453 行（1 表头 + 32,452 数据行），0 公式，任务卡所需表头缺失 0。
- 10 个大类；品类商品数之和 `7007`，等于全表唯一商品编码数。
- 候选 BatchKey `32427` = 可分类唯一 Batch `32402` + 冲突 BatchKey `25`。
- 各品类可分类、已过期、可执行、库存 0、不可分类合计分别为 `32402 / 2232 / 2493 / 0 / 25`。
- 每个品类、每个方案均验证：`补查 + 仅历史基线 = 该品类已过期批次`。
- 全表方案经 Sol 最终重跑核对为：3% `408/1824`、5% `629/1603`、10% `1176/1056`、5% clamp(3,30) `555/1677`、5% clamp(3,60) `629/1603`、5% clamp(7,60) `629/1603`。
- `git diff --check` 通过；运行中产生的未跟踪 `tools/__pycache__/*.pyc` 已精确删除，不属于交付物。

## 代码事实复核

- `ExcelFileClassifier` 当前存在 `商品大类 == 食品` 限定；Planner / Executor 当前使用 `food / food_v1`。
- Reader 的 11 字段契约、ProductCode / BatchKey、局部增量、冲突组、累计到货历史最大值、库存 0 优先、Task 聚合、启动重算、Reminder 查询和正式提交后更高阶段处理权威均有现有代码 / 测试依据。
- 代码中未发现 `HasCompletedInitialImport` 或等价的全应用首次导入标志；设计没有虚构其已存在。
- 仓库 migration 仍为 8 条；本卡没有创建 migration。

## 复核结论与产品门禁

- 数据字段足以完成本轮画像和候选模拟，没有触发“字段不足即停”门禁。
- 真实数据没有库存 0 样本，因此库存 0 工作量不可由本表评价；设计仍保留其最高优先规则。
- 真实表中“是否该做临期折扣”有 26,485 个空值，不得作为全品类强制准入条件。
- 产品经理已确认商品大类 + policy/version 范围级基线、`5% clamp(3,60)`、窗口内直接 open task、窗口外仅历史基线、到期当天 expired 和原表临期折扣标识不作强制准入；任务卡 F 节为当前权威。
- 剩余产品阻断仅为各管理范围完整的 5 折 / 2 折 / 收仓 Stage policy 与版本映射。
- 在产品经理确认前，不创建实现任务，不修改生产代码或 migration，不进入 Stage 8。

本轮没有重新运行 .NET Release 测试：接任事实恢复已在同一 `master@a0199124588f8b264251cfd91c689d001440b40d` 重跑 692/692；V1-F01 此轮无任何 tracked 生产、测试或项目文件差异。本记录不把该历史基线冒充为本卡再次运行。

## G 节补充分析｜Sol 独立复核通过

Terra 已补充提交 `V1-F01-STAGE-POLICY-AND-FIRST-DAY-WORKLOAD.md`，并最小更新只读 `tools/v1_f01_analyze.py`，以增加源表“折扣日期”与 food_v1 三候选节点的数值交叉对照及按 ProductCode 的首日工作量聚合。源表没有独立 2 折、下架或收仓日期字段；中性“折扣日期”不能认定为业务权威。Sol 使用工作区捆绑 Python 两次重跑，聚合 JSON SHA-256 均为 `B233ECF01AE95E86B91E1B2F9D3C000D2DA778C3C7DE961CC775AC44F3745E82`，源表指纹仍为 `2522641 bytes` 与 `BBD91AE4E40E5381D749F8DB8F4CC0A600FB88D8C1CF6EA160C7C33EC1A3F0F6`。

Sol 已复核真实 20 表头、`32,402 = 31,184 非空 + 1,218 空`、10 类对账、三候选节点各自分母、629 / 2,493 批次守恒、`465 + 1,175 - 366 = 1,274` 商品任务并集、合并阶段 `200 / 430 / 179 / 465`、批次压力平均 `2.450549...`、P50 `2`、P90 `5`、最大 `12`。结果通过；但 Stage policy 因缺少三节点语义证据仍是产品阻断，不得据此开始 V1-F01 实现。

## H 节公司规则输入｜Sol 独立复核通过

- 公司规则已替代 Excel 中性 `折扣日期` 的 policy 推断；分析脚本不再用该列计算或证明 policy。
- Terra 运行：`python -B tools/v1_f01_analyze.py --source D:\下载\东莞壹号广场KKV店商品效期折扣判断表_2026-09-01-17-19-31.xlsx --output obj/V1F01/terra-company-policy-summary.json`。
- 读取前后源文件：`2,522,641 bytes`、`BBD91AE4E40E5381D749F8DB8F4CC0A600FB88D8C1CF6EA160C7C33EC1A3F0F6`；输出为汇总 JSON，不含完整门店明细。
- H2：应季搭配 797 商品 / 1,212 批因季节、反季和上下图决定缺失不可映射；赠品小样 7 商品 / 7 批因原始 policy 来源缺失不可映射；六个通用长效类本表均无 `<=6个月` 批次。
- H3：已确认 policy 可计算 / 不可计算为 `31,183 / 1,219` 批；历史补查 `629 / 465`（批 / Task），当前可执行 `3,730 / 1,407`，合并去重 `4,359 / 1,474`，ProductCode 交集 398，合并 Stage 为 `255 / 543 / 174 / 502`（5折 / 2折 / 收仓 / 过期），压力平均 / P50 / P90 / 最大为 `2.96 / 2 / 6 / 14`。
- Terra 结论：只建议 `food_expiry_v1`、`pet_expiry_v1`、`general_long_expiry_v1` 三个确认 policy 分组；季节性及赠品范围保持 `unresolved`。月=30天仅为与现有系统一致的模拟换算，未当作新的业务规则。
- Sol 使用工作区捆绑 Python 独立重跑两次，两个汇总 SHA-256 均为 `5D84A2415E1827637A46D47EF40AD5BFCE3840574BF7AA956FDD9ECEC9A9F44A`；原表指纹保持 `2,522,641 bytes`、`BBD91AE4E40E5381D749F8DB8F4CC0A600FB88D8C1CF6EA160C7C33EC1A3F0F6`。
- Sol 已核对 `31,183 + 1,219 = 32,402`、应季 797 商品 / 1,212 批 / 48 个中小类组合、赠品 7 项、六类六个月分桶、629 / 3,730 / 4,359 批次、`465 + 1,407 - 398 = 1,474`、四阶段 `255 + 543 + 174 + 502 = 1,474`、逐类合计及压力分位数，均通过。
- 差异仅限任务卡、分析文档、验收记录和只读脚本；无 `src/**`、生产测试、项目、依赖、schema、migration 或数据库修改，未启动 WPF。状态保持 `READY_FOR_PRODUCT_DECISION`，不进入实现。

## I 节最终冷启动模拟｜Sol 独立复核通过

- 产品口径已收敛：应季搭配与赠品小样正常导入但 V1 不参与效期管理；六个通用长效类未来 `<=6个月` 样本导入并提示规则未覆盖，但不产生效期任务。
- 只读脚本新增 A--D 四种首次基线过滤与 `3% clamp(3,30)` / `5% clamp(3,60)` 两个历史窗口的 8 组组合；A + 5% 保持 1,474 基准。
- 首轮结果：5% 历史补查 `629批 / 465 Task`；3% 历史补查 `403批 / 334 Task`。八组首日 Task 为 A5/B5/C5/D5=`1,474/1,219/676/502`，A3/B3/C3/D3=`1,455/1,179/583/373`。
- Sol 独立运行两次，两个汇总 JSON SHA-256 均为 `C131E5FA119A4D98D4B5D284C64DF0BBF3A08C6309BCF75BA9AED04F0A829393`；脚本内源文件指纹均保持 `2,522,641 bytes`、`BBD91AE4E40E5381D749F8DB8F4CC0A600FB88D8C1CF6EA160C7C33EC1A3F0F6`。
- Sol 已断言 2 个窗口 x 4 个方案、每组四阶段合计、10 类任务合计、相对 1,474 差额、5% 的 629/465、3% 的 403/334，以及应季搭配 / 赠品小样在全部组合均为 0 Task；全部通过。
- 差异仍仅限任务卡、分析、验收和只读脚本；状态保持 `READY_FOR_PRODUCT_DECISION`，等待最终方案选择，不进入实现。
