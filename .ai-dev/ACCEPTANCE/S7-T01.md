# S7-T01 正式验收记录｜本地数据库安全备份基础

- 验收日期：2026-08-31
- 执行单：`D:\下载\S7-T01.md`
- 实现提交：`30cd485258dcc415f7896a2a89ce880369232760`
- 当前结论：独立验收通过；S7-T01 正式完成并归档
- 独立验收：Codex Sol

## 实现范围

- 复用 Stage 2 `PreImportSnapshotService` 的 SQLite `BackupDatabase` 在线一致性能力，没有创建第二套底层复制体系。
- 将共享快照完整性检查从 `quick_check` 提升为 `integrity_check`；S7-T01 从 EF 当前模型读取预期 migration，不在新业务入口硬编码 migration=8。
- 新增 `LocalDatabaseBackupUseCase`，默认使用现有正式库路径和 `%LOCALAPPDATA%\StoreExpiryInspector\backups` 独立目录。
- 成功返回备份记录 ID、文件身份、UTC 创建时间、大小、SHA-256、migration 列表和 `verified`；同时写入既有 `BackupRecord` 和同名 JSON 元数据。
- 备份数据库先由既有临时文件流程验证并原子发布，元数据失败时清理本次产物；同一进程同时只允许一个备份，失败后可重试。
- 未实现恢复、导出、保留期自动删除、GUI、S7-T02、schema、migration 或依赖变更。

## 本轮技术门禁

| 门禁 | 结果 |
|---|---|
| S7-T01 专项 | 6/6，通过；合并覆盖执行单 16 项备份行为 |
| S7-T01 + Stage 2 共享快照回归 | 12/12，通过 |
| Stage 6 四个权威测试类 | 52/52，通过 |
| Stage 5 四个权威测试类 | 51/51，通过 |
| Stage 4 八个权威测试类 | 179/179，通过 |
| Stage 3 十个权威测试类 | 170/170，通过 |
| Release 全量 | 641/641，0 失败、0 跳过 |
| Release build | 0 warning、0 error |
| EF model | 无模型漂移 |
| migration | 仓库 8 条，未新增 |
| dependency | 工程文件无 diff，未新增依赖 |
| `git diff --check` | 通过 |

专项使用隔离 SQLite 数据库验证：WAL 运行状态下包含已提交数据且排除未提交事务、`integrity_check=ok`、migration 与当前应用期望一致、SHA-256/大小/JSON/`BackupRecord` 一致、失败不发布成功集合、临时文件清理、既有备份不覆盖、缺失/不可用路径明确失败、并发第二次调用拒绝及失败后重试成功。

## 正式数据保护门禁

- 自动化测试全部使用 `%TEMP%` 下隔离数据库；没有调用默认正式库创建备份，没有启动 GUI。
- 用户在正常本机 PowerShell 会话中对正式路径 `%LOCALAPPDATA%\StoreExpiryInspector\data\app.db` 执行只读 `Get-Item` 与 `Get-FileHash -Algorithm SHA256`；回执为 `299008 bytes`、SHA-256 `F3D423DF14B882D7BFE87780A81CF5879F074AF4880601CBEDB6B475A964F522`，与目标值逐字一致。
- Codex Sol 另行只读复核应用进程为 `0`；没有启动 WPF，没有写正式数据库、WAL/SHM、last-run 或运行日志。
- 独立验收开始时为 `master @ 30cd485258dcc415f7896a2a89ce880369232760`，工作区 clean，`git diff --check` 通过。

## 停止门禁

S7-T01 已满足一致性备份、验证、失败保护、Stage 3～6 回归、正式数据保护及独立验收门禁，正式完成并归档。停止；不得创建、编号、派发或实施 S7-T02。
