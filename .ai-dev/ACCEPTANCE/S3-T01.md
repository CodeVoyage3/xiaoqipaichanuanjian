# S3-T01 独立验收记录｜纯效期阶段与下一触发日计算

- 验收结论：通过
- 验收日期：2026-08-27
- 任务卡提交：`ea2493263302783362e8cc4f6e1fbe7b2ccecab5`
- 实现提交：`b59f65b9394e66bbb23a81eb438c03c74082c911`
- 执行模型：GPT-5.6 Luna（max）
- 独立验收：GPT-5.6 Sol

## 实现结论

- 实现差异严格为 `ExpiryStageCalculator.cs` 和 `ExpiryStageCalculatorTests.cs` 两个新增文件；没有修改既有实体、EF、migration、项目依赖、导入、WPF 或治理基线。
- 公开计算入口只接收显式 `DateOnly businessDate`、`DateOnly expiryDate`、正整数保质期值和严格大写 `D/M/Y` 单位；不接收生产日期，不读取系统时钟。
- D 使用原值，M 使用受检乘法乘 30，Y 使用受检乘法乘 365；0、负数、非法单位及 M/Y `Int32` 溢出均明确抛出参数或溢出异常。
- 270 天及 `9M` 使用短效期规则，271 天使用长效期规则；节点当天立即切换，业务日期等于或晚于有效日期时直接为过期。
- 阶段代码严格复用 `none / discount_50 / discount_20 / withdraw / expired`。没有 `current_phase`、`highest_phase`、`normal` 或其他平行代码。
- `NextTriggerDate` 由当前阶段和当前规则阈值直接映射；非过期结果均晚于业务日期，过期结果为 `null`。
- 计算只返回单个当前结果，不回放历史阶段，不生成任务、事件或任何 S3-T02+ 状态。

## Sol 独立审查

- 从任务卡提交到实现提交的完整 diff 只有两个授权文件，共新增 291 行；`git diff --check` 通过。
- 短效期边界覆盖剩余 31/30/29、15/14/13、8/7/6、1/0/-1 天；长效期覆盖 91/90/89、61/60/59、15/14/13、1/0/-1 天。
- 测试同时覆盖 270/271、9M、D/M/Y、有效日当天、跨月、跨年、无生产日期、空/空白/小写/未知单位、非正值、M/Y 溢出、离线跨阶段和重复调用确定性。
- 生产与测试文件未引用 `DateTime.Now/Today/UtcNow`、`DateOnly.FromDateTime`、DbContext、EF、ProductTask、LifecycleEvent、ImportPlan、Open XML 或文件系统。
- 算法为纯整数阈值和 `DateOnly` 自然日运算；不存在外部状态、数据库或系统时间依赖。

## 独立验证证据

- S3-T01 专项：47/47 通过，0 失败、0 跳过。
- Release 全量：225/225 通过，0 失败、0 跳过。
- Release build：0 警告、0 错误。
- EF：`has-pending-model-changes` 明确报告模型无变化。
- migration：仍为既有 8 条；没有新增 migration、Designer 或 ModelSnapshot 变化。
- 依赖：两个项目文件均未变化，没有新增或升级依赖。
- `git diff --check` 通过；验收运行未留下数据库或临时业务文件，验收前实现工作区干净。

## 范围与后续门禁

- 本卡没有把计算结果写入 Batch，也没有实现任务聚合、启动补算、阶段升级、库存归零、批次 0 件停止、累计到货恢复、Excel 后置状态机、WPF 或 UI。
- S3-T01 已正式通过。当前仍不得自动创建或派发 S3-T02；必须等待用户下一步明确批准。
