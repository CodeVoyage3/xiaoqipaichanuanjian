# S2-T07 独立验收记录｜原始工作簿最近两份保留策略

- 验收结论：通过
- 验收日期：2026-08-27
- 任务卡提交：`e5cd633fbe921d81c0bb31da915e1a4a4b582789`
- 实现提交：`20892286c0f3a89da99eb650dc49cb46bd4af8a9`
- 验收测试补充提交：`b29f0f8f6a4091c2c7be894ca0feb110a7bba1fe`
- 执行模型：GPT-5.6 Luna（max）
- 独立验收：GPT-5.6 Sol

## 实现结论

- 生产改动只有既有 `ConfirmedImportExecutor` 中一个固定私有裁剪方法及两次调用语句，共 21 行；没有 schema、migration、依赖、实体、配置或前序规则变化。
- 新 Workbook 已在 S2-T06 事务内保存后，查询带 Workbook 的 `Succeeded` Import，按 `ConfirmedAtUtc DESC, Id DESC` 排序并删除 `Skip(2)` 的旧 Workbook。
- 删除发生在同一事务 Commit 前；新导入或裁剪失败时，旧 Workbook 删除与全部正式写入一起回滚。仅删除 Workbook 子记录，Import 和其他历史永久保留。
- 未建立 Repository、策略模式、公共清理 API、后台任务或文件版本系统；未涉及 Undone、撤销、恢复、回填或 Stage 3+。

## 独立审查与补充

初始实现及 0→5、文件变化、裁剪 DELETE 故障测试正确，但缺少已有两份时的完全无变化/解析失败，以及新 Workbook 写入前普通事务失败证据。Sol 要求 Luna 仅补测试；最终增加真实 S2-T04 no_changes、损坏 xlsx 解析失败和 Product BEFORE UPDATE 事务故障，生产代码未再变化。

## 独立验证证据

- S2-T07 专项：5/5。
- S2-T01 至 S2-T06 回归：131/131。
- Release 全量：136/136。
- Release build：0 警告、0 错误。NuGet 资产重放的 `NU1900` 网络告警在编译门禁中单独屏蔽；官方源漏洞审计经联网重试成功，两个项目均无已知漏洞。
- EF：8 条 migration；`has-pending-model-changes` 明确报告模型无变化。
- 真实样表：SHA-256 `20fe1898dba98f48bcd8b83673f2001fe3ed0dfc01a89aa4ebdff9d1af6cacce`，397,308 字节，未变化。
- `git diff --check` 通过；实现范围只有执行器和新增专项测试；无 `.db`、`.db-wal`、`.db-shm` 或 `.tmp` 残留。

## 保留与原子性结论

- 0→1→2→3→4→5 实测 Workbook 数量为 1、2、2、2、2；Import 始终累积，最终 5 条全部字段可查询。
- 第 3 次后保留第 2、3 次，第 4/5 次继续滚动；相同确认时间由更大 Import ID 决胜，不依赖文件名或查询偶然顺序。
- 最终两份 Workbook 的 Import 关联、原始字节、SHA、文件名和保存时间准确；旧 Workbook 被删除但 BackupRecord、Issue 和业务历史不裁剪。
- 文件变化、无变化、解析失败、早期正式事务失败和裁剪 DELETE 失败均不改变原两份。两类事务失败均保留可验证快照，ChangeTracker 清理且不会二次提交。

## 明确未实现

本卡未定义 Undone 是否占用保留名额、是否删除或恢复 Workbook；未实现撤销、快照恢复、完整备份恢复、Excel 回填、工作簿重建、效期、任务、UI 或 Stage 3+。`S2-T08` 尚未创建或派发。
