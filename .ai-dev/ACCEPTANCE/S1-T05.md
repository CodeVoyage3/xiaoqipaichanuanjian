# S1-T05 验收记录

- 验收日期：2026-08-26
- 结论：通过
- 执行模型：GPT-5.6 Luna（max）
- 任务卡提交：`8e9ef860668226e5df9e20b67be62e73140c629f`
- 实现提交：`02c3feeb6546790d71933ed5da6a55c366ecebdf`

## Sol 独立审查

- 首个 Luna 会话在自测期间因额度中断；接任的 Luna（max）只收尾既有 S1-T05 改动、去除重复测试 helper、执行门禁并提交。
- 实现只修改任务卡授权的 9 个文件；`.ai-dev/**`、UI、App、项目/solution、依赖、既有领域实体、既有配置与既有三条 migration 均未变化。
- 新增且仅新增 `inventory_adjustments` 一张业务表、一实体一配置和一条 `AddInventoryAdjustments` migration。
- 表严格只有 `id`、`product_id`、`excel_stock_qty_snapshot`、`adjusted_stock_qty`、`adjusted_at_utc` 5 个字段。
- 未实现库存更新服务、商品归零联动、生命周期事件、导入来源预留、Excel、UI 或 Stage 2+ 功能。
- `ReadTableSql` 与 `ReadForeignKeyDeleteActions` 移入已有 SQLite 测试辅助类并由两组测试复用，旧私有重复实现已删除；未引入测试基类或业务断言 helper。

## 数据完整性验收

- Excel 库存快照与修正后库存均接受 0 和正数，并由 SQLite CHECK 拒绝负数。
- `product_id` 外键拒绝不存在的商品；PRAGMA 实测删除动作为 `NO ACTION`。
- 存在修正历史的商品无法删除，修正历史不会级联丢失。
- `(product_id, adjusted_at_utc, id)` 普通索引存在且字段顺序正确；同时间记录按 ID 稳定排序。
- 没有擅自增加“两个库存值必须不同”、原因、人员、软删除或来源字段。

## 独立验证结果

- 从 `AddInspectionHistory` 写入 Product、Batch、Task、TaskItem、Draft、DraftItem、Inspection、InspectionItem、InspectionItemRevision 后升级：九类旧数据全部保留。
- `dotnet build StoreExpiryInspector.slnx --nologo --no-restore`：0 警告，0 错误。
- `dotnet test StoreExpiryInspector.slnx --nologo --no-restore`：26 通过，0 失败，0 跳过。
- `dotnet ef migrations has-pending-model-changes ... --no-build`：无模型漂移。
- `AddInspectionHistory → AddInventoryAdjustments` 增量脚本：990 字节，1 个 `CREATE TABLE`、1 个 `CREATE INDEX`、0 个 `ALTER TABLE`、0 个 `DROP TABLE`。
- 官方 NuGet 源在线漏洞审计：应用与测试项目均无已知漏洞。
- `git diff --check`：通过；测试临时数据库残留为 0。
- 实现提交后工作区干净。

## 阶段状态

`S1-T05` 通过。Stage 1 尚未整体完成；本轮不创建下一张任务卡，不进入 Stage 2。
