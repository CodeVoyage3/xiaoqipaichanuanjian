# S19-T02 验收边界

日期：2026-09-12（Asia/Shanghai）  
状态：`IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`

## Sol 独立技术验收结果

- fresh 治理基线=`41adce6cda174816e6afd9c937a40d9d0a2222dc`；Terra 最终实现=`db36d34d538949504ce3bf3276ee70b8ea2b4fd1`；未 push。
- 初版 `6124bad` 曾删除 S17 第一阶段生产接线，Sol 判 `FAIL`；R1 `2fe82c4` 恢复接线/注入但旧 S17 测试文件越界，仍判 `FAIL`；最终 `db36d34` 将新增断言归入 S19 专项，S17 文件与治理基线全等。
- 最终范围精确为 `App.xaml.cs`、`MainWindow.xaml.cs`、`UpdateNotificationViewModel.cs`、`WpfDialogService.cs` 与新增 `S19T02DomesticDownloadFallbackTests.cs`；`git diff --check=PASS`，禁止文件无 diff。
- S17 第一阶段 fallback 保持；同一 `gitee.CheckAsync` 注入第二阶段。白名单实际调用次数、GitHub/Gitee 版本身份、URL、全部其他 outcome（含 `SizeMismatch`）阻断、同窗三按钮、默认浏览器、重试/取消、单 worker 与正常 Verified 安装链均通过独立审查。
- Sol fresh S19+S17+S15=`44/44 PASS`。包含 S9-T04/S14 的原始直接组合为 `43 PASS / 7 FAIL`，在 `41adce6` 隔离基线同命令同样 `43/7`、同测试/同错误，均为既有 v1.0.7/migration 9 陈旧断言，不归责本实现且未为凑数修改。
- final `db36d34` Release App build=`0 warning / 0 error`；不 build Updater；`FULL=NOT_RUN / NO_FULL`。
- GUI candidate：`C:\Users\39037\.codex\visualizations\2026\09\12\01a0955b-013b-7531-8012-6e79baa16dd1\S19-T02-GUI-Acceptance`。生产 DLL SHA256=`5E4B1A1722E11E38BA82A749192606E49C0B220BE8A1C8A2FDC241AB529919C7`，informational version=`1.0.9+db36d34d538949504ce3bf3276ee70b8ea2b4fd1`；Sol 独立 smoke exit 0、PASS marker、进程0、新增 TEMP 根0。
- `GUI_CANDIDATE_TECHNICAL_READY=PASS` 不是用户 GUI 验收。S19-T02 继续 `NOT_ACCEPTED`。

## 技术验收

- fresh 实现必须基于治理提交，独立 clean Terra worktree 可追溯，正式 dirty 工作区原样保留。
- 生产 diff 仅限四个批准文件；禁止路径、版本、Schema/migration、依赖均无 diff。
- 第二阶段 Gitee 调用白名单精确为 `NetworkUnavailable / RateLimited`；全部其他 `UpdatePackageOutcome` 均为 `NO FALLBACK`，其中 `SizeMismatch=SAFE_BLOCK`。
- 只有 GitHub `LatestVersion == Release.Version == Gitee LatestVersion` 且现有 checker 返回合法 `pan.quark.cn` URL 才显示网盘入口。
- Gitee 不覆盖 GitHub 发行身份；手动入口只调用 Windows 默认浏览器，不进入 downloader/install/Updater。
- 同一现有窗口显示“点击网盘下载 / 重试在线更新 / 取消”；fallback 关闭不触发稍后提醒，初始稍后提醒语义不变。
- 重试复用原 `PrepareUpdateAsync` 和 `CheckedRelease`，一次点击仅一个 worker；正常 Verified→install→Updater 路径无变化。
- S19-T02 精准专项及真正受影响的 S17/S15/S14/S9-T04 直接回归通过；Release App build `0 warning / 0 error`；`FULL=NOT_RUN / NO_FULL`。

## 用户 GUI 最小清单

由稳定模拟入口复现：GitHub 检查成功 → 新版 X → 立即更新 → 第二阶段 `NetworkUnavailable`。

1. 看到“在线更新下载失败”及 `[点击网盘下载] [重试在线更新] [取消]`。
2. 点击网盘下载打开正确夸克地址，提示关闭，软件继续运行。
3. 重试重新进入原在线更新流程，不产生并发 worker。
4. 取消后软件继续正常使用，不出现“稍后提醒”二次提示。

GUI PASS 前：`S19-T02=IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`；Stage19 不得关闭。

