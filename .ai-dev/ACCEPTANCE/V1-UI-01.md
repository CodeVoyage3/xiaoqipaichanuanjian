# V1-UI-01 技术验收｜导航、筛选与全局视觉降噪

日期：2026-09-03。当前结论：**TECHNICALLY_ACCEPTED / WAITING_USER_GUI_ACCEPTANCE**。

## 治理与实施基线

- GitHub `main` 开工基线：`453c95b00e040832c6705f04fb0d42c0e34c8a51`；开工时本地与 `origin/main` 一致，ahead/behind `0/0`。
- Sol 治理提交：`38fabb0`。
- Terra 原实现：`6ae83ffc648e2b49cce034ac274edb94f0399396`。
- Sol 首轮 diff 审查发现共享普通/次级/链接样式仍为蓝色后，未代写生产代码、未复用原 Terra；全新 Terra 返修：`c551df2dd0113e46d845c074bce9a5c622dcb780`。
- Sol 二轮审查发现 Primary/Danger 可能被共享模板中性 Hover/Pressed 覆盖后，再由全新 Terra 明确模板状态优先级：`ad72b6f0a8f04ce961febd60397ed531ea5f3e1f`。
- 三名 Terra 均在各自提交后停止，未 push；Sol 对从开工基线到最终实现 HEAD 的完整合并 diff 独立复验。

## 完整 diff 与范围结论

- 生产变化仅为 `src/StoreExpiryInspector/App.xaml`、`src/StoreExpiryInspector/UI/MainWindow.xaml`、`src/StoreExpiryInspector/UI/Stage4ViewModels.cs`。
- 测试变化仅为 `S4T10UiRefreshStaticAuditTests.cs`、`Stage4ViewModelTests.cs`、`V1F03I04TodayInspectionViewModelTests.cs`；其余为本卡治理文档。
- `App.xaml` 只对既有共享 Button 状态做集中降噪：普通/Secondary/Link 使用中性灰，Focus 保留蓝色，Primary 与 Danger 保留各自语义色；不是全局 Theme 重写。
- 左上品牌文字删除、盾牌保留、折叠箭头右置；导航展示顺序正确，命令和页面权威未改。
- 阶段筛选复用 `StageFilterOption` 与 canonical Stage；大类从既有真实待排查查询结果 `CategoryName` 去重生成。搜索/阶段仍走既有查询，ViewModel 仅编排大类交集与筛选后分页、计数、空态。
- 完整 diff 没有 Application、Domain、Infrastructure、Schema、ModelSnapshot、migration、dependency、`.csproj` 或 `.slnx` 变化；I01～I04、ProductTask、Stage、Reminder、Excel、History/Revision、Backup/Restore 均无生产差异。
- `StageBadgeResources.xaml` 无差异；“应季搭配”“赠品小样”规则所在业务链无差异。
- 无范围越界，未启动 Stage 8/9，未实施在线升级。

## 功能与自动化结论

- 阶段默认全部、单独阶段筛选已有回归；阶段控件已由横向按钮组改为 ComboBox。
- 大类默认全部，来自实际任务数据，去重、单独筛选正确。
- 自动化分别锁定搜索+阶段、搜索+大类、阶段+大类、搜索+阶段+大类交集。
- 清空一次恢复搜索、阶段、大类和第一页。
- 筛选后商品数、空状态、上一页/下一页与页数均以最终交集为准。
- 静态契约锁定品牌区、导航顺序、两类 ComboBox、共享中性状态、Primary/Danger 状态优先级与 StageBadge 冻结。

## Sol 独立新鲜证据

| 门禁 | 结果 |
|---|---|
| V1-UI-01 / 查询 / UI 静态专项 | 67/67，通过 |
| 相关 UI / ViewModel 回归 | 183/183，通过 |
| I01～I04 精确回归 | 119/119，通过 |
| Release 全量 | 894/894，0 失败、0 跳过 |
| Release build | 0 warning / 0 error |
| EF model drift | `No changes have been made to the model since the last migration.` |
| migration | 9 条；最后一条 `20260901155124_AddPolicyAndBaselineFoundation` |
| Schema / ModelSnapshot / dependency / project | 无差异 |
| `git diff --check` | 通过 |
| WPF / 正式数据库 | 未启动或机械操作 WPF；未访问或修改正式数据库；应用进程 0 |

## Git 状态

- 最终实现 HEAD：`ad72b6f0a8f04ce961febd60397ed531ea5f3e1f`；技术验收文档提交以当前 `git rev-parse HEAD` 为准。
- 工作区在技术复验后干净；本轮远端只做读取。治理 push 曾被当前安全策略拒绝，因此本轮提交尚未推送，`origin/main` 仍为开工基线 `453c95b`，未绕过策略。

## 用户 GUI 验收

只需用户验证：

1. 左上角标题已删除，折叠箭头移动到右侧后视觉和点击是否自然；
2. 今日排查是否已经位于待排查任务下面；
3. 待排查任务的“阶段 + 大类 + 搜索”组合筛选是否符合实际操作习惯；
4. 整体蓝色是否明显减少，普通按钮/输入框/分页是否不再满屏蓝边，主次关系是否更舒服。

不重新验收 I01～I04。用户回执前 V1-UI-01 不得写为 GUI `PASSED` 或 `CLOSED`，不得启动 Stage 8。
