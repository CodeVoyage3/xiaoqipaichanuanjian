# S17-T01 验收边界

日期：2026-09-09（Asia/Shanghai）

## Sol 独立技术验收结果

- fresh 治理基线：`bf67e90cc9e631cb3b125f3a7a6f51c95ef509a4`；Terra source：`ee83b0c10f252d1ee136464e0a2bb6d09452a34f`；实现分支远端 SHA 一致。
- 完整 diff：11 文件、299 insertions / 10 deletions；`git diff --check` 通过；Stage16、Schema、migration、ModelSnapshot、UpdateSafety、下载器、安装准备、签名/manifest/SHA/rollback/ZIP/安装器均无 diff。
- 代码审查：GitHub 非网络 outcome 原对象返回，`CheckedRelease` 与自动更新链保持；仅 `NetworkUnavailable` / `RateLimited` 调用 Gitee。Gitee 使用固定 Raw、独立 5 秒、256 KiB、exact-three-fields、严格三段版本及 `pan.quark.cn` HTTPS allowlist。手动分支仅 `Process.Start(... UseShellExecute=true)`，不进入包下载、安装准备或 Updater。
- Sol fresh 专项：S17 `27/27 PASS`。
- Sol fresh 回归：S9T04 + S14T01 + S15T01 + S17 `56/56 PASS`；S9T03 非 STA/WPF `21/21 PASS`。
- S9T03 单一 `MainWindowCanReachCoreReadyOnStaWithoutAnyRuntimeDatabase`：候选 `0/1`，约 26 秒 synthetic core-ready timeout；同一命令在治理基线亦 `0/1` 且约 26 秒同错，定性为 `BASELINE_REPRODUCED / NON_BLOCKER`，不记录为 PASS。
- Sol fresh Release build：App 与 Updater 均 `0 warning / 0 error`；FileVersion 均 `1.0.7.0`。
- EF：`No changes have been made to the model since the last migration.`；migration count `9`；禁止路径 diff `0`。
- `FULL = NOT_RUN / NO_FULL`；未发现经证实的跨域风险。正式数据库访问 `NO`；正式 dirty 工作区保持原五项未触碰。
- 结论：`TECHNICAL_REVIEW = PASS`；`S17-T01 = IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`。仍须用户完成下列 GUI 清单，当前不得 `ACCEPTED / CLOSED`。

## 技术验收

- fresh Terra worktree 与实现提交可追溯，实际 diff 严格落在任务范围。
- GitHub 主通道所有既有行为、`CheckedRelease`、安全协议与自动更新链保持不变。
- Gitee 仅由 `NetworkUnavailable` / `RateLimited` 触发；固定 Raw、5 秒有界、256 KiB、严格 JSON/版本与 `pan.quark.cn` allowlist 全部有自动化证据。
- 手动分支只打开默认浏览器，不调用下载器、安装准备或 Updater；所有异常不影响主程序。
- S17 专项与 S9T03/S9T04/S14T01/S15T01 回归通过；Release App/Updater build 通过；EF no drift；migration count 9。
- Stage16、数据库、业务 UI、安装结构与现有 dirty 现场无变化；`NO_FULL` 除非发现经证实的跨域风险。

## 用户 GUI 最小清单

1. GitHub 正常时仍显示“立即更新”，既有自动更新行为不变。
2. GitHub 指定失败、Gitee 有新版时，主界面先可用；窗口显示完整说明与“下载最新版”；点击打开批准的夸克页面，应用保持运行且不自动安装。
3. GitHub 与 Gitee 均失败时无阻塞或报错弹窗；重启后仍先尝试 GitHub。

## 当前状态

`IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`

技术验收通过后也必须停在 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`，等待用户 GUI 回执；不得自判 `ACCEPTED / CLOSED`。
