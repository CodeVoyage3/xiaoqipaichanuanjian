# S1-T04 验收记录

- 验收日期：2026-08-26
- 结论：通过
- 执行模型：GPT-5.6 Luna（max）
- 任务卡提交：`906ea7675084cd98098147a59e3b48aa4fb2317c`
- 实现提交：`9d11224de7d5f449fca2f047b1ea56f83239849b`
- 有效日期非空修复：`38c0d7d4e022de1c20625daf99e807da0b286f1d`
- 任务唯一性补测：`7647ca3f55d02e762a167c68477f4f248d184755`

## Sol 独立审查

- 实现提交仅修改任务卡授权的 11 个文件；`.ai-dev/**`、UI、App、项目/solution、依赖、既有领域实体和既有两条 migration 均未变化。
- 新增且仅新增 `inspections`、`inspection_items`、`inspection_item_revisions` 三张业务表及一条 `AddInspectionHistory` migration。
- 三个实体各有独立 `IEntityTypeConfiguration<T>`；`StoreDbContext` 仅增加 3 个 `DbSet` 与 3 个显式配置注册。
- 未新增提交/编辑服务、状态重算、Excel、UI、Repository、UnitOfWork、通用基类、反射扫描或 Stage 2+ 实现。

## 审查修复

1. 首次实现错误地允许 `expiry_date_snapshot` 为 `NULL`；退回后改为实体非空、EF required、migration `TEXT NOT NULL`，并补真实 SQLite 拒绝测试。
2. 首次测试没有隔离验证同一任务第二条正式记录被唯一索引拒绝；退回后补充同任务同商品的重复记录测试。

## 数据完整性验收

- `task_id` 唯一；同一正式记录内 `(inspection_id, batch_id)` 唯一。
- 任务/商品与正式记录/批次均使用复合外键拒绝跨商品关联。
- 商品编码、排查人 Trim 后必填；阶段值域受 CHECK 约束。
- 库存、累计到货、正式排查件数和修改前后件数均拒绝负数；正式件数与有效日期拒绝 `NULL`。
- 修改前后值不得相等；修改历史具有 `(inspection_item_id, changed_at_utc, id)` 稳定排序索引。
- PRAGMA 实测所有新外键删除动作为 `NO ACTION`；被引用的商品、批次、任务、正式记录和明细无法删除或级联删除。

## 独立验证结果

- 从 `AddTasksAndDrafts` 写入 Product、Batch、Task、TaskItem、Draft、DraftItem 后升级：六类旧数据全部保留，三张新表可用。
- `dotnet build StoreExpiryInspector.slnx --nologo --no-restore`：0 警告，0 错误。
- `dotnet test StoreExpiryInspector.slnx --nologo --no-restore`：22 通过，0 失败，0 跳过。
- `dotnet ef migrations has-pending-model-changes ... --no-build`：无模型漂移。
- `AddTasksAndDrafts → AddInspectionHistory` 增量脚本：4,484 字节，3 个 `CREATE TABLE`，0 个 `ALTER TABLE`，0 个 `DROP TABLE`。
- 官方 NuGet 源在线漏洞审计：应用与测试项目均无已知漏洞。
- `git diff --check`：通过；测试临时数据库残留为 0。

## 阶段状态

`S1-T04` 通过。Stage 1 尚未整体完成；本轮不创建下一张任务卡，不进入 Stage 2。
