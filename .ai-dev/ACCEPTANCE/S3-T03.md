# S3-T03 验收记录｜启动补算 Application 用例

- 验收状态：通过
- 验收日期：2026-08-27
- 任务卡提交：`730e9d4c43ed11bf6f2f696a631c827ab0c41dc1`
- Luna 实现提交：`a8d098b72b76d40bdebc43023da679797b27e88d`
- 验收模型：GPT-5.6 Sol
- 后续门禁：未创建 S3-T04；必须等待用户明确批准

## 仓库与实现事实

- 实现 diff 只新增 `Application/StartupRecalculationUseCase.cs` 和 `StartupRecalculationTests.cs`，没有实体、EF 配置、DbContext、migration、项目依赖、导入或 UI 变化。
- 候选 LINQ 在 materialize 前限定 `TrackingStatus == "active"`、`NextTriggerDate != null`、`NextTriggerDate <= BusinessDate`；真实 SQLite 执行计划使用 `IX_batches_tracking_status_next_trigger_date`，未先加载历史 Batch 再筛选。
- 每个命中批次直接调用 S3-T01 `ExpiryStageCalculator.Calculate` 得到当天唯一阶段和下一触发日；没有阈值复制、逐日循环、历史阶段回放或历史任务生成。
- 待排查结果按商品分组并调用 S3-T02 `ProductTaskAggregator`；`none` 不进入聚合，没有第二套 ProductTask/TaskItem/Draft 规则。
- Batch 变化与聚合器写入处于同一个 DbContext 和外层事务。多商品后续写入故障时，真实 SQLite 验证 Batch、Task、Item、Draft 整体回滚，ChangeTracker 清空。
- 自然效期推进只原样传递既有 AttentionVersion；Batch 的 AttentionVersion 与 HandledAttentionVersion 均不修改。已有有效 Draft 的原 Item 阶段升级继续由 S3-T02 保留内容并标记重新确认。
- 输入显式提供 `DateOnly BusinessDate` 和 UTC 更新时间；生产用例未读取系统当前时间。

## Sol 独立审查

- 从任务卡提交到实现提交逐行审查完整 diff；生产用例 143 行，无接口加单实现、Repository、UnitOfWork、通用状态机、规则引擎、EventBus、Outbox 或测试故障开关。
- 禁止路径检查确认未修改或引用 Excel、Open XML、ImportPlan、ConfirmedImportExecutor、LifecycleEvent、库存归零、累计到货判断、恢复、WPF/App 启动、提醒或 Stage 4+。
- `ConfirmedImportExecutor` 未修改，Stage 3 启动补算没有进入 Stage 2 导入执行器。
- S3-T04 任务卡不存在。

## 独立验证证据

- S3-T03 专项真实 SQLite：12/12 通过，0 失败、0 跳过。
- S3-T02 专项回归：21/21 通过，0 失败、0 跳过。
- S3-T01 专项回归：47/47 通过，0 失败、0 跳过。
- Release 全量：258/258 通过，0 失败、0 跳过。
- 使用现有本地 NuGet 缓存并关闭在线漏洞审计完成 restore；Release build：0 警告、0 错误。没有新增或升级依赖。
- EF `has-pending-model-changes`：明确报告模型无变化。
- migration：仍为既有 8 条。
- `git diff --check` 通过；验收前实现工作区干净。

## 结论与保留边界

S3-T03 达成启动/恢复运行时的 Application 补算能力：只扫描已到触发日的活动批次，直接计算当前阶段并复用商品任务聚合，事务与幂等门禁通过。当前未接入真实 WPF 启动、托盘、电源恢复或提醒；该运行入口继续保留到 S3-T07。

S3-T03 正式验收通过。不得自动创建、派发或实现 S3-T04，等待用户下一步明确批准。
