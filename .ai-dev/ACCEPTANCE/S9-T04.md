# S9-T04 独立验收

2026-09-05 TECHNICALLY_ACCEPTED / CLOSED。开工c6200ca8507c7f8d99f7f40047b6be291d6ff70b=origin/main，clean/0/0，无既有T04后按用户授权创建。

| 门禁 | 必需证据 | 状态 |
|---|---|---|
| 角色与diff | 全新Terra medium/priority实施；Sol不代写生产，完整审查 | 通过：全新Terra实施，Sol完整diff/独立复验 |
| Manifest | schema1、数值版本/repo/tag/asset/platform/source/target绑定 | 通过：schema1，数值三段SemVer/身份/迁移声明绑定 |
| RSA | raw bytes RSA-PSS/SHA256，A-I负例；生产锚点空时fail-closed | 通过：原始字节RSA-PSS SHA256；生产锚点空，fail-closed |
| 下载 | 流式size/hash/限额/redirect/失败清理与完整HTTP成功 | 通过：真实loopback HTTP完整包Verified，限制/负例/清理 |
| ZIP | 路径/链接/大小写/炸弹/禁止内容/真实展开/内部版本与migration声明 | 通过：128项核心含安全ZIP负例及420文件完整包 |
| UI与生命周期 | 立即更新至Verified停止，进度节流/取消/single-flight/晚到保护 | 通过：实际WPF12/12、真实退出1/1、重复取消/single-flight |
| 真GitHub | 新鲜匿名public/Release数量/latest及安全头 | 通过：public/0/latest404，生产客户端NoPublishedRelease |
| publish与secret | fresh self-contained、静态payload/private-key/PAT扫描 | 通过：420文件164297044字节，已知secret扫描无实际命中 |
| 最终Release/build | 无filter0failure/error/aborted，build0/0 | 通过：无filter Release1023/1023，0failure/error/timeout/aborted/skip；build0/0 |
| EF与范围 | 无漂移、migration9固定末条、diffcheck、无禁止改动 | 通过：EF无漂移，9条固定末条，禁止范围无变化，diffcheck通过 |

生产私钥未建立，禁止创建测试生产密钥；最终应明确生产trust anchor待正式发行阶段注入。T02旧安装器仍历史产物。本卡绝不替换/启动下载程序或迁移正式数据；测试数据全部TEMP/GUID，正式数据根禁止访问。详细矩阵以Task及用户原始授权为准，不以局部成功代替整体验收。

## 进行中与退回历史

- Sol治理4f29b94；新建/root/s9_t04_fresh_terra，GPT-5.6 Terra medium/priority。生产候选85e34af6fc5e8d0f8f8bbd913be38cedf0f995e1仅新增下载器与两条测试；实施者Debug 2/2及build0/0不是完整验收。UI未接线、完整矩阵和实际HTTP缺失，已退回同一Terra补齐；不建返修卡、不push。
- Sol独立TEMP/baa6263a-458c-403e-8b1d-264748236c91准备46个合成ZIP与TcpListener网络路由，测试RSA只在内存，候选只静态读从未运行。首次尝试引用Release时旧ref未含T04类型，验收程序编译失败，改用已编译Debug候选进行提前反馈；最终仍须当前提交Release独立复验。
- 首次independent-draft-first.json有11项失败：合法4文件ZIP、未知长度、合法CDN、并发成功均UnsafeArchive；arm64及版本不符分类被parser压成InvalidManifest；package/manifest/sig404和manifest429分类错误；正文短超时未生效。其他ZIP负例虽被拒绝，但基础合法包也失败，不能把这些拒绝当相应防护充分证据。
- 独立源码定位：非主文件audit临时输出流未关闭即File.Delete，Windows FileShare.None使正常.json错误拒绝；仅两主文件测试未覆盖。原始ZIP extra/CRC/actual展开限额/secret内容、缓存约束/cleanup状态、UI/生命周期尚需补齐。首次结果保留，不改写为通过；Sol未修改生产代码。
- 新鲜匿名GitHub证据00:23:40～42 +08:00：repo200/public、list200/0、latest404，无Authorization；安全rate-limit头见S9-T04-GITHUB-SMOKE.json。另GitHub官方cli小资产仅读响应头302至批准CDN，见S9-T04-GITHUB-REDIRECT.json；非本产品实际下载成功证据，签名query未记录。采集脚本最初把latest错误对象Count=1作为count，归档已更正null，不把它当Release数量。
- 第二候选d2e5779返修后，合法4文件ZIP/未知长度/批准CDN/并发单次下载已Verified、包体短超时及package404通过；Sol矩阵扩至101项，77通过24失败，independent-d2e5779.json保留。错误Verified包括业务Backup/data/logs路径、CRC错误、加密flag、hardlink extra、非法字符、local/central名称不一致、private/PAT标记、reparse、尾部垃圾、未知extra；另存在Manifest/HTTP分类及源目标声明问题。已退回同一Terra先完成核心安全审计，再处理完整UI，不推送。
- d2e5779的UI只加异步调用，仍每次创建服务、进度回调为空、没有真实UI进度/single-flight保证，不能冒称完成UI接线验收。此状态仅未验收开发候选，真实生产锚点空保持网络前安全拒绝。
- 第三轮Sol独立Debug 103项97通过6失败，合法420文件self-contained ZIP通过；失败为asset_not_uploaded、duplicate_manifest_asset、manifest_large及manifest404/signature404/manifest429。结果independent-core-draft3.json保留。随后增加允许JSON内secret、原始ZIP头/偏移、非法trust anchor、审计取消、同版本及检测至下载集成；必须在最终Release重跑，不把旧负例通过当新候选已通过。
- 源码审查继续退回无界metadata复制、整包内存读取、清理失败结果、原始ZIP范围/CRC、阶段取消和UI实际接线。实施中出现secret关键字误杀合法System.Private.* runtime名称，Sol提供根因；不得通过移除CRC或弱化断言解决。

- 后续修复和独立专项：04f295c中间114/114、后续121/121仅Debug阶段证据；继续源码审查发现有界metadata/缓存持有期间重hash、审计取消和畸形JSON分类需要补齐。3875a87矩阵124项3失败（签名合法array/string-schema/null-schema错误分类），由9124ac6修复并9b04b00加入3条仓库回归。
- UI验收纠正：Sol早期harness误构造业务App，WPF自动排入OnStartup，隔离参数保护在logger/DB前拒绝，产生残余窗口；这是harness错误，不冒称生产bug。改plain Application加载实际资源、注入合成Shell后恢复通过。真正的取消反馈/晚到事件/任务管理由Terra修复，最终Release UI12/12，含两轮取消再试。
- 实际退出发现9660a6a仍不足：App.OnExit时MainWindow已空，worker尚未结束、partial残留。Sol子进程注入2秒取消清理延迟，验证真实App.ExitApplication/OnExit；bb2571f在Window.Closed同步取消并等待线程池worker，最终4dbf088回归通过。此前200ms延迟可能幸运通过，不用它替代最终严格退出证据。
- 最终冻结生产/测试4dbf088ff024a9818418e33bc67868dd0447b604；Terra提交后停止、未push。Sol最终Release真实HTTP核心128/128、WPF12/12、退出1/1、真实匿名客户端NoPublishedRelease均通过。初始失败JSON保留于S9-T04-SOL-VERIFY，不覆盖为成功。

## 最终独立结论

生产/测试4dbf088ff024a9818418e33bc67868dd0447b604之后仅Sol证据与治理收口。Release专项27/27，核心128/128、WPF12/12、实际退出1/1；fresh无filter Release1023/1023，01:35:23～01:46:09 +08:00，0failure/error/timeout/aborted/skip；build0warning/0error。EF has-pending-model-changes无漂移，list --no-connect为9条，末条20260901155124_AddPolicyAndBaselineFoundation，未查询正式库已应用状态。

新鲜self-contained win-x64发布420文件164297044字节，FileVersion1.0.0.0、ProductVersion1.0.0+生产提交。完整ZIP88096081字节已Verified；签名改manifest/签名/错误key/错误padding/缺失/非法全部拒绝，合法SHA错误签名同样拒绝；合法签名错误hash→HashMismatch，长度错→SizeMismatch，平台/内部版本/不安全ZIP拒绝。失败/取消本次缓存清理，实际进程退出在worker清理后完成。UI成功仅准备完成，无替换、调用Updater、更新触发退出或执行候选。

所有T04合成输入/缓存在TEMP/GUID，测试RSA只内存。正式DB/Backup/更新目录未访问；未执行更新migration、未创建Release/tag/asset、无Schema/依赖/installer身份变化、无重置/Undo。生产trust anchor仍空，不以测试key冒充；正式发行阶段仍需建立离线私钥保管及注入可信公钥。无实际私钥/PAT/长期secret入repo/publish；扫描是已知标记与人工完整diff检查，不宣称所有编码secret均可识别。

证据索引S9-T04-DOWNLOAD-RESULT.json；协议ANALYSIS/S9-T04-UPDATE-PACKAGE-PROTOCOL.md；可复现源码、最终JSON、原始失败、截图、发布清单、TRX计数/hash及构建/EF输出在S9-T04-SOL-VERIFY。合成客户端0.9.9→最终1.0.0包，不代表真实1.0.1 Release成功；真实匿名客户端仍NoPublishedRelease。全量已有高规模/真实Excel开关关闭的空return不算这些场景证据，NuGetAudit=false不算在线漏洞审计。自动化WPF不代替门店用户GUI/干净机器；T02旧安装器仍历史产物。

Stage9 IN_PROGRESS / WAITING_NEXT_AUTHORIZATION；T04 CLOSED。下一步仅建议独立Updater身份/journal/隔离staging切换与回滚及S9-T01保护/握手先决条件，须用户另授权，未创建S9-T05。普通push main后fetch clean/HEAD=origin/0/0的最终SHA以本次Git回执为准；不改写历史。
