# S8-T02 验收｜查询、分页与高规模读性能优化

- 开工 main：`0ede8b901fb5e6cbc1c2f2824d6f8a6c7a54f901`。
- 状态：`TECHNICAL_ACCEPTANCE_PASSED / CLOSED`（2026-09-04）。
- 实现：全新 GPT-5.6 Terra / medium / priority（用户已允许）。
- 独立验收：Codex Sol。

## 原验收要求（最终结果见文末）

1. 全部原 100k blocker 消失，规模仍 100k Batch / 300k History。
2. Dashboard、待排查/Today 数据库分页筛选、Reminder 候选、历史分页排序/item count 语义正确，真实 UI 调用已接入。
3. Before/After：Dashboard、首载、深页、搜索、Stage、大类、三条件、Today/大类、Reminder、历史列表/详情；最慢5项。
4. SQL、参数数、command count、EXPLAIN、实际 materialized 行数；剩余 SCAN/TEMP B-TREE/correlated count 和客户端计算如实说明。
5. 如需索引：具体表/列/查询/当前plan/预计收益，但本卡不实施。
6. 全 diff、专项、Release、build、EF、9 migrations、Schema/ModelSnapshot/dependency/project/Git 验证。

## 历史暂停事实（保留溯源）

- 治理基线：`a1c62fec42792f65cf83584ad3e34199ecbd9277`。全新 Terra `/root/s8_t02_fresh_terra` 已创建，medium / priority；没有复用 S8-T01 代理。
- Terra 尚未提交，实现只在工作区：`InspectionTaskQuery.cs`、`Stage4ViewModels.cs`，未完成 Today/Reminder/History 全卡范围。
- Terra 报告已运行查询专项：11 failed / 4 passed / 15；随后四类定向组合：15 failed / 55 passed / 70。主要失败包括 EF 对 TaskListRow 后排序不可翻译，以及待排查旧测试期望不匹配。不是通过结果，Sol 未复跑。
- 组合命令：`dotnet test tests\StoreExpiryInspector.Tests\StoreExpiryInspector.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~InspectionTaskQueryTests|FullyQualifiedName~Stage4ViewModelTests|FullyQualifiedName~V1F03I04TodayInspectionViewModelTests|FullyQualifiedName~InspectionHistoryQueryTests"`。
- 隔离问题：初版 Shell 无条件注入默认 `LoadOpenTaskCategories`，即使 `taskLoader` 是测试替身。Shell 构造会自动 `_ = PendingTasks.LoadAsync()`；运行的测试含这种构造，因此无法排除默认正式数据库访问。
- Sol 只读取代码确认：`CreateContext()` 会 `Directory.CreateDirectory`，默认 SQLite 连接未限定只读；虽然 category query 为 AsNoTracking SELECT，不能据此声称完全无文件系统变化。没有读取、哈希、修复或恢复正式数据库以作调查。
- Terra 在停止前已加入有条件 category loader 接线，但没有验证；所有测试与实施已停止。正式库是否实际打开、是否产生目录/空库/日志/sidecar 变化均未核实，不把未知写成未发生。
- 没有实现提交、没有新高规模 JSON、没有全量 Release/build/EF 验收，禁止关闭 S8-T02。等待用户允许先修复并验证隔离边界后继续；不申请或暗含正式库访问授权。

S8-T01 历史 897/897 不作为本卡新鲜验收。

## 新授权恢复记录

- 用户批准先隔离修复及验证，再继续原 S8-T02；明确不调查/读取/哈希/修复正式库。历史疑似访问仍保持未核实，不因新门禁而改写历史事实。
- 重新 fetch main：`0d1d42a0667909e52264c5cd091297a827338788`。旧 diff 保存为 `C:\Users\39037\.codex\visualizations\2026\09\03\01a06754-503b-72c0-b8f1-9e10cd9cad3f\S8-T02-recovery\old-terra-uncommitted.patch`；SHA256 `8242678ECCAB4D526D93C9E0BB0CA7FD732A4A845E823F46E6458FE9F2C174C9`，已通过 reverse apply check。
- 仅恢复两份旧生产文件后，index/worktree diff 均为空，master=origin/main；未清理或改动正式运行目录。
- 新代理 `/root/s8_t02_isolation_new_terra`，GPT-5.6 Terra / medium / priority。旧代理不复用。先隔离设计审查、隔离专项、独立门禁，再放行查询优化。
- 恢复时隔离门禁尚未完成；后续独立门禁及最终结果如下。

## 第一阶段独立隔离门禁（2026-09-04）

- 新 Terra 提交：`e294a6b2e6e25e22f92c6a4a2cb30df27ae26630`。
- 三文件：InspectionTaskQuery（大类 DISTINCT 接口）、Stage4ViewModels（注入模式 fail-closed / 默认 context factory 接缝）、S8T02IsolationTests。
- Sol 审查完整 diff 后独立执行隔离专项：7/7；关联 Shell/ViewModel/查询组合：155/155，0失败/跳过。
- 隔离专项覆盖注入 task/category/history/detail、构造自动加载、导航/刷新，以及 Import 执行默认依赖阻断；测试实际默认 Context factory 接缝计数=0，counter+throw 在任何路径/文件操作前保护。
- 实库测试显式使用 `TEMP/StoreExpiryInspectorTests/<GUID>/app.db` 并校验 DataSource；没有打开/读取/哈希/修复正式库验证安全。此结论只适用于本轮受控执行，不回写历史疑似访问事实。
- 工作区干净；diff check 通过；无 schema/index/migration/依赖/project 变化。Sol 已放行同一新 Terra 进入本卡第二阶段查询优化，不复用旧问题代理。

## Sol 新鲜 Before 对照（2026-09-04）

- 原 S8-T01 JSON 已不在文档记录的 TEMP 路径；不把历史 hash 当成本轮已重新核验的文件。
- 在独立 TEMP 工作树 `S8-T02-sol-before-20260904`、固定提交 `0ede8b901fb5e6cbc1c2f2824d6f8a6c7a54f901` 重跑唯一 `MeasuresIsolated100kBatch300kInspectionBaseline` 测试。未运行旧 Shell/ViewModel 测试或启动应用；所有 SQLite 参数显式指向该测试生成的独立 TEMP 数据库。
- 命令：`S8_T01_PERF=1; S8_T01_COMMIT=0ede8b901fb5e6cbc1c2f2824d6f8a6c7a54f901; dotnet test tests/StoreExpiryInspector.Tests/StoreExpiryInspector.Tests.csproj -c Release --no-restore -p:NuGetAudit=false --filter FullyQualifiedName~MeasuresIsolated100kBatch300kInspectionBaseline --logger "console;verbosity=normal"`（前两个值为进程环境变量）。测试 1/1 通过表示记录成功，并非旧性能路径无 blocker。
- JSON：`C:\Users\39037\AppData\Local\Temp\StoreExpiryInspectorS8T01\S8-T01-1e64e11d1b51427c8259afaaff38e5a7\S8-T01-baseline.json`；SHA256 `2C2205DD35F7BF95F41BD8C36E934E977A056F622A3211A65D215E27B3D26BD4`。
- 实际 100,000 Batch / 300,000 Inspection / 300,000 InspectionItem；SQLite 225,427,456 bytes。固定 seed 未改。此基座 logical fingerprint 是 count/min/max/首尾样本指纹，不是全库逐字段比较。
- Stage median/max=7241.5049/7527.403 ms；搜索=39.6453/45.7675 ms；历史全量=921.4756/949.3628 ms；snapshot 单次=6436.4205 ms。
- Dashboard、待排查首/深页/大类/三条件、Today/大类、Reminder 仍记录 `too_many_sql_variables`。失败发生在预热，JSON 的耗时是失败耗时而非有效 median/max；捕获器尚未启用，不能声称这些路径有真实 SQL/plan 记录。
- 此 Before 不单独构成通过依据；After 与最终验收见下。

## Sol 最终独立验收（2026-09-04）

### 实现与正确性

- 新 Terra 提交：隔离 `e294a6b2e6e25e22f92c6a4a2cb30df27ae26630`；查询 `50539c23a99c35da02da5bd65720a7a7433c9ac7`；busy与计量修复 `88d1896c609fdc65c21c1192976e6f1dd22f0b0b`；测试补充 `81a0c1c76fb713bdae2f95b9821927806c293f94`。新 Terra 已停止；Sol 未写生产代码。
- Sol 完整审查恢复main至最终提交全部生产/测试diff。数据库匿名投影排序后再构造TaskListRow，修复EF不可翻译；旧测试按真实分页/count接口更新，并以真实SQLite业务断言验证，不放宽业务结果掩盖错误。
- Dashboard数据库汇总+top20；待排查/Today数据库筛选/count/稳定分页50；历史数据库排序分页50及当前页item聚合；Reminder轻量候选，无超大IN。类别全选和选中ID有效性走轻量ID接口，校验分块900。
- 覆盖全部8种筛选子集、无命中、多页/超界重查、null日期末尾、无TaskItem、多Batch不重复Task；Today跨页/类别选择、52项导出转交及关闭ID后刷新；History/Today忙碌时命令禁用与绑定通知。保留Stage权威算法、blank/0/positive、生命周期、Reminder日期/幂等、详情/Revision及Excel语义。
- Shell任一数据loader注入后，遗漏的默认数据依赖fail-closed；task/category/Today等替身不会因构造自动LoadAsync回退生产依赖。默认factory接缝counter+throw在任何默认路径/文件操作前阻断，专项断言调用0；SQLite显式TEMP/GUID并核对DataSource。本轮未访问正式库，不调查旧事件。
- Today/History仅新增沿用现有样式的分页操作、可访问名称及busy保护，无全局UI重做。没有启动真实GUI；查询耗时不是渲染/首帧或人工操作验收。

### Sol独立命令结果

最终81a0c1c（高规模生产基线另行标注）：

- 隔离：`dotnet test tests/StoreExpiryInspector.Tests/StoreExpiryInspector.Tests.csproj -c Release --no-build --no-restore -p:NuGetAudit=false --filter FullyQualifiedName~S8T02IsolationTests`，9/9。
- 正确性：同项目Release，以 `S8T02IsolationTests|InspectionTaskQueryTests|InspectionHistoryQueryTests|Stage4ViewModelTests|V1F03I04TodayInspectionViewModelTests|S5T03InspectionHistoryViewModelTests|S5T04InspectionHistoryEditViewModelTests|DailyReminder|V1F02` 各项 `FullyQualifiedName~` OR筛选，150/150，0失败/跳过。
- 全量：`dotnet test tests/StoreExpiryInspector.Tests/StoreExpiryInspector.Tests.csproj -c Release --no-build --no-restore -p:NuGetAudit=false`，912/912，0失败/跳过，1m45s。高规模门控测试另行显式运行，不靠普通全量推断。
- Build：`dotnet build StoreExpiryInspector.slnx -c Release --no-restore -p:NuGetAudit=false`，0 warning / 0 error；NuGetAudit=false不代表在线漏洞审计通过。
- EF：`dotnet ef migrations has-pending-model-changes --project src/StoreExpiryInspector/StoreExpiryInspector.csproj --configuration Release --no-build`，无漂移；设计时factory为 `Data Source=:memory:`。
- Migration：`dotnet ef migrations list --project src/StoreExpiryInspector/StoreExpiryInspector.csproj --configuration Release --no-build --no-connect`，9条，末条 `20260901155124_AddPolicyAndBaselineFoundation`。代码清单不代表正式库应用状态；隔离库实际migration9另有证据。
- Git完整diff check通过；无Schema、ModelSnapshot、migration、索引、PRAGMA策略、dependency、csproj或slnx改动。推送前fetch远端仍为0d1d42a，无落后；治理归档普通HEAD:main推送，不改写历史。

### 独立After与证据

- Sol先build，再设置进程环境 `S8_T01_PERF=1`、`S8_T01_COMMIT=88d1896c609fdc65c21c1192976e6f1dd22f0b0b`，执行 `dotnet test tests/StoreExpiryInspector.Tests/StoreExpiryInspector.Tests.csproj -c Release --no-build --no-restore -p:NuGetAudit=false --filter FullyQualifiedName~MeasuresIsolated100kBatch300kInspectionBaseline --logger "console;verbosity=normal"`，1/1通过，1m27s。81a0c1c仅补两个测试文件，生产与基座相对88d1896 diff为空。
- After JSON：`C:\Users\39037\AppData\Local\Temp\StoreExpiryInspectorS8T01\S8-T01-ff89f899303547d693e813643b8512d0\S8-T01-baseline.json`；SHA256 `8FCF92343B77F926A79BD00D8D8F88D13769F260D9618B25D4A503ECA70E3F5F`。
- Before/After JSON与实际SQL归档在 `C:\Users\39037\.codex\visualizations\2026\09\03\01a06754-503b-72c0-b8f1-9e10cd9cad3f\S8-T02-evidence` 的before/after目录，复制后hash一致；没有复制任何数据库文件。JSON保留原TEMP路径，SQL同名归档；原始证据为本机文件，不宣称已上传GitHub。
- 两次真实100,000 Product/Batch、300,000 Inspection/InspectionItem、399,998 ProductTask、99,998 TaskItem、2 Draft/Item、1 Revision；seed未改。
- SQLite和snapshot均225,427,456bytes、integrity=ok、FK违规0、migration9。前后计数及采样指纹一致：`e39f316c7eff133f7fcff6a34483672dc31c91abf1f9ca527415e1ca964f6933`；仅count/min/max/首尾样本，不是全库逐字段证明。排除大类合计2Product/2Batch，openTask及Reminder eligible均0。
- 20条After路径无variable-limit、timeout、lock、crash或OOM blocker。

### Before/After（ms）

After为1次预热+3次测量median/max，随后无计时采集实际SQL/参数/命令/DTO计数及EXPLAIN；snapshot仅1次。失败=旧预热too_many_sql_variables，没有有效median。

| 路径 | Before median | After median / max | 命令数 | DTO数 |
| --- | ---: | ---: | ---: | ---: |
| Dashboard | 失败 | 389.02 / 651.19 | 9 | 20 |
| 待排查首页 | 失败 | 387.64 / 443.19 | 2 | 50 |
| 待排查深页 | 失败 | 752.40 / 791.12 | 2 | 50 |
| 单条命中搜索 | 39.65 | 128.18 / 140.40 | 2 | 1 |
| Stage | 7241.50 | 174.68 / 217.42 | 2 | 50 |
| 大类 | 失败 | 470.83 / 472.11 | 2 | 50 |
| 搜索+Stage | 未独立测 | 317.28 / 343.45 | 2 | 50 |
| 搜索+大类 | 未独立测 | 440.65 / 521.82 | 2 | 50 |
| Stage+大类 | 未独立测 | 322.75 / 348.76 | 2 | 50 |
| 三条件 | 失败 | 295.99 / 318.65 | 2 | 50 |
| 任务详情 | 1.23 | 4.12 / 5.21 | 6 | 1 |
| Today首载 | 失败 | 355.55 / 420.34 | 2 | 50 |
| Today大类 | 失败 | 513.66 / 553.39 | 2 | 50 |
| 历史列表 | 921.48（全量） | 628.39 / 653.86（分页50） | 3 | 50 |
| 历史详情 | 0.32 | 0.37 / 0.82 | 2 | 1 |
| Revision | 0.36 | 0.35 / 0.58 | 1 | 1 |
| ProductTask聚合无变更 | 0.75 | 1.14 / 10.26 | 3 | 未计量 |
| Reminder候选 | 未单独测 | 1126.28 / 1175.47 | 1 | 99998 |
| Reminder+预提醒全评估 | 失败 | 1657.34 / 1844.64 | 4 | 199996 |
| snapshot单次 | 6436.42 | 12815.98（单次） | 0（非EF） | 未计量 |

最慢5条：snapshot、提醒全评估、提醒候选、深页、历史分页。Stage约41倍改善；本次主要分页交互查询均在0.8s以内，不是承诺SLA。搜索和任务详情变慢；snapshot也变慢且代码未改、只有单次观测，原因未建立，不声称所有路径优化。历史Before全量300k与After50条不是同形状吞吐比较；旧Before诊断采集方式与After计时外采集不同，不虚构严格统计显著性。

### SQL/计划、候选与局限

- 本次列表2命令、历史分页3命令，没有应用端逐行N+1；全系统N+1仍为not_proven。correlated SQL子查询不等于应用多次查询。
- 待排查沿用 `IX_tasks_product_id_open` 部分索引，相关MIN经 `IX_task_items_task_id_product_id` 与Batch主键；CASE/最近日期排序仍TEMP B-TREE。最终50行才投影草稿/count，无100k DTO全量后内存筛选。
- 历史仍扫描Inspection并TEMP B-TREE排序；页内聚合走覆盖索引 `IX_inspection_items_inspection_id_product_id`。本次50条页ID参数50，更大合法页按900分块，无无界IN。
- Reminder沿用open索引、Product主键与scope baseline EXISTS索引；预提醒走 `IX_batches_tracking_status_next_trigger_date`。99,998候选仍执行原权威规则，全评估199,996候选DTO是实际工作量，未截断结果冒充优化。
- reader计数为EF ReadCount操作，可含EOF：典型50条列表53、历史104，不是SQL扫描行数或精确实体物化数。DTO计数来自结果，未知标null；内存/GC是快照或累计值，不是峰值/单次增量。SCAN也可能是索引/覆盖索引扫描，不等于全表DTO物化。
- 仅索引候选：`inspections(submitted_at_utc DESC, id DESC)`对应历史排序，可能减少TEMP B-TREE，收益及写入成本未验证。open任务Stage部分索引也不能独自消除CASE/MIN排序。本卡未新增任何索引，后续实施需另批，不建正式Task。
- 未执行100k全选Excel实际导出；52项跨页选择完整转交已测。既有Today导出用例超大IN潜在限制不在本轮读查询压测覆盖范围，不声称修复或100k导出通过。
- snapshot变慢、导入/中断/损坏/灾难恢复压力留待后续授权；没有严重未解决的本卡blocker，Stage8整体未完成。

结论：S8-T02技术验收通过并关闭。治理普通推送后停止，不创建S8-T03、不启动Stage9或在线升级；正式库访问继续禁止，历史隔离事件保持未知。
