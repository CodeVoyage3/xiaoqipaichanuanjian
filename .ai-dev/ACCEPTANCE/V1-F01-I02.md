# V1-F01-I02｜Sol 独立技术验收

- 日期：2026-09-02
- 结论：`TECHNICALLY_ACCEPTED / WAITING_I03_APPROVAL`
- 治理基线：`7208b2c`
- Terra 实现及修复：`9976957`、`10212d7`、`c9489f1`、`e4436f8`
- 验收角色：Sol；未直接采信 Terra 自报结果

## 交付事实

- 现有 Reader → Classifier → Planner → Executor → Lifecycle/Startup 链路已支持 10 个批准源大类，并保存稳定 canonical CategoryCode。
- 食品为 `food / food_expiry / 1 / Managed`；宠物为 `pet / pet_expiry / 1 / Managed`。
- 日用、美妆、家居、香氛香水、文具、潮流玩具在总效期 `>180` 天时为各自 canonical scope + `general_long_expiry / 1 / Managed`；`<=180` 天保存为 Unresolved、空 policy/version，并持久化 `expiry_policy_unresolved` ImportIssue。
- 应季搭配、赠品小样保存 Product / Batch，分别为 `seasonal_assortment`、`gift_sample`，状态 Excluded、空 policy/version，不产生效期 Task。
- 未知大类继续计入既有 skipped/unsupported 统计，同时新增 `unsupported_product_category` 可审计问题；10 个批准大类不计为 skipped。
- 同 ProductCode 的源内或既有 scope/policy 身份冲突产生 `product_scope_policy_conflict`；不改绑、不复制，不生成该商品的 Product/Batch/库存动作或生命周期输入。
- 导入后与启动重算均要求匹配且已完成的 ScopeBaseline；未完成基线的 Managed 商品只保存导入和身份，不提前产生效期 Task。I02 未创建 ScopeBaseline 或 BatchBaseline 业务事实。

## Sol 审查、退回与修复

1. 首轮审查发现 scope/policy 冲突前已加入 ExplicitProductStock，可能让冲突商品进入库存生命周期，并可能在缺失 Product 时触发 KeyNotFound；同时强制持久化/无基线测试不足。退回后由 `10212d7` 在 planner 根因位置后移库存动作、强化 orchestrator 防御，并补真实确认导入测试。
2. Sol 首次受影响专项实际为 115/116：`ConfirmedImportExecutorTests.ExecutesConfirmedChainAtomicallyAndPersistsOnlyApprovedFacts` 失败。根因是 unknown category 虽新增 ImportIssue，却丢失既有 SkippedRow/UnsupportedCategoryCount。`c9489f1` 恢复兼容统计，并补 10 类同表持久化测试。
3. Sol 首次 Release 全量为 721/723：一项为 I02 使旧 startup fixture 缺少 completed food baseline，另一项为既有 S7T03 5 秒异步超时。`e4436f8` 只补合法 baseline fixture；S7T03 未改，Sol 精确复跑后未复现。

## Sol 最终独立门禁

- 受影响链路：117/117，通过 117、失败 0、跳过 0。覆盖 Classifier、Planner、Executor、确认导入生命周期、StartupRecalculation、PostImportLifecycle。
- 首轮全量失败项精确复跑：3/3，通过 3、失败 0。
- Release 全量：723/723，通过 723、失败 0、跳过 0，耗时 1m11s。
- Release build：0 warning / 0 error。
- EF `migrations has-pending-model-changes`：`No changes have been made to the model since the last migration.`
- EF `migrations list --no-connect`：9 条；最后一条仍为 I01 的 `20260901155124_AddPolicyAndBaselineFoundation`。
- 相对治理基线无 migration、ModelSnapshot、schema、`.csproj`、`.slnx`、依赖、WPF 或 Reminder 变更；`git diff --check` 通过。
- 未运行在线 NuGet 漏洞审计；本卡未新增依赖，Release test/build 均以现有恢复结果 `--no-restore` 通过。
- `StoreExpiryInspector` 进程为 0；未启动 WPF，未访问、迁移或修改正式数据库。

## 产品与回归结论

- 10 类 canonical 映射在一次真实确认导入测试中持久化为 10 Product + 10 Batch；无基线时零 Task。
- Managed / Excluded / Unresolved、policy nullability、180D/6M 边界、超大合法总效期、食品/宠物/六类通用长效映射均有专项证据。
- Excluded 与 Unresolved 的保存、ImportIssue、零生命周期，以及 source/existing scope conflict 的数据库不变性均有端到端证据。
- BatchKey 冲突、库存冲突、重复行、局部增量、LastSeenImportId、Seen BatchKey、MaxArrivalQty、库存事实及既有食品导入由受影响专项与最终 Release 全量共同回归通过。

## 范围结论与停点

- I02 技术验收通过；I01 migration 仍是 V1-F01 唯一 migration。
- 未执行方案 C、3% 历史过期补查、首次接管、非食品冷启动 Task、提前 3 天提醒、I03/I04、V1-F02 或 Stage 8。
- I03 正式任务卡和实施代理均未创建；等待产品经理下一次单独批准。
