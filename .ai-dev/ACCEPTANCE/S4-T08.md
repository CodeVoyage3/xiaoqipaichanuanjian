# Acceptance S4-T08｜正式提交、超库存确认与完成/陈旧状态 UI

- 任务卡提交：`07970db37a25dde42c6733907474028518d08165`
- Luna 实现提交：`c9ce1e20cc21da1e2da8d3c029a58bc435519a5d`
- Sol 审查修正提交：`96a034c13f48510b3812c87654506d48df79f5a8`
- 独立审查与验收：GPT-5.6 Sol
- 结论：通过

## 一、交付范围

本卡把 S4-T04 `InspectionSubmissionUseCase` 接入 S4-T07 详情页，交付正式提交、提交互斥、保存队列稳定门禁、Submitted/AlreadySubmitted、超库存事实绑定与二次确认、完成/陈旧/并发关闭/not_found/Error 映射，以及提交后 Detail、Dashboard、PendingTasks 的真实刷新。

UI 未直接访问 EF、创建 `Inspection`/`InspectionItem`、写 `HandledAttentionVersion`、停止 0 件 Batch、删除 Draft 或复制 S4-T04 正式提交规则；未新增 schema、migration、依赖、第五种 stale 状态、S4-T09 或 Stage 5 功能。

## 二、独立审查修正

Sol 完整审查实现提交后，以独立提交修正并锁定三个真实问题：

- 初次详情加载结束时补发 `CanEdit` / `CanSubmit` 通知，避免 open 页面仍保持不可编辑或不可提交表现。
- `IsSubmitting` 变化时同步刷新超库存确认命令，确保二次确认门禁与提交互斥状态一致。
- 超库存只读数值绑定显式使用 `Mode=OneWay`，避免 WPF 默认 TwoWay 写回只读属性导致最终 Release 主窗口启动失败。

修正仅涉及两个 UI 文件和一个 S4-T08 专项测试文件，没有扩大业务范围。

## 三、自动化与技术门禁

- S4-T08 专项：27/27。
- S4-T01～S4-T08 精确回归：177/177。
- Stage 3 十个权威测试类：170/170。
- Release 全量：525/525，0 失败、0 跳过。
- SQLite 正式历史约束专项：6/6；`IX_inspections_task_id` 唯一约束仍在。
- Release build：0 警告、0 错误。
- EF model drift：`No changes have been made to the model since the last migration.`
- migration：仍为 8 条。
- 相对 S4-T07 基线无项目依赖变化；`git diff --check` 通过。
- 在线 NuGet 漏洞审计按本轮上限重试一次，因本机 SSL/TLS 凭据问题未完成：无法加载 NuGet 服务索引，`Authentication failed`，`安全包中没有可用的凭证`。未用历史结果冒充本轮成功，保留后续风险接受。

## 四、最小真实 Release WPF 与 SQLite 验收

Sol 使用最终 Release EXE 和隔离 `%LocalAppData%\StoreExpiryInspector` 数据完成必要用户路径：

- 修正后的最终 Release 一次启动到真实主窗口；默认 1268×754、当前系统 150% DPI 下无明显裁切，首页与任务列表正常显示。
- 接续同一隔离库中的正常提交可靠证据：Task 400 已真实 completed，只有 1 条 Inspection、3 条 InspectionItem；0 件 Batch 为 `stopped / batch_checked_zero`，两个正数 Batch 保持 active；`HandledAttentionVersion = AttentionVersion`，有效 Draft 已删除。启动时 Dashboard 由导入后的 538 显示为 537，任务已从开放列表移除。
- 真实打开 Task 406，页面存在“完成排查”；已保存的两条 CheckedQty 均为 1、有效库存为 1。首次提交真实显示超库存 Warning：当前库存 1、排查合计 2、超出 1；“确认仍然提交”可用。
- 确认后页面真实显示“排查已完成”且只保留返回动作；Task 406 从 PendingTasks 消失，Dashboard 待排查 537→536、过期 432→431。
- SQLite 确认 Task 406 只有 1 条 Inspection、2 条 InspectionItem，Task 为 completed、`close_reason` 保持 `null` 批准语义、有效 Draft 已删除；两个正数 Batch 保持 active，AttentionVersion 未变。
- 在隔离库准备 Task 407 的最小陈旧样本后，页面真实显示“效期阶段或到货情况已更新，请重新确认”。点击完成排查被 Application 拒绝，页面提示“当前排查状态已变化，请重新确认”并禁用错误提交；SQLite 确认 Task 仍 open、Inspection 为 0、Draft 保留、`requires_reconfirmation = 1`。
- completed 页面无编辑、库存修正、清空草稿或再次正式提交入口；没有明显视觉异常。

保存队列、dirty 后先保存、保存失败禁止提交、重复点击、AlreadySubmitted、旧超库存确认失效、事务回滚、刷新失败隔离和四状态/Error 映射由专项自动化证明，未用 GUI 重演。

## 五、用户数据恢复

恢复前确认应用进程为 0，并记录：

- 隔离活动库：2,195,456 字节，SHA-256 `702991489C279F44A527F228CEA7AC4E5E192C898E682CAF2315B05FED6E8FB6`。
- 原用户备份库：299,008 字节，SHA-256 `F3D423DF14B882D7BFE87780A81CF5879F074AF4880601CBEDB6B475A964F522`。

两者明确不同。随后先旁移隔离活动目录，再把 `StoreExpiryInspector.s4t08-backup-20260828` 恢复为正式 `StoreExpiryInspector`；恢复后数据库大小和 SHA-256 与原备份完全一致。隔离目录、旁置备份别名和四个自动化测试临时目录均已删除，应用进程仍为 0；恢复后未再次启动应用，未修改原用户运行状态或日志。

## 六、执行治理与停止门禁

仓库保留了任务卡、实现提交和独立 Sol 审查修正提交三个事实，但 Git 无法独立证明实现执行代理确为一名全新的 GPT-5.6 Luna（max）；本项不作为代码正确性证据伪造补全。

S4-T08 已独立审查、验收并归档。当前立即停止；未创建、编号、派发或预实现 S4-T09，未进入 Stage 5，等待用户单独批准。
