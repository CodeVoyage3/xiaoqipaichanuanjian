# V1-F01-I01 Terra 实施证据

- 实现者：GPT-5.6 Terra；日期：2026-09-01；待 Sol 独立审查、复跑和最终结论。
- 唯一 migration：`20260901155124_AddPolicyAndBaselineFoundation`；现有 `food_v1` 在升级中改为 `food_expiry / 1 / managed`。专项升级测试验证保留 Product 身份且不创建 Task、Inspection、ScopeBaseline 或 BatchBaseline。
- 新增 Product 管理状态/policy version、范围基线及批次基线审计实体与 SQLite 约束；食品导入身份归一为 `food_expiry / 1 / Managed`。未接入生命周期、Reminder、WPF、正式数据库或 I02+。

## 实际命令与结果

- `dotnet test ... --filter "FullyQualifiedName~V1F01I01FoundationTests|FullyQualifiedName~ProductBatchDatabaseTests"`：25/25 通过；涵盖 Managed 默认、Excluded null policy 持久化、范围 policy 限制、Task 来源、catchup 范围和 policy 每个临界点。
- `dotnet test ... --filter "FullyQualifiedName~BackupMetadataDatabaseTests|FullyQualifiedName~SettingsAppStateDatabaseTests"`：10/10 通过；旧 schema raw insert 返回的 Product 已 attach，恢复原 helper 的 tracked-entity 语义。
- `dotnet restore StoreExpiryInspector.slnx --ignore-failed-sources -p:NuGetAudit=false`：通过。
- `dotnet build StoreExpiryInspector.slnx -c Release --no-restore -p:NuGetAudit=false`：0 warnings / 0 errors。
- `dotnet ef migrations has-pending-model-changes ...`：无 pending changes。
- `dotnet ef migrations script ...`：脚本生成；migration 数量 9；`git diff --check` 通过。
- Release 全量 `dotnet test` 已串行发起，但本机工具超时遗留测试子进程，清理本轮可精确归属 PID 后仍未在 Terra 时限内取得完整最终计数；不得视为 Terra 全量通过，交由 Sol 独立复跑。

## 风险与待验收

- EF 生成 migration script 时提示 SQLite `SqlOperation` 与 products rebuild 的审阅提示；专项旧库升级测试已实际通过，Sol 应独立复核该脚本和全量结果。
- 在线 NuGet 漏洞源不可用；离线 restore 使用 `NuGetAudit=false`，不构成在线漏洞审计通过。

## Sol 独立验收（2026-09-02）

结论：`TECHNICALLY_ACCEPTED / WAITING_I02_APPROVAL`。

### 审查、退回与修复证据

- Sol 首次独立运行 I01 Foundation + ProductBatch：12/13，通过 12、失败 1。失败证明 `Excluded` 的显式空 policy 被 EF store default 替换，违反状态契约；Sol 未放行。
- Terra 根因修复 `67efc68b401117eab74b5a53aba0b123199f36de`：Product 使用明确领域默认 `food_expiry / 1 / Managed`，nullable policy/version 不再使用 store default；同时收紧范围 V1 policy、Task 来源、catchup 来源和端点测试。Sol 随后独立复跑相关专项 25/25 通过。
- Sol 首次 Release 全量：709/711，通过 709、失败 2、跳过 0。失败来自 7 个历史 migration 测试 helper 的 raw SQL 分支返回未跟踪 Product，后续 `LastSeenImportId` 未持久化；不是生产 migration 数据丢失，Sol 再次未放行。
- Terra 根因修复 `1863fb55436beaa1a67d55b1ee4cdaf1bf394488`：全部 7 个同型 helper 在旧 schema raw insert 后 Attach 返回对象，恢复原 tracked-entity 语义；未修改生产 migration。

### Sol 最终独立门禁

- I01 Foundation + ProductBatch：25/25，通过 25、失败 0。
- Release 全量：711/711，通过 711、失败 0、跳过 0，耗时 1m21s。
- `dotnet restore StoreExpiryInspector.slnx --ignore-failed-sources -p:NuGetAudit=false`：通过；这是离线恢复，不是在线漏洞审计。
- Release build：0 warning / 0 error。
- EF `has-pending-model-changes`：无模型漂移。
- EF `migrations list --no-connect`：9 条；唯一新增项为 `20260901155124_AddPolicyAndBaselineFoundation`。
- EF 全量 migration script 已生成到忽略目录 `obj/V1F01/i01-sol-migrations.sql`。SQLite provider 给出 `SqlOperation` 与 pending products rebuild 审阅提示；Sol 已核对生成顺序为先加列、迁移 `food_v1 -> food_expiry / 1 / managed`，再按完整列复制重建 products；专项旧库升级也实际通过。
- `git diff --check` 通过；相对 I01 治理基线无 `.csproj`、`.slnx`、WPF、Reminder 或依赖变更。
- migration 文件数 9；`StoreExpiryInspector` 进程 0。未启动 WPF，未访问或迁移正式数据库。

### 范围结论

- I01 产品/Schema 契约、旧库升级、空库、约束、policy 端点和既有回归均通过。
- 未实现全品类映射、首次冷启动、I02+、提前 3 天预提醒、V1-F02 或 Stage 8。
- I01 技术验收完成；未经产品经理单独批准，不创建或执行 I02。
