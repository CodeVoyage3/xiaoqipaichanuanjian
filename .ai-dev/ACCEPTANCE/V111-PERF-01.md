# V111-PERF-01 验收规范

日期：2026-09-16（Asia/Shanghai）
状态：`CLOSED / ACCEPTED / TECHNICAL_PASS / USER_GUI_PASS`

## A. 静态与范围验收

- `RenderOptions.ProcessRenderMode = RenderMode.SoftwareOnly` 在任何 WPF Window 创建前执行。
- 无 Registry 写入、无 Intel/GPU vendor 特判、无系统图形设置修改。
- 除必要启动设置和一个直接专项外无生产/测试扩散。
- migrationCount=10；migration11=NOT_CREATED；Schema、ModelSnapshot、CurrentSchemaIdentity diff=0。
- Version、Installer、Updater、Release Contract、signing、GitHub/Gitee/Quark diff=0。

## B. 自动化门禁

- 直接专项=`PASS`。
- Release App build=`PASS`。
- `git diff --check=PASS`。
- `FULL=NOT_RUN / NO_FULL`。

## C. 当前宿主机内存与 CPU

- 使用当前物理宿主机真实 Intel GPU/driver，不以 Windows Sandbox 替代最终内存验收。
- 记录 Private Working Set、Private Bytes、VMMap Private Data、native Heap、WriteCombine、CPU。
- 64 MiB WriteCombine 块不再出现；WriteCombine 不得恢复为数百 MiB；Private WS 相比 893.8 MiB 出现数量级下降。
- 同等空闲状态 Private WS >250 MiB：`PAUSE / NOT_ACCEPTED`。
- 记录空闲、页面切换、长列表滚动 CPU；不得持续高 CPU、明显卡顿或持续打满单核。

## D. 用户真实 GUI

用户本人连续操作建议 15～30 分钟，覆盖：首页、待排查任务、今日排查、排查详情、商品明细、商品详情、排查历史、数据导入、备份恢复、设置、提醒弹窗、托盘最小化/恢复、最大化/恢复、长列表滚动，以及 Windows 125%/150% 缩放。

重点观察滚动/切页流畅性、字体清晰度、表格闪烁、弹窗与托盘恢复。最终回执仅接受 `GUI PASS` 或 `GUI FAIL`。

## 关闭条件

必须同时满足 `SOL TECHNICAL PASS + MEMORY/CPU PASS + USER GUI PASS`，才允许 `CLOSED / ACCEPTED`。本验收不授权版本变更、tag、Release 或发布。

## 当前验收结果

- implementation=`310ab77d8df38c18c1a3f95de7bd301892bb418c`；Sol integration=`426a686`。
- 静态/范围审查=`PASS`；专项=`1/1 PASS`；Release App build=`PASS / 0 warning / 0 error`；FULL=`NOT_RUN / NO_FULL`。
- Version=`1.1.1`；migrationCount=`10`；migration11=`NOT_CREATED`；Schema/ModelSnapshot/CurrentSchemaIdentity/Installer/Updater/Release diff=`0`。
- 当前物理宿主 Intel GPU/driver、TEMP/GUID 候选：Private WS=`77.8 MiB`，Private Bytes=`117.3 MiB`，VMMap Private Data=`9.1 MiB`，native Heap=`51.1 MiB`，WriteCombine=`0 blocks / 0 MiB`，64 MiB WriteCombine=`0`，启动主窗=`1.7 s`，Responding=`true`，10 秒空闲 CPU=`0.05%`。
- 与正式 v1.1.1 的 893.8 MiB Private WS 相比下降约 91.3%，满足数量级下降及 `<250 MiB` 门禁。
- 页面切换 287 秒：CPU average=`0.116%`、P95=`0.418%`、max=`0.847%`，Responding=`true`。
- 长列表滚动/最大化恢复 104 秒：CPU average=`0.268%`、P95=`0.793%`、max=`1.2%`，Responding=`true`。
- 交互后 VMMap Total Private WS=`170.2 MiB`、Total Private=`208.9 MiB`、Private Data=`175.5 MiB`；WriteCombine=`0 blocks / 0 MiB`，64 MiB WriteCombine=`0`。
- `SOL TECHNICAL PASS=PASS`；用户完整真实 WPF 回执=`GUI PASS`。
- `FINAL ACCEPTED=YES`；V111-PERF-01=`CLOSED / ACCEPTED`。
- 验收后已结束 PID 29936 并删除隔离 TEMP/GUID 数据根；VMMap、CPU CSV 与治理证据保留。
- main integration/push=`NOT_RUN / AUTHORIZATION_PENDING`；Version/Release=`UNCHANGED / NOT_STARTED`。
