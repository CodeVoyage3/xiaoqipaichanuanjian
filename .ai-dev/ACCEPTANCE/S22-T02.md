# S22-T02 验收规范

日期：2026-09-14（Asia/Shanghai）
状态：`CLOSED / ACCEPTED`

最终结论：

- `TECHNICAL PASS = PASS`
- `USER GUI PASS = PASS`
- `FINAL ACCEPTED = YES`
- final implementation candidate=`ba02834c03dec5b40a3d0886771afc7fddc0d837`，已确认包含此前 implementation `8ccc5e84b5aad8c3eae740c764506ef502662e7c` 及 GUI R1。

## 技术验收

- 库存增减严格比较本次 ExcelStockQty 与导入前持久化 ExcelStockQty；人工修正 EffectiveStockQty 不改变分类。
- 库存变 0 只认已有商品 before `> 0`、after `= 0`。
- ProductCode 相同而名称/条码变化不得计为新增商品。
- Batch identity 严格复用现有有/无生产日期唯一键。
- 缺失基准严格为导入前最近一次 `Succeeded && !IsUndone`；主数按批次去重，可显示涉及商品数。
- 缺失待办只认缺失批次关联的 open ProductTask；统计过程不得产生任何业务写入。
- 首次导入五项无历史基准指标显示 `—`。
- 成功页异常总数等于提交后本次 ImportId 的实际 ImportIssue 数；未支持大类不得重复计数。
- 解析失败、未确认、确认失败、事务回滚、Undone 导入不得成为比较基准。
- 缺失不得导致 Product/Batch 删除、库存归零、Task 关闭或生命周期变化。
- 结果只在本次 ImportViewModel 生命周期内展示；不承诺历史回看。

## UI 验收

- 仍位于现有“数据导入”页，无新增一级导航。
- “导入成功”层与“本次数据变化”层层级清楚；七类变化在 1024×600、125% 下可读且可滚动。
- 首次导入的不可用指标明确为 `—`，不会误解为零变化。
- 新增用蓝色/中性色，库存减少和变 0 用橙色，缺失待办为重点警示，异常为红色；不把全部变化做成危险红。
- 现有异常明细仍可读，不出现重复计数或业务建议。

## 范围门禁

- migrationCount 必须保持 `10`；migration11、ModelSnapshot、CurrentSchemaIdentity diff=`0`。
- Version、Installer、Updater、Release Contract 和发布元数据 diff=`0`。
- `FULL=NOT_RUN / NO_FULL`；正式数据库不得访问，SQLite 证据只用 TEMP/GUID 隔离数据。
- Terra 不得自行将本卡标记为 technical pass、GUI pass、accepted 或 closed。

## 最终关闭条件

1. Terra 实现提交返回当前 Sol。
2. Sol 独立审查与最小技术门禁通过。
3. 用户本人完成真实 WPF GUI 验收并明确确认 PASS。

三项全部满足后，S22-T02 才允许 `CLOSED / ACCEPTED`。

## Sol 独立技术验收结果

- final implementation=`8ccc5e84b5aad8c3eae740c764506ef502662e7c`。
- 独立 diff/业务审查=`PASS`：库存严格复用 ImportPlan 的 ExcelStockQty before/after；缺失查询只读；摘要读取失败不把已提交导入改成失败或开放重复执行。
- S22-T02 + S4T06 ImportViewModel + ExcelImportPlanner=`63/63 PASS`。
- ConfirmedImportExecutor + ConfirmedImportLifecycleOrchestrator + ImportConfirmationGuard=`36/36 PASS`。
- Release App build=`PASS / 0 warning / 0 error`；`git diff --check=PASS`。
- migrationCount=`10`；migration11=`NOT_CREATED`；Schema、ModelSnapshot、CurrentSchemaIdentity、Version、Installer、Updater、Release diff=`0`。
- `FULL=NOT_RUN / NO_FULL`；正式数据库未访问；Sol technical=`PASS`。
- 该轮用户真实 WPF GUI 尚未执行；最终结果已由下方正式收口记录覆盖。

## 用户 GUI 验收入口

- 双击 `docs/S22-T02-开始验收.cmd`；入口生成 TEMP/GUID 隔离数据库与专用 Excel，复制 Excel 路径并启动当前 Release WPF。
- 逐项步骤和固定预期数字见 `docs/S22-T02-GUI验收步骤.md`。
- fixture 端到端固定数字=`6/6 PASS`；外部 TEMP/GUID 持久生成=`1/1 PASS`；PowerShell 语法、CMD CRLF 与 PowerShell UTF-8 BOM=`PASS`。
- 用户回执必须为 `S22-T02 GUI PASS` 或 `S22-T02 GUI FAIL`；明确 PASS 前不得收口。

### GUI 入口 R1 修复

- 用户首次双击入口在生成 fixture 前失败：`--no-restore` 复用了 CodexSandboxOffline 的 NuGet 资产路径，普通 Windows 用户无法解析 SQLite 程序集；该失败属于验收入口，不是生产业务失败。
- 入口现固定使用当前 `$env:USERPROFILE\.nuget\packages`，先执行 `dotnet restore --packages ... --force-evaluate --ignore-failed-sources`，再运行 fixture 与 Release build。
- 使用 `C:\Users\39037\.nuget\packages` 的精确复验：restore=`PASS`、fixture=`1/1 PASS`、Release App build=`0 warning / 0 error`。
- 用户真实 WPF GUI 仍为 `NOT_RUN / PENDING`。

## GUI 返修 R1（2026-09-14）

- 用户首轮真实 GUI 回执=`FAIL`：成功页残留预览阶段 UI，七项指标缺少独立边界和清晰层级。
- 本轮仅调整 Succeeded 状态 UI：成功后隐藏影响摘要、导入前提示、取消与确认导入；底部改为“再次导入 / 完成”。
- “本次数据变化”改为首行 4 张、次行 3 张独立 Border 卡片；标签、主数字、辅助说明均位于对应卡片内部并复用既有资源色。
- `ImportIssue=0` 时仅显示“未发现数据异常”；存在异常时继续复用既有明细表，未修改异常统计逻辑。
- S22-T02 + S4T06 ImportViewModel=`32/32 PASS`；外部 TEMP/GUID fixture=`1/1 PASS`；Release App build=`0 warning / 0 error`；`FULL=NOT_RUN / NO_FULL`。
- 隔离渲染已生成完整成功页、1024×600 滚动到底部及七项实际结果截图；该证据不替代用户本人复验。
- 用户最终真实 WPF GUI 回执=`S22-T02 GUI PASS`。
- 本卡正式 `CLOSED / ACCEPTED`；不得再修改已冻结产品语义或 GUI 结构。

## 正式收口

- 库存变化冻结为本次 `ExcelStockQty` 对比上一次有效 `ExcelStockQty`；首次导入五项历史指标显示 `—`。
- 缺失仅识别、统计、展示，不归零、不删除 Product/Batch、不关闭 Task、不改变 Lifecycle；异常取事务提交后实际 ImportIssue。
- 成功页固定为“文件来源 → 导入成功 → 本次数据变化 → 导入异常 → 再次导入/完成”，七项使用 4+3 独立卡片。
- targeted tests=`32/32 PASS`；Release App build=`0 warning / 0 error`；收口无额外生产改动，未重跑。
- migrationCount=`10`；migration11=`NOT_CREATED`；Schema/Version/Installer/Updater/Release diff=`0`；`FULL=NOT_RUN / NO_FULL`。
