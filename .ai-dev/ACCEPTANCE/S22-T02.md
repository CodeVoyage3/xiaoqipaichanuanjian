# S22-T02 验收规范

日期：2026-09-14（Asia/Shanghai）
状态：`SOL_TECHNICAL_PASS / USER_GUI_PENDING / NOT_ACCEPTED`

## 技术验收

- 库存增减严格比较本次 ExcelStockQty 与导入前持久化 ExcelStockQty；人工修正 EffectiveStockQty 不改变分类。
- 库存变 0 只认已有商品 before `> 0`、after `= 0`。
- ProductCode 相同而名称/条码变化不得计为新增商品。
- Batch identity 严格复用现有有/无生产日期唯一键。
- 缺失基准严格为导入前最近一次 `Succeeded && !IsUndone`；主数按批次去重，可显示涉及商品数。
- 缺失待办只认缺失批次关联的 open ProductTask；统计过程不得产生任何业务写入。
- 首次导入五项无历史基准指标显示 `—`。
- 成功页异常总数等于提交后本次 ImportId 的实际 ImportIssue 数；未支持大类不得重复计数。
- 解析失败、未确认、确认失败、事务回滚、Undone 导入不得成为比较基准。
- 缺失不得导致 Product/Batch 删除、库存归零、Task 关闭或生命周期变化。
- 结果只在本次 ImportViewModel 生命周期内展示；不承诺历史回看。

## UI 验收

- 仍位于现有“数据导入”页，无新增一级导航。
- “导入成功”层与“本次数据变化”层层级清楚；七类变化在 1024×600、125% 下可读且可滚动。
- 首次导入的不可用指标明确为 `—`，不会误解为零变化。
- 新增用蓝色/中性色，库存减少和变 0 用橙色，缺失待办为重点警示，异常为红色；不把全部变化做成危险红。
- 现有异常明细仍可读，不出现重复计数或业务建议。

## 范围门禁

- migrationCount 必须保持 `10`；migration11、ModelSnapshot、CurrentSchemaIdentity diff=`0`。
- Version、Installer、Updater、Release Contract 和发布元数据 diff=`0`。
- `FULL=NOT_RUN / NO_FULL`；正式数据库不得访问，SQLite 证据只用 TEMP/GUID 隔离数据。
- Terra 不得自行将本卡标记为 technical pass、GUI pass、accepted 或 closed。

## 最终关闭条件

1. Terra 实现提交返回当前 Sol。
2. Sol 独立审查与最小技术门禁通过。
3. 用户本人完成真实 WPF GUI 验收并明确确认 PASS。

三项全部满足后，S22-T02 才允许 `CLOSED / ACCEPTED`。

## Sol 独立技术验收结果

- final implementation=`8ccc5e84b5aad8c3eae740c764506ef502662e7c`。
- 独立 diff/业务审查=`PASS`：库存严格复用 ImportPlan 的 ExcelStockQty before/after；缺失查询只读；摘要读取失败不把已提交导入改成失败或开放重复执行。
- S22-T02 + S4T06 ImportViewModel + ExcelImportPlanner=`63/63 PASS`。
- ConfirmedImportExecutor + ConfirmedImportLifecycleOrchestrator + ImportConfirmationGuard=`36/36 PASS`。
- Release App build=`PASS / 0 warning / 0 error`；`git diff --check=PASS`。
- migrationCount=`10`；migration11=`NOT_CREATED`；Schema、ModelSnapshot、CurrentSchemaIdentity、Version、Installer、Updater、Release diff=`0`。
- `FULL=NOT_RUN / NO_FULL`；正式数据库未访问；Sol technical=`PASS`。
- 用户真实 WPF GUI=`NOT_RUN / PENDING`，因此本卡继续 `NOT_ACCEPTED / NOT_CLOSED`。
