# S22-T01 验收记录

日期：2026-09-14（Asia/Shanghai）
状态：`TECHNICAL_PASS / GUI_FAIL / UI_REWORK_AUTHORIZED / NOT_ACCEPTED`

## 治理基线

- fresh baseline=`origin/main@d97e7bda89c2ab047bf09edb80073c069ed9090b`。
- Stage22=`IN_PROGRESS / S22-T01_TECHNICAL_ACCEPTANCE_READY / WAITING_USER_GUI`。
- S22-T01=`IMPLEMENTED / SOL_TECHNICAL_PASS / WAITING_USER_GUI / NOT_ACCEPTED`。
- S22-T02=`NOT_CREATED / NOT_STARTED`；migration11=`FORBIDDEN`。
- 正式 dirty 工作区历史 `1 modified + 4 untracked` 必须保持原样。

## 技术验收门禁

- 商品列表一行一个 Product；搜索、大类/阶段/任务状态筛选、50 条分页、清空/刷新、空态/错误态正确。
- 当前总库存严格取 `Product.EffectiveStockQty`；不得将批次累计到货求和。
- 最近到期日、最高风险阶段仅聚合 active Batch；阶段优先级复用 `ExpiryStageCalculator`。
- 待处理任务为唯一 open task 的当前 TaskItem 数；最近导入来自成功未撤销 LastSeen Import。
- 商品详情不重复 KPI，不含商品概览、保质期、总效期或重复待处理字段。
- 批次以生产日期 + 到期日期识别；无批次号；无“当前库存”；`CurrentArrivalQty` 只能标“累计到货”。
- 去排查只复用现有 open task 详情；无待办显示 `—`。
- 最近导入信息只显示最近一次。
- Schema、migration 文件、ModelSnapshot、CurrentSchemaIdentity diff=`0`；migrationCount=`10`。
- 版本、Installer、Updater、Release Contract 和发布元数据 diff=`0`。
- S22-T01 专项、直接相关旧回归、必要 Release App build PASS；`FULL=NOT_RUN / NO_FULL`。
- SQLite 只使用 TEMP/GUID 隔离数据库，不访问正式数据库。

## Sol 技术验收证据

- governance=`8a69d09af4db5d3558d4bfbd4c8c97a9ed61095a`；production candidate=`5c0ffec9f1e88d165bb54b3f17355421ba304f24`。
- S22-T01 专项=`1/1 PASS`；S18-T03 导航聚焦回归=`2/2 PASS`。
- Release App build=`PASS / 0 error`；仅有离线 NuGet 漏洞源 `NU1900` warning。
- Schema、migration、ModelSnapshot、CurrentSchemaIdentity、版本、Installer、Updater、Release diff=`0`；migrationCount=`10`。
- 正式 dirty 工作区仍为历史 `1 modified + 4 untracked`；`FULL=NOT_RUN / NO_FULL`；未 push。
- Sol 技术结论=`PASS`；最终状态仅到 `WAITING_USER_GUI`。

## 用户 GUI 验收

2026-09-14 首轮用户 GUI=`FAIL`：功能骨架基本正确，但视觉仍接近默认 WPF 控件拼装，与冻结原型差距明显。已授权仅做 UI 返修；以下清单待新截图复验。

商品明细页：

- 左侧导航正确，“商品明细”位于“数据导入”上方。
- 一行一个商品；搜索和三个筛选布局合理；无库存筛选。
- 冻结字段、分页、详情入口正确，无明显拥挤、截断或错位。
- 只出现正常、5折、2折、收仓、过期。

商品详情页：

- 顶部身份信息不与 KPI 重复，5 个 KPI 只显示一次。
- 不出现商品概览、保质期、总效期或重复待处理字段。
- 批次明细为主要内容；按生产日期 + 到期日期识别，无伪批次号、无批次当前库存。
- 待办批次“去排查”清楚，无待办显示 `—`。
- 最近排查日期是辅助信息；最近导入信息位于底部辅助区域且只有最近一次。
- 1024×600、125% 缩放下可用，整体结构与两张冻结原型一致。

用户 GUI PASS 前：S22-T01 不得 `CLOSED / ACCEPTED`。
