# S1-T06 验收记录

- 验收日期：2026-08-26
- 结论：通过
- 执行模型：GPT-5.6 Luna（max）
- 任务卡提交：`41e17740bc2369c879bdae962b35d295cd6b4820`
- 实现提交：`099926a4c5dd4064cf3ae7baa1cdad5c61c16cee`

## Sol 独立审查

- 实现提交仅修改任务卡授权的 13 个文件；`.ai-dev/**`、UI、App、项目/solution、依赖、既有领域实体与既有四条 migration 均未变化。
- 新增且仅新增 `imports`、`import_workbooks`、`import_issues` 三张业务表及一条 `AddImportPersistence` migration。
- 三个新实体各有独立 `IEntityTypeConfiguration<T>`；`StoreDbContext` 仅增加 3 个 DbSet 与 3 个显式配置注册。
- Product/Batch 配置只增加既有 `LastSeenImportId` 到 `imports(id)` 的 `NoAction` 外键，没有新增导航或修改业务字段。
- 未新增 Excel/Open XML、导入/撤销服务、快照或事务编排、最近两份裁剪、状态机、生命周期事件、UI、Repository、UnitOfWork、通用基类或 Stage 2+ 实现。
- 执行中产生的根目录临时 migration script 已在实现提交前删除，未进入版本库。

## 数据完整性验收

- `imports` 精确包含任务卡批准的 17 个字段；所有计数拒绝负数，撤销标记与时间保持一致。
- 导入文件名、状态、问题类型和安全摘要 Trim 后非空；SHA-256 只接受 64 位小写十六进制。
- `import_workbooks` 精确包含 6 个字段；BLOB 必填且非空，同一导入最多一份工作簿。
- `import_issues` 精确包含 6 个字段；文件级异常允许空行号，非空行号必须大于 0，并可按 `(import_id, row_number, id)` 稳定排序。
- 工作簿、异常、Product 和 Batch 的导入外键均拒绝无效 ID，PRAGMA 实测删除行为为 `NO ACTION`。
- Product/Batch 的 `last_seen_import_id` 仍允许 NULL；引用后导入记录不能删除，历史不会级联丢失。
- 原始文件哈希索引为普通索引，不擅自禁止重复文件记录；状态值域与“两份工作簿”裁剪留给 Stage 2 单一业务实现。

## 迁移与旧数据验收

- 从 `AddInventoryAdjustments` 写入 Product、Batch、Task、TaskItem、Draft、DraftItem、Inspection、InspectionItem、InspectionItemRevision、InventoryAdjustment 后升级：十类旧数据全部保留。
- 增量脚本为 9,659 字节，新建 `imports`、`import_issues`、`import_workbooks` 和两个预期临时重建表 `ef_temp_batches/products`。
- 增量脚本只删除并重建 `batches`、`products` 以增加外键；其他八张既有业务表没有 DROP 或临时重建。
- 完整 migration script 成功生成，共 26,835 字节。

## 独立验证结果

- `dotnet build StoreExpiryInspector.slnx --nologo --no-restore`：0 警告，0 错误。
- `dotnet test StoreExpiryInspector.slnx --nologo --no-restore`：32 通过，0 失败，0 跳过。
- `dotnet ef migrations has-pending-model-changes ... --no-build`：无模型漂移。
- 官方 NuGet 源在线漏洞审计：应用与测试项目均无已知漏洞。
- `git diff --check`：通过；测试临时数据库残留为 0；实现提交后工作区干净。

## 阶段状态

`S1-T06` 通过。Stage 1 尚未整体完成；本轮不创建下一张任务卡，不进入 Stage 2。
