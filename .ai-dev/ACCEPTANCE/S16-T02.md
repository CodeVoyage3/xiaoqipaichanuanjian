# S16-T02｜安装完成后自动启动主程序｜验收门禁

日期：2026-09-10（Asia/Shanghai）
状态：`GUI_ACCEPTED / CLOSED`

## Sol 独立验收

- 审查真实 diff、Inno Setup Run/postinstall 条件、Updater→Setup 参数、Updater 最终重启职责、App 单实例机制与覆盖安装顺序。
- 普通人工安装必须默认在成功完成后启动一次最新版 App；取消、失败、中断或未完成不得启动。
- Updater 必须显式关闭 Setup 自启动，silent/verysilent/suppressmsgboxes 不产生意外 UI/启动；Updater 原有唯一最终重启与 ACK/事务协议保持。
- 只跑 S16-T02 直接专项；Release Setup build、必要 App/Updater build、EF no drift、migration `9`。`FULL = NOT_RUN / NO_FULL`。

## 用户验收

- A 全新安装：成功完成后新版主界面自动出现且只有一个实例。
- B 手工覆盖 v1.0.7：替换完成后 v1.0.8 自动启动、版本/原数据正常且只有一个实例。
- C 在线更新：自动化若不能充分证明无双启动，再准备最小真实验收。

技术验收通过后状态仅为 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`；不得自判人工安装通过、关闭 S16-T02/Stage16、tag/Release/Quark/Gitee 发布 v1.0.8。

## Sol 独立技术验收记录

- 实现提交：`70512165526e63b50b8086fcd4be3e82c197e38d`；实际生产改动仅为 Inno `[Run]` 与 App/Updater `1.0.8` 版本，另有一个直接专项测试文件。
- 人工安装机制：`postinstall nowait skipifsilent` + `ShouldLaunchApplication`；明确识别 `/NOPOSTINSTALLRUN`、`/SUPPRESSMSGBOXES` 与 `WizardSilent`。
- Updater 真实链路复核：当前版本从不启动 Setup；它直接完成 ZIP 程序树切换并由既有 `StartNormalApplication` 唯一重启 App。Setup 的明确禁用开关保留给任何无人值守调用方，未改变在线更新协议。
- 双启动风险：当前 Updater 与 Setup 不在同一执行链；App 既有 Mutex 保留。取消/失败不会进入 Inno 安装成功后的 `[Run]`。
- 直接专项 `3/3 PASS`；App build `PASS`；Updater build `PASS`；Setup build `PASS`。
- EF `has-pending-model-changes`：无漂移；migration 列表 `9`；未连接正式数据库。
- 候选 Setup：`D:\wendang\ChatGPT\门店效期排查软件\TestResults\8c9d7190-567f-4ae2-88af-8f0cc245e07b\StoreExpiryInspector-Setup-1.0.8.exe`，`75350802` bytes，SHA256 `C5680C9FF9DBEB9291B6E5DBD07B7997A059762DD347F7EF1B87100FCFE9183A`。
- 候选内 App/Updater FileVersion 均为 `1.0.8.0`；`FULL = NOT_RUN / NO_FULL`。

## 返修验收门禁

- 旧候选 SHA256 `C5680C9FF9DBEB9291B6E5DBD07B7997A059762DD347F7EF1B87100FCFE9183A` = `REJECTED_CANDIDATE`。
- 安装期间 `InstallMutex` 必须继续存在；普通安装文件替换完成后、postinstall App 创建前必须 `CloseHandle(InstallMutex)` 并置零。
- 自动启动的真实 App 必须取得 `Local\StoreExpiryInspector.SingleInstance`，继续完成主窗口显示与托盘初始化，不得触发现有“已在运行”提示。
- `/NOPOSTINSTALLRUN`、silent/verysilent、取消、失败继续不启动；Updater 仍不调用 Setup，并由既有 `StartNormalApplication` 唯一重启。
- 只跑返修直接专项、Release Setup 与必要 App/Updater build；`FULL = NOT_RUN / NO_FULL`。Sol 独立验收后重新生成新候选并停在等待用户重验 A/B。

## 返修 Sol 独立技术验收记录

- 实现 `cd7f7c893012e8f21c82495e4abfc55092be0e54`：`PrepareToInstall` 的安装保护 Mutex 保留；普通 `[Run]` 条目 Check 成立后先调用 `ReleaseInstallMutexForApplicationLaunch`，关闭并清零句柄，然后才由 Inno 执行 App。
- App 单实例代码、主窗口/托盘初始化路径与 Updater `StartNormalApplication` 均无 diff；Updater 仍不调用 Setup。
- 直接专项 `5/5 PASS`。
- Sol 独立 TEMP/GUID TestMode 安装：Setup exit `0`；App 自动启动；`APP_OWNS_MUTEX=True`；`MAIN_WINDOW_SHOWN=True`；`DUPLICATE_PROMPT=False`；`TRAY_KEEPALIVE_AFTER_CLOSE=True`；隔离卸载 exit `0`。
- `/NOPOSTINSTALLRUN + /VERYSILENT /SUPPRESSMSGBOXES`：Setup exit `0`、App 未启动；`/SILENT`：Setup exit `0`、App 未启动。
- Sol 独立 Release Setup build `PASS`；候选内 App/Updater FileVersion 均为 `1.0.8.0`；`FULL = NOT_RUN / NO_FULL`。
- 新候选：`D:\wendang\ChatGPT\门店效期排查软件\TestResults\c2a4a47b-82c1-449c-88de-62de223b038c\StoreExpiryInspector-Setup-1.0.8.exe`，`75342179` bytes，SHA256 `8017D31F3FB09AFE1BDDA2B565CD31A353B2282F028E38068F88419A77C0124D`。
- 当前仅等待用户重新执行 A 全新安装与 B 手工覆盖安装；不得关闭或发布 v1.0.8。

## 用户最终验收结果

- A 全新安装：`PASS`。
- B 覆盖安装、原数据、单实例/托盘：`PASS`。
- S16-T02 = `GUI_ACCEPTED / CLOSED`；不关闭 Stage16，不发布 v1.0.8。
