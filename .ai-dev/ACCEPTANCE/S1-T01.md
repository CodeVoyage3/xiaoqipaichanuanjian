# S1-T01 验收记录

- 验收日期：2026-08-26
- 结论：通过
- 执行模型：GPT-5.6 Luna（max）
- 实现提交：`91c37ad48d0d7ac747484b8d2be079b5c7e72404`
- 约束修复：`6673634ac122993845c22496f5563b61812503a3`
- SQLite 安全更新：`8dc744105983f9e4c49e4116ad500d0d26239e4c`

## Sol 独立审查

- 提交范围只包含任务卡授权路径，工作区干净。
- 未修改 `.ai-dev`，未实现任务、草稿、导入、备份或 Stage 2+ 功能。
- 商品与批次关系禁止级联删除。
- 两类批次唯一键使用 SQLite 部分唯一索引，没有拼接字符串键。
- 商品编码由 EF 写入时 Trim，数据库同时拒绝带首尾空格或空白编码。
- `category_code`、`policy_code` 保留默认值但不锁死食品取值，未来品类无需推翻主表结构。
- 初始 migration、Designer、ModelSnapshot 一致；无 pending model changes。
- SQLite 原生 bundle 固定为 2.1.13，同线覆盖 EF Core 传递依赖中的已知高危版本。

## 独立验证结果

- `dotnet list StoreExpiryInspector.slnx package --vulnerable --include-transitive --no-restore`：两个项目均无已知易受攻击包。
- `dotnet build StoreExpiryInspector.slnx --nologo --no-restore`：0 警告，0 错误。
- `dotnet test StoreExpiryInspector.slnx --nologo --no-restore`：7 通过，0 失败，0 跳过。
- `dotnet ef migrations has-pending-model-changes ... --no-build`：无模型漂移。
- WPF EXE 实际启动后持续运行 3 秒，随后只终止该验收进程。

## 已验证业务/数据约束

- 首次迁移、表和关键索引。
- WAL、外键约束。
- 商品编码 Trim 与唯一性。
- 有/无生产日期的两类批次唯一性。
- 不同商品允许日期相同。
- 保质期单位限制。
- Excel/有效库存与当前/历史最高累计到货负数拒绝。
- 类别/策略代码非空白且允许未来稳定代码。

## 未验证/未实现

- 未做主窗口视觉验收；本任务只要求默认窗口可编译启动。
- Windows 10 门店具体版本兼容性仍需后续实机验收。
- Stage 1 其余实体尚未实现，Stage 1 整体未完成。
- 安装、Excel、任务引擎、业务 UI 与备份均不属于本任务。
