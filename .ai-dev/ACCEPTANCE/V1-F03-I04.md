# V1-F03-I04｜Sol 独立技术验收

## 结论

- 日期：2026-09-03。
- 状态：`GUI_RETEST_R4_REPAIR_TECHNICALLY_ACCEPTED / GUI_ACCEPTANCE_FAILED / WAITING_USER_RETEST`。
- 开工基线：`50fbc80fdaa9817f53a900691e6b14c152fc32ae`；治理提交：`6a4cf8a`。
- 原 Terra 实现/返修：`a14eed7`、`d784f6f`、`39db873`；GUI blocker Terra 修复：`e2f1b21`。
- 结论只覆盖静态、ViewModel、Application、数据库隔离测试与构建门禁；不替代用户真实 WPF GUI 验收。

## GUI 重验 R4 增量返修（2026-09-03）

- 用户最新真实 GUI 回执为 R3 大部分通过、剩余 4 项；正式任务卡/治理提交：`.ai-dev/TASKS/V1-F03-I04-GUI-RETEST-R4-REPAIR.md` / `f4a585b`，基线为 `7c5fe71355f7ef6e58a335a0dba921f42c8537a5`。
- 当前 Sol 话题内全新 GPT-5.6 Terra（medium）实现提交：`a988d14`；Sol 两轮完整 diff 审查退回补正：`bf9e896`、`a360aca`。Terra 未 push、未修改验收或交接文档。
- Today 大类 ComboBox 的透明 ToggleButton/HitTest 根 Border 覆盖完整可见矩形；文字区、主体区与箭头区均进入同一展开控件，Hover/Press/Focus 由外层 Chrome 统一表达。ItemsSource、SelectedCategory、大类集合、TaskId 与选择逻辑未改。
- Today 商品名称补齐真实垂直居中，Ellipsis、Tooltip、星号列宽、行高、virtualization/recycling 与业务选择均不变。
- 确认窗字段区使用独立 32px 控件行和错误文字行；排查人/日期标签分别与 TextBox/DatePicker 居中，Validation 文案不重叠、不推动控件。DatePicker 继续使用真正 WPF PART，并复用既有 `CalendarIcon`，日期业务门禁未改。
- 确认表固定为条码、商品名称、当前阶段、生产日期、有效日期、本次排查数量六列；当前阶段来自 Preview canonical Stage，经唯一 `StageLabels → ExpiryStageCalculator.ToDisplay` 映射显示，并使用居中轻量 Badge。未恢复“校验状态”，I02 异常汇总、浅红行、Tooltip 与校验未改。
- 相对治理基线仅修改 `MainWindow.xaml`、`TodayInspectionConfirmationWindow.xaml`、`TodayInspectionViewModel.cs` 与 I04 专项测试；大类筛选业务、I01～I03 核心算法均无 diff。
- Sol 新鲜 R4/I04 专项：29/29；I01～I04 与 WPF/UIUX 组合：137/137；ProductTask/生命周期/Reminder/商品导入/历史等相关回归：401/401；Release 全量：883/883，失败 0、跳过 0。
- Release build：0 warning / 0 error；EF `has-pending-model-changes` 无漂移；`migrations list --no-connect` 为 9 条，最后一条仍为 `20260901155124_AddPolicyAndBaselineFoundation`。
- 无 `.csproj`、`.slnx`、migration、ModelSnapshot 或依赖差异；`git diff --check f4a585b..HEAD` 通过。未启动 WPF，`StoreExpiryInspector` 进程 0，未访问生产数据库；本轮未将离线依赖事实冒充在线漏洞审计。
- 本节只证明 R4 技术门禁通过。真实 GUI 仍须用户本人重验 4 项，因此 I04/V1-F03 均不得 CLOSED 或进入 Stage 8/9。

## GUI 重验 R3 增量返修（2026-09-03）

- 正式任务卡/治理提交：`.ai-dev/TASKS/V1-F03-I04-GUI-RETEST-R3-REPAIR.md` / `372fd4a`；基线为 `9838f99b824188c6eb88835b4c1a8b0834231adf`。
- 当前 Sol 话题内全新 GPT-5.6 Terra（medium）实现提交：`cd26879`；Sol 两轮完整 diff 审查退回补正：`55b0cf8`、`1da4667`。Terra 未 push、未修改验收或交接文档。
- Shell 非折叠侧栏使用最小足够的 208 宽度，品牌文字完整显示并保持图标/文字居中；折叠 72 与导航行为不变。Today StageBadge 双向居中，大类筛选复用局部 ComboBox 模板且筛选/TaskId 选择逻辑未改。
- I01 门店可见“当前阶段”复用唯一 canonical Stage 中文映射，真实导出显示“过期/收仓”等业务文案；隐藏 Stage 快照继续保存 canonical 值。门店可见库存表头与 I02 严格 Reader 均为“总库存”，未使用合并单元格。
- 真实 I01 workbook 测试重开实际 `.xlsx`，锁定中文阶段、“总库存”、中文大类、A:L AutoFilter、M:Y 隐藏稳定身份，并将同一文件交给 I02 Reader 回读。
- 确认窗口五列内容统一垂直居中；真正 WPF DatePicker 使用局部模板收敛边框、圆角、日历按钮及 Hover/Focus/Validation，默认今天与禁止未来日期业务门禁不变。
- 正式调用 I03 前，基于 Preview 的 canonical `expired` 与完整可应用任务聚合过期正库存批次；空白/0/其他 Stage 不触发。“返回检查”零 I03 调用且保留填写结果，“确认无误，继续提交”后才进入既有编排；后续超库存/stale 等门禁不变。合计使用 `long` 防止多批次 Int32 溢出。
- Sol 新鲜 R3/I01/I04 专项：32/32；I01～I04 与 WPF/UIUX 静态组合：98/98；ProductTask/生命周期/Reminder/商品导入/历史等相关回归：440/440；Release 全量：883/883，失败 0、跳过 0。
- 离线 `NuGetAudit=false` restore 后 Release build：0 warning / 0 error；普通构建与 EF 命令的在线漏洞源返回 NU1900，未宣称在线漏洞审计通过。
- EF `has-pending-model-changes`：无漂移；`migrations list --no-connect`：9 条，最后一条仍为 `20260901155124_AddPolicyAndBaselineFoundation`。
- 无 `.csproj`、`.slnx`、migration、ModelSnapshot 或依赖差异；`git diff --check 372fd4a..HEAD` 通过。未启动 WPF，`StoreExpiryInspector` 进程 0，未访问生产数据库。
- 本节只证明 R3 技术门禁通过。真实 GUI 仍须用户本人重验本轮增量，因此 I04/V1-F03 均不得 CLOSED 或进入 Stage 8/9。

## 用户 GUI 重验剩余问题返修（2026-09-03）

- 正式任务卡/治理提交：`.ai-dev/TASKS/V1-F03-I04-GUI-RETEST-REPAIR.md` / `701bcc8`。
- 全新 GPT-5.6 Terra（medium）实现提交：`ed3c218`；Sol 完整 diff 审查后退回补正，追加提交 `2366dde`。Terra 未 push、未修改治理文档。
- Today 主表冻结为选择、条码、商品名称、大类、当前最高阶段、总库存六列；删除任务状态，保持浅灰完整网格、表头略深、虚拟化与全部任务不截断。
- 大类下拉来自当前合法任务的既有中文大类结果；默认全部。全选/取消只作用于当前筛选结果，选择按 TaskId 保留，导出仍使用全部实际勾选 TaskId。
- 新导出与 I02 严格读取表头同步将门店可见“商品当前库存”改为“总库存”；隐藏库存快照、稳定身份、格式版本和业务语义均未改变。
- 每次成功导出保存最新结果并显示包含任务数、批次数、完整路径的模态；打开文件/所在文件夹绑定该次结果。默认文件名为 `今日排查计划_yyyyMMdd_HHmmss.xlsx`。
- 回导确认表仅保留条码、商品名称、生产日期、有效日期、本次排查数量五列；异常通过顶部汇总、浅红行和 Tooltip 表达，I02 校验未复制或削弱。
- 排查日期使用现有原生 WPF DatePicker，默认业务当天且上限为当天；排查人/日期具备红色字段状态。首次提交缺少排查人、回导失败、未完成/失效/陈旧及提交失败均有明确业务阻断弹窗，内部异常不直接展示。
- 正式提交后的 Today 列表仍以权威 reload 移除 completed Task；仅保存填写结果的 Task 不会提前隐藏。
- Sol 新鲜专项/I01～I03 回归：73/73；ProductTask/生命周期/Reminder/导入/历史/UIUX 相关回归：449/449；Release 全量：877/877，失败 0、跳过 0。
- Release build：0 warning / 0 error；EF 无漂移；migration `--no-connect` 为 9 条，最后一条仍为 `20260901155124_AddPolicyAndBaselineFoundation`。
- 无 `.csproj`、`.slnx`、migration、ModelSnapshot 或依赖差异；`git diff --check` 通过。在线 NuGet 漏洞源返回 NU1900，未宣称在线漏洞审计通过。
- 未启动 WPF；`StoreExpiryInspector` 进程 0；未访问生产数据库。真实 GUI 仍须用户在保留的隔离环境重验本轮 11 项，因此 I04/V1-F03 均不得 CLOSED。

## 用户 GUI 验收返修合并（2026-09-02）

- 用户确认 R2 的页面闪烁及白屏转圈闪退已经修复；本轮只处理剩余表格、选择命中、回导模态与门店文案，并回归旧修复。
- 正式任务卡/基线：`.ai-dev/TASKS/V1-F03-I04-GUI-ACCEPTANCE-REPAIR.md` / `4525eb3`。
- 全新 GPT-5.6 Terra（medium）提交：`a845737`、Sol 退回修正 `c65fe5a`、Excel 隐藏/重排身份回归 `147eef4`，均未由 Terra push。
- Sol 完整 diff 审查退回并纠正：主表列顺序、0 件状态误表达、1024×600 下过高模态窗口、二次确认 owner 层级，以及表头边框只依赖主题默认值。
- Today 页面移除外层纵向 ScrollViewer 与固定 240 高度，使用 `Auto,*` 布局让 DataGrid 自身滚动，保留 virtualization/recycling 和全部任务；主表只显示选择、条码、商品名称、大类、当前最高阶段、商品当前库存、任务状态。
- 主表与确认表均冻结完整横竖网格和显式表头边框；单元格焦点不再形成整行蓝色选择，业务选择继续只由绑定 `IsSelected` 的 CheckBox 表达。
- Excel 回导后打开独立“排查结果确认”模态；六列为条码、商品名称、生产日期、有效日期、本次排查数量、校验状态。日期显示 `yyyy-MM-dd`，空白显示未填写，0/正数均显示可提交，长原因只进入 Tooltip。
- 门店界面删除“保存待确认结果”、Draft/Task/“集中正式提交”等本流程技术文案；“提交数据”先调用 I02 原子 Draft Application，全部完成后显示“确认提交”，只有确认才调用 I03；取消不调用 I03。
- I01 Excel 的中文大类、A:L AutoFilter、冻结/打印/稳定隐藏身份保持不变；新增回归证明数据行隐藏并改变物理顺序后，I02 仍按稳定身份匹配且不依赖行号或可见顺序。
- Sol 新鲜 GUI/I01～I03/UIUX 专项：110/110；ProductTask/生命周期/Reminder 回归：158/158；Release 全量：871/871，失败 0、跳过 0。
- 离线 `NuGetAudit=false` Release build：0 warning / 0 error；在线 NuGet 漏洞源本轮 NU1900，不宣称在线漏洞审计通过。
- EF `has-pending-model-changes`：无漂移；`migrations list --no-connect`：9 条，最后一条仍为 `20260901155124_AddPolicyAndBaselineFoundation`。
- 无 `.csproj`、`.slnx`、migration、ModelSnapshot 或依赖清单变化；`git diff --check` 通过。未启动 WPF，`StoreExpiryInspector` 进程 0，未访问生产数据库。
- 本节只证明返修技术门禁通过。用户尚未重验真实 GUI，因此 I04/V1-F03 继续保持 GUI FAILED，不得 CLOSED 或进入 Stage 8/9。

## 用户 GUI 阻断（2026-09-02）

用户在保留的隔离环境完成真实数据导入后打开“今日排查”，页面白屏转圈并闪退，因此此前技术结论不能作为 I04/F03 收口依据。Windows `.NET Runtime` 事件 1026 已定位未处理 `InvalidOperationException`：只读属性 `TodayInspectionTaskViewModel.HasValidDraft` 被 WPF 默认按 TwoWay/OneWayToSource 绑定。当前 I04 与 V1-F03 均保持 GUI FAILED，等待用户重验。

## GUI blocker repair 与 Sol 独立复验

- 正式缺陷卡/治理基线：`.ai-dev/TASKS/V1-F03-I04-GUI-BLOCKER.md` / `c532583`。
- 全新 GPT-5.6 Terra（medium）最小修复：`e2f1b215134cb662fbeeef2d11491576162319e4`，未自行 push。
- 生产改动只有 `MainWindow.xaml`：`HasValidDraft` 绑定显式增加 `Mode=OneWay`；可编辑 `IsSelected` 继续为 TwoWay。未修改查询、I01～I03、Schema、migration、依赖或交互。
- 新增修复前会失败的静态 XAML 契约测试，锁定只读 Draft 复选框必须使用 OneWay。
- Sol 独立 I04 专项：10/10；I01～I04：98/98；I04 + 关键 WPF/Application/UIUX：246/246；Release 全量：862/862，失败/跳过均为 0。
- 离线 `NuGetAudit=false` restore 后 Release build：0 warning / 0 error；在线漏洞源仍不可达，不宣称在线漏洞审计通过。
- EF `has-pending-model-changes`：无漂移；`migrations list --no-connect`：9 条，最后一条仍为 `20260901155124_AddPolicyAndBaselineFoundation`。
- 包清单未变；无 `.csproj`、`.slnx`、migration、ModelSnapshot 差异；`git diff --check` 通过。
- 修复复验未启动 WPF、未访问或修改隔离/正式数据库；现场复核 runtime、marker、受保护原目录仍存在，应用进程为 0。
- 真实 WPF GUI 尚未重验，所以本节只证明技术修复通过，绝不把 GUI 验收标记为通过。

## 第二轮 GUI 阻断（2026-09-02）

用户实际操作录像显示，第一轮闪退修复后进入“今日排查”仍有明显整页重绘感、左侧导航整体变灰和 Loading 卡顿，任务列表列宽/字段层级混乱且缺少大类与清晰任务状态，无法用于真实门店操作。第二轮日志没有新的崩溃事件；代码与隔离数据证据定位到每次进入无条件全量加载、UI 线程逐条发布 576 个行 ViewModel、列表加载复用全局导航 busy，以及未冻结虚拟化/可读列契约。当前继续保持 GUI FAILED/BLOCKED，等待全新 Terra R2 修复、Sol 独立复验与用户再次重验。

## R2 修复与 Sol 独立复验

- 正式任务卡/基线：`.ai-dev/TASKS/V1-F03-I04-GUI-BLOCKER-R2.md` / `ff85a27`。
- 全新 GPT-5.6 Terra（medium）提交链：`2b1d021`、`62879a7`、`32c757d`、`b0e3f7c`，均未自行 push。
- Sol 两轮完整 diff 退回修正：保留 PreviewRows 可观察通知、让 StageBadge 接收 canonical stage、恢复 1024×600 纵向可达、批量抑制全选 O(n²) 汇总通知、以可交互 CheckBox 模板支持单项选择，并更新获批 Today 数字列后的 R02 全窗口静态计数。
- 查询保持 3 次 `AsNoTracking` 批量 read；新增 interceptor 测试锁定非 N+1，并通过既有 `ProductCategoryScopes` 返回大类中文显示。
- 576 行 ViewModel 在后台构建并以一次 `Tasks` 属性替换发布；全部 Task 保留。全选/取消只各发布一次 SelectedCount/命令汇总通知；单项选择仍 TwoWay 可交互。
- 任务列表显式 row/column virtualization、recycling、content scroll，固定自身 240 高度；页面只允许纵向滚动，外层无横向滚动。八列固定为选择、商品编码、商品名称、大类、当前最高阶段、批次数、商品当前库存、任务状态；名称 ellipsis + ToolTip，阶段复用现有 StageBadge，数字列居中。
- Today 列表 loading 与业务 action busy 已分离：加载不再禁用整个 Shell 导航；已有成功列表时离开再返回不重复查询，显式刷新和提交后刷新仍取得当前事实。
- Sol GUI blocker/任务查询/UIUX-R03 专项：33/33；I01～I04：103/103；关键 WPF/Application/UIUX：266/266；V1-F01/F02 + ProductTask/生命周期/启动/提醒：161/161。
- Release 全量：868/868，失败 0、跳过 0；Release build（离线 `NuGetAudit=false`）：0 warning / 0 error，不宣称在线漏洞审计通过。
- EF 无漂移；migration `--no-connect` 仍为 9；依赖清单、`.csproj`、`.slnx`、ModelSnapshot、migration 均无变化；`git diff --check` 通过。
- 未启动 WPF，未访问或修改隔离/正式数据库；runtime、marker、受保护原目录仍存在，应用进程为 0。
- 本节仅证明 R2 技术修复通过。第二轮用户 GUI 结论仍为 FAILED，必须由用户重新验证首批 5 项，不能据此关闭 I04/F03。

## 范围与独立审查

相对治理提交仅变更：

- `MainWindow.xaml`：新增独立“今日排查”导航与高密度任务/Preview/Draft/提交页；“数据导入”入口保留。
- `MainWindow.xaml.cs`：标准 Save/Open 文件对话框及集中超库存明细确认。
- `Stage4ViewModels.cs`：Shell 导航、I01/I02/I03 默认委托与提交后刷新编排。
- `TodayInspectionViewModel.cs`：会话状态、选择、异步防重入、Draft 完整度门禁、集中确认与刷新状态。
- `V1F03I04TodayInspectionViewModelTests.cs`：I04 专项。

Sol 完整 diff 审查后两次退回 Terra，修复了 500 条任务截断、Preview 陈旧原因丢失、旧 Draft readiness、超库存提交时间漂移、刷新 fire-and-forget、旧 Preview 可重复应用及部分刷新误报。最终实现只调用 I01 `TodayInspectionPlanExportUseCase`、I02 `InspectionPlanDraftApplyUseCase`、I03 `BulkInspectionSubmissionUseCase`；没有在 WPF 复制 Excel、陈旧、Draft、Submission、0 件停止或生命周期算法。

## Sol 新鲜自动化证据

- I04 专项：9/9。
- I04 + I01～I03 + Shell/详情/导入/历史/备份/UIUX-R02/R03：245/245。
- V1-F01/F02 + ProductTask/PostImport/Startup/Reminder：148/148。
- Release 全量：861/861，失败 0，跳过 0。
- Release build：0 warning / 0 error。
- EF `has-pending-model-changes`：无模型漂移。
- EF `migrations list --no-connect`：9 条；最后一条 `20260901155124_AddPolicyAndBaselineFoundation`。
- `git diff --check 6a4cf8a..HEAD`：通过。
- 无 `.csproj`、`.slnx`、migration 或 ModelSnapshot 差异；顶级包仍为 Open XML 3.5.1、EF Core 10.0.10 与 SQLitePCLRaw 2.1.13。
- 在线 NuGet 漏洞数据源本轮返回 NU1900，未将包清单检查冒充在线漏洞审计成功。
- `StoreExpiryInspector` 进程 0；未启动 WPF，未访问生产数据库。测试使用隔离临时 SQLite；EF migration 列表使用 `--no-connect`。

## 已验证契约

- “今日排查”与商品源“数据导入”入口、ViewModel、文件对话框及 reader 路径明确分离。
- 当前 open 任务不受 500 条截断；支持全选、取消和部分选择，无选择不导出；I01 保持最终合法性与不覆盖权威。
- I02 Preview 显示商品/批次、blank/0/正数、行错误、陈旧/失效数量与原因；Preview 零写入。
- 排查人/日期门禁、I02 原子 Draft 应用和 `InspectionDraftReadiness` 控制正式提交；incomplete Task 不传给 I03。
- I03 只被批量调用；超库存显示 Task/商品/库存/合计，确认与 stale 重试复用同一 UTC 提交意图。
- Submitted/AlreadySubmitted 等待首页、今日任务、原任务、当前详情和历史刷新；成功后清除旧 Preview/Draft/确认会话，部分刷新失败不反转正式提交结果。
- 无 Schema、依赖、Reminder、商品源导入、Revision、Stage 8/9 或全局 UI 重构越界。

## 待用户真实 GUI 验收

仍需用户本人在隔离环境验证 1024×600、Windows 125%、真实 Save/Open 对话框、Excel 填写/打印、键盘焦点、超库存弹窗、提交刷新、0 件批次停止、历史可见及商品源导入不回归。

用户入口：双击 `docs/V1-F03-I04-开始验收.cmd`。脚本先确认应用进程为 0，旁置保护完整原运行目录及自启动事实，再用新的隔离运行目录启动本次 Release；再次双击可重开同一隔离会话。验收结束并从托盘退出后，双击 `docs/V1-F03-I04-结束并恢复.cmd`，必须看到 `RESTORE_PASS`。脚本遇到重解析点、sidecar、旧旁置目录或身份不一致会停止，不会强删或覆盖。

首次用户点击暴露 Windows 批处理 LF 换行及 Windows PowerShell 5.1 对无 BOM UTF-8 的解析兼容问题；入口已改为 ASCII/CRLF，PowerShell 脚本固定 UTF-8 BOM，并由两个真实 `.cmd` 的无副作用 `Check` 均返回 `ENTRY_CHECK_PASS`。失败发生在脚本解析前，未创建验收会话或旁置运行目录。

用户未确认全部 GUI 项通过前：I04/F03 不标记 CLOSED，不建立 F03 总体最终收口，不 push 当前本地验收状态，不进入 Stage 8。
