# S18-T03 Acceptance｜左侧导航栏展开/收起与企业 SaaS 视觉重构

日期：2026-09-11（Asia/Shanghai）
状态：`R1_TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`

## S18-T03-R1 用户返修门禁

- [ ] A｜展开和收起的菜单分组节奏对应；收起仍隐藏全部分组标题文字，七项不再是无层级连续列表。
- [ ] B｜当前页面为排查历史时，展开明确选中 Clock +“排查历史”，收起仍明确选中同一个 Clock。
- [ ] C｜切换今日排查及其他页面后，展开/收起始终保持同一个正确选中项。
- [ ] D｜Hover 与 Selected 可明确区分，Hover 不覆盖或伪装 Selected。

R1 技术范围仅为导航 XAML / Style 状态映射和轻量分组表现；`Stage4ViewModels.cs`、业务、Shell 命令、右侧页面、数据库、Schema/migration、版本、宽度、Logo、品牌文字、七项 Geometry、ToolTip 和持久化全部冻结。只运行两项最小静态契约、必要一次 Release App build 与 TEMP 下纯 GUID 隔离 GUI；`FULL=NOT_RUN / NO_FULL`。技术完成后只能标记 `R1_TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`，以上 A～D 仍由用户真实 GUI 验收。

R1 implementation=`733bc909b0f4b487508b69ddc3ed2f87d08538cd`；生产 diff 仅三个批准导航文件，`Stage4ViewModels.cs` 与全部冻结项零 diff。静态契约=`2/2 PASS`；Release App build=`0 warning / 0 error`；隔离 preflight 使用 TEMP 下纯 GUID，进程响应且生成独立 `app.db`/日志。验收入口仍为 `C:\Users\39037\.codex\visualizations\2026\09\10\01a08c1e-770a-79c3-b0cb-6e628ca64f1c\S18-T03-GUI-Acceptance\启动S18-T03验收.cmd`，每次启动动态生成新的纯 GUID 数据根。

## 首轮技术门禁（历史）

## 技术门禁

- [x] implementation 来自治理 push 后 fresh `origin/main` 的全新 GPT-5.6 Terra / medium / clean worktree。
- [x] 生产 diff 限于批准的导航文件；`Stage4ViewModels.cs`、右侧页面、业务层、Schema/migration、版本、Updater/Installer 无 diff，正式数据库未访问。
- [x] 同一 `ShellColumn` 在展开/收起时为 `220 / 72 DIP`；没有第二套 Shell、菜单或图标。
- [x] 展开状态含品牌“效期排查”、四个分组标题、固定顺序七菜单及底部版本；收起隐藏这些文字，只保留同一组七个 Path。
- [x] 七项图标精确为 Home / Clipboard / Calendar / Clock / Upload / Database / Gear，展开/收起引用同一 Geometry；无第三方依赖。
- [x] 收起状态七项 ToolTip 文案正确；当前项继续由既有 `CurrentPage` / 可见性状态驱动，设置仍为 `Settings_Click`。
- [x] 导航状态覆盖：无文件默认展开、保存收起、重启读取收起、保存展开、文件缺失/损坏/读取失败安全回退展开；路径属于当前 `RuntimeDataRoot.RootDirectory` 并兼容 TEMP/GUID。
- [x] 右侧既有 Shell 命令和页面映射无变化；未来风险仍归首页；排查详情及所有业务页面无布局重构。
- [x] S18-T03 专项与必要 Shell 直接回归通过；一次 Release App build `0 warning / 0 error`；`FULL=NOT_RUN / NO_FULL`。
- [x] TEMP/GUID 隔离 GUI preflight 可操作；正式数据库=`NO ACCESS`；Schema/migration=`NO CHANGE / 9`。

## Sol 独立技术验收记录

- governance=`25b4485d0e479c89482e8ab61cf957de5e421835`；implementation=`35ae161ae11ad80c7acfa0cc2fd610cd62663367`；最终 diff 为 3 个批准生产文件、1 个 S18-T03 专项文件及 4 处旧导航静态断言更新。
- Sol 在 push 前拦截两轮布局问题：品牌 Grid 必须 Stretch 且中列为 `*` 才能让箭头靠右；折叠时品牌 margin 必须容纳在 72 DIP，Path margin 必须归零并把文字间距移到 TextBlock。最终均已修正。
- Sol 独立 S18-T03=`2/2 PASS`；`ShellKeepsOnlyApprovedPagesNavigable=1/1 PASS`；Release App build=`0 warning / 0 error`；`git diff --check=PASS`。
- 四个被动更新的历史静态方法额外核对为 `1/4 PASS`；两个失败是当前基线已有的 300 DIP 旧否定断言，一个是旧 helper 不支持 worktree `.git` 文件。失败证据保留，不扩修、不计入本卡通过门禁。
- GUI 入口：`C:\Users\39037\.codex\visualizations\2026\09\10\01a08c1e-770a-79c3-b0cb-6e628ca64f1c\S18-T03-GUI-Acceptance\启动S18-T03验收.cmd`。数据根为 TEMP 下纯 GUID `3f478a82-3e92-4d57-a93e-410b50d8c3c2`；候选已取得真实主窗口句柄并保持运行。
- 首次入口错误使用“前缀+GUID”，应用按现有安全门禁拒绝；错误进程已关闭，脚本已修正并成功重启。Computer Use 原生应用接口不可用，因此 Sol 不自判视觉 A～H。

## 用户 GUI A～H

- [ ] A｜展开约 220：Logo、“效期排查”、收起按钮、四组及菜单顺序正确。
- [ ] B｜七图标清楚、不同且语义为 Home / Clipboard / Calendar / Clock / Upload / Database / Gear。
- [ ] C｜收起约 72：只显示同一套菜单图标，品牌/分组/菜单/版本文字全部消失。
- [ ] D｜展开、收起、再展开时，每项始终是同一图标。
- [ ] E｜收起 Hover 的七项 ToolTip 名称正确。
- [ ] F｜当前项为浅蓝灰底、左侧蓝条、蓝色图标/文字，无深蓝大块。
- [ ] G｜退出并重启后保持上次展开/收起状态；异常状态文件不影响启动。
- [ ] H｜右侧现有业务页面未被重做或弄乱；收起后仅自然获得更多宽度。

技术通过后保持 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`；只有用户 A～H 全部确认后才允许 S18-T03 和 Stage18 关闭并进入 v1.0.9 `RELEASE_PREPARATION`。
