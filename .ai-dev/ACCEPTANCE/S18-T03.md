# S18-T03 Acceptance｜左侧导航栏展开/收起与企业 SaaS 视觉重构

日期：2026-09-11（Asia/Shanghai）
状态：`NOT_RUN / NOT_ACCEPTED`

## 技术门禁

- [ ] implementation 来自治理 push 后 fresh `origin/main` 的全新 GPT-5.6 Terra / medium / clean worktree。
- [ ] 生产 diff 限于批准的导航文件；`Stage4ViewModels.cs`、右侧页面、业务层、Schema/migration、版本、Updater/Installer 无 diff，正式数据库未访问。
- [ ] 同一 `ShellColumn` 在展开/收起时为 `220 / 72 DIP`；没有第二套 Shell、菜单或图标。
- [ ] 展开状态含品牌“效期排查”、四个分组标题、固定顺序七菜单及底部版本；收起隐藏这些文字，只保留同一组七个 Path。
- [ ] 七项图标精确为 Home / Clipboard / Calendar / Clock / Upload / Database / Gear，展开/收起引用同一 Geometry；无第三方依赖。
- [ ] 收起状态七项 ToolTip 文案正确；当前项继续由既有 `CurrentPage` / 可见性状态驱动，设置仍为 `Settings_Click`。
- [ ] 导航状态覆盖：无文件默认展开、保存收起、重启读取收起、保存展开、文件缺失/损坏/读取失败安全回退展开；路径属于当前 `RuntimeDataRoot.RootDirectory` 并兼容 TEMP/GUID。
- [ ] 右侧既有 Shell 命令和页面映射无变化；未来风险仍归首页；排查详情及所有业务页面无布局重构。
- [ ] S18-T03 专项与必要 Shell 直接回归通过；一次 Release App build `0 warning / 0 error`；`FULL=NOT_RUN / NO_FULL`。
- [ ] TEMP/GUID 隔离 GUI preflight 可操作；正式数据库=`NO ACCESS`；Schema/migration=`NO CHANGE / 9`。

## 用户 GUI A～H

- [ ] A｜展开约 220：Logo、“效期排查”、收起按钮、四组及菜单顺序正确。
- [ ] B｜七图标清楚、不同且语义为 Home / Clipboard / Calendar / Clock / Upload / Database / Gear。
- [ ] C｜收起约 72：只显示同一套菜单图标，品牌/分组/菜单/版本文字全部消失。
- [ ] D｜展开、收起、再展开时，每项始终是同一图标。
- [ ] E｜收起 Hover 的七项 ToolTip 名称正确。
- [ ] F｜当前项为浅蓝灰底、左侧蓝条、蓝色图标/文字，无深蓝大块。
- [ ] G｜退出并重启后保持上次展开/收起状态；异常状态文件不影响启动。
- [ ] H｜右侧现有业务页面未被重做或弄乱；收起后仅自然获得更多宽度。

技术通过后保持 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`；只有用户 A～H 全部确认后才允许 S18-T03 和 Stage18 关闭并进入 v1.0.9 `RELEASE_PREPARATION`。
