# S1-T07 验收记录

- 验收日期：2026-08-27
- 结论：通过
- 执行模型：GPT-5.6 Luna（max）
- 任务卡提交：`de33c2f21dd396e5c0e3c264cafb4af33c8c80c8`
- 实现提交：`f65e489a25063fda6ebf88ef3d4cd555f31b5cc0`
- 既有测试基线修正：`f34396d485b3a8f0f1d7502185cef657996caa12`

## Sol 独立审查

- 实现只增加 `BackupRecord`、独立 EF 配置、DbContext 注册、`AddBackupMetadata` migration 和专项测试；未修改项目/solution、依赖、UI、App、既有实体、既有配置或既有五条 migration。
- 新增且仅新增 `backups` 一张业务表；实体只承载任务卡批准的 6 个字段，配置保持一实体一配置，DbContext 仅注册 DbSet 与配置。
- 未发现备份文件读写、恢复、哈希计算、自动保留 7 份、后台任务、日志、UI、生命周期事件或 Stage 2+ 实现。
- 首轮全套测试发现既有导入测试将业务表清单硬编码为 13 张；仅在该既有断言中加入 `backups`，未改变生产代码、迁移或业务边界，随后全套测试通过。

## 数据完整性验收

- `backups` 精确包含 `id`、`backup_type`、`file_path`、`sha256`、`created_at_utc`、`verification_status`。
- 五种备份类型可保存，未知类型由真实 SQLite CHECK 拒绝；路径和验证状态 Trim 后非空。
- SHA-256 只接受 Trim 后 64 位小写十六进制；错误长度、大写和非十六进制均被拒绝。
- 同路径、同哈希可重复保存；`(backup_type, created_at_utc, id)` 为字段顺序正确的普通索引，不是唯一索引。
- `backups` 无外键和级联删除；本卡未引入任何关系或删除行为。

## 迁移与旧数据验收

- 从 `AddImportPersistence` 写入 Product、Batch、Task、TaskItem、Draft、DraftItem、Inspection、InspectionItem、InspectionItemRevision、InventoryAdjustment、Import、ImportWorkbook、ImportIssue 后升级：13 类旧数据全部保留。
- 增量 migration script 为 1,089 字节，仅新建 `backups` 表和 `IX_backups_backup_type_created_at_utc_id` 索引；`ALTER TABLE`、`DROP TABLE`、`ef_temp_*`、唯一索引均为 0。
- 完整 migration script 成功生成，为 27,921 字节，包含 6 条 migration 记录。

## 独立验证结果

- 刷新官方 NuGet 元数据后，`dotnet build StoreExpiryInspector.slnx --nologo --no-restore`：0 警告，0 错误；未压制告警。
- `dotnet test StoreExpiryInspector.slnx --nologo --no-restore`：37 通过，0 失败，0 跳过。
- `dotnet ef migrations has-pending-model-changes ... --no-build`：无模型漂移。
- 官方 NuGet 源漏洞审计：应用与测试项目均无已知漏洞。
- `git diff --check`：通过；测试临时数据库残留为 0；验收前工作区干净。

## 阶段状态

`S1-T07` 通过。Stage 1 尚余生命周期事件留痕、设置与运行状态、本地运行日志基础设施；本轮不创建下一张任务卡，不进入 Stage 2。
