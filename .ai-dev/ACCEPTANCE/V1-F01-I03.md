# V1-F01-I03｜Sol 独立技术验收

- 日期：2026-09-02
- 结论：`TECHNICALLY_ACCEPTED / WAITING_I04_APPROVAL`
- 治理解阻提交：`2502a10`
- Terra 实现与修复：`7df47c2` 至 `9859988`
- 验收角色：Sol；未直接采信 Terra 自报结果

## 交付事实

- 新增单一 Application 用例 `ColdStartScopeBaselineUseCase`，并在确认导入的既有外层事务中按确定性 Managed scope 执行首次接管。
- V1 只接受 `policy_version = 1`；非 version 1、无关成功 Import、Excluded、Unresolved、空或不匹配 policy 均在业务写入前拒绝或排除，不污染 version 1 事实。
- 相同完成范围幂等 no-op；不同 canonical scope 相互独立；空的未完成基线只允许原 Import 与 BusinessDate 重试。
- 固定执行库存 0、管理/policy 门禁、Stage/历史窗口分类、BatchBaseline、ProductTask 聚合、ScopeBaseline 最后完成；异常由同一事务整体回滚，回滚后可重试。
- 方案 C 已落地：5折、2折只建基线；收仓、到期当天、窗口内历史过期生成 open ProductTask；窗口外历史过期和库存 0 只建对应基线。
- 历史窗口严格使用真实 `expiry_date - production_date`，算法为 `clamp(ceil(days * 3%), 3, 30)` 且包含边界；缺失或倒置生产日期保存稳定 ImportIssue，不进入补查任务。
- Task 继续复用 `ProductTaskAggregator`，保持批次触发、商品聚合、每商品至多一个 open Task 与既有 Stage 优先级；首次接管不伪造 completed Task、Inspection、Revision、LifecycleEvent 或 HandledAttentionVersion。

## Sol 审查与退回

1. 首轮审查退回 3% 取整错误、全局 Task 历史误阻断、库存 0 已停止批次漏基线、请求前置校验与测试矩阵不足；Terra 修复后继续复审。
2. 后续复审补齐 incomplete baseline 身份一致性、version 1 零污染、scope 隔离、上下限与端点、事务回滚、历史权威、same-import 去重、真实样本审计和源文件前后 hash。
3. Sol 首次专项编译发现真实样本断言误放入合成测试；退回修复后独立专项通过。
4. 最终审查发现成功 Import 与触发 scope 未关联；修复为至少一个完全匹配 Managed Product 的 `LastSeenImportId == CreatedImportId`，并增加无关 Import 零污染测试。

## Sol 最终独立门禁

- I03 与确认导入编排专项：23/23，通过 23、失败 0、跳过 0。
- I01/I02 相关回归：137/137，通过 137、失败 0、跳过 0。
- 指定真实 Excel + 隔离 SQLite：1/1；源文件执行前后保持 2,522,641 bytes，SHA-256 `BBD91AE4E40E5381D749F8DB8F4CC0A600FB88D8C1CF6EA160C7C33EC1A3F0F6`。
- 真实样本首日 open ProductTask：583；5折 0、2折 0、收仓 210、过期 373；8 个 Managed scope baseline 完成，Excluded / Unresolved 零 Task，BatchBaseline disposition 守恒。
- Release 全量：735/735，通过 735、失败 0、跳过 0。
- Release build：0 warning / 0 error。
- EF `migrations has-pending-model-changes`：无模型变化。
- EF `migrations list --no-connect`：9 条；最后一条仍为 I01 的 `20260901155124_AddPolicyAndBaselineFoundation`。
- 相对 I03 治理解阻基线无 migration、ModelSnapshot、schema、`.csproj`、`.slnx`、依赖、WPF 或 Reminder 变化；`git diff --check` 通过。
- `StoreExpiryInspector` 进程为 0；未启动 WPF，未访问、迁移或修改正式数据库；所有运行验证使用临时隔离 SQLite。

## 范围与停点

- I03 技术验收通过，`BLOCKED_SCHEMA_APPROVAL` 已由 D-027 的 version 1 固定契约解除。
- 未实现 I04、基线后的完整正常生命周期收口、Reminder 修改或提前 3 天预提醒；未进入 V1-F02 或 Stage 8。
- 未创建 I04 正式任务卡，未派发 I04 Terra；等待产品经理单独批准。
