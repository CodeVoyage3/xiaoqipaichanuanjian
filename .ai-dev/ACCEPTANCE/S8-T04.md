# S8-T04 Acceptance｜异常退出、事务中断与数据库一致性

状态 `TECHNICALLY_ACCEPTED / CLOSED`。用户已明确批准仅既有隔离SQLite损坏回归用于标准Release全量验收；944/944通过，暂停解除。开工基线2142cf72588fb727cca6efd58509c113646de1e2。

- [x] 全新Terra medium/priority身份、commit、完整diff。
- [x] TEMP/GUID子进程真实硬Kill，精确握手、fail-closed、默认factory/loader0，无正式库访问。
- [x] 大导入六类点各3次，另100k pre/postcommit各3次。
- [x] Inspection/Item partial、Task/handled附近、pre/postcommit各3次。
- [x] InventoryAdjustment归零partial/pre/postcommit各3次。
- [x] 每次全业务before/expected/after指纹及counts；无混合/重复/孤儿。
- [x] 每次reopen、integrity ok、FK0、migration9、sidecars观察、无遗留锁、权威读取。
- [x] Sol独立重跑矩阵并如实保留失败次数；相关业务回归。
- [x] Release944/944、0failure，build0warning/0error、EF无漂移、migration9、Schema/ModelSnapshot/index/依赖/csproj/slnx不变、diff --check。
- [x] 明确是否发现一致性bug和生产代码变化，如修复提供真实crash before/after。
- 发布方式：按授权普通push main，发布后核对clean及HEAD=origin/main、0/0；实际最终SHA和同步状态以本轮Git发布回执为准。停止、不创建S8-T05。

## 未验证与范围限制

已完成进程级硬中断，不以S8-T03受控异常通过冒充硬中断。仅为完成Release全量，用户特别放行仓库既有的隔离SQLite损坏回归；没有新增损坏场景，不扩大为S8-T05。真实断电、磁盘/SSD/介质/文件系统损坏安全及新的灾难矩阵未被本卡证明。Undo永久取消，重置数据不实施。

## 最后全量门禁与正式关闭（2026-09-04）

- 恢复前重新fetch：origin/main仍2142cf7，本地HEAD为bfa87d93fbfb333bd3db5fcb3b5718d867e55eaf，clean、ahead3/behind0，与暂停回执一致；实现为9eff272，src/tests相对此实现无变化。本地旧main引用61a057e仅为远端祖先（0/132），不是验收工作基线；发布时仅允许正常fast-forward，无force或历史改写。
- Sol执行无filter的标准全量命令：`dotnet test StoreExpiryInspector.slnx -c Release --no-restore -p:NuGetAudit=false --logger "console;verbosity=normal" --logger "trx;LogFileName=s8t04-full-release.trx" --results-directory <本轮TEMP/GUID>`。设置`S8_T04_RUN_100K=0`，沿用已有48/48专项，不额外机械重跑21分钟的完整高规模矩阵；套件内常规42次硬Kill按既有测试自然执行。未修改任何测试或生产实现。
- 真实全量结果：944 executed / 944 passed / 0 failed / 0 error / 0 timeout / 0 aborted / 0 notExecuted，exit0，12.1949分钟。不是838/838过滤回执；高规模gate关闭的空返回不冒充新压力证据。
- TRX逐项复核本次获准的4类：PreImportSnapshotServiceTests 6/6；S7T02DatabaseRestoreTests 9/9；S7T03DatabaseBackupRestoreViewModelTests 30/30；ImportUndoEligibilityTests 42/42，共87/87。真实损坏对象均为既有测试创建的TEMP/GUID SQLite或其备份；未访问、复制、哈希、损坏、恢复或调查正式数据库，也没有恢复Import Undo产品规划。
- TRX：`C:\Users\39037\AppData\Local\Temp\StoreExpiryInspectorS8T04Sol\bc9bacb459a9413abaf455c5d3dc2811\s8t04-full-release.trx`；SHA256 `8097D52F940C1CEEC09494707AA2E551DF4FC4D5796E338DEC61BB58084F59BE`。
- 随后Sol独立Release build exit0、0warning/0error。EF无模型漂移；migration list --no-connect仍9，末条`20260901155124_AddPolicyAndBaselineFoundation`；设计时`:memory:`，不连接正式库。Schema/ModelSnapshot/index/PRAGMA策略/dependency/csproj/slnx和生产代码无变化；git diff --check通过。未执行在线NuGet漏洞审计。
- 原48/48 Process.Kill权威证据继续保留，原TRX SHA256复核不变：36次提交前完整回滚，12次提交后保持完整成功状态；无已确认生产一致性bug。旧开发失败记录不删除、不改写成成功。正式标记TECHNICALLY_ACCEPTED / CLOSED；Stage8仍IN_PROGRESS，等待下一卡授权，S8-T05未创建。

## 实施与Sol独立回执

实施代码与Sol新鲜复跑如下；开发阶段证据单独保留，不混计最终结果。

### 后续开发证据审计

- 小矩阵开发版父26204留下21份通过JSON，但仍缺最终隔离负例、InspectionItem部分写入和完整sidecar门禁，不代替最终验收。
- Import10k首轮父5688实际只有5份通过JSON（precommit两份、postcommit三份）；precommit第三次root `b637217c0e5346b8ae2700a0b7e15e1c`只有marker/before，没有最终验收证据，必须记为未完成，不能报6/6。其余roots为c683ee8e0fe24d1e9404fb1cbf3c07f5、f0821f5a88444440864e8c11a66ded3b、114e6eb2051b43eb9e6595c72b837c1f、6fc726715ce14716a96d5d0e2c0dd3dc、db977dba71f7474093745643e90a2c2d。
- 该轮Terra长命令未取得可靠最终exit；Terra自行终止其报告为本轮创建的残留测试host。Sol没有将该轮记为CLI全通过，也没有手动清理旧host。后续长矩阵由Sol持有命令session并获取最终exit。
- 最终矩阵保留Inspection头partial并另加InspectionItem partial，因此计划为48次（Import24、Inspection15、Inventory9），每点固定3次。
- Sol静态复核发现既有Release含PreImportSnapshotServiceTests的字节翻转/损坏文件、S7T02DatabaseRestoreTests的截断、S7T03DatabaseBackupRestoreViewModelTests的header翻转、ImportUndoEligibilityTests的无效snapshot写入。这与本卡禁止人工损坏要求冲突，已单独请求用户澄清是否允许仅既有隔离回归；未获答复前不执行相关用例，不将过滤回归写成Release全量。
- Sol当前代码设计时EF `has-pending-model-changes --configuration Release --no-build` exit0无漂移；`migrations list --no-connect` exit0列9条，末条20260901155124_AddPolicyAndBaselineFoundation。设计时使用`:memory:`，清单不证明正式库applied状态，也未访问正式库。

### Sol开工基线（不替代最终验收）

- 新实施者`/root/s8_t04_fresh_terra`，Terra medium，治理af8fee1。
- 已有Release产物独立隔离专项9/9；Import/生命周期/正式提交/库存/Revision相关基线267/267，0failure/skip（no-build/no-restore）。最终代码仍需重新运行。

### 最终代码独立验收

- Terra最终测试提交`9eff2724a9bb5dcb857d8bec6129d3ef3b713b46`，仅新增S8T04CrashConsistencyTests及S8T03三处helper private→internal，无生产改动。开发中100k动态Skip不受现有runner支持，已改成现有S8T03式显式gate早返回；该门禁关闭不算压力证据。
- Sol隔离12/12，实际隔离10项（S8T02九项+共享ValidateThenInitialize负例一项），另两项仅100k开关关闭检查，不能算100k通过。
- 上一轮Sol过滤Release `FullyQualifiedName!~S8T04CrashConsistencyTests&FullyQualifiedName!~PreImportSnapshotServiceTests&FullyQualifiedName!~S7T02DatabaseRestoreTests&FullyQualifiedName!~S7T03DatabaseBackupRestoreViewModelTests&FullyQualifiedName!~ImportUndoEligibilityTests`：838/838，0failure/skip，exit0，约1m21s。覆盖Import、正式排查、库存/Revision、Stage2/3等其余回归；当时排除的87条未运行，故此回执不能称全量；本轮944/944已补齐。
- Sol最终矩阵命令：设置`S8_T04_RUN_100K=1`，`dotnet test tests/StoreExpiryInspector.Tests/StoreExpiryInspector.Tests.csproj -c Release --no-build --no-restore -p:NuGetAudit=false --filter FullyQualifiedName~S8T04CrashConsistencyTests`。父testhost23904、session13857；exit0，19/19，21.5144分钟。其中16个Theory参数行各固定3次真实强杀=48/48；另1个输入guard、2个未启用worker/diagnostic空返回，后两项不是额外crash证据。
- TRX统一位于`C:\Users\39037\AppData\Local\Temp\StoreExpiryInspectorS8T04Sol\21f75e5a30954ac0be9eb2372a894bf4`：`s8t04-final-matrix.trx` SHA256 `9FC15DB04AFFFD8ACB61D2895401451D71E8CD2BF844FD443E6BCF47BF915576`；`s8t04-filtered-release.trx` SHA256 `FBC3A641B3D88F31A247997726F4243B7F16C1B76F429ACCB0B057AB663F6387`。
- 48份逐次证据路径、SHA256、指纹、counts及sidecars摘要已保存到同目录治理文件`S8-T04-CRASH-RESULT.json`。Sol另按parent PID汇总原始JSON，核对16组均恰好iteration1/2/3，48/48 pass，36 precommit after=before、12 postcommit after=committed expected，integrity48ok/FK48零/migration48九，未混入开发试跑。
- 所有48个实际worker实例均已退出：从原始marker读取Int64 start ticks核对，当前有1个PID被复用但不是原worker；没有向复用PID发出任何终止操作。
- 独立`dotnet build StoreExpiryInspector.slnx -c Release --no-restore -p:NuGetAudit=false` exit0、0warning/0error；EF无漂移、migration --no-connect9。`git diff 2142cf7 -- src *.csproj *.slnx`为空，完整diff与diff --check通过；无Schema/ModelSnapshot/index/PRAGMA策略/依赖/工程变化。未做在线NuGet漏洞审计。

| 权威链 / 规模 | 检查点 | 每点次数 | 成功 / 失败 |
| --- | --- | --- | --- |
| Import / 10k行 | started、products_partial、batches_partial、post_task、precommit、postcommit | 3 | 18 / 0 |
| Import / 100k行 | precommit、postcommit | 3 | 6 / 0 |
| InspectionSubmission | Inspection头partial、InspectionItem partial、task_handled、precommit、postcommit | 3 | 15 / 0 |
| InventoryAdjustment归零 | partial、precommit、postcommit | 3 | 9 / 0 |

### 状态判定、隔离与限制

- 实际机制为父端`Process.Kill(entireProcessTree:true)`，只终止Process.Start创建的树；marker token/PID/start ticks验证，强杀前取得worker handle，等待runner及worker退出再重开。SQL点在真实DbCommand成功/DataReaderDisposing阶段，commit点为真实EF transaction事件；没有在生产代码中加入环境变量开关或改写事务边界。
- 新worker在Initialize前通过共享ValidateThenInitialize校验参数、TEMP/GUID/root containment及重解析点；负例注入计数delegate确认拒绝时Initialize调用0。Shell默认factory零调用由S8T02九项专项继续证明；代码审查确认所有本卡真实Context均传显式路径。不声称做过全机文件访问审计，不调查或改写旧轮正式库疑似访问事实。
- precommit比较全业务表原始字段/BLOB/主键与counts；postcommit与另一份相同合成XLSX经真实成功链生成的参考比较。仅规范化`imports.pre_import_snapshot_path`和`backups.file_path/sha256/created_at_utc`四列：每份成功Import另验证TEMP安全路径、唯一BackupRecord关联、verified、合理时间和实际文件SHA，不忽略业务列。
- 100k成功事实三次均为50,000 Product、100,000 Batch、5,001 ProductTask；三次precommit均回到10 Batch种子状态。10k成功为5,000 Product/10,000 Batch/501 ProductTask。正式排查混合0/positive，成功1 Inspection/2 Items；blank等冻结规则由上述业务回归覆盖。
- 每次指纹核验后重新经Application读取Dashboard、SearchOpenTasks、History ListPage及有记录时GetDetail，再修改既有Settings的一项合法值证明可写、无stale lock。各次WAL/SHM/journal均记录重开前后事实；WAL/SHM正常存在不算错误，没有删除sidecar制造通过。未发现不可重开的sidecar状态。
- 最终48次未发现partial write、数据库duplicate/orphan或不确定结果；未确认生产一致性bug，生产代码零修改。开发阶段曾出现I/O错误，后续只读可重开与最终48次通过不等于已经证明旧I/O根因，旧失败继续保留。
- 外部snapshot文件可按既有Import契约残留；本卡不声称crash后自动清理、未使用Restore冒充回滚或Undo，也没有证明真实断电/SSD/文件系统/介质损坏恢复。

### 历史暂停门禁（本轮授权后已解除）

上一轮全量Release与人工损坏禁令冲突，保守排除4个旧测试类共87条，仅通过838/838，故当时未关闭、未push。用户现明确放行这4类既有隔离回归，已由本轮无filter的944/944解除该暂停；历史过滤回执仍不被改称全量。不自动创建S8-T05。

未来S8-T05仅建议，不是Task：隔离副本header/page/truncate损坏的fail-closed检测；备份SHA/metadata/migration不一致拒绝；可信大库WAL-safe snapshot与恢复后指纹/FK/history/revision核验；恢复保护备份/staging/替换/最终验证失败时原库或完整目标库的一致性。当前库损坏导致保护备份无法生成时，应先验证既有阻断行为，不擅自绕过保护或自动修复。均需该卡新授权。

### 基座开发失败记录（不能删除或算作通过）

- Terra报告首次试运行错误地将testhost marker PID与Process.Start返回的dotnet PID比较，导致握手等待；报告有两名worker按精确marker PID清理。当前没有有效硬中断矩阵回执。此项是测试基座缺陷，不是已确认生产一致性bug；完整目录/PID/清理结果待补证，最终固定矩阵与开发失败分组统计。
- 后续Terra报告6个case到达硬杀点，但父端Verify复用命令时FK reader未关闭，校验抛异常，仍不计通过。Sol只读TEMP marker实际找到11个开发标记（parent37376三笔、25060两笔、28228六笔），已要求按stdout补全统计，不能只写2+6。Sol精确只读查询11个worker PID（976/27916/38036/29668/37268/25708/36300/30648/2128/32924/37276）均已不存在；未执行额外kill，也未读数据库。
- 再后续开发暴露settings singleton错误新增、Dictionary引用比较和一次`SQLite Error 10: disk I/O error`（父Snapshot读表阶段）。测试改为更新既有设置、按键值比较counts、测试连接Pooling=False且移除ClearAllPools。I/O根因尚未独立确定，不能声称已修复生产bug；原现场不删除，不把试跑失败算通过。

### Sol独立中间硬杀（不代替最终矩阵）

- 当前测试侧DLL独立命令：`dotnet test tests/StoreExpiryInspector.Tests/StoreExpiryInspector.Tests.csproj -c Release --no-build --no-restore -p:NuGetAudit=false --filter "FullyQualifiedName~S8T04CrashConsistencyTests&DisplayName~inventory&DisplayName~precommit" --logger "console;verbosity=normal"`。
- exit0，1/1用例，内部固定3/3强杀，27.7655s。父testhost7800；本卡TEMP/StoreExpiryInspectorS8T04下roots：76d020c878654f65aef1b3aa61e43b8d、37235fb16547464b8b3a0134de0fec68、35cc149b652a4052a45ca5e0db951ea1。均精确TransactionCommitting、Process.Kill(entireProcessTree:true)、after=before、integrity ok/FK0/migration9。Terra并行小矩阵父38748使用独立GUID，不混用为Sol证据。
- Terra后续小矩阵在inspection/postcommit iteration1出现再次I/O错误，失败root `2a9cd4ca144b4ab3994722ccd87dbb73`，marker worker21792、parent38748；DB4096/WAL1091832/SHM32768bytes，现场保留。Sol用Python sqlite3.50.4 `mode=ro`独立重开：integrity ok、FK0、Inspection1/Items2、Taskcompleted、migration9；同版.NET只读重开亦由Terra通过。此后基座在Kill前取得worker进程句柄并核对startTicks、Kill后等待该句柄退出，补扩展错误码记录；旧I/O原因不能据后续通过断言。
- 新版DLL Sol独立inspection/postcommit 1/1用例、内部3/3，21.4241s、exit0（上述命令filter改为DisplayName~inspection&DisplayName~postcommit）。父38728；roots 036e997e0b3f4fe58e04964cd2d3a237、dd203d03670e45c1aca0bae7fa312237、6bd95bf4e2544e19bd131a7be0504221，after=完整committed expected，integrity ok/FK0/migration9。尚不代替最终完整矩阵。
