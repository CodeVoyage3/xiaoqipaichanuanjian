# S19-T01 Acceptance｜首次冷启动工作量与排查计划分批回导优化

日期：2026-09-12（Asia/Shanghai）

状态：`IMPLEMENTATION_RESUMED / MINIMAL_SCHEMA_CHANGE_AUTHORIZED / NOT_ACCEPTED`

## Sol 阻塞结论（2026-09-12）

- 回导/部分提交相关定向回归曾达到 `114/114 PASS`，但这不构成整卡技术通过。
- 最终冷启动 isolated 专项=`5/11 PASS，6/11 FAIL`；合法的 1%、min 1/max 7 结果写库时被现有 `CK_batch_baselines_catchup_window (3..30)` 拒绝。
- `git diff --check=PASS`；正式数据库=`NO ACCESS`；FULL=`NOT_RUN / NO_FULL`。
- 已触发任务停止条件。未进行用户 GUI A～I，未合并/push 实现，未改 migration/ModelSnapshot，S19-T01/Stage19 不得关闭。

## 授权后的新增技术门禁

- [ ] 第 9 条 migration 字节级不变；第 10 条只调整 `CK_batch_baselines_catchup_window` 为 `BETWEEN 1 AND 30`。
- [ ] 旧库 `3/7/8/11/30` 全部成功升级并原样保留；新规则 `1/2/7` 可写。
- [ ] `0/31` 仍被数据库拒绝；非 `expired_catchup_task` 的 `catchup_window_days` 仍只能为 NULL。
- [ ] migration 总数 `9→10`，configuration/ModelSnapshot 同步，无其他 schema 漂移。
- [ ] 升级失败经既有 Schema Update 保护快照/恢复闭环回到原 migration 9 数据；不得修改历史值以迁就 Down。

## Sol 技术门禁

- [ ] 实现来自治理后 fresh origin/main 的全新 GPT-5.6 Terra / medium / clean worktree；正式 dirty 工作区未触碰。
- [ ] 冷启动边界为 1% 向上取整、min 1/max 7；覆盖边界及 30 天=1、约 6 月=2、约 1 年=4、约 2 年=7。
- [ ] 当天刚过期、首次收仓、正常 5 折/2 折/收仓/过期、提前 3 天、库存 0 与无生产日期安全处理回归不变。
- [ ] 新导出 Excel 只有 A:L 十二字段，不存在隐藏系统列；任意正常 `.xlsx` 文件名可回导。
- [ ] 有生产日期三元组、无生产日期二元组按当前 DB 唯一识别；不使用名称、条码或模糊匹配。
- [ ] 空白跳过且不写 0；明确 0 正式完成；文字/负数/小数逐行跳过。
- [ ] 部分填写可提交；未填 items 保留于合法 successor open task，未被 Handled/完成/停止提醒。
- [ ] completed task 与 Inspection 精确覆盖已提交子集；successor task 精确覆盖剩余当前有效子集；正式历史、提醒状态一致。
- [ ] 第二次导出仅依据当时当前状态，允许剩余减少、消失、变化或新增。
- [ ] 同文件有效、空白、错误、陈旧混合时，有效行成功；状态变化/无法唯一匹配逐行跳过。
- [ ] Preview 后状态竞态在 Apply/Submit 再校验并逐行降级，不覆盖当前 DB。
- [ ] Excel/WPS 占用、损坏、工作表缺失/A:L 结构破坏为文件级失败，并映射为门店可读提示；完整异常仅入日志。
- [ ] loading 超阈值可见；快速读取无明显闪屏；成功/失败/取消均关闭，UI 不假死且无虚假百分比。
- [ ] 导入结果汇总有效/未填/状态变化/填写错误，详情可查看/复制且无 StackTrace。
- [ ] 原有单批次、全部填写、超库存确认流程直接回归通过。
- [ ] Schema/migration/ModelSnapshot/依赖/版本/Updater/Installer/发布渠道无 diff；migration 仍为 9；正式数据库 `NO ACCESS`。
- [ ] S19-T01 最小专项与必要直接回归全部通过；Release App build=`0 warning / 0 error`；`git diff --check=PASS`；FULL=`NOT_RUN / NO_FULL`。

## 用户真实 WPF GUI 验收

- [ ] A｜大表导入超过短暂等待时有明确 loading，界面不假死。
- [ ] B｜180 行只填 80 行时，80 行可以正式提交。
- [ ] C｜100 行空白不写 0，仍继续待排查。
- [ ] D｜第二次重新导出按软件当前真实状态生成。
- [ ] E｜0 与空白行为明确不同。
- [ ] F｜错误/陈旧行不拖垮有效行。
- [ ] G｜Excel 只有 A:L，无隐藏系统字段。
- [ ] H｜正常改名后仍能回导。
- [ ] I｜文件占用、损坏、结构变化提示门店看得懂且有下一步。

只有以上用户 GUI 项明确 PASS 后，S19-T01 与 Stage19 才可 `GUI_ACCEPTED / CLOSED`。技术通过只能停在 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`。
