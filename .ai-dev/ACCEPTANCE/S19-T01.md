# S19-T01 Acceptance｜首次冷启动工作量与排查计划分批回导优化

日期：2026-09-12（Asia/Shanghai）

状态：`R4_UI_POLISH_IMPLEMENTED / FINAL_GUI_RETEST_READY / NOT_ACCEPTED`

## R4｜最终 UI 收口

- 候选启动器首版误用非 TEMP/GUID 数据根，运行时按安全合同拒绝；该入口已修正。一次性同结构 `%TEMP%\{GUID}` smoke 已实际取得标题为“门店效期排查软件”的响应主窗口后删除；最终候选根=`%TEMP%\78958c41-8cbe-49f7-a20b-95f575b4af9d`，重新复制 DB SHA256=`3F70174D16C69E97A4DA3E83725402A0908619D62B718C3C5C2C28670FF68818`，sidecar=`0`。
- 用户现场确认：R3 正式提交 PASS；180→填80/空100→提交80→剩余100 PASS；第二次导出100、A:L、任意文件名、行级容错与三类文件级错误均 PASS。GUI A 5000 行读取逻辑 PASS，但视觉反馈过弱。
- implementation=`443e926`：TodayInspection 页面复用既有 `IsReadingPlan` 显示居中原生 `IsIndeterminate=True` overlay，遮罩主体并阻止交互；原 async `finally` 保证成功、文件失败、行级完成和异常均清理状态。
- 确认窗口直接复用现有 Preview row 的分类与 `Reason`，显示“处理结果”“原因”；筛选仅投影 `VisiblePreviewRows`，不重算 valid/stale，不修改 `PreviewRows`、可提交统计或 Submit payload。
- R4 精准专项=`4/4 PASS`：busy/overlay 成功清理、文件失败清理、固定 `valid=1 / blank=98 / stale=80 / invalid=1` 五类筛选、筛选前后 Preview rows/summary 与提交 TaskIds 不变。
- Release App build=`0 warning / 0 error`；`git diff --check=PASS`；无业务 UseCase、migration/schema、Excel A:L、Updater/Installer、版本或发布链变化；FULL=`NOT_RUN / NO_FULL`，旧 `99/99`、`114/114`、`36/36` 未重跑。
- R4 GUI candidate=`S19-T01-R4-Final-UI-GUI/启动S19-T01-R4最终UI复验.cmd`，包含 5000 行 loading 文件与从已验收 R3 状态复制出的独立数据根。只复验 A overlay 与 B 结果/原因/筛选；用户明确 PASS 前仍 `NOT_ACCEPTED`，Stage19 不关闭。

## GUI blocker R3｜正式提交再次把合法 0/0 判为 stale

- R2 GUI 已确认预览=`有效80 / 未填写100 / 状态变化0`；填写排查人并确认提交后，正式提交仍显示状态变化 80，确认窗口未关闭，后续 A～I 停止。blocker 发现时状态退回 `GUI_BLOCKER_FOUND_R3 / REPAIR_REQUIRED / NOT_ACCEPTED`。
- 对当前 R2 隔离库 80 条 Draft 只读逐项核对：task 非 open、item 缺失、tracking 非 active、item/batch attention 不一致、draft attention stale、stage 不一致、RequiresReconfirmation、商品/基线不合格、handled 结构非法均为 `0`；仅 `HandledAttentionVersion >= AttentionVersion` 命中 `80/80`。
- repair=`f38f4cd`：从 `InspectionSubmissionUseCase` partial 当前事实校验删除错误的“相等即已处理”判断；`HandledAttentionVersion >= 0` 且 `<= AttentionVersion` 的结构保护及其余当前事实保护保留。剩余 item 重建同步允许合法 attention=`0`，避免 0/0 pending 被丢弃。
- 提交/批量提交/Excel 回导直接专项=`99/99 PASS`。真实 cold-start 0/0 导出→填写→Preview→Apply→Partial Submit=`Submitted`；180→填80/空100→Preview 80/100/0→Submit 后 `80 Inspection + 100 open pending`，Bulk outcome=`Submitted`；八类提交负例保持阻断。
- Release App build=`0 warning / 0 error`；`git diff --check=PASS`；无 migration/schema、冷启动 1%、Excel A:L、Preview、UI、Updater/Installer、版本或发布变化；FULL=`NOT_RUN / NO_FULL`，既有 `36/36`、`114/114` 未重跑，正式数据库=`NO ACCESS`。
- 新 GUI 只先重验 180→导出→填80/空100→预览80/100/0→填写排查人→提交并确认。PASS 必须为正式提交成功、确认窗口自然关闭、今日排查刷新、约 100 条待排查且已提交 80 条不再出现；之后才恢复其余 A～I。
- R3 GUI candidate=`eedd651e35364d87b351ee5484128310e805c626`；入口=`S19-T01-R3-PartialSubmission-GUI/启动S19-T01-R3验收.cmd`；全新隔离根=`%TEMP%\54dfe6b7-8dfd-4253-84da-bdb56aa1a06f`。Release 候选实际启动响应、取得主窗口并生成独立 `data/app.db` 后已关闭；R2 及更早候选作废。

## GUI blocker R2｜冷启动 0/0 被误判 stale

- 用户现场已成功导入并导出 180 批次；填写 80、留空 100 后，预览为有效 0、未填写 100、状态变化 80，原 A～I 再次停止。blocker 发现时状态退回 `GUI_BLOCKER_FOUND_R2 / REPAIR_REQUIRED / NOT_ACCEPTED`。
- 对该隔离库 180 行只读核对：task=`open`；item/batch stage=`expired`；item/batch AttentionVersion=`0`；HandledAttentionVersion=`0`；RequiresReconfirmation=`false`；tracking=`active`。逐项统计仅 `HandledAttentionVersion >= AttentionVersion` 命中 `180/180`，其余 stale predicate 均为 0。
- repair=`d754f2fff5bb07d98589d0f3d7269eb155671df6`：仅从 `InspectionPlanDraftApplyUseCase.Resolve` 删除该冗余门禁；仍要求合法 open task item、stage/attention 一致、无需重新确认、managed、库存正数且未终止、batch active。
- 真实 cold-start `0/0` 导出→填数量→回导及既有/新增直接专项=`13/13 PASS`；task closed、attention mismatch、stage mismatch、RequiresReconfirmation、库存 0、停止跟踪、open item 不存在七类负例全部 stale。
- Release App build=`0 warning / 0 error`；无 migration/schema、冷启动 1%、Excel A:L、部分提交、UI、Updater/Installer、版本或发布变化；FULL=`NOT_RUN / NO_FULL`，正式数据库=`NO ACCESS`。
- 新 GUI 只先重验：180 导入→导出→填 80/空 100→预览应为有效 80、未填写 100、状态变化 0。PASS 后才继续提交及原 A～I。

## GUI blocker R1 historical｜导入前快照 migration 10 门禁

- 用户现场确认商品 Excel 已识别 `180 商品 / 180 批次 / 数据异常 0`，正式导入被“导入前 SQLite 快照未通过验证”阻断；A～I 随即停止，旧 GUI 候选作废。
- 根因：`PreImportSnapshotService.RequiredMigrationIds` 与直接测试仍固定 9 条；当前 migration 10 数据库与白名单无法 `SequenceEqual`。这是生产回归，不是用户数据或隔离环境问题。
- blocker 发现时状态已退回 `GUI_BLOCKER_FOUND / REPAIR_REQUIRED / NOT_ACCEPTED`。
- repair implementation=`536bd4126f137328573eaeafb8579e9ff4701bde`：只把 `20260912083448_AdjustCatchupWindowConstraint` 加入服务及测试白名单，并新增最新空库明确成功用例；无 migration/schema/业务/UI/版本/发布链变化。
- 直接专项 `PreImportSnapshotServiceTests + ConfirmedImportExecutorTests = 23/23 PASS`：覆盖 migration 10 空库 VERIFIED、带业务数据快照与源数据不变、快照 10 条 migration、正常 Excel 越过门禁正式导入，以及损坏源库、缺失结构、快照篡改继续阻断。
- Release App build=`0 warning / 0 error`；`FULL=NOT_RUN / NO_FULL`；未重跑既有 `36/36`、`114/114`；正式数据库=`NO ACCESS`。
- 新 GUI 只先重验“180 批次初始化 Excel 能正式导入”。该关 PASS 后才恢复原 A～I；用户确认前仍 `NOT_ACCEPTED`，Stage19 不关闭。

## Sol 技术结论（2026-09-12）

- 最终技术候选=`9fbfb6c2c2dbfe505b0a1f2c728d3db3ee2b5d03`；以获批双亲 merge 接回 `origin/main@b3ef8ab380957d35cfa415ae21ced88399b0a6e9`。
- 继承回导/部分提交定向回归=`114/114 PASS`；相关生产代码在该证据后未再修改。
- 冷启动、migration 9→10 兼容、SchemaUpgradeSnapshot 恢复与必要直接回归=`36/36 PASS`。
- migration 9 与 Designer Git blob 字节身份不变；migration 10 SQL 仅重建 `batch_baselines`、保留既有列/约束/三外键/三索引，并把目标约束改为 `1..30`。
- 旧值 `3/7/8/11/30` 原样升级；`1/2/7` 可写；`0/31` 与非 catchup 非 NULL 被拒；失败恢复后 DB SHA、9 条迁移与历史值精确回到升级前，无 sidecar。
- EF pending model changes=`NONE`；migration count=`10`；Release App build=`0 warning / 0 error`；`git diff --check=PASS`；正式数据库=`NO ACCESS`；FULL=`NOT_RUN / NO_FULL`。
- GUI A～I=`NOT_RUN`。用户明确 PASS 前 S19-T01/Stage19 不得 CLOSED。

## 授权后的新增技术门禁

- [x] 第 9 条 migration 字节级不变；第 10 条只调整 `CK_batch_baselines_catchup_window` 为 `BETWEEN 1 AND 30`。
- [x] 旧库 `3/7/8/11/30` 全部成功升级并原样保留；新规则 `1/2/7` 可写。
- [x] `0/31` 仍被数据库拒绝；非 `expired_catchup_task` 的 `catchup_window_days` 仍只能为 NULL。
- [x] migration 总数 `9→10`，configuration/ModelSnapshot 同步，无其他 schema 漂移。
- [x] 升级失败经既有 Schema Update 保护快照/恢复闭环回到原 migration 9 数据；不得修改历史值以迁就 Down。

## Sol 技术门禁

- [x] 实现来自治理后 fresh origin/main 的全新 GPT-5.6 Terra / medium / clean worktree；正式 dirty 工作区未触碰。
- [x] 冷启动边界为 1% 向上取整、min 1/max 7；覆盖边界及 30 天=1、约 6 月=2、约 1 年=4、约 2 年=7。
- [x] 当天刚过期、首次收仓、正常 5 折/2 折/收仓/过期、提前 3 天、库存 0 与无生产日期安全处理回归不变。
- [x] 新导出 Excel 只有 A:L 十二字段，不存在隐藏系统列；任意正常 `.xlsx` 文件名可回导。
- [x] 有生产日期三元组、无生产日期二元组按当前 DB 唯一识别；不使用名称、条码或模糊匹配。
- [x] 空白跳过且不写 0；明确 0 正式完成；文字/负数/小数逐行跳过。
- [x] 部分填写可提交；未填 items 保留于合法 successor open task，未被 Handled/完成/停止提醒。
- [x] completed task 与 Inspection 精确覆盖已提交子集；successor task 精确覆盖剩余当前有效子集；正式历史、提醒状态一致。
- [x] 第二次导出仅依据当时当前状态，允许剩余减少、消失、变化或新增。
- [x] 同文件有效、空白、错误、陈旧混合时，有效行成功；状态变化/无法唯一匹配逐行跳过。
- [x] Preview 后状态竞态在 Apply/Submit 再校验并逐行降级，不覆盖当前 DB。
- [x] Excel/WPS 占用、损坏、工作表缺失/A:L 结构破坏为文件级失败，并映射为门店可读提示；完整异常仅入日志。
- [x] loading 超阈值可见；快速读取无明显闪屏；成功/失败/取消均关闭，UI 不假死且无虚假百分比。
- [x] 导入结果汇总有效/未填/状态变化/填写错误，详情可查看/复制且无 StackTrace。
- [x] 原有单批次、全部填写、超库存确认流程直接回归通过。
- [x] Schema diff 仅获批 migration 10 / configuration / ModelSnapshot 约束 `1..30`；无其他 schema、依赖、版本、Updater、Installer 或发布渠道 diff；migration=`10`；正式数据库 `NO ACCESS`。
- [x] S19-T01 最小专项与必要直接回归全部通过；Release App build=`0 warning / 0 error`；`git diff --check=PASS`；FULL=`NOT_RUN / NO_FULL`。

## 用户真实 WPF GUI 验收

- [ ] A｜5000 行 loading 功能已 PASS；R4 居中 overlay 视觉仍待用户最终复验。
- [x] B｜180 行只填 80 行时，80 行可以正式提交。
- [x] C｜100 行空白不写 0，仍继续待排查。
- [x] D｜第二次重新导出按软件当前真实状态生成。
- [x] E｜0 与空白行为明确不同。
- [x] F｜错误/陈旧行不拖垮有效行。
- [x] G｜Excel 只有 A:L，无隐藏系统字段。
- [x] H｜正常改名后仍能回导。
- [x] I｜文件占用、损坏、结构变化提示门店看得懂且有下一步。
- [ ] R4-B｜结果/原因直接可见，填写错误/状态变化/全部筛选与“1 条可提交”保持一致。

只有以上用户 GUI 项明确 PASS 后，S19-T01 与 Stage19 才可 `GUI_ACCEPTED / CLOSED`。技术通过只能停在 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`。
