# S5-T01 验收记录｜正式排查历史只读查询基础

- 验收日期：2026-08-29
- 结论：通过，完成后停止
- 批准任务卡：`.ai-dev/TASKS/S5-T01.md`
- Luna 实现提交：`e741df4e128b43c2b45d0e97c320aae15471f424`
- 执行：全新 GPT-5.6 Luna（max）
- 独立审查与技术验收：GPT-5.6 Sol

## 实现与范围

- 新增 `InspectionHistoryQuery`，提供正式排查历史列表与单条详情两个 Application 只读入口。
- 列表只认关联 `ProductTask.Status == "completed"` 的 `Inspection`，按 `SubmittedAtUtc DESC, InspectionId DESC` 稳定排序，并返回正式记录、商品快照、提交时间和明细数量。
- 详情只读取 `Inspection` / `InspectionItem` 已落库快照，明细按 ID 稳定排序；不存在或关联任务未完成时返回 `not_found`。
- 查询均使用 `AsNoTracking`，不读取 Draft、当前库存或当前 Batch 状态，不运行效期计算，不调用任何写用例。
- 未修改 Domain、EF Configuration、DbContext、migration、Stage 3 生命周期、Stage 4 Draft/Reconfirm/InventoryAdjustment/Submission、WPF 或依赖。
- 未实现 Revision、历史修改/删除/撤销、复杂筛选、历史页面或 S5-T02。

## Sol 独立门禁

- S5-T01 专项：9/9，通过。
- Stage 4 八个权威测试类：179/179，通过。
- Stage 3 十个权威测试类：170/170，通过。
- Release 全量：541/541，0 失败、0 跳过。
- Release build：0 warning、0 error。
- EF `has-pending-model-changes`：无模型漂移。
- migration list：8 条，未新增 migration。
- dependency diff：空；`dotnet list package` 解析版本与基线项目文件一致。
- `git diff --check`：通过；实现提交后工作区 clean。

Stage 4 closeout / handoff 将“Stage 3 十个权威测试类”记为 184/184，但仓库当前按 S3-T01～S3-T07 的十个既有权威类精确过滤实际为 170/170；Stage 4 归档前的 S4-T08 与 Stage 3 总验收也记录为 170/170。本验收保留历史档案原文，仅按本次可复现命令报告 170/170，不用额外无关测试凑数。

## 结论与停止门禁

S5-T01 正式验收通过。正式历史列表、详情与严格只读边界成立，Stage 3 / Stage 4 权威行为无回归，schema、migration 和依赖不变。

完成后停止；不得创建、编号、派发或实施 S5-T02，等待用户下一步批准。
