# S1-T08 验收记录

- 验收日期：2026-08-27
- 结论：通过
- 执行模型：GPT-5.6 Luna（max）
- 任务卡提交：`5cdc676034d67ffb649f95d067472a6e13fe9dbf`
- 实现提交：`539f54e4e6b3f65c6229ac18185a8cf607b0fb14`

## Sol 独立审查

- 实现只增加 `AppSetting`、`AppState`、两个独立 EF 配置、DbContext 注册、`AddSettingsAndAppState` migration 和专项测试。
- 两个既有测试仅在精确业务表清单中机械加入 `app_state`、`settings`；未修改其他断言。
- 未修改项目/solution、依赖、UI、App、既有实体、既有配置或既有六条 migration。
- 未发现提醒调度、到期检查、注册表、自启动执行、托盘、日志业务、生命周期事件、备份业务、Excel、UI 或 Stage 2+ 实现。
- 最近成功导入时间仍以 `imports` 为唯一事实源；未增加软件版本号、数据目录或其他重复/推测字段。

## 数据完整性验收

- `settings` 精确包含 `id`、`reminder_minute_of_day`、`auto_start_enabled`；默认单例行为 `id=1`、600 分钟、开启自启动偏好。
- `app_state` 精确包含 `id`、`last_reminder_date`、`last_normal_run_date`；默认单例行两个日期均为 NULL。
- 两表均由真实 SQLite CHECK 限制 `id=1`；主键与 CHECK 共同保证最多一个合法单例行。
- 提醒分钟边界 `0`、`600`、`1439` 可保存，`-1`、`1440` 被拒绝；布尔列只接受 `0/1`。
- 两个运行日期可独立保存、清空并读回；两表无外键和额外索引。

## 迁移与旧数据验收

- 从 `AddBackupMetadata` 写入 Product、Batch、Task、TaskItem、Draft、DraftItem、Inspection、InspectionItem、InspectionItemRevision、InventoryAdjustment、Import、ImportWorkbook、ImportIssue、BackupRecord 后升级：14 类旧数据全部保留。
- 增量 migration script 为 1,090 字节，只创建 `app_state`、`settings` 并插入两条默认行；索引、`ALTER TABLE`、`DROP TABLE`、`ef_temp_*` 均为 0。
- 完整 migration script 成功生成，为 29,008 字节，包含 7 条 migration 记录。

## 独立验证结果

- `dotnet build StoreExpiryInspector.slnx --nologo --no-restore`：0 警告，0 错误。
- `dotnet test StoreExpiryInspector.slnx --nologo --no-restore`：42 通过，0 失败，0 跳过。
- `dotnet ef migrations has-pending-model-changes ... --no-build`：无模型漂移。
- 官方 NuGet 源漏洞审计：应用与测试项目均无已知漏洞。
- `git diff --check`：通过；测试临时数据库残留为 0；验收前工作区干净。

## 阶段状态

`S1-T08` 通过。Stage 1 尚余本地运行日志基础设施与生命周期事件留痕；生命周期事件继续等待其余底座完成。本轮不创建下一张任务卡，不进入 Stage 2。
