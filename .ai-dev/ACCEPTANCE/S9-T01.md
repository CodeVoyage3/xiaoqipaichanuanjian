# S9-T01 Acceptance

状态：TECHNICALLY_ACCEPTED / CLOSED（2026-09-04，Sol独立验收）。开工fetch main=7c1fa2d4b0178314816e79663765f952c66d3095，clean、0/0；最终候选0c6d0e4e37b3d18f3fdefa65c7c80f0108d41b11。Stage9仍IN_PROGRESS，等待下一卡授权。

- [x] 新需求覆盖旧总纲禁止在线升级规则，Stage9/本卡建档，历史保留。
- [x] 全新Terra medium/priority实现提交；Sol完整diff独立审查。
- [x] 版本1.0.0单一来源与无布局变化；持久化全链路审计。
- [x] 安全隔离接缝不回落正式库、无真实自启动写入；专项通过。
- [x] win-x64 self-contained多文件publish成功，大小/版本/runtime证据。
- [x] 脱离源码启动真实发布WPF，移目录后再次运行，合成SQLite/日志/目录写入正确，安装目录未写业务数据。
- [x] Sol新鲜Release build0/0、完整tests0failure、EF无漂移、migration9/固定末条、diff --check及范围门禁。
- [x] 安装器/GitHub匿名更新源/独立Updater/程序和DB回滚架构及blocker如实登记。
- [x] 正式库未访问；Stage8边界保留；不创建S9-T02。最终治理提交后普通push并fetch核对clean/0/0，实际最终SHA及推送结果以Git回执/最终回复为准，不预写自指SHA。

Stage8 984/984是历史证据，不是本卡复跑。真实断电/SSD/磁盘/文件系统/bit rot/不可读介质未证明；合法外来WAL来源不可验证；严重坏当前无法保护时Restore阻断，不直接救援。

## 最终Sol技术门禁

- 最终候选0c6d0e4上无filter Release991/991，0failure/error/timeout/aborted/notExecuted/skip，实际UnitTestResult991全部Passed，exit0；21:17:07～21:27:46（+08:00），runner10m38s。原超时项包含在全量中通过；不以单项回执代替本次完整运行。
- 命令：`dotnet test StoreExpiryInspector.slnx -c Release --no-build --no-restore -p:NuGetAudit=false --logger "trx;LogFileName=full-release.trx" --results-directory <本轮TEMP>`，此前独立build0/0；源码/测试锁定，最终再build仍0warning/0error。
- TRX：`TEMP/S9T01-Sol-662b5863bb574469b798b1597f86cd5a/full-release.trx`；SHA256 `AAA81EED9A57E2C1BDBCF39BD861BB2D32AE96471FDAD5BB3D4F3737DE3130DF`。全部计数及时间原文见S9-T01-RESULT.json。
- 沿用标准全量的10个显式空return：T01 PERF1、T03三档成功3/高规模失败1、T04两条100k2/worker与readonly诊断2、真实Excel1；S8_T01_PERF/S8_T03_RUN_HIGH_SCALE/S8_T03_RUN_HIGH_SCALE_FAILURE/S8_T04_RUN_100K=0，worker/readonly/真实Excel环境变量清除。runner skip=0不表示这些高规模场景实跑。本卡不重新宣称100k专项或真实Excel证据，Stage8历史保留。
- 最终EF has-pending-model-changes exit0，无漂移；migration list --no-connect九条，最后20260901155124_AddPolicyAndBaselineFoundation。设计时仍:memory:；--no-connect的pending状态未知提示不代表正式库已检查。NuGetAudit=false不等于在线漏洞审计通过。
- 完整diff与diff --check通过，Application/Domain/Schema/ModelSnapshot/index/migration/生产PRAGMA/NuGet包未改，UI仅版本取值无布局改动。正式库未读/哈希/复制/打开，所有运行DB为显式新TEMP/GUID；此证据不是全机I/O追踪。
- 无剩余S9-T01技术blocker；Stage9最终交付仍待安装器、公开Release资产、签名协议、Updater/跨版本恢复及Windows干净机器矩阵。验收范围仅本卡底座，不冒称最终门店交付。

## 候选与独立审查

最终生产/测试候选`0c6d0e4e37b3d18f3fdefa65c7c80f0108d41b11`；全新Terra `/root/s9_t01_terra_medium` medium（模型可用priority服务档），提交e86d91c、f1eb6c5、abb31bb、0c6d0e4，均未push并已停止。Sol治理开工19a3277，Sol未写生产代码。
Sol完整审查所有src/tests及publish脚本：路径集中但正式默认路径不变；显式新TEMP/GUID、祖先普通目录、既有根/未知参数/smoke缺根拒绝；在Initialize前完成配置，隔离mutex、自启动调用前拒绝；smoke只隔离运行，等待真实WPF Loaded、Dashboard/PendingTasks首轮读完成且无错误，Stopwatch有界超时。
版本唯一数值源csproj Version=1.0.0，AssemblyVersion/FileVersion由其派生为1.0.0.0，ProductVersion含Git SHA；XAML不再重复硬编码版本。无布局/业务/Application/Domain/Schema/index/ModelSnapshot/migration/NuGet包变更。publish profile不强加普通build/test RID，不trim/单文件/AOT。

## Sol发布与运行证据

最终命令：`$env:NuGetAudit='false'; & .\tests\S9T01-PublishSmoke.ps1`。经审查授权提升权限仅解决官方NuGet TLS；exit0、PASS。最终目录：`TEMP/a19c9aa7-f978-4241-91c0-56a480882d2d/publish-b`；完整递归文件大小164508896 bytes（156.8879 MiB）。前一候选abb31bb发布164506960 bytes保留为开发过程，不混作最终产物。
第一次publish-a直接运行，工作目录为本轮TEMP/working，退出后核验全文件相对路径/SHA256/长度不变；先验证移动目标都在本轮GUID根，再Move到publish-b，原publish-a消失，第二次直接运行后再次核验程序目录不变。两次创建不同全新隔离数据根，不声称这里验证了跨版本数据迁移或旧库升级。无需dotnet run、Visual Studio、Python或SQLite工具启动发布EXE。
真实smoke成功日志两次均包含startup_recalculation_completed、s9_t01_smoke_ready；SQLite与logs在指定隔离根，backups/pre-import目录亦在该根。独立只读检查两份app.db各339968 bytes，integrity=ok、FK=0、migration9且末条固定。源码默认用户数据目录未被运行测试使用或探测。
runtimeconfig为includedFrameworks：Microsoft.NETCore.App和Microsoft.WindowsDesktop.App均10.0.10，无framework外部依赖配置，携带coreclr/hostfxr/e_sqlite3。Sol另以不存在的DOTNET_ROOT/X64且禁用multilevel查找直接运行，exit0，host trace确认hostfxr/CoreCLR从publish-b载入；第三份合成DB同样完整性/FK/migration通过。仍不是无开发环境的干净Windows机器实测，也不是人工GUI验收。
三份隔离DB、日志、publish JSON与host trace的路径/哈希见`S9-T01-RESULT.json`。TEMP产物保留，可能被系统后续清理；不把原始SQLite或runtime二进制提交仓库。

## 失败过程保留

- 首次fetch被沙箱.git/FETCH_HEAD权限阻断，提升权限后成功，开工状态实查clean/0/0。
- Terra首次与Sol首次脚本publish受沙箱NuGet TLS/NU1301阻断，脚本非零停止未启动WPF。提升权限从官方源还原runtime packs后真实发布与移动双跑成功；不是修改凭据或关闭TLS验证，不嵌入token。
- Sol第一次全量发现S6T02旧“App无Timer”静态断言失败，取消；测试修订仅允许隔离smoke方法中的有界timer，正常前后代码仍受无Timer门禁。
- 后续全量发现S3T07“App只取一次UtcNow业务时间”静态断言失败，取消；保留旧断言，Terra将smoke超时改为Stopwatch单调计时。中途一次重编译被全量testhost持有测试DLL阻断，属并行构建文件锁，不冒称测试失败或成功；取消后最终重新build/full。
- Terra最终相关定向38/38为实施者回执；不冒充Sol独立全量。Sol最终全量结果在下节完成后记录，未完成前不关闭。
- 0c6d0e4第一次全量在S7T03 UnstableDraftSaveIsSettledBeforeEnteringBackupPage的5秒异步等待超时后取消；该生产/测试链本卡未改。Sol单项原样复跑1/1、667ms、exit0（focused-timeout-rerun.trx），再在发布结束后单独启动无filter全量。不能用单项通过代替全量，也不将一次超时确定归因于生产bug或环境负载。

## GitHub与安装升级边界

匿名请求仓库API200/private=false/public；Release列表200且0条、latest404。当前无private token blocker，但没有实际更新资产可下载验证。首次安装器方案沿用Inno Setup/lowest当前用户；实际安装器、快捷方式/首装自启动、卸载、签名manifest、Updater与跨版本保护恢复仍需后续卡，不能作为本卡已交付功能。没有改变仓库可见性、创建Release或泄露凭据。
安全协议、数据逐项路径、自启动机制、程序替换/失败回滚、migration staging/保护/恢复及精确下一卡建议见`.ai-dev/ANALYSIS/S9-T01-UPDATE-ARCHITECTURE.md`。S9-T02只建议，尚未创建。
