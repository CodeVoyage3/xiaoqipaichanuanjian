# V1-F01-I04｜Sol 独立技术与隔离 GUI 验收

- 日期：2026-09-02
- 结论：`ACCEPTED / V1-F01_CLOSED`
- 开工治理提交：`2904691`
- 实现与修复提交：`060ea0a` 至 `71c1614`
- 验收角色：Sol；未直接采信 Terra 自报结果

## 交付事实

- `PostImportLifecycleUseCase` 与 `StartupRecalculationUseCase` 只放行具有匹配完成 ScopeBaseline 的 Managed、批准 policy、version 1 商品，并复用 `ExpiryPolicyCalculator` 与既有 `ProductTaskAggregator`。
- 基线完成后的新 BatchKey、阶段升级、真正突破 MaxArrivalQty、正式提交后的更高阶段再次触发、库存 0 与正式 0 件停止均继续使用既有生命周期、AttentionVersion / HandledAttentionVersion 与 Inspection / Revision 权威。
- Excluded、Unresolved、无匹配完成基线或无有效 policy 的商品不进入正常生命周期；Reminder 查询只消费符合相同资格的 open ProductTask。
- I03 首次 5折/2折基线不会被启动重算补成历史 Task；I03 冷启动不重跑，ScopeBaseline / BatchBaseline 不由 I04 创建或改写。
- WPF 仅将核心导入文案由“食品效期 Excel”改为“商品效期 Excel”，没有新增页面或 UI/UX 重构。

## Sol 独立技术门禁

- I04 受影响专项：99/99，通过 99、失败 0、跳过 0。
- I01～I03 相关回归：159/159，通过 159、失败 0、跳过 0。
- 手工库存修正回归：30/30，通过 30、失败 0、跳过 0。
- Release 全量：751/751，通过 751、失败 0、跳过 0；首次 750/751 暴露合法 fixture 缺少完成范围基线，修复 fixture 后由 Sol 重新完整运行通过。
- Release build：0 warning / 0 error。
- EF `migrations has-pending-model-changes`：无模型变化。
- EF `migrations list --no-connect`：9 条；最后一条仍为 I01 的 `20260901155124_AddPolicyAndBaselineFoundation`。
- 相对 `3ef97fe` 无 migration、ModelSnapshot、schema、依赖、`.csproj` 或 `.slnx` 变化；`git diff --check` 通过。
- 未实现提前 3 天预提醒，未进入 V1-F02、V1-F03 或 Stage 8。

## Sol 隔离 WPF GUI 验收

- 使用同一 HEAD 源码的隔离路径测试构建与隔离 SQLite；仅在被 Git 忽略的测试副本中重定向 LocalApplicationData，生产跟踪源码未改变。
- 真实 Excel 导入成功：7,007 个商品、32,402 个批次；10 个源大类均保存为 canonical CategoryCode，8 个 Managed scope baseline 完成。
- 真实样本于业务日期 `2026-09-02` 展示 576 个 open ProductTask：过期 384、收仓 192、5折 0、2折 0；与 I03 在 `2026-09-01` 的 583 对账口径不同源于业务日期推进，不是硬编码或规则修正。
- 应季搭配 797 个商品 / 1,212 个批次、赠品小样 7 个商品 / 7 个批次均正常保存为 Excluded，policy/version 为空，Task 为 0。
- 额外导入 1 条日用 180 天边界样本：保存为 Unresolved，policy/version 为空，显示“规则未覆盖”提示，`expiry_policy_unresolved` ImportIssue 为 1，Task 为 0。
- 任务列表显示 576 个 Managed Task；非食品商品可正常进入列表与详情，详情商品身份、批次和过期 Stage 表达正确。
- 到点实际出现 Windows 提醒：“当前有 576 个商品待排查。最高紧急阶段：过期。”；数据库对账证明全部提醒候选来自 Managed open Task，Excluded / Unresolved 为 0。
- 未执行正式提交、库存修改、备份或恢复；原始 Excel 前后 SHA-256 均为 `BBD91AE4E40E5381D749F8DB8F4CC0A600FB88D8C1CF6EA160C7C33EC1A3F0F6`。

## 环境、边界与停点

- 正式 `%LOCALAPPDATA%\StoreExpiryInspector` 入口仍为既有不可解析 reparse point；未删除、修复或绕过该入口，正式数据库未访问、迁移或修改，不能把历史正式库 hash 冒充本轮现场读取。
- 隔离 SQLite 完整性为 `ok`；隔离 WPF 已退出，`StoreExpiryInspector` 进程为 0；临时下载副本已移除，忽略目录内保留隔离验收证据。
- I04 与 V1-F01 验收完成；不创建 V1-F02 任务卡，不派发后续 Terra，等待产品经理单独批准。
