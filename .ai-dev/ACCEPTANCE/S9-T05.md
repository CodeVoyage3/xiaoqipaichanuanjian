# S9-T05 独立技术验收

2026-09-05 `TECHNICALLY_ACCEPTED / CLOSED`；Stage9 `IN_PROGRESS / WAITING_NEXT_AUTHORIZATION`。开工 freshly fetched `main=origin/main=a8e1414030864c67e5cc82ee81c6062dc20c724d`、clean、0/0。产品版本仍为 1.0.0；未创建 S9-T06。

## 角色与实现

- fresh Terra 实施生产代码并提交、不 push；首个 agent 上下文耗尽后，由 fresh Terra continuation 完成。生产与测试提交为 `b115d93`、`0b3803f`、`011883e`、`efebdf9`、`c71a843`、`cbe906d`。Sol 只写治理、两条滞后测试契约修正，并做完整 diff 与独立复验；未代写生产代码。
- 新增 `StoreExpiryInspector.Updater` 独立 `win-x64` self-contained 程序。主程序把 updater 和候选包准备到 app 外的操作工作根；正式契约位置为数据根 `updates/<operationId>`，Updater 不依赖待切换 app 内 runtime/DLL。
- journal 状态为 `Prepared → MainExitRequested → MainExited → CandidateStaged → OldAppPreserved → SwitchStarted → CandidateActivated → CandidateStarted → WaitingForHealthAck → Committed → Completed`，失败走 `RollbackRequired → RollbackStarted → OldAppRestored → RollbackVerified → RolledBack`，无法安全恢复则 `FailedNeedsManualRecovery`。
- app/staging/old 必须分别精确绑定 `app`、`app.staging-<operationId>`、`app.old-<operationId>` 且互异；完整树使用 relative path、size、SHA256 指纹。包消费前重验 T04 签名、manifest、RID、版本、archive、migration9 与文件哈希。
- WPF 进入维护 gate，禁用窗口、等待草稿稳定、停止 reminder、准备外部 updater 后正常退出。候选验证模式在 Initialize/Migrate/补算/reminder 之前，仅用 immutable/read-only SQLite 加载核心 UI 和读取健康信息；ACK 后才提交。

## Sol 新鲜独立证据

| 门禁 | 结果 |
|---|---|
| app self-contained publish | exit 0；app 及嵌套 Updater 均含本地 `coreclr.dll` / `hostfxr.dll` |
| updater self-contained / 独立运行 | 独立输出存在；无参数 exit 2；正式构建对测试 TEMP journal exit 1 且 journal 字节不变 |
| 实际 WPF smoke / verification | 均 exit 0；ACK operation/version/PID/start/migration9/integrity/FK/coreRead/uiLoaded 全通过 |
| 合成 migration9 DB | SHA256 `13102791AA58A6C886B1DED28446E469ED150181DAB84F76F3ECE81C44FB79AE` 前后不变；无 WAL/SHM |
| preparer staging 中硬杀 | 3/3；旧树不变、staging 清理、journal 未产生、候选进程停止 |
| 成功路径硬杀恢复 | 9 checkpoints × 3 = 27/27；最终 Completed、active=candidate、正常启动、candidate.zip 删除 |
| 回滚路径硬杀恢复 | 3 checkpoints × 3 = 9/9；最终 RolledBack、active=old、正常启动、候选包和证据保留 |
| 补充失败矩阵 | 8/8：malformed/wrong-type ACK、old ACK fail、缺 candidate DLL、old/candidate FileShare.None、TEMP root AccessDenied、operation junction forged ACK |
| live parent | 可在界内退出时 Completed；超时则 exit 1 / FailedNeedsManualRecovery，不硬杀父进程 |
| 双 Updater | 第二实例 exit 1、journal 字节不变；首实例被测试硬杀后重入 Completed |
| fresh 无过滤 Release | 1040/1040，0 failure、0 skip，10m56s |
| Release build | 0 warning / 0 error；正式/测试 Updater 中间输出已隔离，无并行 CS2012 |
| EF / migration | `has-pending-model-changes` exit 0；`list --no-connect` 9 条，末条 `20260901155124_AddPolicyAndBaselineFoundation` |
| diff / secret | `git diff --check` 通过；无真实 private-key block、credential assignment、PackageReference、Domain、migration、ModelSnapshot、installer 或版本差异 |
| GitHub Release | 匿名公共 API fresh HTTP 200、正文 `[]`：0 Release、0 Asset；本卡未创建任何正式发布对象 |

首次全量为 1037/1039：两个旧静态断言仍要求 XAML `StartupUri` 和旧 busy 文本。Sol 只把它们更新为当前显式 normal window 启动与统一 maintenance gate 契约，定向 2/2 后全量 1039/1039。随后 Terra 修复正式/测试 Updater 并行输出冲突并新增 1 个契约测试；最终全量为上表 1040/1040。首次独立 build 的 CS2012 文件锁复现两次，根因修复后连续构建及 Sol 最终构建均 0/0。

逐项原始摘要见 `S9-T05-HARD-KILL-RESULT.json` 与 `S9-T05-UPDATER-RESULT.json`。所有运行证据只来自 TEMP/GUID 合成 migration9 数据；没有探测、读取、复制、哈希或修改正式安装根、正式数据根、数据库或备份。

## 边界与停止

- 没有创建 GitHub Release/tag/asset，没有配置生产 trust anchor 或私钥，没有发布真实更新包；开发机合成通过不等于门店在线升级已经投产。
- 没有 Schema、migration、ModelSnapshot、Domain、业务算法、AppId、正式根、生产 SQLite PRAGMA、依赖或产品版本变化；未执行 migration。
- 成功后删除 operation candidate.zip 与受控下载缓存，保留 old 完整树、journal、日志和外部 updater；回滚/人工恢复路径保留候选包与证据。
- 下一卡只建议 `S9-T06：首次签名正式 Release 与干净机端到端升级验收`。范围须单独授权 1.0.1 版本、生产公钥/离线私钥保管、tag/Release/assets、Win10/11 当前用户从 1.0.0 到同 Schema 1.0.1 的下载、切换、回滚、SmartScreen/签名与分批发布门禁；不含 Schema/migration、正式业务数据操作、重置或 Undo。

本卡关闭后普通 push `main`，重新 fetch 核对 clean、HEAD=origin/main、ahead/behind 0/0，然后停止。
