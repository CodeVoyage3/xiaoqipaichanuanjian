# S9-T01｜产品与Windows x64发布、安装及在线升级架构基线

状态：TECHNICALLY_ACCEPTED / CLOSED（2026-09-04，Sol独立验收）。授权基线7c1fa2d4b0178314816e79663765f952c66d3095；最终生产/测试候选0c6d0e4e37b3d18f3fdefa65c7c80f0108d41b11。

交付：全新Terra提交e86d91c/f1eb6c5/abb31bb/0c6d0e4并停止；Sol独立全量991/991、build0/0、EF无漂移、migration9/末条固定、发布164508896bytes、移目录真实WPF双跑、三份合成DB完整性/迁移及本地runtime加载验证通过。普通push归档后停止，不创建S9-T02。完整证据与失败历史见Acceptance/S9-T01.md及S9-T01-RESULT.json。

## 允许实施

1. Sol修订总纲在线升级冲突，保留原规则与覆盖日期；建立Stage9及本卡治理。
2. 全新Terra审计发布、版本、完整持久化路径、开机启动调用链；仅增加win-x64 self-contained多文件publish配置，明确不trim、不单文件、不新增包或Schema。
3. 在csproj显式冻结Version=1.0.0、Assembly/File版本策略；界面显示从程序集版本取得，移除重复硬编码。不得借机改UI布局。
4. 为真实发布EXE隔离启动建立最小安全接缝：集中默认数据根（保持原正式默认路径不变），显式隔离参数仅允许TEMP下本卡GUID普通目录，拒绝相对路径、越界及ReparsePoint；初始化前处理，错误fail-closed，绝不能回落正式路径。所有数据库、backups/pre-import、日志路径统一使用该根。隔离运行禁止写真实自启动注册表，并隔离单实例标识。不要搬迁、探测、打开正式目录或正式库。
5. 留下可重复的发布验证脚本/专项测试，直接启动发布EXE、观察实际WPF初始化与隔离SQLite/日志、移到第二目录再运行，安装目录文件前后核对。可用有界隔离smoke退出参数产生启动完成证据，但必须跑真实启动/Shell链，不能用测试worker代替WPF。仅自己创建的TEMP/GUID，禁止旧Junction隔离脚本。
6. 写技术基线文档：持久化逐项位置；当前用户安装EXE推荐方案；GitHub匿名可见性/Release资产验证；独立Updater进程/目录/信任边界、manifest候选、失败回滚/migration保护契约及后续阻断门禁。Sol独立研究复核。

## 不允许实施

完整安装器、下载器、在线检查UI、Updater覆盖/回滚执行器、迁移新Schema、更新服务器、强制升级、总部/设备管理、Undo、重置、新业务功能。不发布Release、不改变仓库可见性、不嵌入token。不读正式数据库。发现数据在安装目录则先停安装升级实施并定义迁移契约。

## 验收与交付

Terra仅提交自身本卡实现与文档，停止、不push。Sol审全部diff、独立Release build0warning/0error、全量tests0failure、EF无漂移、migration9及固定末条、diff --check、禁止项审查；真实self-contained发布与移目录运行/数据隔离证据。不得将旧984/984冒充新跑，门禁空return与真实高规模证据分开。
若私有Release不能匿名读取必须报告架构blocker并提出安全公开分发仓库或无客户端secret的分发端选择，未经用户授权不建立外部资源。最终文档区分本卡基座验收和后续未实现能力。
