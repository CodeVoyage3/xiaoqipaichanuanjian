# S9-T03｜公开版本元数据契约与离线友好更新检查/提示

2026-09-04 用户正式授权；2026-09-05 TECHNICALLY_ACCEPTED / CLOSED。最终生产/测试15434e53a6809fd654337fee0332c851c238a922；Sol独立协议40/40、WPF16/16、相关回归32/32、Release1017/1017、build0/0、EF无漂移/migration9通过，详见Acceptance。Stage9等待下一授权，不创建T04。
开工fetch：main=origin/main=82c3fd16423c9772e4c2f4f41a8b56cbbf67c669，clean、0/0；TASKS/ACCEPTANCE未有T03。Stage8、S9-T01/T02仍CLOSED。

## 角色与禁止项

Sol仅治理、完整diff、独立复验，不写生产代码。必须全新GPT-5.6 Terra medium/priority实施，不能复用T01/T02；创建失败即停止。Terra提交后停、不push；Sol验收通过后普通push main。
仅本卡，不创建T04，不下载更新包、不实现Updater/manifest签名/ZIP解压/程序替换/跨版本migration/正式Release/tag/上传资产/重置/Undo。不新增Schema、ModelSnapshot、migration、index、生产PRAGMA、NuGet依赖。Version仍1.0.0，当前版本读正式程序集，禁止第二套硬编码。
正式数据根/DB/backup禁止探测、访问、哈希、复制。需要SQLite的WPF测试只能显式TEMP/GUID合成根；预检不执行正常启动链。S9-T02 AppId、安装路径/权限、卸载保数据、降级及schema preflight契约不变。其49MB安装器仅为历史产物，Stage9总交付须重建。

## 稳定通道契约

生产匿名HTTPS固定CodeVoyage3/xiaoqipaichanuanjian的GitHub Releases稳定元数据，优先GET /repos/CodeVoyage3/xiaoqipaichanuanjian/releases/latest。无Authorization/PAT/OAuth/secret；private/不可用不得要求用户token。非draft且非prerelease、严格vMAJOR.MINOR.PATCH，数值SemVer比较，拒绝非法/缺失/不支持元数据，解析当前版本失败也不得提示更新。
Application结果至少UpdateAvailable、UpToDate、NoPublishedRelease、RemoteOlder、NetworkUnavailable、RateLimited、InvalidRemoteMetadata、Cancelled。200解析；latest404正常无已发布版本；403/429限流；5xx/timeout/DNS/TLS归合理不可用；取消独立。外部URL/正文不执行或自动打开。
固定User-Agent、短超时（建议5秒）、CancellationToken、响应流大小上限（建议256KiB）、说明纯文本长度上限（建议1000字符）。拒绝任意重定向突破固定元数据目标；不读取/执行assets。空说明合法、长说明截断，Unicode/控制字符安全处理。

## 启动和提示

正常主窗口和核心业务就绪后后台一次异步检查，不等待网络初始化DB/主窗。每正常进程最多一次自动检查，无Timer/页面切换/Reminder循环请求。明确once/single-flight，任务异常被观察，真正退出取消，晚到结果不得操作已关闭/Dispose UI。既有关闭到托盘行为不重做。
只有UpdateAvailable可见轻量非阻断提示，沿用现有WPF资源/键盘/文本可访问性，不重设计Shell。显示发现新版本、当前vX.Y.Z、最新vX.Y.Z、可选纯文本摘要；稍后提醒隐藏且本进程不再提示该版本，下次正常启动允许再提示，无持久snooze。
建立可测试UpdateRequested命令/Application边界。若显示立即更新，点击明确告知尚未启用，不假成功、不无反馈，不下载、不退出、不动文件。其他所有结果不弹阻断窗口或假新版，不影响Dashboard/Pending/Today/History/Reminder/Import/Backup/Restore。

## 实施与独立门禁

可注入HttpMessageHandler/transport合成覆盖：新/同/旧版本、1.0.10>1.0.9、1.10.0>1.9.9、draft/prerelease、404/403/429/500、timeout/DNS/TLS、非法JSON/tag/缺字段、超响应/超长及空说明、取消、重复触发、快速退出和晚到结果。
Sol独立协议/Application/UI-VM/启动非阻塞取消专项，真实匿名GitHub smoke（public/列表/latest/安全rate-limit头/时间/无Authorization/业务映射），fresh self-contained publish与显式隔离WPF启动，最终无filter Release全量0failure/error/aborted、build0warning/error、EF无漂移、migration9末条20260901155124_AddPolicyAndBaselineFoundation、完整diff及diff --check。合成协议测试不能依赖网络；真实GitHub不创建Release。全量期间不并行build/publish。
全部通过才TECHNICALLY_ACCEPTED / CLOSED，Stage9 IN_PROGRESS / WAITING_NEXT_AUTHORIZATION；重新fetch确认无冲突，普通push、fetch核对clean/HEAD=origin/0/0后停止。架构需token/Schema/downloader或阻塞启动则BLOCKED/NOT_ACCEPTED，不擅自扩卡。
