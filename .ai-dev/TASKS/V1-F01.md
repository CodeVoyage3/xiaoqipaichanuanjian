# V1-F01｜全品类效期规则与首次冷启动治理（设计与真实数据模拟）

## 授权、角色与状态

- 日期：2026-09-01。
- 基线：`master@a0199124588f8b264251cfd91c689d001440b40d`；UIUX-R03 已归档，Stage 8 未开始。
- 状态：`COMPLETED`；I01～I04 已逐卡批准、实施并完成 Sol 独立验收，V1-F01 于 2026-09-02 收口。
- Sol 负责范围治理、任务卡、独立复核和最终产品门禁。
- 本卡必须由一个全新的 GPT-5.6 Terra（推理强度 medium，平台默认标准速度）执行，不复用旧代理。
- Terra 完成后由 Sol 独立复核；产品经理确认最终规则前，不进入实现。

## 唯一真实数据源与只读门禁

- 原始文件：`D:\下载\东莞壹号广场KKV店商品效期折扣判断表_2026-09-01-17-19-31.xlsx`
- 文件大小：`2522641 bytes`
- SHA-256：`BBD91AE4E40E5381D749F8DB8F4CC0A600FB88D8C1CF6EA160C7C33EC1A3F0F6`
- 模拟业务日期固定为 `2026-09-01`，不得从系统当前日期隐式取得。
- 原表只读：不得修改、另存覆盖、删除行、清洗回写或改变格式。
- 不得虚构、补齐或推断缺失字段；空白、错误、未知单位和歧义值必须保留为数据质量事实。
- 首先核对工作表、真实表头、字段类型、公式/值、行数及关键字段可用率。若冷启动模拟所需字段不存在、含义无法从表头/仓库解析契约证明，或数据质量不足以可靠计算，必须列出缺失/阻断清单并停止模拟与推荐，不得自行猜测。
- 分析脚本只能读取上述原表；所有中间结果写入仓库内忽略目录或临时目录，不得写回原表。

## 项目口径修正

- 历史 Junction 事故已经关闭。
- 本轮或后续工具读取 `%LOCALAPPDATA%\StoreExpiryInspector` 失败，只能记录为“工具侧读取失败”。
- 未取得新的 `fsutil` / `reparsepoint` 等系统证据前，不得重新认定异常 Junction，不得创建、删除或修复 Junction。

## 任务目标

在不修改生产代码、schema 或数据库的前提下，以仓库事实和上述真实门店全品类历史批次 Excel 为依据：

1. 审计现有实现中的食品限定与全品类适配事实；
2. 建立可审计的全品类数据画像；
3. 设计“品类 / 效期管理范围级”的首次冷启动基线；
4. 对动态历史过期补查窗口进行真实数据多方案模拟；
5. 给出可供产品经理选择的推荐方案、边界和对现有系统的影响，不实施该方案。

## A. 当前实现审计

逐项追踪真实调用链、实体、配置、测试与 UI，不仅搜索文案：

1. Excel 导入与表头解析；
2. `Product` / `Batch` 身份与持久化；
3. 生产日期、有效日期 / 到期日期、保质期及天 / 月 / 年单位；
4. `food_v1`、Stage 计算和阶段窗口；
5. `ProductTask` 聚合与最高阶段；
6. `StartupRecalculation`；
7. 新 BatchKey、新增到货及历史累计到货最大值；
8. 商品库存 `= 0` 的最高优先终止；
9. 提醒候选和触发；
10. WPF 搜索、列表、详情、历史和导入展示；
11. `category_code`、`policy_code` 及任何全局首次导入 / 初始化状态假设。

审计表至少包含：位置、当前事实、是否食品限定、全品类风险、可复用权威、未来可能影响；不得把尚未实现的设计写成现状。

## B. 真实 Excel 字段与数据画像

必须从原表确认而非按需求名称假设，重点核对：

- 商品大类 / 分类；
- 商品编码；
- 商品名称；
- 条码；
- 生产日期；
- 有效日期 / 到期日期；
- 保质期；
- 保质期单位（天 / 月 / 年）；
- 累计到货、库存及现有导入所需字段。

数据画像至少按真实品类拆分输出：

- 商品数（明确去重键）；
- 批次数（明确 BatchKey 口径）；
- 已过期批次数；
- 日期 / 保质期 / 单位 / 库存字段的缺失、非法和歧义计数；
- 重复商品编码、重复或冲突 BatchKey、条码非唯一等身份质量事实；
- 保质期长度分布和极端值；
- 无法进入模拟的行数及原因。

不得删除历史数据来改善画像；所有排除项必须在质量统计中可追溯。

## C. 冷启动模型设计

禁止使用整个应用唯一一次 `HasCompletedInitialImport` 的简单方案。设计必须以“品类 / 效期管理范围”为独立基线边界，并说明：

- 基线身份、建立时间、适用规则 / 策略版本和幂等锚点；
- 新增品类只建立自己的首次基线，不重置已运行品类；
- 首次历史批次如何分类：
  1. 当前仍处于 5 折 / 2 折 / 收仓等可执行阶段；
  2. 近期已过期、需要补查；
  3. 很久以前已过期、只建立历史基线；
  4. 库存 `= 0`；
  5. 基线后新 BatchKey；
  6. 基线后新增到货。
- 历史基线只记录“已见 / 已纳入基线”事实，不创建或伪造 `Inspection`、`InspectionItem`、Revision 或 completed Task。
- Seen BatchKey 永久性、新 BatchKey 生命周期、历史累计到货最大值、库存 `= 0` 优先级和后续更高阶段再开任务均保持现有权威。

## D. 真实数据模拟

只对字段充分且口径可由源表与仓库证明的数据执行。历史过期补查窗口不得固定写死为 7 天。

至少比较：

- `3% × 批次自身保质期天数`；
- `5% × 批次自身保质期天数`；
- `10% × 批次自身保质期天数`；
- 上述比例配必要的最小 / 最大天数候选组合；
- 若现有阶段窗口能够从权威代码明确反推，再增加“阶段窗口反推”方案；否则说明为何不模拟。

必须明确并统一：天 / 月 / 年换算规则、端点包含关系、取整方式、生产日期缺失时能否计算、到期当天语义以及异常 / 负值处理。任何规则选择都必须作为候选假设展示，不得伪装成已批准业务规则。

每个方案按品类至少输出：

- 品类；
- 商品数；
- 批次数；
- 已过期批次数；
- 首次会生成的补查批次数；
- 进入历史基线的批次数；
- 当前仍处于可执行阶段的批次数；
- 库存 `= 0` 终止批次数；
- 无法分类批次数；
- 极端案例及原因。

全表合计必须能与品类明细对账；结果应可由脚本固定输入和固定业务日期重复生成。

## E. 交付物

至少提交：

1. `.ai-dev/ANALYSIS/V1-F01-CURRENT-IMPLEMENTATION-AUDIT.md`
2. `.ai-dev/ANALYSIS/V1-F01-DATA-PROFILE-AND-SIMULATION.md`
3. `.ai-dev/ANALYSIS/V1-F01-COLD-START-DESIGN.md`
4. `.ai-dev/ANALYSIS/V1-F01-RECOMMENDATION.md`
5. 如确有必要，一份最小、可重复运行的只读分析脚本 / 测试性工具及其运行说明；不得引入新依赖。
6. 机器可读中间结果可放在 `obj/V1F01/`，不得提交原始 Excel、副本或含完整门店明细的派生文件。

推荐文档必须包含边界案例表，以及对现有 schema / migration / Import / Lifecycle / Task / Reminder / UI 的影响分析；任何 schema 变化只能列为候选设计，不创建 migration。

## 冻结边界与停止条件

- 不修改 `src/**` 生产业务逻辑或 WPF；不弱化、删除或改写既有测试。
- 不修改正式数据库，不启动 WPF，不执行正式导入。
- 不新增 migration、依赖、Stage 8、UIUX-R04 或下一张任务。
- 不把预提醒设计、今日排查计划导出 / 回导并入本卡。
- 不提交原始 Excel 或包含完整门店商品 / 批次明细的派生数据。
- 字段不足时按本卡只交付阻断清单、现有实现审计、数据清单和模拟程序 / 方法设计，然后停止。
- Terra 完成后停止，由 Sol 独立复核；未经产品经理确认最终规则，不进入实现。

## Sol 独立复核门禁

Sol 必须重新核对：

1. 原表 SHA-256 未变化，Git 未纳入原表或完整派生明细；
2. 画像总计与品类明细对账，缺失 / 排除项不被静默删除；
3. 3% / 5% / 10% 与上下限方案可重复生成，固定日期和取整 / 端点规则明确；
4. 推荐结论由真实结果支持，没有虚构字段或模拟数字；
5. 冷启动设计不伪造正式排查历史，不破坏六项既有生命周期权威；
6. `src/**`、项目文件、依赖、schema、migration、正式数据库和 Stage 8 均未改变；
7. 结论明确标注为待产品经理确认，不构成实现授权。

## F. 第一轮产品决策（2026-09-01 已批准）

下列规则已由产品经理正式确认，本轮补充分析不得重新讨论或降级为候选：

1. 冷启动管理范围采用“商品大类 + `policy_code / policy_version`”的范围级基线，不使用全应用唯一首次导入开关。
2. 历史过期补查窗口采用 `clamp(ceil(批次实际保质期天数 × 5%), 3, 60)` 天，端点包含。
3. 窗口内历史过期批次直接进入正常 open task，不增加人工复核队列；来源必须可审计为“首次历史补查”。
4. 超出窗口的历史过期批次只建立历史基线，不创建或伪造 Inspection、Revision 或 completed Task。
5. 到期当天继续定义为 `expired`。
6. 原 Excel“是否该做临期折扣”不得作为全品类强制准入条件。
7. Seen BatchKey、历史累计到货最大值、库存 `= 0` 最高优先、后续新 BatchKey、阶段升级及正式提交只处理当前阶段等既有生命周期权威保持不变。

上述确认仅冻结产品规则，**仍未批准生产代码、schema 或 migration 实现**。

## G. 生产实现前最后一轮补充分析

本节仍属于 V1-F01。不得创建 V1-F02，不得进入实现。原执行代理均已结束，必须由一个全新的 GPT-5.6 Terra（medium，平台默认标准速度）接管，先阅读本卡、四份现有分析、Sol Review、当前 Git 状态和本节要求，不得重置已有成果。

### G1. 全品类 Stage policy 对账

继续只读使用本卡唯一真实 Excel，首先从原表真实表头确认 5 折日期、2 折日期、下架 / 收仓日期的实际列名、值类型、空值情况和可证明语义；不得按需求称呼猜列，不得把 Excel 日期直接认定为业务权威。

以仓库 `food_v1` 权威代码对相同批次按生产日期、有效日期、保质期和单位计算 5 折、2 折、收仓节点，并与 Excel 对应列只作一致性对账。必须明确：

- 当前算法的端点和 D / M / Y 换算；
- 可比较批次的定义；
- 三个 Excel 日期字段的空值分别单独统计，不计为一致或不一致；
- 同 BatchKey 冲突组继续整体隔离，不静默选择一行；
- 典型案例只输出去标识化、最小必要字段，不提交完整门店明细。

按 10 个商品大类及全表分别输出：

1. 可比较批次数；
2. 三个 Excel 日期字段各自的非空可比较数和空值数；
3. 5 折日期一致数 / 不一致数 / 一致率；
4. 2 折日期一致数 / 不一致数 / 一致率；
5. 收仓 / 下架日期一致数 / 不一致数 / 一致率；
6. 三节点完全一致批次数；
7. 存在任一差异批次数；
8. 典型差异案例；
9. 差异天数分布和是否呈现稳定品类规律。

结论必须回答：

- 10 个大类是否可以共用一套 Stage policy；
- 若可以，是否建议把 `food_v1` 候选抽象为通用 `expiry_v1`（只分析名称与边界，不实施）；
- 若不可以，哪些大类存在可重复的稳定差异，应按实际规则簇如何分组，不得简单每类一个 policy。

### G2. 首日 open ProductTask 工作量模拟

使用已批准的 `5% clamp(3,60)` 历史补查窗口、固定业务日期 `2026-09-01` 和现有 `ProductTask` 聚合 / 阶段优先级权威，重新模拟：

1. 629 个历史补查批次涉及的唯一商品数及聚合 ProductTask 数；
2. 2,493 个当前可执行批次涉及的唯一商品数及聚合 ProductTask 数；
3. 两组按 ProductCode 真实去重后的首日 open ProductTask 数；
4. 同一商品多个批次 / 阶段时，按 `expired > withdraw > discount_20 > discount_50 > none` 取最高阶段；
5. 聚合后任务阶段分布：5 折、2 折、收仓、过期；
6. 每个 ProductTask 的待排查批次数：平均值、P50、P90、最大值；
7. 两组重叠商品数，不得把两组商品数直接相加；
8. 以上全部按 10 个商品大类及全表输出并对账。

必须说明首日模拟与真实系统运行之间仍存在的边界，例如 Stage policy 尚待本轮对账、同 BatchKey 冲突隔离、本表无库存 0 样本；不得把模拟任务量冒充运行数据库实际创建结果。

### G3. 补充交付与门禁

- 新增 `.ai-dev/ANALYSIS/V1-F01-STAGE-POLICY-AND-FIRST-DAY-WORKLOAD.md`。
- 可最小更新 `tools/v1_f01_analyze.py`，机器可读聚合结果继续写入忽略的 `obj/V1F01/`；不得提交完整明细。
- 更新 `.ai-dev/ACCEPTANCE/V1-F01-DESIGN-REVIEW.md` 只能记录 Terra 证据为待 Sol 复核；最终结论由 Sol 独立重跑后填写。
- 不修改 `src/**`、既有生产测试、项目文件、依赖、schema、migration 或数据库；不启动 WPF；不进入 Stage 8；不创建或开始 V1-F01 生产实现。
- Terra 完成后停止。Sol 必须重新运行分析至少两次，核对输出哈希、原表指纹、逐类 / 全表守恒、日期空值分母、商品去重、阶段最高优先级、压力分位数和禁止范围。
- 最终状态保持 `READY_FOR_PRODUCT_DECISION`，等待产品经理批准 V1-F01 实现。

## H. 公司现行 Stage policy 权威输入与设计收口（2026-09-01）

本节取代 G1 中“依据 Excel 中性字段 `折扣日期` 证明 Stage policy”的分析路径。公司现行《效期折扣规则》截图及产品经理本节转录是 Stage policy 的业务权威；Excel 仅继续作为真实数据画像、边界映射和工作量模拟数据源，不得用其中 `折扣日期` 反推、证明或覆盖下列规则。本节仍属于 V1-F01 设计，不构成生产实现授权。

### H1. 已确认公司规则

仅对有有效期的商品应用临期 Stage；无保质期商品禁止临期折扣。到期当天仍为 `expired`；“报废盘亏”是 expired 后门店动作，不新增 ExpiryStage。

1. 食品：
   - 总效期 `<= 9个月`：剩余 `<= 1个月` 为 5 折、`<= 14天` 为 2 折、`<= 7天` 为收仓；
   - 总效期 `> 9个月`：剩余 `<= 3个月` 为 5 折、`<= 2个月` 为 2 折、`<= 14天` 为收仓；
   - 总效期 7--9 个月且单店销售较差时可提前一个月申请折扣，须区经评估；本卡只记录为人工例外，不自动化为普通 Stage。
2. 宠物：不区分总效期，剩余 `<= 3个月` 为 5 折、`<= 2个月` 为 2 折、`<= 14天` 为收仓。
3. 日用 / 美妆 / 家居 / 香氛香水 / 文具 / 潮流玩具：公司规则只明确总效期 `> 6个月` 时，剩余 `<= 6个月` 为 5 折、`<= 3个月` 为 2 折、`<= 14天` 为收仓。总效期 `<= 6个月` 未给出规则，不得沿用食品或自行补全。
4. 日用季节性商品：公司规则只明确总效期 `> 6个月` 的反季商品，剩余 `<= 12个月` 为 5 折、`<= 6个月` 为 2 折、`<= 14天` 为收仓；当季商品参考日用标准。夏季为 3--9 月，冬季为 10--2 月，具体仍跟随当季季节品上下图时间决定。缺少可靠季节属性或上下图决定时不得假定反季。

月阈值用于本次可重复模拟时，必须明确写出与现有系统一致的换算约定及其局限；不得把分析换算约定冒充截图未表达的额外产品规则。

### H2. 三个边界核对

只读使用本卡同一份真实 Excel，完成：

1. `应季搭配`：按商品大类 / 中类 / 小类 / 商品名称等真实字段分析 797 个商品的构成，判断能否充分映射为“日用季节性商品”。若混有不同性质商品，按分布列出并保持未决范围，不得强行整类映射。
2. `赠品小样`：分析 7 个商品的中类、小类、名称、保质期及单位，判断是否有可靠商品属性可映射回食品 / 美妆 / 日用等原始 policy。没有可靠来源信息时标为 `policy 未确定`，不得猜测。
3. 通用长效组短效边界：对日用、美妆、家居、香氛香水、文具、潮流玩具分别统计总商品数、总批次数，以及总效期 `> 6个月`、`= 6个月`、`< 6个月` 的商品数和批次数；存在 `<= 6个月` 时列出中类 / 小类分布、去标识化典型商品和数量。该范围 policy 未给出，必须保持不可计算。

商品数必须按真实 ProductCode 去重；批次数继续使用既有有效 BatchKey 口径并隔离冲突组。任何字段不足均须明确列为阻断，不得由名称之外的信息猜测。

### H3. 真实 policy 首日工作量重算

继续使用固定业务日期 `2026-09-01`、已批准历史补查窗口 `clamp(ceil(实际保质期天数 * 5%), 3, 60)`（端点包含）、库存 `= 0` 最高优先和现有 ProductTask 最高阶段聚合权威。当前可执行阶段须由 H1 已确认规则重新计算，不再以 `food_v1` 代算全品类。

- 无 policy、总效期不满足已确认适用条件、季节属性 / 上下图决定不足及无保质期范围，分别计入“不可计算”，不得塞入任何任务数；
- 历史补查窗口命中的已过期批次按已批准规则进入 open task，其来源为“首次历史补查”；历史补查计算不得因当前非过期 Stage policy 未确定而被静默丢弃，但无有效期 / 无法计算实际保质期者仍不可计算；
- 分别输出各大类可计算 / 不可计算批次、5 折 / 2 折 / 收仓 / 过期任务分布、历史补查 ProductTask、当前可执行 ProductTask、两组按 ProductCode 去重后的首日 open ProductTask；
- 同商品多批次 / 多来源按 `expired > withdraw > discount_20 > discount_50 > none` 聚合；输出重叠商品数及每任务批次数平均、P50、P90、最大值；
- 所有品类明细与全表合计必须守恒，并明确模拟结果不是数据库实际创建结果。

### H4. policy 分组与交付

提出最少且可审计的 `policy_code / policy_version` 分组：相同规则共用 policy，不得简单每个大类一个 policy；人工例外、季节判定和规则未覆盖范围不得伪装为已确定 policy。候选命名只用于设计，不修改 schema、migration 或生产代码。

更新或新增 `V1-F01-STAGE-POLICY-AND-FIRST-DAY-WORKLOAD.md` 及最小只读分析脚本。最终只向产品经理汇报三个边界核对、policy 分组、重算后的首日任务量与新增阻断项。Terra 完成后由 Sol 独立重跑至少两次并对账；状态保持 `READY_FOR_PRODUCT_DECISION`。

## I. 最后一轮冷启动收敛模拟（2026-09-01）

本节只比较首次启用的风险覆盖与门店工作量，不扩展 Stage policy 研究，不进入实现。

1. `应季搭配`、`赠品小样`正常导入并保存，但 V1 明确不参与效期管理：不计算 Stage、不生成 ProductTask、不 Reminder、不参与首次历史补查；不再列为 policy 未决范围。
2. 日用 / 美妆 / 家居 / 香氛香水 / 文具 / 潮流玩具未来若出现总效期 `<=6个月`，正常导入并标记 `policy unresolved / 规则未覆盖`，不生成效期任务，导入结果须可提示。本表该样本为 0，不补造规则。
3. 公司规则图继续是 Stage policy 唯一权威，不使用 Excel 中性字段 `折扣日期` 反推。

只运行以下四个首次基线方案：

- A：当前 5 折、2 折、收仓、到期当天及历史过期补查均进入任务；当前 `5% clamp(3,60)` 基准为 1,474 Task。
- B：既有 5 折建立基线，不进首次任务；保留 2 折、收仓、到期当天及历史补查。
- C：既有 5 折和 2 折建立基线，不进首次任务；保留收仓、到期当天及历史补查。
- D：既有 5 折、2 折、收仓均建立基线，不进首次任务；只保留到期当天及历史补查。

每个方案分别使用两个历史过期补查窗口：

- 当前候选：`clamp(ceil(实际保质期天数 * 5%), 3, 60)`；
- 收敛候选：`clamp(ceil(实际保质期天数 * 3%), 3, 30)`。

共 8 组组合。每组只输出历史补查批次 / ProductTask、首日总 ProductTask、5 折 / 2 折 / 收仓 / 过期分布、各品类任务数，以及相对 1,474 基准减少量。每个 ProductTask 继续按 ProductCode 聚合并取最高阶段；既有低风险阶段仅建立基线，不伪造 Inspection / Revision / completed Task，不破坏接管后的新 BatchKey、新到货、库存 0 终止或阶段升级权威。

只允许更新本卡、既有分析文档、验收记录和最小只读脚本；不修改 `src/**`、生产测试、schema、migration、数据库，不启动 WPF，不进入实现。完成后停止，状态保持 `READY_FOR_PRODUCT_DECISION`。

## J. 产品经理最终批准与设计收口（2026-09-01）

V1-F01 设计规则正式批准：

1. 冷启动范围唯一身份为 `scope_key + policy_code + policy_version`。
2. `应季搭配`、`赠品小样`正常导入保存，但 V1 不参与效期管理。
3. 六类通用长效商品总效期 `<=6个月` 时正常导入、标记规则未覆盖、不生成效期任务。
4. 历史过期补查正式采用 `clamp(ceil(实际保质期天数 * 3%), 3, 30)`，端点包含。
5. 首次冷启动正式采用方案 C：既有 5 折 / 2 折只建立基线；当前收仓、到期当天、窗口内历史过期生成 open Task；窗口外仅历史基线。
6. 历史补查来源必须可审计，不创建或伪造 Inspection、Revision、completed Task。
7. 基线完成后恢复完整正常生命周期，四个 Stage 均按正常规则创建或升级 Task。
8. 公司规则中的月份在 V1 固定为 30 天：1/2/3/6/12 月 = 30/60/90/180/360 天。
9. 到期当天为 `expired`。
10. Seen BatchKey、MaxArrivalQty、库存 0、Task 聚合、AttentionVersion / HandledAttentionVersion 和后续更高 Stage 权威保持不变。

设计状态由 `READY_FOR_PRODUCT_DECISION` 更新为 `DESIGN_APPROVED`。实施拆分见 `.ai-dev/ANALYSIS/V1-F01-IMPLEMENTATION-SPLIT.md`。本轮只批准 Sol 形成拆分提案，不批准任何一张生产实现卡；未创建实现 Terra，未修改生产代码。等待产品经理批准拟议 I01。

## K. I01～I04 实施与最终收口（2026-09-02）

- I01 建立 policy/status、ScopeBaseline 与 BatchBaseline 持久化底座，并新增 V1-F01 唯一 migration；I02 完成 10 类 canonical 范围映射及 Managed / Excluded / Unresolved 导入；I03 完成 version 1、方案 C 与 3% 动态历史补查；I04 完成基线后 policy-aware 正常生命周期、Reminder 防御及最小 WPF 收口。
- 每卡均由单独批准的全新 GPT-5.6 Terra（medium）实施并停止，Sol 独立审查、复跑和验收；I04 最终 Release 751/751、build 0/0、EF 无漂移、migration=9。
- Sol 已使用真实全品类 Excel、边界样本和隔离 SQLite/WPF 完成 GUI 验收；正式数据库未访问或修改。
- V1-F01 状态为 `COMPLETED`。本收口不授权 V1-F02、V1-F03 或 Stage 8，不创建后续任务卡或代理。
