# V1-F03-I04｜用户 GUI 验收返修合并

## 1. 授权、状态与硬边界

- 日期：2026-09-02。
- 开工基线：`master@688d21b0cc31a7d951752f3ca79dcc319b2b4773`，与 `origin/main` 同步，工作区开工前干净。
- 当前状态：`V1-F03-I04_TECHNICALLY_ACCEPTED / GUI_ACCEPTANCE_FAILED_PENDING_REPAIR`；I04 与 V1-F03 不得关闭。
- 用户确认 R2 已修复页面切换闪烁及白屏转圈闪退；本卡必须回归这些修复，但不得重新设计或回退加载路径。
- 本卡只处理本文件冻结的 GUI/交互阻断。禁止 Stage 8、Stage 9、Schema、migration、新依赖、Reminder、商品源导入改造及 I01～I03 业务算法重写。
- 本卡提交后，必须在当前 Sol 话题内创建全新 GPT-5.6 Terra（medium、平台标准速度）；Sol 不直接修改生产代码，不复用旧 Terra。

## 2. 今日排查主页面

- 页面永久内容只保留：任务表、全选、取消全选、部分选择、导出排查计划、导入排查结果。
- 删除页面底部永久内嵌的“回导与待确认结果”、Preview 表格、人员日期输入、Draft/提交操作区。
- “导入排查结果”使用现有独立 OpenFileDialog；I02 Preview 成功后打开独立模态窗口“排查结果确认”。关闭或取消模态窗口返回今日排查页，不遗留可误提交的隐藏会话。
- 保留 R2 的 ViewModel 单例、异步加载、单次列表发布、局部 loading、重复进入不重查及全部合法任务可访问语义；禁止恢复 500 条截断。

## 3. 今日任务高密度表格

- 可见列固定为：选择、条码、商品名称、大类、当前最高阶段、商品当前库存、任务状态。
- 商品编码、批次数仍可留作内部 ViewModel/Application 事实，但不得作为主表可见列。
- 表头和每个数据单元格均显示清晰、纤细、统一的横竖网格线；复用现有主题 Border/GridLine 资源，不新增第二套视觉系统。
- 选择列固定最左；条码给足宽度；商品名称占主要剩余宽度，单行省略并提供完整 Tooltip；大类、阶段、库存、状态保持紧凑且不互相覆盖。
- 所有可见单元格垂直居中；商品名称左对齐；阶段、库存、状态水平和垂直居中。阶段继续复用现有 `StageBadgeTemplate`。
- 行/列 virtualization、recycling 与内容滚动必须保留。500+、576 及更大合法任务不截断，滚动后新出现的行仍可交互。

## 4. 选择命中与视觉权威

- 必须定位并修复“最后一条完整可见行的复选框无法点击”。调查命中测试、遮挡层、父级 ScrollViewer、固定高度/裁剪、行高、边距、Z 顺序及虚拟化容器复用，以最小根因修复为准。
- 每个完整可见行和滚动后新出现行的复选框都可鼠标/键盘选择；`SelectedCount` 立即更新；滚动回收后状态不丢；选择绑定正确 TaskId；全选、取消、部分选择保持正确。
- DataGrid 当前单元格、焦点、悬停不得使用与勾选相同的整行蓝色表达。`IsSelected` 复选框是唯一选择权威；如保留已勾选行底色，只能严格绑定业务 `IsSelected`。
- 不以关闭虚拟化、扩大固定空白或隐藏最后一行作为修复。

## 5. “排查结果确认”模态窗口

- 模态流程：`导入排查结果 → 选择 Excel → I02 零写入 Preview → 排查结果确认 → 填排查人/日期 → 提交数据`。
- 可见汇总继续显示涉及商品、批次、可应用、空白、错误及陈旧/失效数量；错误与状态必须有文字，不只用颜色。
- Preview 表可见列仅为：条码、商品名称、生产日期、有效日期、本次排查数量、校验状态。
- 删除商品编码、“商品/批次”、独立“批次”、独立“原因”列。完整长原因通过状态 Tooltip、选中行说明或模态窗口说明区域展示。
- Preview 表同样显示完整横竖网格线；名称允许省略并提供 Tooltip；日期统一显示 `yyyy-MM-dd`，不得显示 Excel serial。
- 数量空白显示为空白并对应“未填写”；`0` 显示数字 `0`，与正整数一样属于正常可提交数量，不显示“0 件”。
- 校验状态只使用门店可理解的短文案，例如：可提交、未填写、数据错误、数据已变化、任务已完成、已失效、需要重新导出。
- 排查人必填；排查日期必填、默认今天、不得晚于今天；尽量复用既有输入和校验。

## 6. Draft 与正式提交交互

- 删除“保存待确认结果”按钮及全部用户可见 Draft/Task/“集中正式提交”等技术术语；Draft 仍仅作为 I02/I03 间内部状态。
- 主操作按钮命名为“提交数据”。点击后先调用既有 I02 Draft Application 完成当前事实复检和原子 Draft 写入；不得由 UI 直接访问 DbContext/Draft 实体。
- 若本次任务仍有必填空白，不得把该 Task 传给 I03，并提示：`仍有未完成排查项，请填写完整后提交。`
- 完整时提示：`排查结果已填写完整，可以提交数据。`
- 在任何 I03 调用前必须显示二次确认模态：标题“确认提交”；正文“是否提交本次排查数据？提交后将生成正式排查记录，请确认数据无误。”；按钮“取消”与“确认提交”。取消不得产生正式写入或调用 I03；确认后才调用一次既有 I03。
- 超库存确认继续展示商品、当前库存及本次排查合计，并保持 I03 当前事实签名/陈旧重确认权威。不得在 WPF 复制提交、0 件停止、completed 或生命周期算法。

## 7. Preview 显示事实的最小边界

- 允许为 UI 显示最小扩展 I02 Reader/Preview 行只读 DTO，携带现有 Excel 可见列中的条码、生产日期和有效日期，并使用一致的日期解析/显示。
- 该扩展不得改变 A:Y 表头、格式版本、隐藏身份、排序、重复检测、数量语义、陈旧校验、Draft Application 或错误判定。
- 若无法在不改变 I02 业务算法的前提下提供所需显示事实，立即停止并报告。

## 8. Excel 回归契约

- 保持既有 I01 文件：中文大类、A:L 标准 AutoFilter、冻结首行、标题、合理列宽、横向打印、单页宽、一批次一行及空白填写列。
- 用户在 Excel 中筛选、隐藏或重排行后，I02 仍必须只按隐藏的 TaskId/TaskItemId/ProductId/BatchId/AttentionVersion/快照身份匹配，绝不依赖 Excel 行号。
- 本卡优先补回归证据；现有 I01 已满足的行为不得无意义重写。

## 9. 最低自动化证据

- 主表只有七个批准可见列，商品编码/批次数不可见；条码、大类、阶段、库存、状态绑定完整。
- 两张表均有完整横竖网格；商品名称省略+Tooltip；日期不是 serial；状态与数字对齐、垂直居中。
- 最后一条完整可见行、滚动后行、虚拟化回收行都可勾选，TaskId 与 `SelectedCount` 正确；全选/取消/部分选择及 500+ 全量访问不回归。
- DataGrid 焦点/当前单元格视觉与勾选语义分离；R2 无整页 loading、无重复昂贵加载、无旧只读 TwoWay 崩溃回归。
- Excel 中文大类、AutoFilter、重排/过滤后隐藏身份匹配回归。
- 独立模态打开/关闭/取消状态正确；Preview 六列正确；日期、空白、0、正整数、短状态及长原因 Tooltip/说明正确；无独立原因列。
- 排查人/日期门禁；空白 Task 不正式提交；点击“提交数据”先应用 I02；二次确认取消零 I03 调用，确认才调用一次 I03；超库存、刷新、历史及 I01～I03 回归。

## 10. Terra 交付与最终停点

- Terra 只提交本卡最小 production+test commit，报告根因、文件清单、调用路径、测试计数和未验证项，不 push，不把 GUI 标记通过。
- Terra 停止后由 Sol 独立审查完整 diff，执行缺陷专项、I04、I01～I03、关键 WPF/Application/UIUX、V1-F01/F02、Release 全量、Release build、EF drift、migration=9、依赖/项目文件及 `git diff --check`。
- Sol 更新 `.ai-dev/ACCEPTANCE/V1-F03-I04.md` 与 `.ai-dev/HANDOFF/LATEST.md`，commit 并普通 push `master:main` 后停止。
- 最终仍为 `GUI_ACCEPTANCE_FAILED / WAITING_USER_RETEST`。用户先重验页面与选择，再继续 Excel 导出/回导/提交；不得自动进入 Stage 8/9。
