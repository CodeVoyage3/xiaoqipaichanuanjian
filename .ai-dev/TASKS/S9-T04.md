# S9-T04｜更新包下载、Manifest签名与安全校验

2026-09-05 用户正式授权，仅本卡。TECHNICALLY_ACCEPTED / CLOSED。
开工fetch：main=origin/main=c6200ca8507c7f8d99f7f40047b6be291d6ff70b，clean、0/0；Task/Acceptance实查无T04。Stage8、T01～T03 CLOSED，产品仍1.0.0。无适用AGENTS.md。

## 角色和停止线

全新GPT-5.6 Terra medium/priority实施，不能复用T01/T02/T03，提交后停止且不push。Sol仅治理、完整diff和独立复验，不写生产代码。无法创建新Terra即停。
仅检测新版→用户确认→同一Release的Manifest/签名→原始字节验签→协议/版本/平台/资产绑定→流式ZIP下载→size/hash/安全审计/内部版本→VerifiedUpdatePackage，至此停止。
禁止T05、程序文件替换/独立Updater事务/运行下载EXE/主程序退出或新版重启/DB migration/正式Release、Tag、Asset/重置/Undo。无Schema/ModelSnapshot/index/生产PRAGMA/依赖/installer身份和目录契约变化。AppId仍{8F90E64E-5B0D-4FA8-A854-EEA2F4D1EC14}、lowest、自启动/卸载保数据不变；T02安装器历史产物，最终交付重建。
所有测试显式TEMP/GUID，严禁探测/打开/复制/哈希正式DB、Backup或正式更新目录。下载不使用DB、不进入Restore maintenance、不锁业务。全量已有合成测试不构成正式库授权。

## 必须冻结的协议

见ANALYSIS/S9-T04-UPDATE-PACKAGE-PROTOCOL.md。schemaVersion=1，严格三段数值SemVer，channel=stable，rid=win-x64；绑定固定repo、Release/tag、package文件名/bytes/SHA256、target migration列表、允许source version及source migration范围、minimumProtocolVersion。Manifest版本与已检查Release tag相同；文件名StoreExpiryInspector-<version>-win-x64.zip，与同Release唯一资产相等。Manifest/update-manifest.sig名称固定。不使用Setup EXE/Release body URL。
RSA-PSS/SHA256验证下载的原始manifest bytes；禁止重序列化后验签。.sig原始RSA签名字节，测试独立RSA keypair只在内存/TEMP/GUID，不提交或进publish。生产仅固定公钥trust anchor契约，目前留空并明确SigningNotConfigured/fail-closed，禁止测试公钥冒充生产锚点。无需生产私钥即可完成隔离自动化。

## 安全网络与缓存

生产初始URI必须从固定repo + 已检查稳定tag + 经该Release枚举验证的唯一资产名构建；刷新元数据必须按原tag/release身份，不可重新latest漂移。无Authorization/token。关闭自动redirect，手动最多3次；只HTTPS443/无userinfo，无localhost/IP/file/UNC/自定义host，最终仅批准的GitHub资产CDN路径。Sol官方资料与真实响应头见协议；未知host/路径变更fail-closed，不泛化任意HTTPS。
metadata/manifest有小体积上限；package上限256MiB（现有self-contained展开约157MiB，允许ZIP压缩后合理增长且有硬上限）；总展开512MiB、单文件256MiB、最多4096 entries、压缩比上限200:1，检查实际读取而非只信central directory声明。若实际payload不兼容则报告，不静默弱化。
缓存只新建当前TEMP直接GUID或TEMP/产品/updates/GUID。拒绝ReparsePoint祖先、非本次拥有目录。package.download流式写、增量SHA256、实际bytes核对，超声明立即终止；阶段/总超时、CancellationToken。失败/取消清理仅本次未验证缓存，不删任何既有数据；清理失败明确IoFailure且绝不Verified。保留成功可丢弃候选，禁止把它当未来安装授权，未来消费须重新校验防止TOCTOU。

## ZIP与内部身份

采用明确受限ZIP格式，不可靠或不支持特征拒绝；不得用“ZipArchive能打开”替代审计。拒绝绝对/drive/UNC、两种分隔穿越、ADS、空/非法/Windows保留名/尾点空格、重复和大小写/父文件目录冲突、symlink/reparse/hardlink及可表达链接extra、加密/不支持格式、路径逃逸、数量/实际展开大小/单文件/压缩比越界和畸形ZIP。只应用文件，拒绝DB/sidecar、Backup/data/log/settings用户数据、Excel、脚本命令、私钥/PFX/PAT/token；按现有发布清单冻结允许类型，不能只靠几个黑名单。
主StoreExpiryInspector.exe必须在根且是win-x64 PE，FileVersion与Manifest一致；配套StoreExpiryInspector.dll程序集/产品版本与Manifest一致，静态读取不得Assembly.Load/执行候选。targetMigrations与候选程序集静态migration元数据相符；source范围基于当前产品代码契约，不探测正式数据库，只验证声明兼容性，不保证实际DB迁移可行。可仅将必要文件流式放入同一隔离scratch做非执行版本读取，其余解压流审计；不可写真实app。

## 结果与UI

至少Verified、Cancelled、NetworkUnavailable、RateLimited、ManifestMissing、SignatureMissing、InvalidManifest、InvalidManifestSignature、UnsupportedProtocol、UnsupportedPlatform、VersionMismatch、AssetMissing、AssetTooLarge、SizeMismatch、HashMismatch、UnsafeArchive、PackageVersionMismatch、IoFailure；可另加SigningNotConfigured/SourceNotSupported。禁止统一false/Exception。
接现有立即更新命令，准备/下载/校验/已准备状态明确；成功只说“更新包已准备完成，安装更新功能将在后续版本启用”。未知Content-Length用已验签manifest长度显示进度，节流不逐chunk刷新WPF。每进程同版本single-flight；退出取消、晚到结果不触碰已关闭UI；可提供简单取消入口。无生产锚点时明确提示未配置，不假下载/成功，不影响核心离线业务。

## 实施测试与Sol独立门禁

必须覆盖用户完整签名A-I和下载1-17矩阵：合法、原始manifest改字节、签名改字节/错误key/缺失/非法、合法SHA错误签名、合法签名错误hash/size；中断/timeout/403/429/404/5xx、未知长度/超长、RID/EXE版本错、traversal/absolute/duplicates/bomb/禁止文件、取消清理/single-flight。额外覆盖恶意redirect/畸形字段/重复JSON属性、无生产key、migration声明与候选不一致；不得为测试开放生产任意host。
合成HTTP完整成功必须经过真实HTTP流，可用注入transport将受限逻辑URI路由到测试loopback listener（测试专用，不改变生产策略），不是仅mock整个下载器。测试私钥不入repo/publish，候选只静态读不运行。真实GitHub只读public/0/latest404，不创造发布状态。
Sol完整diff、parser/RSA/downloader/archive/UI/cancel/single-flight独立专项，真实合成HTTP成功链、真实GitHub无Release、fresh self-contained publish/secret scan、fresh无filter Release0failure/error/aborted、build0warning/error、EF无漂移、migration9末条20260901155124_AddPolicyAndBaselineFoundation、diffcheck。禁止全量期间并行build/publish。所有证据区分历史/实施者/独立/合成/真实。
全部通过才CLOSED；Stage9 WAITING_NEXT_AUTHORIZATION。fetch确认无冲突，普通push main，fetch核对clean/HEAD=origin/0/0后停止。若需生产私钥/token/Schema/Updater、redirect或ZIP无法可靠安全实现、协议冲突，PAUSED_PRODUCT_REVIEW / NOT_ACCEPTED并报告，不弱化安全。

最终Sol独立门禁全部通过：核心128/128、WPF12/12、退出1/1、Release专项27/27及无filter全量1023/1023，build0/0，EF无漂移/migration9。详见Acceptance及DOWNLOAD-RESULT.json。生产trust anchor待正式发行配置，未创建S9-T05；Stage9等待下一步授权。
