# S25-T03｜今日排查主入口强化与日常工作流引导

状态：AUTHORIZED / IN_PROGRESS。用户已确认方案一及产品锁定（2026-09-18）。
Stage25 = IN_PROGRESS；S25-T01 CLOSED / ACCEPTED / REAL_USER_GUI_PASS；S25-T02 CLOSED / ACCEPTED 保留。
基线：626a114914fba6265a1e847f471f44b1e24dc716，完整 Stage25 continuation chain。

## 冻结范围
- 排查管理导航仅调整顺序：今日排查 → 待排查任务 → 排查历史；图标、普通导航行为、选中态及展开/折叠保持；不加 Badge。
- 首页状态区域下、优先处理上增加紧凑入口：今日需排查 N 项为主信息；进入今日排查为明显主按钮；明天需排查 X 项为弱辅助文字；沿用当前风格。
- 首页按钮必须打开今日排查的今日任务，不能保留明天/后天；左侧普通导航原行为保持。
- 查看全部待排查任务保留普通次级链接；待排查和 T01 的选择/导出/回导能力不动。
- 复用 Dashboard.OpenTaskCount / TomorrowPlanCount；不新增独立数据库查询；加载或失败不能把默认 0 当成没有任务。
- 优先仅 MainWindow.xaml、Stage4ViewModels.cs、必要直接测试。

## 禁止
不改今日/明天/后天计算、效期规则、ProductTask、待排查业务、T01/T02、Compatibility Generation、Schema/migration、Version、Installer/Updater/Release Builder；不无关修复、不 Badge/折叠代码/大型模块/全页重做。
不 merge/push main、tag、Release、正式数据读写；保护原 dirty 工作区。

## 执行与门禁
全新 Terra 实施，Sol 独立审 diff、专项和必要 Production Release build。
FULL = NOT_RUN / NO_FULL；专项包括首页数量加载/失败/正常零值、明天/后天返回今日、普通导航保留日期、导航顺序/主次入口与必要首页/T01直接回归。
技术通过后提供新隔离 GUI 候选；USER_GUI_PENDING，用户真实 GUI PASS 前不 CLOSED / ACCEPTED。
