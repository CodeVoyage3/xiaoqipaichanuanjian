# S21-T01｜v1.1.1 Same-Schema Slim Setup

日期：2026-09-13（Asia/Shanghai）
基线：origin/main@c8b8a7db3e4f5740eb7aa22858cc404d84b9f9fa
状态：CLOSED / ACCEPTED

## 唯一目标

同一 installer/StoreExpiryInspector.iss 支持显式 SAME_SCHEMA_SLIM 与历史 CROSS_SCHEMA_FULL。v1.1.1 Slim 保留完整安装和 preflight payload，不嵌 ZIP、manifest、signature，并只允许真实已安装 EXE 版本不低于 1.1.0 的直接覆盖；fresh install 不受最低版本门禁影响。

## 实现

- .iss 强制二选一模式和 MinimumDirectVersion；仅 Full 要求、声明、提取并运行三项嵌入式更新资产。
- 已安装身份和真实 EXE 版本验证完成后，先执行最低直接版本门禁，再执行既有高版本降级阻断；低版本在任何 payload 复制前显示桥接升级提示并停止。
- 既有 installer preflight 保持：无数据库或当前完整 migration10 才允许 Slim；旧、未知/更高、损坏、unsafe WAL/data-root 仍 fail closed。
- Builder 从 Release Contract 的显式 setupCompatibility 取模式；schemaChanged 只做一致性验证。Slim 要求无 schema change 且禁止 cross-schema；Full 还要求已批准的 schema compatibility evidence。
- Builder 仍生成 ZIP、Setup、manifest、signature 四项资产；只有传给 ISCC 的嵌入参数随模式变化。
- receipt 为 schemaVersion 3，新增 setupMode、minimumDirectSetupVersion、embeddedUpdateAssets。
- release contract 只为历史 v1.1.0 记录真实 CROSS_SCHEMA_FULL / 1.0.9 / true；没有新增或伪造正式 v1.1.1 release 条目，v1.1.1 使用 test-only synthetic contract 验证。

## 最终验收与停止线

最终 implementation=`e4573ff06b1a6d7f0823be95c7297fcc3a0abdbb`。Sol 最终技术验收=`PASS`；S21 direct=`12/12 PASS`，Harness=`5/5 PASS`，Installer A–G 全部符合预期，`git diff --check=PASS`。

A fresh 产品链已真实完成，后续只读 evidence replay PASS 且未重新运行 Setup；B、C PASS；D v1.0.9/m9、E invalid/higher schema、F unsafe WAL/data state、G downgrade 均 `EXPECTED BLOCK / PASS`。A/B/C migrationCount=10、integrity=ok、FK=0；全部 updaterTransactions=0。

production Slim Setup=75,345,382 bytes，SHA256=`F5E746D7A01A58DA738C0B19189E1B4CC47FACC0C603EAB99EAEC93129280E27`；相对 v1.1.0 Full 减少 74,433,072 bytes（49.69%）。

精确排除的旧测试 `S16T02InstallerLaunchTests.Candidate_versions_are_1_0_8` 保持源文件 unchanged，处置为 `PRE_EXISTING_STALE_VERSION_ASSERTION / NOT_S21_BLOCKER`，进入 `BACKLOG / FUTURE TEST CLEANUP`。Harness 只修改 `tests/S21T01-RunInstallerSlim.ps1`，属于 test-only。V110 两脚本只补齐显式 Full 模式参数；历史 v1.1.0 source identity、migration9→10 合同、fault rollback disposition、历史断言及测试语义均未改变。

未修改 App/Updater C# production code、migration、CurrentSchemaIdentity、在线更新协议或 ZIP layout。runtime 重复=`OUT_OF_SCOPE / FUTURE REVIEW`。`FULL=NOT_RUN / NO_FULL`；GUI、fault rollback、migration9→10 正式 E2E、online Updater E2E、backup/restore 均 `NOT_RUN`。S21-T02=`NOT_CREATED / NOT_STARTED`，不得创建。
