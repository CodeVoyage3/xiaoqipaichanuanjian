# S19-T01｜首次冷启动工作量与排查计划分批回导优化

日期：2026-09-12（Asia/Shanghai）

状态：`R4_UI_REPAIR_IMPLEMENTED / FINAL_GUI_RETEST_READY / NOT_ACCEPTED`

## 2026-09-12 R4 final UI repair

- 用户复验发现三项纯 UI blocker：默认绿色系统 ProgressBar、原生 ComboBox、hover 后确认窗口/底部布局异常。
- repair=`27b73f6`：蓝色自定义不定进度条；五项横向轻量筛选及数量；窗口从创建即固定测量、受控列宽并移除结果表 ToolTip。筛选只使用现有 PreviewResult，不改提交 payload。
- 精准专项=`4/4 PASS`；Release App build=`0 warning / 0 error`；FULL=`NOT_RUN / NO_FULL`。业务 UseCase、Excel、migration/schema、版本与发布链均未修改。
- 新独立 GUI 根=`%TEMP%\4a3e117b-f3fa-445c-a592-ceb124e2ae34`，入口=`S19-T01-R4-Final-UI-Repair-GUI/启动S19-T01-R4最终返修复验.cmd`；旧 R4 候选作废。
- 只待用户复验 A 蓝色 loading、B 横向筛选、C hover 稳定性；不 push、不发布、不关闭 S19-T01/Stage19。

## 2026-09-12 R4 initial UI polish historical

- 首次 R4 launcher 错误使用候选目录内数据根并被运行时拒绝；已修正为 `%TEMP%\78958c41-8cbe-49f7-a20b-95f575b4af9d`。同结构 disposable smoke 已取得真实主窗口；最终根重新复制且 sidecar=`0`，修正前候选作废。
- 用户 GUI 已确认 R3 正式提交、180→提交80/剩余100及业务流 PASS；5000 行读取逻辑 PASS，但 loading 视觉反馈过弱，确认表缺少直接可读原因与筛选。
- implementation=`443e926`：复用 `IsReadingPlan` 显示页面内居中原生不定进度 overlay；确认窗口直接消费既有 Preview 分类/Reason，增加“处理结果”“原因”和“全部/有效/未填写/状态变化/填写错误”筛选。
- 筛选仅生成显示投影；原始 Preview、可提交数量及 Submit TaskIds 保持不变。R4 精准专项=`4/4 PASS`；Release App build=`0 warning / 0 error`。
- FULL=`NOT_RUN / NO_FULL`，不重跑既有 `99/99`、`114/114`、`36/36`。只待用户复验两个 R4 画面；不 push、不发布、不关闭 S19-T01/Stage19。

## 2026-09-12 GUI blocker R3

- R2 预览 GUI 已达 `有效80 / 未填写100 / 状态变化0`，但确认正式提交后 80 条再次全部 stale；状态曾退回 `GUI_BLOCKER_FOUND_R3 / REPAIR_REQUIRED / NOT_ACCEPTED` 并停止 A～I。
- R2 隔离库 80 条 Draft 的 task/item/tracking/stage/attention/reconfirmation/product/baseline 均为当前合法事实，且 handled 结构合法；唯一命中为提交分支的 `HandledAttentionVersion >= AttentionVersion`（80/80）。
- repair=`f38f4cd` 仅修正 `InspectionSubmissionUseCase` partial 语义及合法 0/0 剩余 item 重建；结构边界和其余 stale 保护不变。直接专项=`99/99 PASS`，包含真实 180/80/100 正式提交与八类负例；Release App build=`0 warning / 0 error`。
- 新 GUI 只重验正式提交闭环；用户明确 PASS 前 S19-T01/Stage19 不关闭。不 push，不发布，FULL=`NOT_RUN / NO_FULL`。

## 2026-09-12 GUI blocker R2

- 用户现场填写 80/空 100 后预览为有效 0、未填写 100、状态变化 80；状态已退回 `GUI_BLOCKER_FOUND_R2 / REPAIR_REQUIRED / NOT_ACCEPTED` 并停止 A～I。
- 隔离库证实 180 个合法 open cold-start items 全为 stage/attention 一致、`0/0`、无需重新确认、active；唯一命中的 predicate 是 `HandledAttentionVersion >= AttentionVersion`。
- repair=`d754f2fff5bb07d98589d0f3d7269eb155671df6` 仅删除该冗余判断；其余当前事实门禁全部保留。真实 cold-start 往返与七类 stale 负例共 `13/13 PASS`；Release App build=`0 warning / 0 error`。
- 新 GUI 只重验 180→填80/空100→预览 `有效80 / 未填写100 / 状态变化0`；PASS 后再提交及继续 A～I。Stage19 不关闭，不 push，不发布。

## 2026-09-12 GUI blocker R1 historical

- 用户现场在 `180 商品 / 180 批次 / 0 异常` 后被导入前 SQLite 快照验证阻断；状态曾退回 `GUI_BLOCKER_FOUND / REPAIR_REQUIRED / NOT_ACCEPTED`，旧 GUI 候选作废。
- 根因为 `PreImportSnapshotService` 当前 migration 白名单漏第 10 条。repair=`536bd4126f137328573eaeafb8579e9ff4701bde`，只同步服务/直接测试白名单并补最新空库用例。
- 直接专项=`23/23 PASS`，Release App build=`0 warning / 0 error`，FULL=`NOT_RUN / NO_FULL`；无 migration/schema/业务/UI/Updater/Installer/版本/发布变化，正式数据库未访问。
- 下一步只先由用户重验 180 批次初始化 Excel 正式导入；PASS 后再继续原 A～I。S19-T01/Stage19 保持未关闭。

fresh baseline：`origin/main@43d82ec3d0a6e09a0964a81ce0d531e888fd5abc`

## 实施者与工作区

- 全新的 GPT-5.6 Terra，reasoning=`medium`，标准速度。
- 必须从本治理提交后的 fresh `origin/main` 建立全新独立 clean worktree；不得复用旧 Terra 或正式 dirty 主工作区。
- Terra 只实现本卡并提交候选，不得 push main、自判 ACCEPTED/CLOSED、改版本或发布。

## 实施合同

### A｜冷启动窗口

把 `ColdStartScopeBaselineUseCase.Classify` 的历史过期补查窗口改为 `ceil(actualShelfLifeDays * 1%)`，`Math.Clamp(..., 1, 7)`；只影响 `ExpiredCatchupTask/ExpiredHistoricalBaseline` 分界。无生产日期、到期日不晚于生产日期仍按既有安全处理。

### B｜A:L 导出与任意文件名回导

- `TodayInspectionPlanExportUseCase` 只创建 A:L 表头/单元格/列定义，不写、不隐藏 M:Y。
- `InspectionPlanResultReader` 只依赖工作表名和 A:L 核心结构；不得读取文件名、TaskId、BatchId、版本或快照列。
- 日期必须安全解析为业务日期；公式、缺字段、结构破坏按文件级或行级合同分类，禁止猜值。

### C｜当前事实逐行解析

- 先以唯一 ProductCode 定位 Product，再以批准的日期键唯一定位 Batch，再定位该 batch 当前唯一 open task item。
- 非空行逐项重验 managed/baseline、商品库存、batch active/nonzero、当前 stage/attention/handled、task open、未完成/未提交、无需重新确认等正式合同。
- 空白行不需要形成 Draft；非法整数、负数、小数、重复业务身份、无法匹配或状态变化只产生该行问题。
- Preview 是说明性快照；Apply/Submit 必须再次校验当前事实，竞态行跳过，其他有效行继续。

### D｜真正部分提交

- 仅本次有效且非空行进入 Draft/Submission；同 ProductTask 可提交子集。
- 权威提交逻辑支持指定 task item 子集；默认未指定时保持原单任务全量行为。
- 同一事务内：创建只覆盖提交子集的 Inspection；只对提交子集执行 0 数量 lifecycle 与 HandledAttentionVersion；完成原 task；清除旧有效 Draft；为未提交且仍有效的剩余 items 建立同商品唯一 successor open task，HighestStage 由剩余 items 重算。
- 最终 completed task items 与 InspectionItems 覆盖一致；successor items 与未提交当前有效集合一致；两集合无重叠、无丢失。后续 `ProductTaskAggregator` 继续汇入唯一 successor open task。
- 行级无有效结果时不创建空 Inspection、不完成任务、不写 0。

### E｜UI 与错误

- 大表读取超过短暂阈值显示“正在读取排查结果，请稍候”“数据较多时可能需要一些时间，请勿关闭软件”等不确定进度；所有出口关闭 loading。
- 文件占用、损坏、工作表/A:L 结构错误分别显示任务要求的大白话与下一步；日志保留完整异常。
- 结果汇总显示有效、未填写、状态变化、填写错误数量；行级详情可查看/复制，不显示 .NET Exception/StackTrace。

## 停止条件

- 若 fresh implementation review 发现上述唯一索引不成立、业务身份可合法命中多个当前 Batch/TaskItem，或正确部分生命周期确需 migration：立即停止并报告，不得发明匹配规则或新增 migration。
- 若需要改正常生命周期、提醒、版本/发布、正式数据库或本卡外页面：立即停止。

## 2026-09-12 阻塞证据

- candidate=`520bcb9a0557b7a002a6188e5ad2b4f115e66339`，仅留在独立 clean worktree，未 push。
- Sol isolated 冷启动专项=`5/11 PASS，6/11 FAIL`；失败根因为 SQLite `CK_batch_baselines_catchup_window` 仍要求 `BETWEEN 3 AND 30`。
- 结论：正确实现明确需要最小 schema migration。当前卡禁止 schema/migration，因此立即停止，等待用户授权。

## 2026-09-12 用户授权修正

- 保留第 9 条 `AddPolicyAndBaselineFoundation` 原样；新增第 10 条 migration。
- 新约束固定为 `BETWEEN 1 AND 30`，用于兼容旧历史 `3..30`；生产业务仍只能生成 `1..7`。
- 必须证明旧值 `3/7/8/11/30` 原样升级，新值 `1/2/7` 可写，`0/31` 拒绝，非 catchup disposition 只能为 NULL，migration `9→10` 且无额外漂移。
- 升级失败回滚复用现有 Schema Update 保护快照/恢复闭环；不得篡改历史数据。

## 验证范围

严格按 `../ACCEPTANCE/S19-T01.md`；FULL=`NOT_RUN / NO_FULL`。Release App build 只跑一次，目标 `0 warning / 0 error`。
