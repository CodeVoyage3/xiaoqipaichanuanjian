# S9-T05｜独立 Updater、持久 Journal、隔离 Staging、程序切换与失败回滚

2026-09-05 用户仅授权本卡；`TECHNICALLY_ACCEPTED / CLOSED`。最终证据见 `../ACCEPTANCE/S9-T05.md`；Stage9 `IN_PROGRESS / WAITING_NEXT_AUTHORIZATION`，未创建 S9-T06。
开工重新 fetch 后 main=origin/main=a8e1414030864c67e5cc82ee81c6062dc20c724d，clean、0/0；TASKS/ACCEPTANCE 无既有 T05。Stage8、S9-T01～T04 保持 CLOSED。

## 角色及范围

全新 GPT-5.6 Terra medium/priority `/root/s9_t05_terra` 负责生产代码和实施测试，提交后停、不 push。Sol 只治理、完整 diff 审查、独立自动化复验，不代写生产代码。不得创建 S9-T06。
唯一目标是 VerifiedUpdatePackage 起点的同 Schema 程序切换与失败回滚底座。允许独立 self-contained Updater 工程；优先复用现有安全验证、维护 gate 与发布模式，不改变业务语义。
产品仍 1.0.0；合成旧客户端可 0.9.9→1.0.0 或专用 fixture。禁止真实 1.0.1、tag、Release、Asset、生产私钥/正式 trust anchor、Setup EXE 更新机制、管理员/UAC/service/SYSTEM/HKLM/提权计划任务。
不改 Domain、效期算法、ProductTask 生命周期、Schema/ModelSnapshot/migration/index、生产 SQLite PRAGMA、Undo、GitHub repo、AppId、安装/数据根、current-user/lowest 规则。migration 仍 9，末条 20260901155124_AddPolicyAndBaselineFoundation。

## 安全身份与隔离

冻结 AppId={8F90E64E-5B0D-4FA8-A854-EEA2F4D1EC14}；正式安装根 %LOCALAPPDATA%\Programs\StoreExpiryInspector，入口 app\StoreExpiryInspector.exe；数据根 %LOCALAPPDATA%\StoreExpiryInspector。
全部自动化使用 TEMP/GUID 独立 install/data/updater root、operationId、mutex/identity；涉及 registry/shortcut 也独立。严禁探测、打开、哈希、复制正式 app/DB/Backup/Programs 身份，严禁候选访问正式数据。
Updater 运行于 app 外的当前用户独立工作目录；沿用 S9-T01 data-root updates/<GUID> 持久 journal 位置契约，隔离测试映射至合成数据根。不得依赖被切换 app 内 DLL/runtime。app/staging/old 同一受控安装根同卷，所有祖先与树内拒绝 ReparsePoint/symlink/junction、UNC/网络盘、relative traversal、ADS/任意路径；身份异常 fail-closed。命令行不得授权任意根/PID/EXE 删除或替换。

## 事务和重验

每操作唯一 GUID；journal 至少绑定 operationId、产品身份、安装根、数据根、source/target version、candidate SHA256、old tree 身份、staged tree、阶段、创建/更新时间、parent PID/start time。安全持久写，阶段单向且可恢复；不可仅布尔标记。
明确 Prepared→MainExitRequested→MainExited→CandidateStaged→OldAppPreserved→SwitchStarted→CandidateActivated→CandidateStarted→WaitingForHealthAck→Committed→Completed，以及 RollbackRequired→RollbackStarted→OldAppRestored→RollbackVerified→RolledBack / FailedNeedsManualRecovery 的实际等价状态机。
每个目录动作前后均有可恢复证据；重启读 journal 并核对完整树身份，不能只按目录存在猜测，不能盲目从头覆盖 old。目录 Move 不声称整体原子。
Verified 不永久可信：消费前重验 operation、签名 manifest 身份、package hash/长度、target version、RID、主 EXE 版本及候选静态 migration9 声明；展开后 relative path/size/SHA256 完整树与可信预期一致。避免重验后文件再改的 TOCTOU。
候选 ACK 前保留完整 old app（全部 EXE/DLL/runtime/native SQLite），不只主 EXE。明确成功后清理政策；允许继续保留 old，不能 ACK 前清理。

## 维护、退出与健康握手

WPF 最小接线：确认安装→维护状态阻止新的 Import/正式提交/History 编辑/草稿/Backup/Restore/其他 DB 写→安全等待现有权威操作结束→停 Reminder/background→准备独立 Updater→传受限 operation identity→正常退出。不得硬杀旧主程序作为正常流程。
Updater 验证旧 PID+启动时间等身份并确认退出、文件占用已安全释放，才切换；防 PID 复用。
候选启动必须在普通 Initialize/Migrate/startup recalculation/提醒写入之前进入升级验证模式，ACK 前禁止普通业务写。pending 恢复优先于普通启动；重启也不得绕过。
本卡绝不跨版本 Migrate、绝不执行 migration10。健康只用已为 migration9 的 TEMP/GUID 合成 DB；准备 fixture 可初始化合成库，实际升级/回滚不迁移，源 DB hash 不变。
ACK 绑定 operationId、candidate version、PID/start time、migration count/last、integrity、FK、必要核心只读查询、真实 UI 核心加载。Process.Start 成功/等待几秒不算 ACK；超时有界。ACK 合法才 Committed/Completed 和开放业务写；测试验证路径应保留 DB 不变证据。
启动失败/崩溃/卡死/无 ACK/身份版本错误/DB 健康失败均回滚。先确认 candidate 退出且不再写，恢复并验证完整 old tree，启动 old 等健康 ACK，才 RolledBack。旧版也恢复失败进入 FailedNeedsManualRecovery，保留 old/candidate/journal/日志，绝不伪报成功。

## 必须实施并独立复验的矩阵

A 正常切换；B 启动失败回滚；C ACK 失败回滚；D ACK timeout；E staging 失败 old 保持；F preserve 失败不切换；G Move 失败恢复；H old DLL/candidate 文件真实占用无混合；I journal checkpoint 真实子进程 Kill+重启；J 双 Updater 互斥；K 伪造 operationId；L 路径逃逸/junction；M 包 hash 改变；N 版本改变；O tree 缺文件；P 成功清理策略；Q rollback 失败保留人工恢复证据。另受控 AccessDenied/IOException/文件锁/目录移动失败，不操作 Defender。
硬 Kill 至少 12 点：journal 创建后、staging 中、staging 完成、old 保全后、切换前、切换中代表点、candidate 激活后、启动前、启动后 ACK 前、ACK 后提交前、rollback 开始后、old 恢复后 journal 完成前。每个关键点至少 3 次，成本合理可 5 次；不允许失败后无限重试直到绿色；异常 catch 不能代替硬终止。
每次记录 checkpoint、iteration、最终状态、old/active 完整 tree hash（relative path/size/SHA256）、candidate PID 状态、journal 状态、DB hash、pass/fail；成功 active=candidate，回滚 active=old，不出现混合树。

## Sol 验收与停止

完整 diff、身份、journal/reentry、staging/switch/rollback、hard Kill、健康 ACK/启动失败/timeout、lock/IO、path/reparse、同 Schema DB、WPF 最小接线专项；fresh app 与 updater 各 self-contained publish、secret scan；fresh 无 filter Release 全量 0 failure/error/aborted；Release build 0 warning/error；EF 无漂移，migration9 固定末条；git diff --check。禁止测试全量期间并行 build/publish。区分实施者/独立、合成/真实、历史/本次证据。
仅全部通过才 TECHNICALLY_ACCEPTED / CLOSED，Stage9 IN_PROGRESS / WAITING_NEXT_AUTHORIZATION；更新 Acceptance、Stage、Handoff、Status 及必要结果 JSON/架构。普通 push main 前 fetch，push 后 fetch+clean+HEAD=origin/main+0/0，然后停止；不创建 T06。
若真实架构无法满足 ACK 前无写、Updater 独立运行、可恢复目录切换、无提权、无迁移、无生产锚点测试、稳定根或无混合树：PAUSED_PRODUCT_REVIEW / NOT_ACCEPTED，报告，不弱化门禁。开发机合成通过也不宣称在线升级可正式投放门店。
