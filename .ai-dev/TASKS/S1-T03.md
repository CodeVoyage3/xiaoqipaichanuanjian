# Task S1-T03｜持久化层结构质量门禁

- 状态：已验收通过
- 实现提交：`d1b47d8aae25e051a2c99bb6940bc62709caa123`
- 验收记录：`.ai-dev/ACCEPTANCE/S1-T03.md`

## 目标

在不改变任何数据库 schema、领域模型或业务行为的前提下，拆分已达 433 行的 `StoreDbContext` 实体映射和 752 行的数据库测试文件，为 Stage 1 后续实体留出清晰边界，避免 God Class 与重复测试辅助代码。

## 需求依据

- 用户要求：继续执行架构质量门禁，避免业务规则散落、God Class 和重复实现。
- `.ai-dev/ARCHITECTURE.md`：业务规则与基础设施边界清晰。
- 已通过基线：`.ai-dev/ACCEPTANCE/S1-T01.md`、`.ai-dev/ACCEPTANCE/S1-T02.md`。

## 仓库事实

- 当前分支：`master`。
- 基线 HEAD：`19fe189c122988c18b5bbea879bad4616b2e3aac`。
- `StoreDbContext.cs`：433 行，包含 6 个实体的全部映射。
- `StoreDatabaseTests.cs`：752 行，混合 Product/Batch 与 Task/Draft 两组测试及公共 SQLite 辅助代码。
- 当前 migrations：`InitialCreate`、`AddTasksAndDrafts`。
- 当前验证基线：16 项测试通过，build 0 警告，模型无漂移，依赖审计无已知漏洞。

## 本次允许修改

- `src/StoreExpiryInspector/Infrastructure/StoreDbContext.cs`
- `src/StoreExpiryInspector/Infrastructure/Configurations/ProductConfiguration.cs`
- `src/StoreExpiryInspector/Infrastructure/Configurations/BatchConfiguration.cs`
- `src/StoreExpiryInspector/Infrastructure/Configurations/ProductTaskConfiguration.cs`
- `src/StoreExpiryInspector/Infrastructure/Configurations/ProductTaskItemConfiguration.cs`
- `src/StoreExpiryInspector/Infrastructure/Configurations/InspectionDraftConfiguration.cs`
- `src/StoreExpiryInspector/Infrastructure/Configurations/InspectionDraftItemConfiguration.cs`
- 删除 `tests/StoreExpiryInspector.Tests/StoreDatabaseTests.cs`
- `tests/StoreExpiryInspector.Tests/ProductBatchDatabaseTests.cs`
- `tests/StoreExpiryInspector.Tests/TaskDraftDatabaseTests.cs`
- `tests/StoreExpiryInspector.Tests/SqliteTestDatabase.cs`

## 本次禁止修改

- 不修改 `.ai-dev/**`、`Domain/**`、`Migrations/**`、项目文件、solution、UI、App 或初始化器。
- 不新增、删除或升级任何依赖。
- 不新增 migration，不改 migration ID、Designer 或 ModelSnapshot。
- 不新增字段、表、索引、约束、关系或业务规则。
- 不改变实体默认值、列名、最大长度、删除行为或测试断言语义。
- 不顺手实现正式排查、库存修正、导入、备份、日志或 Stage 2+ 功能。
- 不创建通用 Repository、UnitOfWork、配置基类、测试基类或反射式自动注册框架。

## 必须实现

### 1. EF 映射拆分

- 每个已有实体使用一个 `IEntityTypeConfiguration<T>` 配置类。
- 每个配置类只负责一个实体的表、列、约束、索引及其必要关系配置。
- `StoreDbContext` 只保留构造函数、6 个 `DbSet` 和对 6 个配置类的显式 `ApplyConfiguration(...)` 调用。
- 不使用重复配置、部分配置留在 DbContext、反射扫描或额外配置抽象层。

### 2. 数据库测试拆分

- Product/Batch 的原有测试移动到 `ProductBatchDatabaseTests.cs`。
- Task/Draft 与 migration 升级测试移动到 `TaskDraftDatabaseTests.cs`。
- 临时数据库生命周期、建库和通用 schema 查询放入 `SqliteTestDatabase.cs`。
- 公共辅助代码只保留真实复用部分；业务断言仍留在对应测试文件，不隐藏在万能 helper 中。
- 原 16 个 `[Fact]` 的覆盖范围与核心断言全部保留，不通过删除/合并测试降低覆盖。

## 验收标准

- [x] `StoreDbContext` 不再包含任何实体具体的 `Property / HasIndex / HasOne / HasCheckConstraint` 映射。
- [x] 6 个实体配置互不重复、职责单一，所有已有映射完整迁移。
- [x] `Migrations/**`、`Domain/**`、项目文件及其他禁止路径与基线 commit 完全一致。
- [x] `dotnet ef migrations has-pending-model-changes` 返回无变化。
- [x] `dotnet ef migrations script` 可成功生成；现有 migration 顺序和名称不变。
- [x] `dotnet build`：0 警告、0 错误。
- [x] `dotnet test`：仍为 16 通过、0 失败、0 跳过。
- [x] 依赖审计仍无已知漏洞。
- [x] 测试不访问 `%LOCALAPPDATA%`，结束后不残留临时数据库。
- [x] 没有新增业务实现、schema 变化、重复 helper 或新的 God Class。

## 必须测试

- 运行现有全部真实 SQLite 测试。
- 用 `git diff --exit-code 19fe189...HEAD -- Domain Migrations csproj slnx` 或等价命令证明禁止路径未变。
- 执行 `dotnet list ... --vulnerable --include-transitive`。
- 执行 `dotnet build`、`dotnet test`。
- 执行 `dotnet ef migrations has-pending-model-changes` 和 `dotnet ef migrations script`。
- 执行 `git diff --check`。

## 执行完成后必须返回

1. 修改、增加、删除文件清单。
2. 映射与测试拆分说明。
3. 禁止路径完整性检查结果。
4. 漏洞审计、build、test、migration model/script、diff check 摘要。
5. 未解决问题/风险。
6. 对每项验收标准逐项说明是否满足。
