# S16-T03｜首页优先处理摘要化与未来风险可见性优化

日期：2026-09-10（Asia/Shanghai）
治理基线：`origin/main@b60bdda4416ebf314f3eca35425fd99fa36ac821`
状态：`GOVERNANCE_FROZEN / IMPLEMENTATION_DISPATCH_AUTHORIZED`

## 目标与固定顺序

- 首页顺序保持：搜索区 → 顶部数据状态条 → 优先处理 → 未来效期风险；禁止反转优先处理与未来风险。
- “优先处理”仅作为首页摘要，常规加载与首页搜索均最多显示现有排序的前 `5` 条；少于 5 条按实际显示，0 条沿用现有空状态。
- 不增加内部滚动、分页、展开/收起或新按钮；继续使用现有“查看全部待排查任务 →”。

## 冻结业务边界

- 顶部 `OpenTaskCount` 必须继续来自全部真实待处理任务总数；首页搜索结果总数继续为真实筛选总数，不能因展示 5 条而变成 5。
- 完全复用 `InspectionTaskQuery.ReadTaskRows` 现有紧急程度、最近有效期、TaskId 稳定排序；首页前 5 条必须与完整列表相同条件下前 5 条一致。
- 完整待排查任务页面、`FixedPageSize = 50`、筛选、分页、数量和排序语义不变。
- S16-T01 未来风险总览/明细的查询、XAML 结构、矩阵、点击、滚轮、固定表头及 7 列全部冻结；S16-T02 关闭状态与安装逻辑冻结。
- 不改任务生成/状态、今日排查、库存、policy/stage、风险、导入、提醒、数据库、Schema、migration、Updater 或 Installer。

## 执行与停止点

- Sol 只做治理与独立技术验收，不直接修改生产代码。
- 治理 push 后，从 fresh `origin/main` 创建本卡全新 GPT-5.6 Terra（reasoning `medium`、标准速度）独立 clean worktree；禁止复用任何 S16-T01/S16-T02 Terra。
- 只跑 S16-T03 最小专项、必要 Release build、EF no drift/migration `9`；禁止 S16-T01/S16-T02 专项、大库性能、更新链、无关回归；`FULL = NOT_RUN / NO_FULL`。
- 技术通过后停在 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`，等待用户最小 GUI 验收；不得关闭 S16-T03/Stage16 或发布 v1.0.8。
