## 2026-09-07 最终Candidate准备（最新裁决）
已批准热修source冻结：即时刷新/布局USER_GUI_ACCEPTED、取消更新文案、更新通知modal及导出Owner、Updater WinExe。仅4个生产文件，无安全状态机/业务/模型/依赖扩大。modal1/1与console2/2 Sol证据继续有效，本轮不重跑。
当前仅Release build、EF NO_MODEL_DRIFT/migration9、全新App/Updater publish/Setup/ZIP/manifest/signature、PE2、签名/tree/secret与隔离启动。禁止full/178/旧GUI重验/公开发布。完成后停止待最终GUI与发布后真实公开升级。HISTORICAL_TEST_ISOLATION_UNCERTAINTY保持，禁止探测正式数据。
S12-T01=IN_PROGRESS/NOT_ACCEPTED，Stage12=IN_PROGRESS，v1.0.4=NOT_RELEASED。旧candidate保持SUPERSEDED_BY_GLOBAL_MODAL_FIX。
## 2026-09-07 当前裁决：modal技术收口，调查升级黑框
Modal事实已接受：31自定义+3原生入口；唯一业务Window.Show为更新通知；其余已有modal；导出失败提示补Owner。本轮modal生产仅WpfDialogService.cs，无业务modeless，主壳Show保留。Terra安全宿主21/21、Sol独立1/1、Release build0/0通过，不full。
历史测试风险只记HISTORICAL_TEST_ISOLATION_UNCERTAINTY：不能证明正式数据被修改，也不能绝对证明从未访问。禁止为追查而读/查/hash/copy/对比/修改正式DB、数据根或备份。该finding属测试流程，不是产品defect，不单独阻塞发布。继续使用安全Dispatcher与TEMP/GUID隔离。
唯一当前产品问题：升级黑色控制台窗口。先查OutputType/ProcessStartInfo/包装/Console与journal，最小WinExe或必要显示参数；保护Updater自身WorkingDirectory及全部安全状态机/协议。只成功链+一个rollback+PE/启动链/日志targeted，不full/178/旧两项重验。
停止点：调查、最小修复及独立专项后回报，先不生成candidate。旧candidate保持SUPERSEDED_BY_GLOBAL_MODAL_FIX，资产不复用不发布。S12-T01=IN_PROGRESS/NOT_ACCEPTED。
## 2026-09-07 用户GUI回执与新增modal finding（当前裁决）
用户已通过首次导入不重启即时出现今日排查、确认窗底部按钮完整可见与提交正常。前轮证据继续有效，不重新验收/机械重跑178项。取消更新文案保留。
唯一新增范围：业务弹窗统一模态。先完整盘点Show/ShowDialog/Owner及async链，再标准WPF Owner+ShowDialog最小修复；不得全局替换、增加主窗禁用状态机/锁/WindowManager，不动Domain/DB/Import事务/任务算法/Updater核心。
仅modal targeted、直接dialog回归、Release build、diff/secret；模型未动沿用NO_MODEL_DRIFT/migration9。默认不full，不能因公共DialogService而扩测。
旧candidate source94d3efc及TEMP/7f1607db-db80-4979-a150-21405e45f298资产标记SUPERSEDED_BY_GLOBAL_MODAL_FIX；原字节与证据保留，不得用于发布。
本次停止点：盘点、最小修复与独立targeted报告后停止，先不生成新candidate。S12-T01与Stage12仍IN_PROGRESS/NOT_ACCEPTED；不发布、不创建Stage13。
# S12-T01｜首次导入即时任务刷新、排查确认布局与更新文案 v1.0.4 热修复

S12-T01 = IN_PROGRESS / NOT_ACCEPTED
目标版本 v1.0.4；Stage12 IN_PROGRESS。

## 角色与工作区
全新 GPT-5.6 Terra / medium / priority 负责生产代码和专项测试。Sol 负责治理、完整 diff 独立审查、独立专项、build/EF/migration、发布与最终升级验证，不写生产代码。
既有未跟踪 docs/门店效期排查软件_操作手册.html 不是本轮产物，禁止修改、删除、移动、add、commit。所有 git add 显式指定本轮路径。

## 唯一范围与顺序
1. 首先精确复现：空库进入今日页，HasLoadedTasks=true 后导入应排查数据，不重启，数据库已生成任务但页面仍空。先保留失败证据，随后仅在导入成功 commit 后复用 TodayInspection.LoadAsync 刷新集合、数量、状态。优先仅修改 Stage4ViewModels.cs，不预改 ImportViewModel 或加载语义。
2. 问题3专项通过后修确认窗口：TodayInspectionConfirmationWindow.xaml 行为 Auto/*/Auto/Auto，表格承担压缩滚动，表单及操作栏可见。不靠增加默认高度。仅专项证明 XAML 不足才调整 code-behind。
3. WpfDialogService.cs 仅将取消准备改为取消更新，不改取消行为。

## 专项与边界
问题3覆盖：提前进入空今日页再导入；未提前进入再导入；无任务数据；重复刷新/切页去重；二次导入无错误旧任务；库存零；应季搭配及赠品小样；首页数量同步。保留最高阶段、同日去重、冷启动、预提醒及全部现有业务规则。
布局覆盖100%/125%/150%及低窗口高度，按钮可见可点、多商品滚动、排查人/日期、提交/取消。不能可靠自动证明的视觉项记 USER_GUI_REQUIRED，不造大型GUI基础设施。
必要邻近回归仅 ImportViewModel、TodayInspectionViewModel、PendingTasks、Dashboard刷新、导入生命周期和相关业务边界。
专项失败先归因单独修复重跑，禁止用full兜底。默认不跑1000+ full。若需要触及任务算法、Import transaction、Domain、DbContext/EF、Reminder、Updater、安装器核心或公共基础设施，停止扩大，Sol解释必要性、专项不足证据和回归/full判断，等待裁决。
禁止新增Schema/migration/ModelSnapshot改动；migrationCount=9，EF NO_MODEL_DRIFT。禁止访问正式数据库、安装根、数据根及备份；自动化只用TEMP/GUID合成环境。

## 验收与发布
Terra完成后先回报根因、真实文件diff、三问题修法、专项/邻近结果、是否局部及full判断、GUI准备状态；不得直接发布。
Sol独立完整diff和专项验收，Release build 0 warning/0 error、EF NO_MODEL_DRIFT、migration9、git diff --check、secret scan。
PRE-RELEASE：专项与邻近回归、build/EF、App/Updater candidate publish/package、manifest/signature/hash、候选升级数据保持、用户最小GUI。此阶段禁止声称REAL_GITHUB_V103_TO_V104_UPGRADE_VERIFIED。
用户候选GUI通过后才发布stable v1.0.4。POST-RELEASE真实公开v1.0.3→GitHub latest v1.0.4→验签下载→Updater→自动重启→版本/原数据验证通过，才记录REAL_GITHUB_V103_TO_V104_UPGRADE_VERIFIED；失败保持NOT_ACCEPTED。
v1.0.3公开资产/tag不可改写。保留RSA-PSS/SHA256协议；Authenticode=NotSigned，已知分发限制不阻塞，不购证书。不机械重跑旧阶段矩阵。
全部技术、用户及发布后升级门禁通过后才更新接受/Stage12关闭，不创建Stage13。
