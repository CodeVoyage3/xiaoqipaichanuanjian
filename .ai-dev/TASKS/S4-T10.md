# Task S4-T10｜Stage 4 核心 UI 定稿重构

- 状态：最终 GUI 验收通过，已归档
- 基线提交：`141113f636b57c491759dcf4036de8b6eb2c2ad3`
- 前置状态：S4-T09 UAT 修复已提交但尚未正式验收；Stage 4 未归档
- 执行：必须使用一名全新的 GPT-5.6 Luna（max），不得复用 S4-T09 Luna
- 产品经理、独立审查：GPT-5.6 Sol
- GUI 验收：用户本人；Sol/Luna 不操作 GUI
- 视觉权威：`.ai-dev/UI/STAGE-4-UI-REFRESH-BASELINE.md` 与其中登记的 5 张定稿图

## 一、治理关系

S4-T10 是用户在 S4-T09 尚未闭环时新增批准的独立 Stage 4 UI 重构任务，不塞入 S4-T09 缺陷提交。S4-T09 保留其 UAT、NuGet 风险接受和隔离数据事实；Stage 4 总验收、正式数据恢复、归档及 Stage 5 handoff 顺延到 S4-T10 用户验收之后。

不得创建 S5-T01，不得进入 Stage 5。

## 二、目标

严格按用户定稿图和刷新基线重构：

1. Design Tokens / 公共 Style。
2. Shell / 左导航。
3. 首页。
4. 待排查任务。
5. 排查详情折叠/展开。
6. 数据导入及 Loading/完成状态。
7. 库存确认等现有弹窗。
8. zh-CN 固定文案与字体一致性。
9. 1024×600、125%、滚轮、键盘和固定底栏兼容性。

本卡是现有功能的视觉与布局重构，不新增业务流程。

## 三、允许修改

- `src/StoreExpiryInspector/App.xaml`
- `src/StoreExpiryInspector/UI/MainWindow.xaml`
- `src/StoreExpiryInspector/UI/MainWindow.xaml.cs` 中纯窗口、滚动、焦点、弹窗布局行为
- 现有 Stage 4 UI ViewModel 中仅为绑定、命令可用性、页面状态展示所需的最小变化
- 对应 Stage 4 UI/静态测试
- 若首页需精确显示当前商品/批次总数，允许对 `InspectionDashboardResult` 及其查询测试最小增加只读 `ProductCount`、`BatchCount`

任何额外 DTO 缺口必须先停下报告 Sol。

## 四、必须复用

- 首页/列表搜索、阶段筛选、50 条分页、权威排序：`InspectionTaskQuery`
- Dashboard 阶段统计和紧急任务：`InspectionDashboardResult`
- 详情、草稿、Reconfirm、库存修正、提交与刷新：既有 Stage 4 Application/UI 编排
- 导入预览、确认、异常、Loading/Retry：既有 Stage 2 + S4-T06 链路
- 单元格复制、首页滚轮、zh-CN 字体：S4-T09 已修复行为

## 五、禁止修改

- Stage 2 导入规则、Stage 3 生命周期、S4-T04 正式提交事务
- Entity、DbContext、EF Configuration、schema、migration、SQLite 业务模型
- 商品身份、搜索语义、任务排序、阶段计算、Reconfirm、超库存或 0 件停止规则
- 排查记录、设置、导入历史、提醒、备份、导出、安装器、Stage 5
- 依赖、UI/MVVM/图标框架、运行时繁转简算法

## 六、定稿图冲突处理

- 业务/任务边界高于图片：图片中的 `.xls`、20MB、导入记录、排查记录/设置可用态不构成新增功能授权。
- 图片之间的产品名差异统一为“门店效期排查软件”。
- 图片示例商品、数字、日期仅作视觉占位，不得写入正式或隔离数据库。
- 图标只允许少量 WPF 原生 Path 资源，不引入第三方包。

## 七、实现要求

- 先集中最小 token/style，再逐页替换；不得重写整套 ViewModel 或引入新框架。
- 首页显示条码，不强制显示编码；待排查列表同时显示条码和编码。
- 待排查列表固定 50 条/页，单元格级复制保持。
- 详情正常批次默认折叠、只读；底栏固定；完成排查唯一 Primary。
- 导入立即 Loading、禁止重复确认、成功明确、失败安全。
- 固定中文统一 zh-CN / Microsoft YaHei UI，不改导入业务数据。
- 控件具有可见焦点；图标按钮有文字或 AutomationProperties.Name。

## 八、自动化与静态验收

Luna 至少提供：

1. 定稿图/基线关键字段、字体、按钮层级、分页和复制行为的最小静态测试。
2. Dashboard 新只读 DTO（若使用）的查询测试。
3. 受影响 Stage 4 ViewModel 专项。
4. Stage 4 权威回归、Stage 3 权威回归、Release 全量和 Release build。
5. EF drift 无变化、migration 仍 8、依赖 diff 为空、`git diff --check` 通过。
6. 静态证明 UI 未访问 EF、创建正式实体、写生命周期字段或解析异常文本。

Sol 独立审查全部 diff 并复跑门禁；不得使用电脑操控替代用户 GUI 验收。

## 九、数据与停止门禁

- 正式数据备份 `StoreExpiryInspector.s4t09-backup-20260828` 继续保护，不得移动、删除、覆盖或用于测试启动。
- 实现和自动化期间不得启动 GUI；用户复测继续使用隔离 LocalAppData。
- 本轮第一阶段只创建任务与基线，不派发实现。
- 实现完成、自动化通过后停止，交付用户四页 GUI 清单。
- 用户通过并关闭应用后才恢复正式数据、核验原大小/哈希并执行 Stage 4 总验收。
