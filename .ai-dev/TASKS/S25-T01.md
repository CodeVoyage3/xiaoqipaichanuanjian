# 2026-09-17 S25-T01 TECHNICAL_PASS / USER_GUI_PENDING

Stage25=IN_PROGRESS；S25-T01=USER_GUI_PENDING；S25-T02=NOT_STARTED。
Sol独立127/127 PASS、Production Release build0warning/0error、forbidden-scope PASS。
最终Terra1f4cf7dd974457bb736358861a17c0e85c01ef16；Version1.1.3、migration10、migration11 NOT_CREATED；FULL=NOT_RUN / NO_FULL。
隔离GUI入口及清单见.ai-dev/ACCEPTANCE/S25-T01.md；真实用户GUI回执前不CLOSED/ACCEPTED。
原dirty工作区不触碰；不main集成/push/tag/Release；T02不得自动启动。

以下旧状态仅为过程记录。

# S25-T01｜待排查任务选中导出与正式结果回导

状态：AUTHORIZED / IN_PROGRESS；GUI = NOT_RUN / USER_PENDING。
基线：588ea7c9417b32074214f268435638c30da995ed。
用户已明确确认 Scope 并授权全新 Terra 实施；治理提交后方可实施。

## 冻结 Scope
待排查宽屏与紧凑表格均增加第一列复选框；以 TaskId 集合管理选择，不依赖 DataGrid SelectedItem。翻页保留选择；搜索文本变化、阶段变化、大类变化、清空筛选、手动刷新清空选择。轻量清空已选仅有选择时可用。若提供表头全选，只影响本页，禁止全筛选结果/全数据选择。
工具栏最小增加：已选 X 项、清空已选、导出已选任务、导入排查结果。零选择导出禁用且方法层防护；导出始终向原 TodayInspectionPlanExportUseCase 传显式非空 TaskIds，禁止 null fallback。导入零选择仍可用，文件重新匹配当前数据库，不依赖历史 UI selection。
保持正式工作表“今日排查计划”、十二列业务合同，无隐藏 TaskId/TaskItemId。身份保持商品编码+生产日期+有效日期匹配当前批次/open TaskItem/Task，继续实时校验。UI 默认文件名可按待排查入口命名。
抽取一个共用回导会话 ViewModel：Preview、填写/确认、Apply、Submit、库存确认、错误反馈、提交结果；今日与待排查共用。Today 日期卡片、未来列表与 selection 不进入共用会话。原确认窗口适配该会话；超库存商品名取本次 Preview/解析结果，不依赖 Today.Tasks。
复用原 Export/Reader/Preview/Apply/Draft/BulkSubmit/单任务正式 Submit。Application 业务层原则上不改；必须改时停止报告 Sol，不自行扩大范围。

## 文件边界
允许 UI/Stage4ViewModels.cs、MainWindow.xaml/.cs、TodayInspectionViewModel.cs、一个新共用回导会话 ViewModel、TodayInspectionConfirmationWindow.xaml.cs，必要时其 XAML；以及直接专项测试。
禁止效期规则、日期/未来计划计算、FutureInspectionPlan、ProductTask 状态机、Inventory 规则、Excel/identity、Schema/migration/CurrentSchemaIdentity、Version、Installer、Updater、Release Builder、已发布资产及无关债务。

## 技术验收
单条/多条/跨页导出；零选择导出阻断但导入可用；明确非空 TaskIds；页内全选隔离（若有）；翻页保留；搜索/阶段/大类/清空筛选/刷新清空；清空已选；两套表格一致；模板十二列/身份不变；清空勾选和新会话仍可回导；任务完成安全跳过/拒绝、库存实时确认、重复回导无重复 Inspection；今日原功能与待排查筛选分页详情导航相关回归。
Terra 提交代码和直接测试，不修改治理，不 push/main/Release，不自判 GUI PASS。Sol 独立 diff、最小专项、Production Release build 0 warning/0 error、禁止范围与版本/migration核对。FULL=NOT_RUN / NO_FULL。使用 TEMP/GUID fixture，禁止正式 DB。
技术通过后 USER_GUI_PENDING；真实用户 GUI PASS 前不得 CLOSED / ACCEPTED。提供隔离 GUI 启动入口及清单。T02 NOT_STARTED。

