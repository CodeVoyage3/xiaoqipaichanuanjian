# S15-T02｜更新提示恢复完整门店版更新说明

S15-T02 = `GOVERNANCE_FROZEN / IMPLEMENTATION_AUTHORIZED / NOT_IMPLEMENTED`

基线：fresh fetch 后 `origin/main == 4ad6ef4d374a1821c6f60fad9b1f4d55bc750cda`；S15-T01 = `GUI_ACCEPTED / CLOSED`。本卡不重复要求 S15-T01 的 GUI 验收。

## 产品规则

1. 发现新版窗口保持标题、当前版本、最新版本和“稍后提醒 / 立即更新”，增加“本次更新”完整说明区。
2. 完整展示 GitHub Release Body 经过现有安全字符清理后的内容；不限制三条、不摘要、不删项、不按关键词过滤、不由客户端改写。
3. 说明区固定最大高度并内部滚动，避免长内容把窗口无限拉高。
4. ReleaseNotes 为空或仅空白时，隐藏“本次更新”标题和整个内容区；不留空白占位，不重复显示 DiagnosticBanner。
5. 从下一次正式版本开始，Release Body 本身必须完整写明门店实际感受到的新增、优化和修复；开发过程、治理证据以及仅开发者需要的技术术语只写入 `.ai-dev`。

## 允许修改

- `GitHubReleaseUpdateChecker.cs`：仅移除 `SanitizeNotes` 的 `.Take(1000)`，保留控制字符清理和 GitHub metadata 整体 256KB 安全上限。
- `UpdateNotificationViewModel.cs`：恢复只读完整 ReleaseNotes 暴露。
- `WpfDialogService.cs`：增加“本次更新”完整说明滚动区。
- 直接专项测试，以及与上述行为直接冲突的旧测试断言。

## 禁止扩围

- 不改请求地址、超时、重定向、Release 判定、版本规则或网络错误语义。
- 不改 downloader、Updater、manifest/signature、journal/rollback、NormalLaunchHandshake、检查频率、非强制更新逻辑、三阶段状态、取消边界或稍后提醒二次文案。
- 不改数据库、SQLite、migration、ModelSnapshot、导航、业务页面、托盘或每日提醒；不新增依赖、协议、服务或数据库字段。
- 不访问正式数据库或正式数据根；不 tag、不 Release、不发布 v1.0.6、不创建 S15-T03。

## 执行门禁

- 只运行直接专项与必要 Release App build；`FULL = NO_FULL`。
- 实施和技术验收通过后，用户只重验“发现新版本”屏的完整说明、滚动、版本、按钮和窗口尺寸，不重复验下载中、更新中、安装中。
