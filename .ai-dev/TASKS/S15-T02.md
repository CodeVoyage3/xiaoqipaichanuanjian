# S15-T02｜更新提示恢复完整门店版更新说明

S15-T02 = `GUI_ACCEPTED / CLOSED`

实际实施基线：fresh fetch 后 `main == origin/main == 1ddb809bff6c21d816b21ddc63397608c5204dfe`；治理冻结前记录的 `4ad6ef4d374a1821c6f60fad9b1f4d55bc750cda` 仅为历史基线，实施未回退。S15-T01 = `GUI_ACCEPTED / CLOSED`，本卡不重复要求其 GUI 验收。

## 产品规则

1. 发现新版窗口保持标题、当前版本、最新版本和“稍后提醒 / 立即更新”，增加“本次更新”完整说明区。
2. 完整展示 GitHub Release Body 经过现有安全字符清理后的内容；不限制三条、不摘要、不删项、不按关键词过滤、不由客户端改写。
3. 说明区固定最大高度并内部滚动，避免长内容把窗口无限拉高。
4. ReleaseNotes 为空或仅空白时，隐藏“本次更新”标题和整个内容区；不留空白占位，不重复显示 DiagnosticBanner。
5. 从下一次正式版本开始，Release Body 本身必须完整写明门店实际感受到的新增、优化和修复；开发过程、治理证据以及仅开发者需要的技术术语只写入 `.ai-dev`。

## 允许修改

- `GitHubReleaseUpdateChecker.cs`：仅移除 `SanitizeNotes` 的 `.Take(1000)`，保留控制字符清理和 GitHub metadata 整体 256KB 安全上限。
- `UpdateNotificationViewModel.cs`：恢复只读完整 ReleaseNotes 暴露。
- `WpfDialogService.cs`：增加“本次更新”完整说明滚动区。
- 直接专项测试，以及与上述行为直接冲突的旧测试断言。

## 禁止扩围

- 不改请求地址、超时、重定向、Release 判定、版本规则或网络错误语义。
- 不改 downloader、Updater、manifest/signature、journal/rollback、NormalLaunchHandshake、检查频率、非强制更新逻辑、三阶段状态、取消边界或稍后提醒二次文案。
- 不改数据库、SQLite、migration、ModelSnapshot、导航、业务页面、托盘或每日提醒；不新增依赖、协议、服务或数据库字段。
- 不访问正式数据库或正式数据根；不 tag、不 Release、不发布 v1.0.6、不创建 S15-T03。

## 执行门禁

- 只运行直接专项与必要 Release App build；`FULL = NO_FULL`。
- 实施和技术验收通过后，用户只重验“发现新版本”屏的完整说明、滚动、版本、按钮和窗口尺寸，不重复验下载中、更新中、安装中。

## 当前实现结果

- Terra implementation：`0afddbdd5cb42f1f7f9d02ca84d1267842a30e00`；remote：`origin/codex/s15-t02-terra`，远端 SHA 相同，worktree clean。
- 修改严格为 3 个授权生产文件与 2 个直接测试文件；未修改 downloader、Updater、协议、状态机、数据库或其他业务 UI。
- 精确专项命令：`dotnet test tests\StoreExpiryInspector.Tests\StoreExpiryInspector.Tests.csproj --filter "FullyQualifiedName~MissingContentLengthStillHasByteLimitAndNotesAreNotTruncated|FullyQualifiedName~ReleaseNotesKeepLineBreaksAndRemoveUnsafeControlCharacters|FullyQualifiedName~S15T01UpdateProgressUiTests" --no-restore`；结果 `10/10 PASS`。
- Release App build：`dotnet build src\StoreExpiryInspector\StoreExpiryInspector.csproj -c Release --no-restore`；`0 warning / 0 error`，无 `NU1900`。
- `FULL = NOT_RUN / NO_FULL`；正式数据库访问 `NO`；scope check `PASS`。
- Terra 实现阶段停止点为等待 Sol 独立技术评审；当时未合并 main，未自判技术或 GUI 通过。

## 技术评审与集成

- Sol 已独立读取并审查真实 Terra diff：`TECHNICAL_REVIEW = PASS`，`REWORK = 0`。
- implementation SHA：`0afddbdd5cb42f1f7f9d02ca84d1267842a30e00`；integration SHA：`f3cb2451442e5dba540796743f038072be7fd32c`；cherry-pick 无冲突，生产代码与 Terra 等价。
- targeted：`INHERITED 10/10 PASS / NOT_RERUN`；Release App build：`INHERITED 0 warning / 0 error / NOT_RERUN`；`FULL = NOT_RUN / NO_FULL`。
- 正式数据库访问 `NO`；过宽 S9T03 synthetic WPF core-ready 27 秒超时保留为 `NON_BLOCKER`，未追加测试。
- 技术接受阶段保留 `origin/codex/s15-t02-terra`，当时唯一下一步为用户 GUI 验收，未替用户判定通过。

## 用户 GUI 验收收口

- 用户已明确回执 `S15-T02 GUI 验收通过`。
- 已确认“本次更新”区域与完整门店版更新说明显示正常，长内容可正常滚动，窗口尺寸合理，当前版本/最新版本与“稍后提醒 / 立即更新”正常。
- technical review `PASS`、targeted `10/10 PASS`、Release build `0 warning / 0 error` 均继承；`FULL = NOT_RUN / NO_FULL`；正式数据库访问 `NO`。
- 本轮未重跑 targeted、Release build、Full 或 GUI，未修改生产代码。S15-T02 = `GUI_ACCEPTED / CLOSED`。
