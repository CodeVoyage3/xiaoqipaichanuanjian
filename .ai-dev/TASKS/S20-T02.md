# S20-T02｜变更影响判定与历史证据复用

日期：2026-09-13（Asia/Shanghai）
基线：`origin/main@870eb4cb0d38cb196d404bce6b0e70cff8376202`
状态：`IMPLEMENTED / NOT_ACCEPTED / WAITING_SOL_REVIEW`

## 唯一目标

Builder 比较上一正式产品源与 CandidateSha，输出 changed files、impact categories、required evidence 与 reusable evidence，并写入 receipt schema v2。它不执行任何受影响测试。

## 冻结规则

- 按 Version 唯一匹配 Release Contract，读取 `previousRelease`；只接受本地 annotated tag，peel 为完整 40 位 commit，并要求其为 Candidate ancestor。
- Git diff 使用 `--raw -z --find-renames --find-copies`；rename/copy 的旧、新路径都参与分类，非普通 mode/type change 为 UNKNOWN。
- 分类优先级为 exact path → longest owned prefix → `tests/**/*.cs` TEST_ONLY fallback → UNKNOWN；一个路径可命中多个类别。
- UNKNOWN 必须 `FAILED / CHANGE_IMPACT`，同时保留完整 changedFiles、unknownFiles 与已识别分类。
- required/reusable evidence 去重、稳定排序、相互排斥；只表示需求或复用资格，不表示 PASS，不自动运行命令。
- 保留原 Schema Gate：`schemaChanged/schemaEvidenceDisposition/schemaEvidenceReference`。

## 范围

仅 5 个 Stage20/S20-T02 治理文件、Release Builder、change impact policy、receipt schema 和既有 Builder 专项测试文件。禁止修改生产代码、Updater、Installer、migration、CurrentSchemaIdentity、业务 UseCase、UI 或 v1.1.0 历史文件。

## 验收边界

只允许直接专项、合成 path 分类、历史 `9bf4ee71… → 18230c3e…` 分类与 `git diff --check`。禁止 FULL、GUI、rollback、migration/Installer/Updater E2E 和完整 Builder dry run。Sol 审查前不得关闭或 push。
