# S8-T06｜Stage 8 最终高规模回归与稳定性收口

状态：TECHNICALLY_ACCEPTED / CLOSED（2026-09-04）。用户正式批准仅本卡；Sol独立100k/300k读、100k导入、回滚2/2、恢复6/6、代表18/18硬Kill、无filter Release984/984、build0/0、EF无漂移、migration9及Git门禁通过。Stage8技术收口，详见Acceptance及STAGE-8-CLOSEOUT.md。开工fetch main=f981211、clean/0/0；最终测试候选17ebb6c，生产/测试未变。

## 角色与安全

全新GPT-5.6 Terra / reasoning medium / priority，不复用T01～T05代理。先检查并复用既有设施，仅必要最小测试/运行入口，提交后停止、不push。Sol维护治理、独立diff及全部技术验收，不写生产代码。默认零生产改动；真实生产bug或明显数量级性能退化必须停止关闭并报告，不自动修复或创建R1。

全部SQLite、XLSX、snapshot、backup、sidecar与证据仅合成TEMP/GUID、显式路径；保留默认factory/loader零调用门禁。不得访问、读取、哈希、复制、调查、恢复正式数据库、正式备份目录、真实Excel或旧隔离疑似事件。禁止Schema/ModelSnapshot/index/migration/生产PRAGMA/dependency/csproj/slnx、业务算法、UI重做、新优化或新防护模式。

## 必须新鲜验证

1. 复用T01/T02基座生成100k Batch/300k Inspection-History，Dashboard、待排查首/深页、搜索、Stage、大类及组合、Today、History、Reminder。记录median/max、SQL/plan、完整规模与只读护栏，无SQL变量超限/OOM/timeout/lock/crash。T02参考ms：Stage174.68、首屏387.64、深页752.40、Dashboard389.02、Today355.55、History628.39、Reminder全评估1657.34；仅比较不是SLA，明显数量级回退停止调查。
2. 真实100k行合成Excel完整parser/validate/plan/snapshot/write/post链，强断言Product/Batch/Task/excluded/ImportRecord/Workbook与integrity ok/FK0；分段计时。T03参考49.21s、Excel7.26MB、逻辑DB45MB。门禁空return不算实测。
3. 最终代码代表性受控写中异常、post-orchestration异常：完整字段/BLOB指纹before=after，integrity ok/FK0；保留T03历史10/10，不机械重复全部矩阵。
4. 先审查T04以来生产事务diff。无相关事务改变则Import、正式排查、InventoryAdjustment各pre/postcommit代表硬Kill，每点沿用3次，核对完整回滚/提交、重开、可读写、integrity/FK；Revision既有回归。标准Release已有crash测试照常执行；历史48/48保留，100k完整强杀无需机械重跑。有相关变更则补受影响完整矩阵。
5. 代表损坏恢复：健康当前+坏备份拒绝且当前不变；坏当前无法保护则阻断且文件不变；健康恢复成功；replace后final失败回退；恢复后权威读取；至少一组大库Backup/Restore新耗时，参考225.43MB/6.95s/22.55s/额外integrity-FK2.28s，不当SLA。
6. 最终候选HEAD无filter Release，0failure/error/aborted，任何skip逐项解释；build0warning/0error、EF无漂移（设计时内存）、migration --no-connect仍9，末条20260901155124_AddPolicyAndBaselineFoundation；完整禁止项diff与git diff --check。

## 必须保留的边界

Current validation cannot prove provenance of a structurally valid foreign WAL accepted by SQLite. Such a WAL may alter business state while integrity/FK/migration checks remain valid.

结构合法外来WAL可改变业务数据而integrity/FK/migration仍正常；结构健康不等于业务来源身份。不新增identity/provenance/quarantine、删sidecar、忽略WAL、一律拒绝或自动Restore。严重坏当前库不能生成合格保护快照时必须阻断；现有Restore不是损坏主库灾难救援工具。

真实断电、SSD控制器、磁盘/文件系统/介质损坏、OS不可读及bitrot未证明。只证明已执行的进程硬杀、合成损坏、备份恢复、高规模读/导入及事务原子性。Undo永久取消，旧代码/测试不是产品能力；设置页重置数据仅未来独立需求，本卡不建Task。

## 收口与停止

全部通过才标S8-T06及Stage8 TECHNICALLY_ACCEPTED / CLOSED，T01～T06 CLOSED；创建STAGE-8-CLOSEOUT.md，逐卡汇总目标/规模/结果/生产修复/风险/状态，更新Acceptance/Stage/Handoff/ProjectStatus。普通push main后必须重新fetch，核对clean、HEAD=origin/main、0/0，停止。Stage9 NOT_STARTED，不做安装器/升级/重置/Undo或后续Task。

真实生产一致性bug、明显性能回退或生产Release失败则S8-T06_BLOCKED / STAGE8_NOT_CLOSED，报告证据并停止扩权。
