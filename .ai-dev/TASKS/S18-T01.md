# S18-T01｜今日提醒弹窗信息层级优化

## S18-T01-R5｜最终紧凑视觉基线（2026-09-10）

- 唯一正向视觉基线：`D:\下载\ChatGPT Image 2026年9月10日 21_43_57.png`，SHA256 `AFC6A06E691F3755CD7B4868F00E39308753E08C9EEC072DC98F089EFC182B7B`；R1～R4 只作拒绝历史。
- 100% DPI 硬目标：Window `620～700 × 360～420`，实际 `Width > 720` 或 `Height > 430` 不得交付；建议优先约 `660 × 390`。
- 标题 `22～26`；顶部双核心卡为横向紧凑条，高 `60～75`；四张统计卡单行、高 `55～70`；汇总、说明与双按钮合并为紧凑 Footer。
- 保留白底、浅蓝/浅红核心卡、蓝色购物车、红色警告、动态阶段 Pill、四色阶段数字、蓝色竖线、浅灰边框和现有按钮视觉。只改布局密度。
- 必须消除 Window/MinWidth/MinHeight/SizeToContent、根 Grid、列宽、卡片和按钮等隐性撑大约束；禁止整体缩放、滚动、四卡两排、位图背景或第三方 UI。
- BUSINESS LOGIC DIFF=`0`；Reminder、口径、阶段、按钮行为、导航、调度、数据库、Schema/migration、其他页面冻结；migration=`9`，FULL=`NOT_RUN`。
- 从本治理提交后的 fresh `origin/main` 新建 clean worktree，由全新 GPT-5.6 Terra（medium）实施；Sol 独立真实 GUI 完成 A～J，任何 NO 均退回 Terra。
- 当前状态仅为 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / WAITING_USER_GUI_ACCEPTANCE`；不得自判接受/关闭，不得派发 S18-T02。

日期：2026-09-10（Asia/Shanghai）
fresh 治理基线：`origin/main@3810289089a8c74f0f42341f61f49d245591a339`
治理提交：`d39b7452bee4962ca03ee9e365bd0d767b118604`
实现提交：`87dd576fc0a69fe567a246dd55debb3aaddd642b`
状态：`IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / GUI_VISUAL_REWORK_REQUIRED / NOT_ACCEPTED`

## S18-T01-R1｜GUI VISUAL REWORK

- `D:\下载\门店效期提醒.png` 是 APPROVED PRODUCT PROTOTYPE / HARD VISUAL BASELINE。
- 用户拒绝的当前 GUI 截图只作为 NEGATIVE REFERENCE；禁止仅改 Margin、Window 高度或字号后再次交付。
- 目标为正式、清晰、轻量、克制的 Windows SaaS 提醒窗：首眼是“今日待排查 + 最高紧急阶段”，第二眼是四类提前 3 天计数，第三眼是涉及商品总数与处理去向。
- 100% Windows 缩放下目标宽 `880～920`、高 `600～640`，优先自然高度；1366×768 与常见缩放下不得裁切、遮挡或让四卡换行。
- 标题 `28～32` SemiBold/Bold，禁止巨大纯黑标题。顶部两卡等宽、高 `125～145`、圆角 `10～14`；今日卡使用浅蓝底，数量与单位分层；最高阶段卡使用警告 Geometry/Path 与动态状态胶囊，不得只裸露红字。
- 四个预提醒卡保持一排、等宽、高 `95～110`、圆角 `8～12`、浅边框 `1`；数字与单位分层，颜色只落在数字或小强调上。禁止四卡外围粗红框。
- 底部总数、说明和按钮整体上收；按钮右对齐、高 `42～46`、间距 `10～12`，继续复用现有主次按钮风格。
- 允许范围仅为提醒弹窗 XAML/程序化 WPF 视觉树、局部资源/Brush/Geometry/布局与纯 UI 构造代码。不得修改 Reminder 数据、业务计算、导航、调度、数据库、Schema/migration/ModelSnapshot、其他页面、版本、Updater、Installer 或依赖。
- 全新 GPT-5.6 Terra（medium、标准速度）必须从本 R1 治理提交后的 fresh `origin/main` 建立独立 clean worktree，真实启动提醒窗并自查；Sol 只做独立治理/diff/技术/GUI preflight，不写生产代码。
- 本轮只做静态 diff/XAML 检查、一次必要 build（若 GUI harness 未覆盖）与隔离真实 GUI；原 `26/26`、Release build、reminder 业务测试和 `FULL` 均不得重跑。

## 执行角色与停止点

- Sol 负责产品合同、范围、独立 diff/专项/Build 审查与 Acceptance；不编写生产代码。
- 实施者必须是本卡全新 GPT-5.6 Terra，reasoning `medium`、标准速度，从本治理提交后的 fresh `origin/main` 建立独立 clean worktree。
- Terra 只实现 S18-T01、运行本卡最小验证并提交；不得实施 S18-T02、改版本、tag、Release 或自判 `ACCEPTED/CLOSED`。
- Sol 技术通过后状态只能为 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`，随后停止等待用户真实提醒弹窗 GUI 验收。

## 批准原型硬合同

原型 A：`D:\下载\门店效期提醒.png`，SHA256 `1B40D985CC0F39DD02E141A287C9C105C3DD540F443024B10986D489B3D9C3F7`，是正式视觉实施与用户 GUI 验收基准。

必须保留并形成同一信息层级：

1. 标题“门店效期提醒”。
2. 第一层视觉重点：左侧“今日待排查”及大数字商品数；右侧“最高紧急阶段”及大阶段标签。
3. 第二层“提前 3 天预提醒”：横向四个真正分区，依次为即将5折、即将2折、即将收仓、即将过期，每区显示阶段名称和明显数字。
4. 底部“涉及商品总数”和“进入‘待排查任务’完成处理”的门店提示。
5. “知道了”仅关闭；“查看待排查任务”关闭弹窗并进入现有完整待排查任务页。

颜色只辅助阶段识别，强度为 5折 < 2折 < 收仓 < 过期；继续使用当前浅色、低饱和 WPF 体系与公共样式。禁止图表、动画、大面积高饱和仪表盘、新说明区、新入口、新导航及示例数字硬编码。WPF 无法像素级复制时只做等价还原，不得自行重设计。

## 现有真实数据口径（必须原样复用）

- `DailyReminderUseCase.Evaluate` 仍是唯一提醒决策入口；提醒时间、当天只提醒一次、时钟回拨、无项目状态与 3 天预提醒窗口不变。
- 今日待排查来自 `InspectionTaskQuery.GetReminderCandidates`：当前 open 且满足现有 reminder eligibility 的任务，按既有阶段优先级 `expired > withdraw > discount_20 > discount_50`，再按最近效期与 TaskId 排序；`DailyReminderRuntimeCoordinator` 按 `ProductId` 去重计数。
- 最高紧急阶段直接复用上述既有排序首项的 `HighestStage`；无正式今日任务时为既有 `none`，不得伪造阶段。
- 四类提前 3 天数字直接复用 `ReminderNotification.UpcomingDiscount50Count / UpcomingDiscount20Count / UpcomingWithdrawCount / UpcomingExpiredCount`，均为现有 `PreReminderItems` 按目标阶段和 `ProductId` 去重计数。
- 涉及商品总数直接复用 `ReminderNotification.ItemCount`，即正式今日商品与预提醒商品 `ProductId` 并集去重数。
- 现有 batch count 字段不是批准原型展示口径，不得替代商品数或新增展示。

## 最小实现范围

- 复用现有 `ReminderNotification`；禁止新增业务查询、重新聚合、复制阶段优先级或改变 `DailyReminderRuntimeCoordinator` 的记录语义。
- 用一个紧凑的 WPF 提醒窗口/对话框替代当前普通文本消息框展示；复用 `App.xaml` 公共按钮、颜色、排版与 `StageLabels`。
- 允许对提醒 channel 的 UI 接线做最小扩展，使“查看待排查任务”能调用现有 `ShellViewModel.NavigateTo(ShellPage.PendingTasks)`；优先保持 `IReminderChannel` 接口不变。
- 预计只涉及提醒 UI/channel、最小 App/MainWindow 导航接线及一个 S18-T01 专项测试文件；不要为匹配预计清单修改无必要文件。
- 不改 Domain、Application 提醒计算、数据库、Schema/migration/ModelSnapshot、库存、任务写路径、调度、Updater、Installer、版本号或依赖。

## 空状态

- 现有 runtime 只有 `Due` 且至少存在正式或预提醒商品时才显示弹窗；不得新增无数据弹窗或伪造阶段。
- 预提醒某类为 0 时仍按批准结构显示 `0`；正式今日任务为 0 而仅有预提醒时，今日待排查显示 `0`、最高阶段显示明确无今日任务的中性状态，且“查看待排查任务”仍只进入现有页面，不声称存在今日任务。

## 最小验证

专项覆盖：今日待排查、最高紧急阶段、四类预提醒、涉及商品总数、两个按钮、0 值/仅预提醒空状态，以及批准原型关键静态结构。直接相关回归仅限现有 daily reminder/runtime/pre-reminder 与受影响导航/UI 契约；必要 Release App build。

`FULL = NOT_RUN / NO_FULL`。不跑 Stage16、安装器、更新链、大库或无关业务测试；不访问正式数据库。

## 2026-09-10 Terra 实施与 Sol 技术验收

全新 GPT-5.6 Terra（medium）从治理后 fresh `origin/main@d39b7452bee4962ca03ee9e365bd0d767b118604` 在独立 clean worktree完成 implementation `87dd576fc0a69fe567a246dd55debb3aaddd642b`。生产 diff 仅修改 `App.xaml.cs` 的现有 PendingTasks 回调接线及 `WindowsMessageBoxReminderChannel.cs` 的提醒展示；没有改 Application/Domain 提醒计算。

Sol 独立确认：两块核心信息、四个低饱和阶段分区、涉及商品总数、门店提示与两个按钮均存在；全部数字直接映射既有 `ReminderNotification`。专项 + V1-F02 + S6-T02 + Shell 直接导航回归 `26/26 PASS`；Release App build `0 warning / 0 error`；diff/check 与禁止路径检查 `PASS`；migration `9`，末条 `20260901155124_AddPolicyAndBaselineFoundation`。

隔离 GUI 入口已用全新 `%TEMP%\GUID` synthetic 数据自检：runtime `due`，今日待排查 `6`、最高阶段 `expired`、四类预提醒各 `1`、涉及商品总数 `10`。这只证明验收入口与真实生产代码链可运行，不替代用户真实 WPF GUI 观感/按钮验收。`FULL = NOT_RUN / NO_FULL`，正式数据库 `NO ACCESS`。

## 2026-09-10 用户 GUI 回执与轻微尺寸返修

用户确认第一层“今日待排查 / 最高紧急阶段”、四阶段预提醒分区、涉及商品总数与提示、以及“查看待排查任务”均 `PASS`，信息结构与业务层级符合批准原型。功能通过项冻结。

唯一返修为弹窗视觉体量：当前约 `960 × 715`，目标为普通 Windows 下约 `880～900 × 620～640`。只允许调整该提醒弹窗的 Window 宽高、Margin、Padding、Grid Row 高度、卡片高度和区块间距；主要减少空白并把底部汇总、说明、按钮整体上收。数字字号原则上不明显缩小，四卡必须保持横向一排，文字不得拥挤、截断或遮挡。

不得修改 ViewModel、Reminder 数据、今日待排查口径、最高阶段、3 天预提醒规则、四阶段计数、导航、调度、数据库、Schema/migration 或任何其他页面。验证仅限 layout 静态 diff 与真实 GUI 无裁切/无遮挡；原 `26/26`、Release build、reminder 业务测试和 FULL 均 `NOT_RUN`。完成后继续交付原 GUI 验收入口，S18-T02 继续 `PLANNED / NOT_DISPATCHED`。
