# S18-T01｜今日提醒弹窗信息层级优化

日期：2026-09-10（Asia/Shanghai）
fresh 治理基线：`origin/main@3810289089a8c74f0f42341f61f49d245591a339`
状态：`GOVERNANCE_FROZEN / IMPLEMENTATION_DISPATCH_AUTHORIZED / NOT_IMPLEMENTED`

## 执行角色与停止点

- Sol 负责产品合同、范围、独立 diff/专项/Build 审查与 Acceptance；不编写生产代码。
- 实施者必须是本卡全新 GPT-5.6 Terra，reasoning `medium`、标准速度，从本治理提交后的 fresh `origin/main` 建立独立 clean worktree。
- Terra 只实现 S18-T01、运行本卡最小验证并提交；不得实施 S18-T02、改版本、tag、Release 或自判 `ACCEPTED/CLOSED`。
- Sol 技术通过后状态只能为 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`，随后停止等待用户真实提醒弹窗 GUI 验收。

## 批准原型硬合同

原型 A：`D:\下载\门店效期提醒.png`，SHA256 `1B40D985CC0F39DD02E141A287C9C105C3DD540F443024B10986D489B3D9C3F7`，是正式视觉实施与用户 GUI 验收基准。

必须保留并形成同一信息层级：

1. 标题“门店效期提醒”。
2. 第一层视觉重点：左侧“今日待排查”及大数字商品数；右侧“最高紧急阶段”及大阶段标签。
3. 第二层“提前 3 天预提醒”：横向四个真正分区，依次为即将5折、即将2折、即将收仓、即将过期，每区显示阶段名称和明显数字。
4. 底部“涉及商品总数”和“进入‘待排查任务’完成处理”的门店提示。
5. “知道了”仅关闭；“查看待排查任务”关闭弹窗并进入现有完整待排查任务页。

颜色只辅助阶段识别，强度为 5折 < 2折 < 收仓 < 过期；继续使用当前浅色、低饱和 WPF 体系与公共样式。禁止图表、动画、大面积高饱和仪表盘、新说明区、新入口、新导航及示例数字硬编码。WPF 无法像素级复制时只做等价还原，不得自行重设计。

## 现有真实数据口径（必须原样复用）

- `DailyReminderUseCase.Evaluate` 仍是唯一提醒决策入口；提醒时间、当天只提醒一次、时钟回拨、无项目状态与 3 天预提醒窗口不变。
- 今日待排查来自 `InspectionTaskQuery.GetReminderCandidates`：当前 open 且满足现有 reminder eligibility 的任务，按既有阶段优先级 `expired > withdraw > discount_20 > discount_50`，再按最近效期与 TaskId 排序；`DailyReminderRuntimeCoordinator` 按 `ProductId` 去重计数。
- 最高紧急阶段直接复用上述既有排序首项的 `HighestStage`；无正式今日任务时为既有 `none`，不得伪造阶段。
- 四类提前 3 天数字直接复用 `ReminderNotification.UpcomingDiscount50Count / UpcomingDiscount20Count / UpcomingWithdrawCount / UpcomingExpiredCount`，均为现有 `PreReminderItems` 按目标阶段和 `ProductId` 去重计数。
- 涉及商品总数直接复用 `ReminderNotification.ItemCount`，即正式今日商品与预提醒商品 `ProductId` 并集去重数。
- 现有 batch count 字段不是批准原型展示口径，不得替代商品数或新增展示。

## 最小实现范围

- 复用现有 `ReminderNotification`；禁止新增业务查询、重新聚合、复制阶段优先级或改变 `DailyReminderRuntimeCoordinator` 的记录语义。
- 用一个紧凑的 WPF 提醒窗口/对话框替代当前普通文本消息框展示；复用 `App.xaml` 公共按钮、颜色、排版与 `StageLabels`。
- 允许对提醒 channel 的 UI 接线做最小扩展，使“查看待排查任务”能调用现有 `ShellViewModel.NavigateTo(ShellPage.PendingTasks)`；优先保持 `IReminderChannel` 接口不变。
- 预计只涉及提醒 UI/channel、最小 App/MainWindow 导航接线及一个 S18-T01 专项测试文件；不要为匹配预计清单修改无必要文件。
- 不改 Domain、Application 提醒计算、数据库、Schema/migration/ModelSnapshot、库存、任务写路径、调度、Updater、Installer、版本号或依赖。

## 空状态

- 现有 runtime 只有 `Due` 且至少存在正式或预提醒商品时才显示弹窗；不得新增无数据弹窗或伪造阶段。
- 预提醒某类为 0 时仍按批准结构显示 `0`；正式今日任务为 0 而仅有预提醒时，今日待排查显示 `0`、最高阶段显示明确无今日任务的中性状态，且“查看待排查任务”仍只进入现有页面，不声称存在今日任务。

## 最小验证

专项覆盖：今日待排查、最高紧急阶段、四类预提醒、涉及商品总数、两个按钮、0 值/仅预提醒空状态，以及批准原型关键静态结构。直接相关回归仅限现有 daily reminder/runtime/pre-reminder 与受影响导航/UI 契约；必要 Release App build。

`FULL = NOT_RUN / NO_FULL`。不跑 Stage16、安装器、更新链、大库或无关业务测试；不访问正式数据库。
