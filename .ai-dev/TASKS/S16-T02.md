# S16-T02｜安装完成后自动启动主程序

日期：2026-09-10（Asia/Shanghai）
治理基线：`origin/main@6580a9d8af6ffa90f373bb795a28a1089721b183`
状态：`IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`

## 执行角色与停止点

- Sol 只负责治理、范围控制与独立技术验收，不直接修改生产代码。
- 实施者必须是本卡全新 GPT-5.6 Terra、reasoning `medium`、标准速度，从本治理提交后的 fresh `origin/main` 创建独立 clean worktree；禁止复用任何 S16-T01/Stage17 Terra。
- 技术通过后仅停在 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`，等待用户本人真实安装验收；不得关闭本卡/Stage16 或发布 v1.0.8。

## 冻结合同

- 全新人工安装或手工覆盖安装成功后自动启动新版主程序；取消或失败不启动；完成后只允许一个 App 实例。
- 不改变数据库位置、用户数据、配置、安装目录规则或在线更新安全协议。
- 实施前必须复核现有 Updater 启动 Setup/重启 App 的职责，普通人工 Setup 启动必须与在线 Updater 重启隔离，避免双启动与 Mutex 冲突。
- 优先使用现有 Inno Setup 最小能力；不新增更新框架。默认 `NO_FULL`。

## 安装与更新职责

- 普通全新安装及手工覆盖安装：只有安装真正成功后，由 Setup 单一职责点默认启动最新版主程序；取消、失败、中断、未完成均不得启动。
- 优先复用 Inno Setup 标准完成页，可提供默认勾选的“安装完成后运行门店效期排查软件”；不得重做安装 UI。
- 在线更新链继续由独立 Updater 在 Setup/文件替换完成后唯一重启 App。Updater 调用 Setup 时必须传入明确参数关闭 Setup 的 post-install App 启动；不得用父进程或进程名猜测来源。
- 必须审查 silent/verysilent/suppressmsgboxes：新增运行项不得在这些在线/静默模式意外启动 App、弹出完成页或造成 Updater 与 Setup 双启动。
- App 现有 Mutex/单实例机制保留；覆盖安装必须先完成旧实例退出与文件替换，再启动新版。一个职责点只启动一次，不增加重复保险启动点。

## 版本与禁止范围

- S16-T02 属于 v1.0.8；App/Updater 最终候选版本为 `1.0.8`。
- 禁止修改 SQLite Schema、migration、ModelSnapshot、数据/配置、导入/效期业务、S16-T01、GitHub/Gitee/Quark、manifest、RSA/signature/SHA256、rollback、ZIP 审计或 ACK 合同。
- 仅当隔离 Setup 自启动所必需时，允许给现有 Updater→Setup 调用增加一个最小明确参数；不得重构 Updater 或改变其事务/最终重启职责。

## 最小技术门禁

- 直接专项验证：普通 Setup 默认成功后启动；Updater 显式禁用 Setup 自启动；silent/verysilent 不意外启动；取消/失败不走启动动作；Updater 原最终启动职责保留。
- Release Setup build 必须通过；按实际改动做必要 App/Updater build；EF 无漂移、migration `9`。
- 不重跑 S16-T01 风险业务专项；`FULL = NOT_RUN / NO_FULL`。

S16-T01 已 `GUI_ACCEPTED / CLOSED`。本卡已获用户正式派发授权，但仍不得与 S16-T01 共用 Terra 或混合提交。

## 技术验收结果

- 全新 GPT-5.6 Terra（medium）从治理后 fresh `origin/main@730de88fb3a96d2d3d5fce54b729092e5325048e` 实施，提交 `70512165526e63b50b8086fcd4be3e82c197e38d`。
- 人工 Setup 使用 Inno `[Run]` 的 `postinstall nowait skipifsilent`；完成页默认勾选运行，`silent/verysilent/suppressmsgboxes` 及 `/NOPOSTINSTALLRUN` 不启动。
- 当前真实 Updater 不调用 Setup，而是直接校验并切换 ZIP 程序树，随后仅由既有 `StartNormalApplication` 启动 App；因此不存在 Updater 与 Setup 双启动路径，也不新增虚构的 Updater→Setup 参数。
- S16-T02 直接专项 `3/3 PASS`；Release Setup、App、Updater build 均 `PASS`；EF 无漂移，migration `9`；`FULL = NOT_RUN / NO_FULL`。
