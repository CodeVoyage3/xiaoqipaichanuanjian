# S18-T03｜左侧导航栏展开/收起与企业 SaaS 视觉重构

日期：2026-09-11（Asia/Shanghai）
fresh baseline：`origin/main@bb72e7ce085652bd378676d74d8dd3eeff948d37`
状态：`GOVERNANCE_FROZEN / IMPLEMENTATION_DISPATCH_AUTHORIZED / NOT_IMPLEMENTED`

## 产品基线

批准原型：`D:\下载\ChatGPT Image 2026年9月10日 23_36_49.png`，SHA256 `2FB55AF5C17454A73ED9C963B347C96212A130B7456DCC5EF7C1C8AA7D908E22`。该图是 `APPROVED PRODUCT PROTOTYPE / HARD VISUAL BASELINE`；文字合同优先，不再做视觉探索。原型右侧仅用于展示导航效果，所有现有业务页面冻结。

## 固定实现合同

- 只重构现有左侧 Navigation；继续复用同一套 `ShellColumn`、Button、Path、`_isNavigationCollapsed`、`ToggleNavigation_Click`、`ApplyNavigationLayout`。禁止第二套 Shell 或折叠菜单。
- 展开宽度 `220 DIP`，收起宽度 `72 DIP`。收起后右侧内容仅因原 Grid 列变窄而自然获得空间，不改右侧布局。
- 展开结构严格为：品牌区 `[现有盾牌 Logo] 效期排查 <`；`工作台 / 首页`；`排查管理 / 待排查任务 / 今日排查 / 排查历史`；`数据管理 / 数据导入 / 数据备份与恢复`；`系统 / 设置`；底部软件版本号。
- 收起时保留 Logo、展开按钮和同一组七个菜单 Path；隐藏品牌文字、分组标题、菜单文字和版本文字。七项顺序与展开状态完全一致，ToolTip 精确为菜单名称。
- 图标语义锁定：`HomeIcon / ClipboardIcon / CalendarIcon / ClockIcon / ImportIcon / DatabaseIcon / GearIcon`。修正今日排查、排查历史、备份恢复的现有错误映射；只新增一个轻量 `DatabaseIcon` Geometry，不引入图标库或第三方依赖。
- 选中项保持浅蓝灰背景、约 3 DIP 左侧蓝条、蓝色图标/文字；未选中为深灰/灰蓝；Hover 仅浅灰蓝。继续收口现有 `NavButtonStyle`，菜单高 `40～42 DIP`、分组标题 `11～12 DIP`，无大块深蓝、复杂动画、Theme 重建或大量分隔线。
- `NavigationBrandText` 固定显示“效期排查”；展开/收起按钮使用轻量左右箭头。

## 唯一新增状态行为

- 展开/收起状态持久化到 `RuntimeDataRoot.RootDirectory` 下的轻量本地 UI 状态文件；不得写业务数据库。
- 首次启动、文件不存在、读取失败或内容损坏均安全回退为默认展开且不阻断启动。
- 点击切换后保存；下次正常启动恢复上次状态。必须兼容既有 `--data-root TEMP/GUID` 隔离根。
- 优先在 `MainWindow.xaml.cs` 内用现有 .NET 能力完成；确有必要时最多增加一个很小的 UI preference store，禁止通用配置框架。

## 冻结边界

- 继续复用 `ShellPage`、`CurrentPage` 及现有六个导航命令；设置继续使用 `Settings_Click`；未来风险继续归首页；不得修改 `Stage4ViewModels.cs`。若无法满足，实施者必须停止说明。
- 生产修改仅允许 `App.xaml` 的导航资源、`MainWindow.xaml` 的导航结构/布局、`MainWindow.xaml.cs` 的导航布局/状态；持久化确有需要时最多新增一个小文件。
- 首页、待排查、今日排查、详情、历史、导入、备份恢复、设置内容及 Reminder、EAN-13、Updater、Installer、版本、发布通道全部冻结。
- `BUSINESS LOGIC DIFF=0`；Schema/migration/ModelSnapshot=`NO CHANGE / 9`；正式数据库=`NO ACCESS`；NO NEW DEPENDENCY；`FULL=NOT_RUN / NO_FULL`。

## 实施与停止点

- 治理 push 后，由全新 GPT-5.6 Terra、reasoning `medium`、标准速度，从治理后 fresh `origin/main` 创建独立 clean worktree，只实施 S18-T03；不得复用 T01/T02 实施者。
- 只跑 S18-T03 专项、必要现有 Shell 导航直接回归和一次 Release App build。禁止 Stage16、T01/T02、条码、Reminder、Installer、Updater、大库和 FULL，除非发现明确跨模块风险并先停止说明。
- Sol 独立审查 diff、专项、Shell 回归、Release build 和 TEMP/GUID 隔离 GUI preflight。
- 技术通过后只能为 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED`；Stage18 只能为 `IN_PROGRESS / WAITING_USER_S18_T03_GUI_ACCEPTANCE`。等待用户 GUI A～H 前不得关闭任务/阶段、进入 v1.0.9 发布准备或发布任何渠道。
