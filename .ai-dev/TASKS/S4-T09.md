# Task S4-T09｜Stage 4 最终 Release 全链路验收、兼容性收尾与阶段归档

- 状态：UAT 发现 5 项缺陷，等待本卡同一名 GPT-5.6 Luna（max）最小修正与复测
- 基线提交：`e7cab4d4a47a532dae9eeb263bc31399acb1b02d`
- 前置验收：S4-T01～S4-T08 已由 GPT-5.6 Sol 独立验收通过
- 执行：一名全新的 GPT-5.6 Luna（max），不得复用 S4-T07/S4-T08 执行会话
- 产品经理、独立审查与总验收：GPT-5.6 Sol
- 最终 GUI/WPF 人工验收：用户本人
- 阶段门禁：本卡是 Stage 4 最后一张任务；完成前不得创建 S5-T01，完成后只生成 Stage 5 handoff 并停止

## 一、目标与非目标

本卡不新增业务功能。目标仅为：

1. 在最终 Stage 4 代码上重跑自动化、架构、数据、依赖与 Release 门禁。
2. 静态核对 1024×600、125% DPI、DataGrid、详情固定区、库存修正与超库存区域的明显兼容性风险。
3. 为用户准备隔离数据方案和精简人工验收清单，由用户本人完成最终 GUI/WPF 验收。
4. 仅在用户报告真实阻断时，以最小修改修复 Stage 4 已批准功能的正确性、裁切、DPI、键盘、状态反馈或主流程问题。
5. 用户验收通过后完成 Stage 4 总验收、closeout 与 Stage 5 handoff。

禁止新增产品能力、schema、migration、依赖、UI 框架、业务状态、历史页、提醒、设置、导出、安装、备份恢复功能或 Stage 5 实现。

## 二、执行治理

- S4-T09 必须使用一名全新的 GPT-5.6 Luna（max）执行代理，不复用上一任务执行会话。
- Luna 只执行仓库、代码、自动化、Release、EF、SQLite、依赖和静态兼容性检查，不代替用户操作 GUI。
- Sol 独立审查 Luna 证据并亲自复核关键终端门禁，不使用电脑操控。
- Git 无法独立证明代理身份时，最终验收如实记录，不伪造代理身份为代码证据。
- 当前只批准 S4-T09；不得创建、编号或派发 S5-T01。

## 三、开工基线与非 GUI 门禁

先核验 branch、HEAD、status、diff、`git diff --check`、最近提交、未批准文件、应用进程和正式用户数据目录，再执行：

1. S4-T01～S4-T09 相关专项与组合测试。
2. Stage 4 全部权威回归。
3. Stage 3 十个权威测试类。
4. Release 全量：0 失败、0 跳过。
5. Release build：0 警告、0 错误。
6. EF model drift：无变化。
7. migration：仍为 8 条。
8. SQLite 正式历史唯一约束及正式提交/草稿/批次停止边界。
9. 相对 S4-T08 基线无项目依赖变化。
10. UI 无 EF 访问、正式实体创建、`HandledAttentionVersion` 写入、Batch 停止、异常文本解析、Stage 3/S4-T04 规则复制或新状态机。
11. `git diff --check` 与最终 status。

在线 NuGet 漏洞审计只合理尝试一次；若仍因 SSL/TLS/凭据或服务索引失败，立即停止并如实报告，由用户决定是否接受该环境性风险。

## 四、静态兼容性审查

在不操作 GUI、不预判用户结果、不大改 UI 的前提下，定向核对：

- `MainWindow` 最小尺寸、默认尺寸、DPI 配置与根布局。
- 首页、待排查列表、DataGrid、详情固定区、底部“完成排查”、库存修正与超库存区域在 1024×600 和 125% 缩放下是否存在源码可证明的必然裁切或不可达风险。
- 长文本换行、滚动容器、Dialog 最大高度、Tab 顺序和键盘焦点可见性。

若未发现必然阻断，不得仅凭推测修改 UI；精确 1024×600 与恰好 125% 的实际结论以用户反馈为准。

## 五、用户人工验收与数据隔离

非 GUI 门禁通过后立即暂停，向用户提供约 12～18 项真实操作清单，覆盖：

- 有效 Excel 预览、确认导入与 Dashboard/列表即时刷新。
- Dashboard、列表搜索/筛选与进入详情。
- 检查人、日期、正数/0 件、多批次、自动保存和草稿恢复。
- 可安全构造时验证 Reconfirm；否则复用已归档自动化和真实验收证据。
- 正库存修正与 0 库存安全确认。
- 正式提交、completed、列表移除、Dashboard 刷新和后台 0 件停止。
- 超库存 Warning、库存/合计/超出事实和确认仍提交。
- 精确 1024×600、恰好 125%、默认/最大化和 Tab 键录入。

人工验收必须使用隔离 `%LocalAppData%\StoreExpiryInspector` 数据。开始前关闭进程并备份正式用户数据；结束后关闭进程、恢复原数据、校验文件大小和 SHA-256、删除隔离目录，并且恢复后不再启动应用。

## 六、缺陷修正边界

用户报告失败时先定位，只允许修复 Stage 4 已批准功能的阻断缺陷。兼容性修正限于必要的 Margin、MinWidth、Grid 尺寸、ScrollViewer、DataGrid 列、Dialog 最大高度、文本换行、DPI 布局或等价最小改动。

任何代码修正仍由本卡这名全新的 GPT-5.6 Luna（max）执行，Sol 独立审查；用户只复验受影响路径。禁止重构全部 ViewModel、重做视觉系统、引入新框架或修改业务规则。

## 七、Stage 4 总验收与归档

只有非 GUI 门禁、用户人工验收、SQLite/日志事实及用户数据恢复全部通过后，才允许生成：

- `.ai-dev/ACCEPTANCE/S4-T09.md`
- `.ai-dev/ACCEPTANCE/STAGE-4.md`
- `.ai-dev/STAGES/STAGE-4-CLOSEOUT.md`
- `.ai-dev/STAGES/STAGE-5-HANDOFF.md`
- 必要的 `PROJECT_STATUS` / `STAGES` 更新

总验收必须证明真实链路：

`Excel → 预览 → 确认导入 → Dashboard → 待排查列表 → 详情 → Draft → Reconfirm → 库存修正 → 正式提交 → 超库存确认 → 0 件停止 → Task completed → Dashboard/PendingTasks 刷新`

并确认 UI 未复制 Application 业务规则、ViewModel 未直查 EF、Stage 3 状态机与 S4-T04 正式提交入口仍唯一、migration 仍为 8 条、无未批准依赖和阻断架构债。

Stage 5 handoff 只记录接任事实、最终 HEAD/测试/build/EF/migration/依赖/UI/用户验收/已知限制、最小必读文件及“排查历史与修改追溯”目标；明确禁止重新实现 Stage 4 正式提交逻辑。

## 八、停止门禁

非 GUI 门禁通过后先停下来等待用户人工验收。Stage 4 归档和 Stage 5 handoff 完成后再次立即停止；不得创建或派发 S5-T01，不得进入 Stage 5 开发。

## 九、2026-08-29 UAT 缺陷与验收事实

用户已完成首轮人工验收。Release 启动、数据导入主流程、首页/待排查基本功能、详情/草稿恢复、库存修正、正式提交、超库存、1024×600、125% 缩放、键盘与退出通过；Reconfirm 复用既有自动化证据。用户明确接受 NuGet 在线漏洞审计因本机 SSL/TLS/凭据环境失败的风险，本卡不得重试。

S4-T09 暂不验收、Stage 4 暂不归档。只允许本卡同一名 Luna 修复：

1. 首页 DataGrid 等嵌套滚动区域不得吞掉鼠标滚轮，且不得全局劫持其它页面滚动。
2. 确认导入后立即显示“正在导入，请稍候…”及明显的不确定进度反馈，期间禁止重复确认；成功后明确显示“导入成功”，失败继续复用现有 Retry。
3. 首页任务表增加现有 `Product.Barcode` 的只读展示链路；不新增字段、migration、身份规则、唯一键或搜索规则。
4. 首页商品名称、商品编码、商品条码使用单元格级选择与复制，`Ctrl+C` 只复制当前单元格；不得新增剪贴板框架或右键菜单。
5. 对 XAML、ViewModel/C# 提示、ResourceDictionary/resx、初始化/测试展示数据及本轮新增文案执行用户可见固定中文的 zh-CN 简体一致性审计，修复包括 `門`、`漢` 在内的明显繁体固定文案，并增加最小静态防回归证据。必须先判断真实来源；导入业务数据只报告不得改写，禁止运行时繁转简算法、第三方转换依赖、数据库修改或 migration。

修复必须保持 1024×600 与 125% 可用，不得提高窗口最小宽度，不得新增依赖或修改 Stage 3、S4-T04、生命周期、schema、migration、正式历史或 Stage 5。自动化门禁通过后只交付上述五项及 1024×600 + 125% 快速复测清单。

正式数据继续保存在 `StoreExpiryInspector.s4t09-backup-20260828`，不得移动、删除、覆盖或用于测试启动。用户复测通过且关闭应用后，恢复数据库并复核 `299008` bytes、SHA-256 `F3D423DF14B882D7BFE87780A81CF5879F074AF4880601CBEDB6B475A964F522`；恢复后不得再次启动应用。
