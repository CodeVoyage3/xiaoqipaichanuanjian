# S18-T02｜排查详情扫码识别、信息区紧凑化与按钮视觉一致性收口

日期：2026-09-10（Asia/Shanghai）
fresh baseline：`origin/main@1979fdd93a6aa766bb5b33e6086df11b995aeae6`
状态：`GOVERNANCE_FROZEN / IMPLEMENTATION_DISPATCH_AUTHORIZED / NOT_IMPLEMENTED`

## 固定范围

A｜真实商品条形码；B｜排查详情信息区紧凑化；C｜全局按钮视觉一致性收口。三项必须同卡完成，不得扩大到详情页重做、Theme 重做或多制式条码体系。

批准原型：`D:\下载\条形码详情页原型图.png`，SHA256 `E79308D4FC5FF5315F4CE61D8D3DD1F0C7FEE6823EBBBF81BB38BAE06F292153`，为 HARD VISUAL BASELINE。用户提供的实际截图只作蓝底/红底深色字及异常禁用态的缺陷证据。

## A｜ProductBarcode 与 EAN-13

- 唯一数据源为现有 `Product.CurrentBarcode → InspectionTaskQuery.GetDetail → InspectionTaskDetail.ProductBarcode → InspectionDetailViewModel.ProductBarcode`。不得以 `ProductCode` 替代，不改值、不补值、不增加 Query/DTO/数据库字段。
- 非正式代码/测试证据显示条码字段允许 null、空值及任意原字符串；样本含 12、13、15 位数字及非数字测试值。当前唯一明确贴近真实导入的样本 `6974098813108` 与批准原型 `6974396950994` 均为 checksum 有效的 EAN-13；未访问正式数据库。
- 本卡只实现 EAN-13。输入必须精确为 13 位 ASCII 数字并通过 check digit；正确生成左右 guard、L/G/R pattern、统一模块宽、足够 quiet zone；原始数字原样显示。其他长度、非数字、空值、错误 checksum 统一显示中性“暂无可用商品条码”，不得崩溃或阻断排查。
- 使用 WPF 原生矢量/矩形或 Drawing，不做位图横向拉伸；保留 `UseLayoutRounding`、`SnapsToDevicePixels` 与锐利边缘。NO NEW DEPENDENCY；若实施者认为必须新增依赖，立即停止并交 Sol 裁决。
- 顶部仍为一张商品卡：左侧商品名称/商品编码，中部条形码/原始数字/“手机扫码查看商品信息”，右侧库存/待排查批次/修正库存。删除左侧重复“商品条码”行，不新增独立条码卡。

## B｜排查信息紧凑化

“排查信息 / 排查人 / 排查日期”置于同一水平行、同一视觉基线并垂直居中；只压缩标题独占行、上下 Padding、Margin 与 Row 高度。输入框、DatePicker、现有绑定、校验、草稿与提交语义保持。

## C｜共享 Button 根因与修复合同

- fresh 审查确认 `App.xaml` 已有隐式 Button 模板及 `PrimaryButtonStyle / SecondaryButtonStyle / DangerButtonStyle`；Primary/Danger 自身已声明白色 `Foreground`。
- 根因是全局隐式 `TextBlock` Style 设置 `Foreground=PrimaryTextBrush`，其 Style Setter 优先于从 Button `ContentPresenter.TextElement.Foreground` 继承的颜色，使纯文本 Content 在蓝/红实底上仍呈深色；页面按钮本身没有缺失语义 Style。
- 只在共享 Button 模板/公共样式层最小修复：模板内文本（含字符串生成文本及内嵌 TextBlock）明确跟随 templated Button 的 `Foreground`；Primary normal/hover/pressed 保持白字，Danger 实底保持白字，Secondary 保持深字，Disabled 使用 `DisabledTextBrush` 并整体弱化。
- 禁止逐页散落 `Foreground="White"`；禁止修改 TextBox、ComboBox、DataGrid、CheckBox、Tab、Navigation、Badge/Pill。静态巡检首页、待排查、今日排查、详情、历史、导入、备份恢复、设置、提醒/更新/确认弹窗，重点为导出排查计划、立即备份、恢复所选备份、查看待排查任务。
- S18-T01 的 `660 × 360 DIP`、布局、文案和行为冻结；只允许按钮文字颜色被公共修复纠正。

## 冻结边界

页面标题、批次表、阶段、日期、累计到货、上次排查、本次数量、正常批次、草稿、库存和完成排查业务、Sticky Action Bar 均不重构。Schema/migration/ModelSnapshot=`NO CHANGE`，migration=`9`；正式数据库=`NO ACCESS`；Updater/Installer/版本/发布通道不变。

## 实施与验证

- 实施者必须是本卡全新 GPT-5.6 Terra，reasoning `medium`、标准速度，从本治理提交后 fresh `origin/main` 建立独立 clean worktree。Sol 不写生产代码，独立审查 diff、算法、数据源、公共样式、专项、Release build 与隔离 GUI。
- 条码专项至少覆盖合法已知 EAN-13 向量、原值不变、错误 checksum、空值、非数字及 ProductCode 不代替；布局静态契约覆盖原字段/同一行/重复条码删除；按钮覆盖 Primary 三态白字、Danger 白字、Disabled 弱化、模板 Foreground 和已知入口。
- 只跑本卡专项与直接相关 UI/Query/ViewModel 回归；一次必要 Release App build，目标 0 warning / 0 error。`FULL = NOT_RUN / NO_FULL`。
- 提供真实可操作的 TEMP/GUID 隔离 GUI：含名称、ProductCode、有效 ProductBarcode、库存和至少一个待排查批次；Sol 必须先打开。验收时明确页面 Barcode 原值。正式数据库不得访问。
- 技术通过后只可记录 `IMPLEMENTED / TECHNICAL_ACCEPTANCE_READY / NOT_ACCEPTED` 并等待用户 GUI。只有用户确认后才可关闭 S18-T02/Stage18；此前禁止 tag、Release、夸克/Gitee 发布。
