# S9-T02｜当前用户首次安装器与数据保留安全门禁

2026-09-04 用户正式授权。IN_PROGRESS / NOT_ACCEPTED。
开工 fetch：HEAD=origin/main=34c336a3f03e823048e5987d102001911527e5b2，clean、0/0；TASKS/ACCEPTANCE中无既有T02。Stage8与T01保持CLOSED。

## 角色与范围

Sol仅治理、完整diff审查、独立自动化复验；必须全新GPT-5.6 Terra medium/priority实施，提交后停止、不push。创建失败即停止，Sol不代写生产代码。
仅本卡。不得创建T03、Updater、正式GitHub Release、重置数据或Undo；不新增Schema/migration/index/NuGet依赖，不改业务算法。正式数据根/DB禁止任何探测、读取、哈希、复制；所有实验只用TEMP/GUID合成数据。

## 冻结交付契约

- 实际生成官方Inno Setup 6安装EXE，PrivilegesRequired=lowest；当前用户，禁止all-users/HKLM/Program Files/admin依赖。
- 正式稳定AppId生成一次并登记，后续1.x不变；AppVersion1.0.0，Assembly/File1.0.0.0；StoreExpiryInspector-Setup-1.0.0.exe。不虚构Publisher/证书/公司身份。
- 固定程序根%LOCALAPPDATA%\Programs\StoreExpiryInspector，固定入口app\StoreExpiryInspector.exe；卸载器/安装元数据在根。无版本路径。
- 完整win-x64 self-contained多文件payload；不trim/AOT/singlefile，不手选或删除DLL/runtime/nativeSQLite。编译器和产物二进制不入Git。
- 数据根保持%LOCALAPPDATA%\StoreExpiryInspector，data/app.db及sidecar、backups/pre-import、logs、settings/runtime/history/workbook BLOB等全部保留，不进安装树/安装包。
- 当前用户桌面和开始菜单快捷方式指向稳定入口。HKCU Run\StoreExpiryInspector为正确引用的EXE绝对路径。真正首装默认on；同版本重装/修复尊重实际Run关闭状态，不根据DB默认true重开。
- 同版本重装可修复程序文件，AppId/path不变、无重复快捷方式/卸载项、完整DB及backup指纹不变。卸载仅删除自身程序/快捷方式/Run/卸载项，无删除数据选项；卸载后重装能继续读原数据。
- 数字/SemVer比较阻断降级，必须在写程序前阻断；更高测试fixture1.0.1不能被1.0.0覆盖，程序树和DB不变。

## 只读preflight

只允许无DB或精确完整当前migration9且integrity ok/FK0。必须区分no_database、current_migration_9_healthy、older_schema、newer_or_unknown_schema、corrupt_or_unreadable、invalid_data_root。少于9、未知/更高/异常history、坏库/不可读/不安全根均阻断；中文说明为保护原数据停止。不得调用Initialize/Migrate/修复/业务写入/假BackupRecord。考虑SQLite WAL/SHM和ReparsePoint，门禁不得产生副作用或忽略已提交WAL；不能通过误判无库回落正式根。

## 测试构建与证据

生产和测试共用同一installer核心。测试只替换独立AppId、显示名、TEMP/GUID程序根和synthetic数据根、独立Run/shortcut名字及必要版本fixture。绝不执行正式身份安装器。测试启动必须支持受控已有synthetic数据，严禁借默认参数落入正式根。测试结束清理自身程序/Run/shortcut/卸载项，保留证据和合成数据。
Sol真实执行Acceptance A-I。保留小型JSON/MD证据；大DB、安装EXE、publish、编译器与日志只留本地/TEMP。审计完整payload/安装EXE无DB/sidecar/backup/log/真实Excel/test evidence/凭据/PAT/token/private key/pfx/密码/源码绝对路径/正式数据。未签名如实记录，不声称解决SmartScreen。

## 最终门禁和停止点

Sol完整diff、安装器及preflight专项、A-I真实矩阵、fresh publish+实际installed EXE启动、最终无filter Release全量0failure/error/aborted、build0warning/0error、EF无漂移、migration9且末条20260901155124_AddPolicyAndBaselineFoundation、diff --check。全量期间不要并行build/publish。
必须登记安装器filename/bytes/SHA256/compilerVersion/AppId/version/installRoot/EXE/dataRoot/privileges/uninstall/shortcuts/Run/fileCount/包安全/签名状态。干净Win10/11和用户向导GUI仍非本卡自动化证据。
若无法安全取得官方编译器，或存在结构性数据/偏好/只读/稳定路径问题，或需要Schema/admin：S9-T02_BLOCKED / NOT_ACCEPTED，停止报告，不自建返修卡。全部通过后治理关闭、fetch、普通push main、fetch核对clean/0/0后停止。
