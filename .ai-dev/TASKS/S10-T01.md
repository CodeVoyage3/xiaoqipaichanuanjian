# S10-T01｜安装器简体中文化、首次安装可选路径与 v1.0.2 首次正式发布

## 当前

`S10-T01 = DEFINED / NOT_STARTED`；`Stage10 = IN_PROGRESS / S10-T01_CURRENT`。

用户已授权本卡实施与通过后的 v1.0.2 正式发布。Stage9 保持 CLOSED，不重开。

2026-09-06 最新产品裁决：**v1.0.2 是本软件首个正式对外发行版本**。现有 GitHub v1.0.0/v1.0.1 仅为 Stage9 开发期真实 Release/在线升级验证产物，并未作为正式产品分发。v1.0.2 发布并完成全部发布后核验之前，不得删除/替换旧 Release 或 tag；v1.0.2 核验全部通过后，删除 v1.0.0/v1.0.1 的 GitHub Release 与对应 tag，但必须永久保留其历史 commit 和 `.ai-dev` 中 Task/Acceptance/Analysis/发布证据。

## 一、实施角色

生产实施必须创建全新的 GPT-5.6 Terra，reasoning medium，priority；不得复用 S9-T07 Terra。Terra 负责生产代码、安装器脚本与实施测试，提交后停止，不自行 push/tag/Release。

Sol 负责治理、完整 diff、独立自动化、Release build、EF/migration、安装器与 Updater 隔离验证、正式发布证据；Sol 不直接写生产代码。

## 二、现状约束

当前 `installer/StoreExpiryInspector.iss`：

- `DefaultDirName` 固定 `%LOCALAPPDATA%\Programs\StoreExpiryInspector`；
- `DisableDirPage=yes`；
- `UsePreviousAppDir=no`；
- 命令行 `/DIR` 被显式阻断；
- `PrepareToInstall` 强制 `WizardDirValue == 固定 InstallRoot`；
- 通用 Inno Setup UI 尚未配置简体中文语言文件。

当前 App 产品版本已为 1.0.2，但尚未创建正式产品 v1.0.2 tag/Release。

## 三、安装器中文化

正式安装器只使用简体中文向导：

- 增加 Inno Setup 简体中文语言配置；
- 安装、下一步、上一步、取消、目录选择、准备安装、完成、卸载等通用 UI 全部显示中文；
- 保持现有自定义安全提示为中文；
- 不新增语言选择页；
- Unicode 中文应用名、目录名和快捷方式不得乱码。

## 四、首次安装可选择程序路径

首次安装必须显示目录选择页，默认仍为：

`%LOCALAPPDATA%\Programs\StoreExpiryInspector`

用户可选择本地普通可写路径，例如：

`D:\门店效期排查软件`

必须支持包含中文和空格的路径。

安装根安全规则：

- 必须是本地文件系统的普通目录；
- 当前用户可写；
- 不允许 UNC/网络路径；
- 不允许 reparse point / junction / symlink；
- 不允许 traversal、ADS、相对路径、根路径异常；
- 不允许需要管理员权限的受保护目录；
- 不允许与固定数据根 `%LOCALAPPDATA%\StoreExpiryInspector` 重叠或互为祖先/子目录；
- 不因选择目录改变 `PrivilegesRequired=lowest`。

不要单纯关闭旧 `WizardDirValue == fixed root` 断言；要把它重构为“chosen install root 的规范化与安全验证”。

## 五、已安装用户不得在升级时搬目录

已有同 AppId 安装时：

- 读取并使用原实际安装目录作为唯一升级根；
- 升级/修复不显示或不允许改变到其他目录；
- `UsePreviousAppDir` 或等价机制必须真实生效；
- 若安装身份记录与实际程序树不一致，fail-closed，不猜目录；
- 不实现升级时程序目录迁移。

用户若未来需要搬家：卸载程序但保留数据，再重新选择新目录安装。本卡不做自动搬家。

## 六、固定数据根保持不变

程序安装目录可选，但数据根继续固定：

`%LOCALAPPDATA%\StoreExpiryInspector`

不得把 app.db、Backup、Excel、journal 等业务数据搬进用户选择的程序目录。

卸载仍保留用户数据的既有产品规则不得改变。

## 七、所有安装根依赖必须改为实际根

审查并修复所有仍假定 `%LOCALAPPDATA%\Programs\StoreExpiryInspector` 的生产路径，包括但不限于：

- 安装器 existing-version 检查；
- shortcut / working directory；
- 开机启动；
- preflight；
- uninstall；
- updater journal installRoot；
- independent Updater 启动；
- app→old / staging / rollback；
- tree fingerprint；
- pending update recovery；
- S9-T07 schema snapshot/rollback 与实际程序根绑定；
- 测试/发布脚本。

运行中的 App / Updater 应从受验证的实际安装身份推导程序根，不能回退到固定常量或用户任意参数。

## 八、开发期 v1.0.0/v1.0.1 的兼容定位

v1.0.0/v1.0.1 是开发阶段公开验证产物，不作为正式产品用户版本继续维护。因此：

- 不再把“真实用户从 v1.0.0/v1.0.1 正式迁移到 v1.0.2”作为产品发行前置条件；
- 新用户正式路径只有：直接下载安装 v1.0.2；
- 仍保留最小自动化兼容回归：同 AppId 旧默认路径安装 → v1.0.2 Setup 覆盖安装时，不得破坏固定数据根、旧数据或安装身份；
- 该自动化仅证明本阶段没有破坏升级/覆盖兼容，不代表对外继续维护旧测试版本；
- Release Notes 不需要面向真实用户说明 v1.0.0/v1.0.1 升级流程，只需将 v1.0.2 描述为首个正式发行版本。

在 v1.0.2 发布并完成匿名核验之前，v1.0.0/v1.0.1 Release/tag 必须保持现状，不能提前删除，以保留当前发布验证链。

## 九、必须自动化验证

至少：

A. fresh default-path install。
B. fresh custom D-drive-like TEMP root install。
C. fresh Chinese/space path install。
D. unsafe UNC reject。
E. reparse/junction/symlink reject。
F. unwritable/protected path reject，无 UAC 自动提权。
G. chosen path 与 data root 冲突 reject。
H. same AppId reinstall/upgrade 保持原目录。
I. 旧默认路径测试安装 → v1.0.2 Setup 覆盖并保数据，作为兼容回归。
J. custom-root shortcuts/startup 指向正确实际 EXE。
K. custom-root uninstall 正常且数据保留。
L. custom-root 下未来测试升级 package 完成 Updater switch/rollback，不依赖默认固定根。
M. S9-T06 working-directory 修复回归。
N. S9-T07同 Schema/跨 Schema安全回归不退化。

所有自动化使用 TEMP/GUID；不得访问正式安装和正式数据库。

## 十、发布前最终门禁

代码冻结后 Sol 独立执行：

- fresh 无 filter Release 全量：0 failure/error/timeout/aborted/skipped；
- Release build：0 warning / 0 error；
- EF：NO_MODEL_DRIFT；生产 migrationCount=9，末条 `20260901155124_AddPolicyAndBaselineFoundation`；
- App/Updater self-contained win-x64 publish；
- installer compile；
- secret scan；
- v1.0.2 ZIP 内容/版本/tree fingerprint；
- RSA-PSS/SHA256 production manifest 签名、签名正负例、ZIP hash/bytes；
- `git diff --check`；
- 不含 TestMode / S9T07_TEST / fixture/hard-kill 测试产物。

## 十一、一次最精简 Win11 人工验收

正式发布前给用户一个未公开候选 Setup，只需要一次：

1. 启动安装器，确认全部向导为简体中文；
2. 首次安装能看到“选择安装位置”；
3. 选择一个非默认本地目录安装；
4. 桌面快捷方式启动正常；
5. 软件显示 v1.0.2；
6. 退出、重开正常。

通过前不得创建正式产品 v1.0.2 tag/Release。

## 十二、正式发布 v1.0.2

人工门禁和全部技术门禁通过后，Sol 才可发布：

- tag：`v1.0.2`
- stable、非 draft、非 prerelease
- `StoreExpiryInspector-1.0.2-win-x64.zip`
- `StoreExpiryInspector-Setup-1.0.2.exe`
- `update-manifest.json`
- `update-manifest.sig`

发布后独立匿名核对：

- latest = v1.0.2；
- tag/source 正确；
- 4项资产名称、size、SHA256 与本地冻结证据一致；
- manifest production 公钥验签通过；
- ZIP hash/bytes/版本与 manifest 一致；
- Setup 为简体中文可选目录版本；
- main/tag/Release source关系记录清楚。

任何一项发布后核验失败，都停止在 `NOT_ACCEPTED`，保留 v1.0.0/v1.0.1 Release/tag，不执行旧版本删除。

## 十三、旧开发期 Release/tag 清理

只有 v1.0.2 上述发布后核验全部通过，才授权执行以下清理：

1. 删除 GitHub Release `v1.0.0`。
2. 删除 GitHub Release `v1.0.1`。
3. 确认两份 Release 已不存在后，删除 git tag `v1.0.0`、`v1.0.1`。
4. 不删除对应历史 commit。
5. 不删除或改写任何 `.ai-dev` 中 S9-T06/S9-T07 及发布链历史证据。
6. 最终匿名核对 GitHub Release/latest：v1.0.2 为首个且当前正式发行版本。
7. 最终核对 main clean、HEAD=origin/main、ahead/behind 0/0。

删除旧 Release/tag 是经用户明确授权的正式产品清理动作，但执行时仍必须遵守上述先后顺序，不得在 v1.0.2 稳定发布前提前删除。

完成后：`S10-T01 = CLOSED`，`Stage10 = CLOSED`。不自动创建 Stage11。