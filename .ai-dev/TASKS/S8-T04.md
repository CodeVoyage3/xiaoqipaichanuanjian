# S8-T04｜异常退出、事务中断与数据库一致性

日期2026-09-04，状态 `AUTHORIZED / IN_PROGRESS`。重新fetch后HEAD=origin/main=`2142cf72588fb727cca6efd58509c113646de1e2`，clean、0/0。工作分支`codex/s8-t04-crash-consistency`。

## 角色与范围

Sol只写治理、独立审查和验收；全新GPT-5.6 Terra/medium/priority实施，不能复用任何旧卡Terra，提交后停止、不push。只有本卡获授权；不创建S8-T05，不实施人工损坏、Stage9、在线升级或重置数据。Import Undo永久取消。

建立真实进程硬终止基座：父进程启动独立测试子进程，调用权威写链，精确检查点发出token/PID/阶段握手；父进程核验并硬Kill自己创建的进程树。不以受控异常、优雅退出、finally/Dispose回滚冒充硬中断。

1. 大Excel：复用S8-T03真实XLSX parser/validation/planner/confirmation/executor/ConfirmedImportLifecycleOrchestrator。至少10k行主矩阵，另在100k验证precommit/postcommit。覆盖transaction started、部分Product持久化、部分Batch持久化、ProductTask后置编排进行中、Commit前、Commit成功后业务返回前。fixture必须真实进入后置编排而非仅空冷启动。
2. 正式排查：InspectionSubmissionUseCase，覆盖Inspection/Item部分写入、Task completed/handled转换附近、Commit前及成功后业务返回前。混合0/positive，blank拒绝另用既有专项验证。
3. 小事务选ManualInventoryAdjustmentUseCase库存归零生命周期联动，覆盖partial/precommit/postcommit；History Revision运行回归，不另扩崩溃链。

每个关键检查点至少3次独立重复，固定矩阵记录每次失败；不得重试到绿。未命中检查点、正常返回或超时均算失败。

## 隔离与实现限制

- 所有DB/WAL/SHM/journal/XLSX/snapshot/marker/log/results显式TEMP/GUID，仅合成数据。严禁读取、哈希、调查、复制、修复正式库或旧疑似访问事件。
- 创建Context前验证绝对路径、TEMP containment、GUID/token、非重解析点及完整参数；缺项fail closed。默认生产factory/loader调用0，保留S8-T02隔离专项。
- 复用现有测试工程、EF/SQLite interceptors和标准库，优先全部测试侧实现。不得新增project/dependency/csproj/slnx；若无法做到立即停止报告。无生产环境变量kill开关，不为测试改变事务结构。
- 不改Schema/index/migration/PRAGMA/journal_mode/synchronous，不做性能优化/全局UI/算法改动。只允许真实crash证据确认的一致性bug最小修复，保留修复前后证据。

## 判据与证据

- precommit重开后全部业务状态=before；postcommit=同一合成fixture经真实链成功执行的committed expected。比较全业务字段/BLOB、主键和counts；非确定路径/耗时若规范化须逐字段解释，不可忽略业务差异。
- 每次reopen成功、integrity_check=ok、foreign_key_check=0、migration=9；无混合状态、重复、孤儿、半批次/半历史。
- 指纹核验后用新连接最小合法写证明无遗留锁；权威Dashboard/当前任务/历史读取正常。记录重开前后sidecar，不删除sidecar制造通过。
- 矩阵字段scenario/checkpoint/iteration/childPID/kill机制/prekill marker/exitcode/before-expected-after指纹及counts/integrity/FK/migrations/reopen/stale lock/sidecars/pass-fail。原始证据留TEMP，不提交DB；允许紧凑报告。

## Sol独立门禁与停止

完整diff、隔离专项、真实硬中断全矩阵、blank/0/positive等业务回归、Release全量0failure、build0warning/0error、EF无漂移（设计时内存）、migration list --no-connect仍9、Schema/ModelSnapshot/index/dependency/csproj/slnx不变、git diff --check。

完成后更新Task/Acceptance/Stage/Handoff/ProjectStatus，普通push main后停止，不创建S8-T05。结论仅进程异常终止，不声称真实断电、SSD/磁盘损坏或文件系统故障通过。
